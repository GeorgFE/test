# Study Lab — Полный дамп

> Интерактивная лаборатория для подготовки к собеседованиям по 1С:Enterprise

> Всего дней: 15 | Карточек: 415 | Диаграмм: 31 | Кодов: 23 | Сценариев: 30 | Теория: 17


---

## Содержание

- [День 1: День 1: Фундамент](#день-1) — 12 карточек
- [День 2: День 2: Запросы и индексы](#день-2) — 20 карточек
- [День 3: День 3: Транзакции и СУБД](#день-3) — 15 карточек
- [День 4: День 4: Кластер и безопасность](#день-4) — 15 карточек
- [День 5: День 5: ACID и инфраструктура](#день-5) — 15 карточек
- [День 6: День 6: Git, Web, хранилище](#день-6) — 15 карточек
- [День 7: День 7: Паттерны и практика](#день-7) — 15 карточек
- [День 8: День 8: Структура и регистры](#день-8) — 30 карточек
- [День 9: День 9: NULL, HTTP, WebSocket, собеседование](#день-9) — 40 карточек
- [День 10: День 10: Мок-собеседование Магнит](#день-10) — 43 карточек
- [День 11: День 11: HTTP-интеграция Магнит](#день-11) — 40 карточек
- [День 12: День 12: СКД и сложные запросы](#день-12) — 40 карточек
- [День 13: День 13: Алгоритмы и задачи на собеседовании](#день-13) — 40 карточек
- [День 14: День 14: Расширения конфигурации](#день-14) — 40 карточек
- [День 15: День 15: Оптимизация и рефакторинг (Middle → Senior)](#день-15) — 35 карточек

---

# День 1: День 1: Фундамент

> Клиент-сервер, регистры, транзакции, блокировки

**Фаза:** Фаза 1 | **Категорий:** 4 | **Карточек:** 12

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Клиент-серверное взаимодействие | ~30мин | 4 | ✅ | ✅ | — |
| 2 | Архитектура регистров | ~20мин | 3 | ✅ | — | — |
| 3 | Транзакции и переменные | ~15мин | 2 | — | ✅ | — |
| 4 | Управляемые блокировки | ~25мин | 3 | ✅ | ✅ | — |

---

## Карточки

### Клиент-серверное взаимодействие

#### 🔴 fc-1: Можно ли создать ТЗ (ТабличнаяЧасть) на клиенте?

**Ответ:** НЕТ. ТабличнаяЧасть — это серверный объект. Она принадлежит объекту (Документу, Справочнику), который тоже серверный. На клиенте можно работать только с ДанныеФормыКоллекция (обёртка формы).

*Сложность: Сложно | Категория: Клиент-сервер*

#### 🔴 fc-2: Какие объекты являются серверными?

**Ответ:** СправочникОбъект, ДокументОбъект, all registers (РегистрНакопленияОбъект и т.д.), Область макета, Запрос, НаборЗаписей, СхемаКомпоновкиДанных. ВСЁ что связано с данными — серверное.

*Сложность: Сложно | Категория: Клиент-сервер*

#### 🟡 fc-3: Что МОЖНО делать на клиенте?

**Ответ:** Работать с элементами формы, показывать предупреждения, управлять интерфейсом, работать с ДанныеФормы (обёртки), локальные переменные, условия, циклы, диалоги выбора файлов/даты. Массивы, Соответствия, ФиксированныйМассив — работают и там и там.

*Сложность: Средне | Категория: Клиент-сервер*

#### 🔴 fc-4: Что возвращает серверная функция на клиент?

**Ответ:** Примитивные типы, массивы примитивов, ФиксированнаяСтруктура, ФиксированныйМассив, ФиксированноеСоответствие. НЕЛЬЗЯ вернуть объект 1С (СправочникОбъект, ДокументОбъект).

*Сложность: Сложно | Категория: Клиент-сервер*

### Архитектура регистров

#### 🟡 fc-5: Какие типы регистров накопления бывают?

**Ответ:** Остатки и Обороты. Остатки — имеют измерения и ресурсы (баланс). Обороты — имеют измерения и ресурсы (движения). Виртуальные таблицы: Остатки, Обороты, СрезПервых, СрезПоследних.

*Сложность: Средне | Категория: Регистры*

#### 🔴 fc-6: РегистрНакопления можно записать только через…?

**Ответ:** ТОЛЬКО через документ. Нельзя записать напрямую из кода (в отличие от Регистра сведений). Движения формируются в обработчике ОбработкаПроведения.

*Сложность: Сложно | Категория: Регистры*

#### 🟡 fc-7: В чём разница РегистрНакопления и РегистрСведений?

**Ответ:** РН — связан с документами (движения). РС — независим, может записываться в любой момент. РН имеет виртуальные таблицы Остатки/Обороты. РС — просто таблица записей.

*Сложность: Средне | Категория: Регистры*

### Транзакции и переменные

#### 🔴 fc-8: Что делает НачатьТранзакцию()?

**Ответ:** Открывает явную транзакцию. Все последующие записи объектов будут в рамках этой транзакции. Если вызвать ОтменитьТранзакцию() — ВСЕ записи отменятся. Важно: вложенные транзакции НЕ поддерживаются.

*Сложность: Сложно | Категория: Транзакции*

#### 🟡 fc-9: Когда нужно использовать явные транзакции?

**Ответ:** Когда нужно записать НЕСКОЛЬКО объектов АТОМАРНО: либо все, либо ни одного. Пример: проведение документа с движениями в 3 регистрах. Если один регистр упадёт — откатываются все.

*Сложность: Средне | Категория: Транзакции*

### Управляемые блокировки

#### 🔴 fc-10: Что такое управляемая блокировка?

**Ответ:** Это механизм платформы 1С для координации доступа к данным. Блокировка устанавливается НЕ в СУБД, а в МЕНЕДЖЕРЕ БЛОКИРОВОК КЛАСТЕРА 1С. Позволяет избежать взаимных блокировок на уровне приложения.

*Сложность: Сложно | Категория: Блокировки*

#### 🟡 fc-11: В чём разница управляемых и автоматических блокировок?

**Ответ:** Управляемые: ставятся разработчиком через ЗаблокироватьДанныеДляРедактирования(), проверяются в кластере 1С, упреждающие. Автоматические: ставятся СУБД при реальном изменении данных, реактивные.

*Сложность: Средне | Категория: Блокировки*

#### 🟡 fc-12: Как работает ЗаблокироватьДанныеДляРедактирования()?

**Ответ:** 1. Формируется ключ блокировки. 2. Проверяется, нет ли уже блокировки. 3. Если есть — ожидание/ошибка. 4. Если нет — блокировка устанавливается в кластере.

*Сложность: Средне | Категория: Блокировки*

### Диаграмма: Путь вызова: Клиент → Сервер → БД

*Как работает вызов серверной процедуры из клиентского кода*

#### Шаг 1: Клиент нажимает кнопку

Пользователь нажимает кнопку на форме. Срабатывает обработчик &НаКлиенте.
- Клиентский обработчик получает управление
- Подготавливает данные для отправки
- Вызывает серверную процедуру

#### Шаг 2: Вызов сервера (сетевой запрос)

Клиент отправляет HTTP-запрос к серверу 1С. Это НЕ мгновенно!
- Данные сериализуются и передаются по сети
- Занимает 10-500мс в зависимости от нагрузки
- Минимизируйте количество серверных вызовов

#### Шаг 3: Выполнение на сервере

Сервер создаёт объекты, выполняет запросы к СУБД, обрабатывает логику.
- Создание объектов — только здесь!
- Выполнение запросов к БД
- Бизнес-логика и валидация

#### Шаг 4: Ответ клиенту

Сервер отправляет результат обратно. Только примитивные типы!
- Нельзя вернуть СправочникОбъект — только ФиксированнаяСтруктура
- Массивы, ТаблицыЗначений — можно через форму
- Ответ приходит как результат функции

### Диаграмма: Жизненный цикл проведения документа

*Что происходит шаг за шагом при проведении документа*

#### Шаг 1: Пользователь нажимает 'Провести'

Начинается процесс записи документа и формирования движений
- Срабатывает ПередЗаписью (можно отменить)
- Объект записывается в БД
- Запускается ОбработкаПроведения

#### Шаг 2: Открытие транзакции

1С автоматически начинает транзакцию
- Все движения и запись — в одной транзакции
- При ошибке — полный откат

#### Шаг 3: Формирование движений

В ОбработкаПроведения создаются записи в регистрах
- Движения.РегистрНакопления.Записать;
- Каждый регистр получает свой НаборЗаписей
- Заполняются измерения, ресурсы, реквизиты

#### Шаг 4: Запись движений и фиксация

Все движения записываются, транзакция фиксируется
- Движения записываются в БД
- Транзакция фиксируется (COMMIT)
- Документ становится 'Проведён'

### Диаграмма: Механизм управляемых блокировок

*Как 1С управляет блокировками в кластере*

#### Шаг 1: Код вызывает ЗаблокироватьДанныеДляРедактирования()

Программист явно запрашивает блокировку в кластере 1С
- Передаётся ключ: что блокируем
- Кластер проверяет: нет ли уже блокировки?
- Блокировка регистрируется в кластере 1С (НЕ в СУБД!)

#### Шаг 2: Проверка в кластере 1С

Менеджер блокировок кластера проверяет конфликты
- Кластер 1С — центральный координатор блокировок
- Проверяются все активные блокировки
- При конфликте — ожидание или исключение

#### Шаг 3: Редактирование данных

После успешной блокировки — можно безопасно редактировать
- Данные заблокированы для других пользователей
- Начинается транзакция
- Чтение и модификация данных

#### Шаг 4: Запись и снятие блокировки

При фиксации транзакции блокировка автоматически снимается
- ЗафиксироватьТранзакцию() снимает блокировку
- При ОтменитьТранзакцию() — тоже снимается

### Код: Правильный вызов сервера из клиента

*Как правильно организовать клиент-серверное взаимодействие*

```bsl
&НаКлиенте
Процедура СоздатьКонтрагента(Команда)
    // Подготавливаем данные на клиенте
    Имя = "ООО Ромашка";
    ИНН = "7712345678";

    // Вызываем сервер (ОДИН вызов!)
    Результат = СоздатьКонтрагентаНаСервере(Имя, ИНН);

    // Работаем с результатом на клиенте
    Сообщить("Создан: " + Результат.Ссылка);
КонецПроцедуры

&НаСервере
Функция СоздатьКонтрагентаНаСервере(Имя, ИНН)
    // Здесь МОЖНО создавать объекты!
    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
    НовыйКонтрагент.Наименование = Имя;
    НовыйКонтрагент.ИНН = ИНН;
    НовыйКонтрагент.Записать();

    // Возвращаем ФиксированнуюСтруктуру
    Возврат Новый ФиксированнаяСтруктура("Ссылка", НовыйКонтрагент.Ссылка);
КонецФункции
```

**Пояснения к строкам:**

- **Строка 1:** &НаКлиенте — директива! Код работает на клиенте. Здесь нельзя создавать объекты 1С.
- **Строка 8:** Один серверный вызов вместо нескольких — паттерн. Минимизируйте round-trips.
- **Строка 14:** &НаСервере — код выполняется на сервере. Здесь можно ВСЁ.
- **Строка 17:** СоздатьЭлемент() — серверный вызов! Создаёт СправочникОбъект в памяти сервера.
- **Строка 20:** Записать() — физически записывает в БД. Только на сервере!
- **Строка 23:** ФиксированнаяСтруктура — можно передать на клиент! Обычную Структуру нельзя.

**Квиз:** Что будет, если убрать &НаСервере и оставить СоздатьЭлемент() в клиентской процедуре?

- ⬜ **A.** Будет ошибка компиляции
- ⬜ **B.** Код выполнится на клиенте (работает)
- ✅ **C.** Будет ошибка выполнения: 'Неверное использование серверного вызова'
- ⬜ **D.** Объект создастся локально на клиенте

*Объяснение: Правильно: Будет ошибка выполнения. Справочники.Контрагенты.СоздатьЭлемент() — серверный метод.*

### Код: Транзакция и переменные — ловушка!

*Частая ошибка: переменные НЕ откатываются при ОтменитьТранзакцию()*

```bsl
&НаСервере
Процедура ПримерТранзакции()
    НачатьТранзакцию();

    Документ = Документы.Приходная.НайтиПоНомеру(123);
    Документ.Комментарий = "Изменён";
    Документ.Записать();

    Статус = "Записано";

    Попытка
        // Какая-то операция, которая упадёт
        ВызватьИсключение("Ошибка!");
    Исключение
        ОтменитьТранзакцию();
    КонецПопытки;

    // Статус = "Записано", но в БД изменений НЕТ!
    // Переменная НЕ откатилась!
КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 3:** Начинаем транзакцию. Все последующие записи в БД будут в рамках одной транзакции.
- **Строка 7:** Документ записан в БД (в рамках транзакции). Если откатим — запись исчезнет.
- **Строка 9:** Локальная переменная Статус. Она НЕ откатится при ОтменитьТранзакцию()!
- **Строка 15:** ОтменитьТранзакцию() откатывает только записи в БД. Переменные в памяти НЕ меняются.
- **Строка 18:** КРИТИЧЕСКИЙ МОМЕНТ: Статус = 'Записано', но документ НЕ записан.

**Квиз:** Что содержит переменная Статус после ОтменитьТранзакцию()?

- ⬜ **A.** Пустая строка
- ✅ **B.** "Записано" — переменные НЕ откатываются
- ⬜ **C.** "Ошибка"
- ⬜ **D.** Будет ошибка

*Объяснение: Статус = 'Записано'. Переменные НЕ откатываются при откате транзакции.*

### Код: Управляемые блокировки — правильный паттерн

*Как правильно ставить управляемые блокировки*

```bsl
&НаСервере
Процедура ИзменитьКонтрагентаБезопасно(Ссылка, НовоеИмя)

    // 1. БЛОКИРУЕМ данные ПЕРЕД чтением
    Блокировка = Новый Массив;
    ЭлементБлокировки = Новый Массив;
    ЭлементБлокировки.Добавить(Ссылка);
    Блокировка.Добавить(ЭлементБлокировки);

    // Блокировка в КЛАСТЕРЕ 1С (не в СУБД!)
    ЗаблокироватьДанныеДляРедактирования(
        РегистрСведений.ЦеныНоменклатуры,,
        Блокировка);

    // 2. Читаем данные ПОСЛЕ блокировки
    Объект = Ссылка.ПолучитьОбъект();
    Объект.Наименование = НовоеИмя;

    // 3. Записываем — блокировка снимется автоматически
    Объект.Записать();

КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 4:** ПРАВИЛО: Блокировка -> Чтение -> Изменение -> Запись. Всегда в таком порядке!
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования ставит блокировку В КЛАСТЕРЕ 1С. Не в СУБД!
- **Строка 11:** Можно блокировать не только справочники, но и регистры, документы.
- **Строка 15:** Читаем данные ТОЛЬКО ПОСЛЕ блокировки. Иначе — потерянные обновления.
- **Строка 20:** При успешной записи транзакция фиксируется, блокировка снимается автоматически.

**Квиз:** Где регистрируется управляемая блокировка?

- ⬜ **A.** В SQL Server / PostgreSQL (в СУБД)
- ✅ **B.** В кластере серверов 1С
- ⬜ **C.** На клиенте приложения
- ⬜ **D.** В файловой базе

*Объяснение: Управляемая блокировка — механизм сервера 1С. Регистрируется в МЕНЕДЖЕРЕ БЛОКИРОВОК КЛАСТЕРА 1С.*

---

# День 2: День 2: Запросы и индексы

> Язык запросов, виртуальные таблицы, оптимизация, функции

**Фаза:** Фаза 2 | **Категорий:** 3 | **Карточек:** 20

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Язык запросов 1С + Филиппов | ~45мин | 11 | ✅ | ✅ | — |
| 2 | Индексы, блокировки форм | ~35мин | 6 | ✅ | ✅ | — |
| 3 | Встроенные функции | ~20мин | 3 | — | — | — |

---

## Карточки

### Язык запросов 1С + Филиппов

#### 🟡 d2-fc-1: Какие JOIN-ы поддерживает 1С?

**Ответ:** LEFT JOIN, RIGHT JOIN (с версии 8.3.6+), INNER JOIN, FULL JOIN. Все виды соединений. Вложенные соединения через ВЫБРАТЬ. Также JOIN через поля (Номенклатура = Документ.Номенклатура).

*Сложность: Средне | Категория: Запросы*

#### 🟡 d2-fc-2: Что такое ВРЕМЕННЫЕ ТАБЛИЦЫ в запросах?

**Ответ:** Это таблицы, существующие только в рамках одного пакета запросов. Создаются через МенеджерВременныхТаблиц. Работают быстрее, чем обычные, потому что в памяти. Хранятся в TempDB (SQL Server). Уничтожаются после завершения запроса.

*Сложность: Средне | Категория: Запросы*

#### 🔴 d2-fc-3: Как работают ИТОГИ ПО в запросе?

**Ответ:** ИТОГИ ПО группируют данные по указанному полю и вычисляют агрегатные функции (Сумма, Количество, Среднее). Итоги формируются ПОСЛЕ основной выборки. Платформа 1С сама решает — использовать виртуальную таблицу или считать сама.

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-4: Что такое ВЫБРАТЬ РАЗРЕШЕННЫЕ РАЗЛИЧНЫЕ?

**Ответ:** ВЫБРАТЬ РАЗРЕШЕННЫЕ позволяет получить уникальные записи даже если есть права RLS, ограничивающие доступ. РАЗЛИЧНЫЕ убирает дубликаты. Важное отличие от обычного ВЫБРАТЬ при RLS.

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-5: В чём разница WHERE и УСЛОВИЕ в виртуальных таблицах?

**Ответ:** WHERE: фильтрует ПОСЛЕ получения данных из виртуальной таблицы (медленно для больших объёмов). УСЛОВИЕ: передаётся ВНУТРЬ виртуальной таблицы как параметр (быстро, используется индекс). Всегда предпочитай УСЛОВИЕ!

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-12: Какие виртуальные таблицы у РегистрНакопления?

**Ответ:** Остатки (текущие остатки на дату), Обороты (движения за период), СрезПервых/СрезПоследних (первое/последнее значение по периодам). Параметры: Период, Условие, Группировка.

*Сложность: Сложно | Категория: Запросы*

#### 🟡 d2-fc-13: Что такое КЛЮЧЕВАЯ ОПЕРАЦИЯ (по Филиппову)?

**Ответ:** Это операция после нажатия пользователем кнопки на форме. Её определяющая характеристика — ВРЕМЯ выполнения. Ключевая операция всегда начинается и заканчивается на клиенте. Нельзя разбить на несколько КО действие, единое для пользователя.

*Сложность: Средне | Категория: Запросы*

#### 🔴 d2-fc-14: Что такое APDEX и как он считается?

**Ответ:** APDEX = (Кол-во_выполненных_за_T + 0.5 * Кол-во_выполненных_за_4T) / Всего_замеров. T = целевое время. Выполнение за T — удовлетворительно. За 4T и больше — неприемлемо. Нужна выборка минимум 100 замеров. Используется в БСП.

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-18: 3 особенности чтения в объектной модели (Филиппов)?

**Ответ:** (1) Платформа ставит УПРАВЛЯЕМЫЕ РАЗДЕЛЯЕМЫЕ блокировки при чтении наборов записей регистров. (2) Чтение может происходить в НЕЯВНОЙ транзакции. (3) Считываются ВСЕ данные объекта (все поля, все ТЗ). Это может вызывать избыточные блокировки!

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-19: Где хранятся временные таблицы 1С?

**Ответ:** В базе TempDB SQL Server. Там же хранятся снимки Snapshot для уровня Read Committed Snapshot. TempDB критична для производительности — если она перегружена, ВСЕ временные таблицы замедляются. Рекомендуется переносить TempDB на быстрый SSD.

*Сложность: Сложно | Категория: Запросы*

#### 🔴 d2-fc-20: FULL vs SIMPLE модель восстановления SQL Server для 1С?

**Ответ:** FULL: журнал транзакций растёт, нужен бэкап для скатия. SIMPLE: журнал скатывается автоматически. Для 1С SIMPLE часто приемлем, т.к. сервер 1С сам управляет транзакциями. Growing journal — частая причина проблем с производительностью!

*Сложность: Сложно | Категория: Запросы*

### Индексы, блокировки форм

#### 🔴 d2-fc-6: Какие индексы платформа 1С создаёт автоматически?

**Ответ:** По измерениям регистров (композитный индекс по всем измерениям). По реквизитам справочников: Владелец, Родитель, Код. По регистратору в регистрах. По ссылке в табличных частях. Порядок полей в индексе = порядок в конфигураторе!

*Сложность: Сложно | Категория: Индексы*

#### 🔴 d2-fc-7: Что такое план запросов и как его читать?

**Ответ:** План запросов — это дерево операций, которые выполнит СУБД. Ключевые операции: Index Scan (использует индекс), Seq Scan (сканирование всей таблицы), Nested Loop, Hash Join. Чем меньше строк сканируется — тем быстрее.

*Сложность: Сложно | Категория: Индексы*

#### 🟡 d2-fc-8: Какой самый частый антипаттерн запросов?

**Ответ:** Запрос внутри цикла (Query in a Loop). Каждый вызов = отдельный запрос к СУБД. Вместо этого: собрать данные в ТЗ, сделать один запрос с ИЛИ или вложенными запросами, или через ВЫБРАТЬ РАЗРЕШЕННЫЕ.

*Сложность: Средне | Категория: Индексы*

#### 🔴 d2-fc-15: Как 1С называет таблицы в SQL Server?

**Ответ:** _Document123 (документы), _AccRg123 (РН), _InfoRg123 (РС), _AccRgAT123 (РБ), _CRg123 (РР). Число = ID объекта конфигурации. Также табличные части: _Document123_VT456. Константы: _Const8, _Const10.

*Сложность: Сложно | Категория: Индексы*

#### 🟡 d2-fc-16: Что такое пессимистическая блокировка формы?

**Ответ:** Когда пользователь открывает форму на редактирование, платформа ставит пессимистическую блокировку на объект. Другой пользователь не может редактировать тот же объект. При попытке получает ошибку 'Данные уже редактируются'. Блокировка снимается при закрытии формы.

*Сложность: Средне | Категория: Индексы*

#### 🟡 d2-fc-17: Что такое оптимистическая блокировка формы?

**Ответ:** Срабатывает когда пользователь пытается записать объект, который был изменён другим пользователем ПОСЛЕ того, как текущий пользователь его прочитал. Ошибка: 'Данные были изменены или удалены другим пользователем'. Название от 'оптимистичного' предположения, что конфликтов не будет.

*Сложность: Средне | Категория: Индексы*

### Встроенные функции

#### 🟡 d2-fc-9: Чем отличается СтрНайти от СтрНайти с чего?

**Ответ:** СтрНайти(Строка, Подстрока) — ищет с начала. СтрНайти(Строка, Подстрока, НаправлениеПоиска.Конец) — ищет с конца. Также есть НачинаяСПозиции. Возвращает номер первого символа найденной подстроки или 0.

*Сложность: Средне | Категория: Функции*

#### 🟡 d2-fc-10: Как правильно округлить число в 1С?

**Ответ:** Окр(Число, Разрядность, РежимОкругления). Режимы: ОкрРежим.Окр15как20 (округление к ближайшему, 0.5→1), ОкрРежим.Окр15как10 (бухгалтерское, 0.5→0). НЕЛЬЗЯ использовать несуществующие ОкрЛ() или ОкрМ()!

*Сложность: Средне | Категория: Функции*

#### 🟡 d2-fc-11: Как очистить ТаблицуЗначений и Массив?

**Ответ:** ТЗ.Очистить() — очищает строки таблицы. ТЗ.Колонки.Очистить() — удаляет колонки. Массив.Очистить() — очищает элементы массива. НЕЛЬЗЯ использовать Массив.Удалить() в цикле — нужен обратный обход или Очистить()!

*Сложность: Средне | Категория: Функции*

### Диаграмма: Виртуальные таблицы: Остатки vs Обороты

*Как платформа 1С преобразует виртуальные таблицы в реальные запросы*

#### Шаг 1: Запрос к виртуальной таблице Остатки

Вы пишете: ВЫБРАТЬ Из РегистрНакопления.Остатки()
- Платформа видит обращение к виртуальной таблице
- Определяет: какой тип регистра
- Формирует параметры: Период, Условие

#### Шаг 2: Платформа генерирует СКРЫТЫЙ запрос

1С автоматически создаёт запрос с SUM и GROUP BY
- Для Остатков: SUM(Приход) - SUM(Расход) GROUP BY Измерения
- Для Оборотов: SUM(Ресурс) GROUP BY Измерения, Период
- Добавляет условие МЕЖДУ для периода

#### Шаг 3: Запрос к СУБД

Сформированный запрос отправляется в PostgreSQL / SQL Server
- СУБД выполняет реальный SQL-запрос
- Использует индексы измерений
- Возвращает результат платформе

#### Шаг 4: Агрегация на стороне платформы

Платформа может досчитать итоги сама, если СУБД не справляется
- ИТОГИ ПО часто считается платформой
- Виртуальные таблицы ИТОГИ — особый случай
- Балансировка нагрузки между 1С и СУБД

### Диаграмма: Как запрос проходит через индекс

*От текста запроса к данным в СУБД — что происходит внутри*

#### Шаг 1: Парсер запроса

1С разбирает текст запроса и строит дерево синтаксического разбора
- Проверка синтаксиса
- Разрешение имён объектов
- Определение источников данных

#### Шаг 2: Построитель плана запроса

1С генерирует план выполнения — какие операции и в каком порядке
- Выбор стратегии: Index Scan vs Seq Scan
- Определение порядка JOIN
- Оптимизация порядка фильтрации

#### Шаг 3: Использование индекса (быстро)

Если есть подходящий индекс — СУБД ищет по B-дереву, O(log N)
- Поиск по ключу индекса
- Быстрый переход к нужной странице
- 10 записей из 1 000 000 за миллисекунды

#### Шаг 4: Seq Scan (медленно)

Если индекса нет или он не подходит — СУБД сканирует ВСЮ таблицу
- Читает каждую строку.
- 1 000 000 строк = секунды или минуты.
- Именно поэтому важно создавать правильные индексы.

### Код: Виртуальная таблица Остатки с УСЛОВИЕ

*Правильный запрос к виртуальной таблице — УСЛОВИЕ передаётся внутрь*

```bsl
// ПРАВИЛЬНО — УСЛОВИЕ передаётся в виртуальную таблицу
ВЫБРАТЬ
    Остатки.Номенклатура КАК Номенклатура,
    Остатки.КоличествоОстаток КАК Остаток
ИЗ РегистрНакопления.Остатки КАК Остатки
ГДЕ Остатки.Номенклатура В ИЕРАРХИИ(&Группа)
    И Остатки.Склад = &Склад

// -> Платформа передаёт условия Склад в параметры
// -> СУБД фильтрует по индексу = O(log N)

// НЕПРАВИЛЬНО — фильтр через левое соединение
ВЫБРАТЬ Остатки.КоличествоОстаток
ИЗ РегистрНакопления.Остатки КАК Остатки
ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Н
    ПО Остатки.Номенклатура = Н.Ссылка
ГДЕ Н.Статус = &Статус  // <- антипаттерн!
// -> СУБД читает ВСЕ остатки, потом фильтрует
```

**Пояснения к строкам:**

- **Строка 1:** УСЛОВИЕ — передаётся ПРЯМО в виртуальную таблицу как параметр. СУБД использует индексы.
- **Строка 6:** Условия ГДЕ по измерениям = параметры виртуальной таблицы. Платформа автоматически оптимизирует.
- **Строка 17:** АНТИПАТТЕРН: фильтрация по полям не измерений через JOIN. Виртуальная таблица вернёт ВСЕ данные.
- **Строка 18:** СУБД сначала получает все остатки (может быть миллионы строк), потом фильтрует. Очень медленно.

**Квиз:** Как правильно фильтровать виртуальную таблицу Остатков по Складу?

- ⬜ **A.** Через ГДЕ после выборки
- ✅ **B.** Через УСЛОВИЕ в параметрах виртуальной таблицы
- ⬜ **C.** Через LEFT JOIN со Справочником.Склады
- ⬜ **D.** Виртуальные таблицы не поддерживают фильтрацию

*Объяснение: УСЛОВИЕ в ГДЕ по измерениям автоматически передаётся в параметры виртуальной таблицы. СУБД использует индекс.*

### Код: Запрос через индекс vs полный скан

*Видим разницу между эффективным и неэффективным запросом*

```bsl
// БЫСТРО — есть индекс по Номенклатуре
Запрос = Новый Запрос;
Запрос.Текст =
    "ВЫБРАТЬ РегистрНакопления.Остатки"
    "ИЗ РегистрНакопления.Остатки КАК Остатки"
    "ГДЕ Остатки.Номенклатура = &Номенклатура";
Запрос.УстановитьПараметр("Номенклатура", Ссылка);
// -> СУБД использует Index Scan = O(log N)

// МЕДЛЕННО — нет индекса по Склад.Ответственный
Запрос2 = Новый Запрос;
Запрос2.Текст =
    "ВЫБРАТЬ * ИЗ Справочник.Склады КАК Склады"
    "ГДЕ Склады.Ответственный = &Ответственный";
// -> СУБД делает Seq Scan = читает ВСЮ таблицу
// -> 100 000 строк за 5 секунд!

// РЕШЕНИЕ: добавить индекс по Ответственный
// или использовать УСЛОВИЕ вместо ГДЕ
```

**Пояснения к строкам:**

- **Строка 6:** Номенклатура — это измерение регистра, по нему АВТОМАТИЧЕСКИ создан индекс. СУБД найдёт строку мгновенно.
- **Строка 14:** Ответственный — это РЕКВИЗИТ справочника. По реквизитам нет индексов по умолчанию!
- **Строка 15:** Seq Scan (Sequential Scan) — СУБД читает каждую строку и проверяет условие. Для 100К строк это медленно.
- **Строка 16:** Решение: добавить индекс по реквизиту или перепроектировать запрос.

**Квиз:** Почему запрос по полю Ответственный медленный?

- ⬜ **A.** Потому что поле текстовое
- ✅ **B.** Нет индекса по реквизиту — СУБД делает полный скан таблицы
- ⬜ **C.** Нужно использовать ВЫБРАТЬ РАЗЛИЧНЫЕ
- ⬜ **D.** Регистр не поддерживает поиск по реквизитам

*Объяснение: По реквизитам справочников нет автоматических индексов. СУБД сканирует всю таблицу. Решение: добавить индекс.*

---

# День 3: День 3: Транзакции и СУБД

> Уровни изоляции, deadlock, PostgreSQL, SQL Server, мониторинг

**Фаза:** Фаза 3 | **Категорий:** 3 | **Карточек:** 15

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Транзакции в 1С | ~40мин | 5 | ✅ | ✅ | — |
| 2 | Блокировки в деталях | ~35мин | 5 | ✅ | ✅ | — |
| 3 | СУБД для 1С-разработчика | ~30мин | 5 | — | — | — |

---

## Карточки

### Транзакции в 1С

#### 🔴 d3-fc-1: Какой уровень изоляции транзакций использует 1С по умолчанию?

**Ответ:** SERIALIZABLE (сериализуемый) — самый строгий уровень. 1С гарантирует, что транзакции выполняются так, будто они идут последовательно. Это обеспечивает целостность данных, но снижает параллелизм. Реализуется поверх СУБД через управляемые блокировки.

*Сложность: Сложно | Категория: Транзакции*

#### 🔴 d3-fc-2: Что происходит при ОтменитьТранзакцию() с переменными?

**Ответ:** Откатываются только ИЗМЕНЕНИЯ В БД. Локальные переменные в памяти НЕ откатываются! Если было: Статус = 'Записано' и потом ОтменитьТранзакцию(), переменная останется 'Записано', хотя данные в БД reverted. Это критическая ловушка!

*Сложность: Сложно | Категория: Транзакции*

#### 🟡 d3-fc-3: Можно ли вложить одну транзакцию в другую в 1С?

**Ответ:** НЕТ. Вложенные транзакции НЕ поддерживаются платформой 1С. Если вызвать НачатьТранзакцию() дважды без фиксации — будет ошибка. Все операции должны быть в ОДНОЙ транзакции.

*Сложность: Средне | Категория: Транзакции*

#### 🟡 d3-fc-4: Что такое НЕЯВНАЯ транзакция в 1С?

**Ответ:** Платформа сама начинает транзакцию при записи объекта (Записать(), Провести()). Разработчик не вызывал НачатьТранзакцию(), но транзакция открылась. Это важно: если внутри есть исключение, платформа сама откатит изменения.

*Сложность: Средне | Категория: Транзакции*

#### 🔴 d3-fc-5: Почему длинные транзакции — это проблема?

**Ответ:** (1) Блокировки держатся дольше — другие пользователи ждут. (2) Растёт журнал транзакций СУБД. (3) Растёт риск взаимных блокировок (deadlocks). Правило: транзакция должна быть максимально короткой — читаем до, открываем, пишем, фиксируем.

*Сложность: Сложно | Категория: Транзакции*

### Блокировки в деталях

#### 🔴 d3-fc-6: Что такое DEADLOCK (взаимная блокировка)?

**Ответ:** Ситуация когда Транзакция A ждёт ресурс, заблокированный Транзакцией B, а B ждёт ресурс, заблокированный A. Обе зависают навсегда. СУБД обнаруживает и убивает одну из них. В 1С: управляемые блокировки снижают риск, но не устраняют полностью.

*Сложность: Сложно | Категория: Блокировки*

#### 🔴 d3-fc-7: Чем управляемая блокировка отличается от автоматической (СУБД)?

**Ответ:** Управляемая: ставится разработчиком ЗАРАНЕЕ через ЗаблокироватьДанныеДляРедактирования(), проверяется в КЛАСТЕРЕ 1С, упреждающая. Автоматическая: ставится СУБД при реальном UPDATE/INSERT, реактивная, может вызвать deadlock.

*Сложность: Сложно | Категория: Блокировки*

#### 🟡 d3-fc-8: Что такое режим управления блокировкой данных?

**Ответ:** Свойство конфигурации: АвтоматическийИУправляемый. В Автоматическом — СУБД сама ставит блокировки. В Управляемом — разработчик сам вызывает ЗаблокироватьДанныеДляРедактирования(). Для кода, пишущего в регистры, нужен Управляемый режим.

*Сложность: Средне | Категория: Блокировки*

#### 🔴 d3-fc-9: Правильный порядок работы с блокировками?

**Ответ:** 1. ЗаблокироватьДанныеДляРедактирования() — блокируем ДО чтения. 2. Читаем данные. 3. Модифицируем. 4. Записываем. Блокировка снимается автоматически при фиксации транзакции. Если сначала прочитать, потом блокировать — потерянное обновление!

*Сложность: Сложно | Категория: Блокировки*

#### 🟡 d3-fc-10: Что такое тупиковое ожидание (таймаут блокировки)?

**Ответ:** Когда транзакция ждёт блокировку дольше разрешённого времени. 1С выдаёт ошибку: 'Таймаут ожидания блокировки...'. Решение: увеличить таймаут в настройках или сократить транзакции. По умолчанию таймаут = 0 (бесконечное ожидание, не рекомендуется).

*Сложность: Средне | Категория: Блокировки*

### СУБД для 1С-разработчика

#### 🔴 d3-fc-11: PostgreSQL: что такое shared_buffers и для чего?

**Ответ:** Память, выделяемая PostgreSQL для кэширования данных с диска. Рекомендация для 1С: 25% от RAM сервера. Если мало — СУБД читает с диска, медленно. Если слишком много — ОС не хватит памяти для других процессов и кэша файловой системы.

*Сложность: Сложно | Категория: СУБД*

#### 🔴 d3-fc-12: PostgreSQL: что такое autovacuum и почему он критичен для 1С?

**Ответ:** Автоматический процесс очистки и анализа таблиц. Удаляет «мёртвые» строки после UPDATE/DELETE. 1С активно удаляет и перезаписывает данные (проведение документов). Если autovacuum не успевает — таблицы разрастаются, запросы замедляются. Нужно мониторить!

*Сложность: Сложно | Категория: СУБД*

#### 🔴 d3-fc-13: Что такое WAL в PostgreSQL и зачем он нужен?

**Ответ:** Write-Ahead Log — журнал упреждающей записи. ВСЕ изменения сначала записываются в WAL, потом в данные. Нужен для восстановления после сбоя (crash recovery). Ускоряет запись (секвенциальный writes вместо random). Но если WAL на медленном диске — всё тормозит!

*Сложность: Сложно | Категория: СУБД*

#### 🔴 d3-fc-14: SQL Server: что такое lock escalation (эскалация блокировок)?

**Ответ:** Когда СУБД повышает уровень блокировки: вместо блокировки отдельных строк — блокирует целую страницу или таблицу. Это происходит когда слишком много строк заблокировано на одном уровне. Порог: ~5000 строк или более. В 1С это может вызывать массовые блокировки!

*Сложность: Сложно | Категория: СУБД*

#### 🟡 d3-fc-15: Какой мониторинг транзакций нужен в продакшене?

**Ответ:** Мониторинг блокировок (консоль администрирования 1С + pg_locks в PostgreSQL). Анализ зависших транзакций. Логирование долгих запросов. Активные подключения. Размер БД и свободное место. Автоматические алерты при росте блокировок.

*Сложность: Средне | Категория: СУБД*

### Диаграмма: Жизненный цикл управляемой транзакции

*Что происходит на каждом этапе явной транзакции в 1С*

#### Шаг 1: НачатьТранзакцию()

Разработчик явно открывает транзакцию. Платформа начинает отслеживать все изменения.
- Открывается транзакция в СУБД (BEGIN TRANSACTION)
- Все последующие записи попадут в эту транзакцию
- Вложенные транзакции НЕ поддерживаются — вызовет ошибку

#### Шаг 2: Чтение и блокировка данных

Сначала блокируем, потом читаем. Управляемые блокировки проверяются в кластере 1С.
- ЗаблокироватьДанныеДляРедактирования() — упреждающая блокировка
- Чтение данных только ПОСЛЕ установки блокировки
- Кластер 1С координирует блокировки между сеансами

#### Шаг 3: Модификация и запись в БД

Изменяем данные и записываем объекты. Всё в рамках одной транзакции.
- Объект.Записать() — отправка в СУБД
- Движения.Записать() — запись в регистры
- ВСЕ изменения — временные, пока не зафиксированы

#### Шаг 4: ЗафиксироватьТранзакцию() или ОтменитьТранзакцию()

Финал: либо подтверждаем все изменения, либо откатываем.
- COMMIT: все изменения становятся постоянными
- ROLLBACK: БД возвращается к состоянию до транзакции
- ПЕРЕМЕННЫЕ НЕ ОТКАТЫВАЮТСЯ — только данные в БД!

### Диаграмма: Как возникает взаимная блокировка (Deadlock)

*Визуализация классического сценария взаимной блокировки*

#### Шаг 1: Транзакция A: блокирует Ресурс 1

Пользователь А начинает редактирование Контрагента 'Озон'. Устанавливается управляемая блокировка.
- ЗаблокироватьДанныеДляРедактирования(Контрагент.Озон)
- Блокировка зарегистрирована в кластере 1С
- Теперь никто не может изменить Контрагента 'Озон'

#### Шаг 2: Транзакция B: блокирует Ресурс 2

Пользователь Б начинает редактирование Контрагента 'Яндекс'. Тоже ставит блокировку.
- ЗаблокироватьДанныеДляРедактирования(Контрагент.Яндекс)
- Блокировка успешно установлена — ресурс свободен
- А не знает, что Б заблокировал ресурс

#### Шаг 3: Транзакция A: ждёт Ресурс 2

Пользователь А пытается заблокировать Контрагента 'Яндекс'. Он уже занят Б!
- A ждёт освобождения Ресурса 2 (Контрагент.Яндекс)
- Блокировка Ресурса 2 удерживается Транзакцией B
- Ожидание...

#### Шаг 4: DEADLOCK! СУБД убивает одну транзакцию

Обе транзакции ждут друг друга бесконечно. СУБД обнаруживает цикл и прерывает одну из них.
- B тоже пытается заблокировать Ресурс 1 — ожидание
- Цикл: A ждёт B, B ждёт A = DEADLOCK
- СУБД выбирает «жертву» и откатывает одну транзакцию
- Решение: единый порядок блокировки ресурсов!

### Код: Явная транзакция: правильная структура

*Полный пример явной транзакции с блокировкой и обработкой ошибок*

```bsl
&НаСервере
Процедура ПереместитьТоварНаСклад(ДокументСсылка, ЦелевойСклад)

    НачатьТранзакцию();
    Попытка

        // 1. Блокируем регистр остатков
        Блокировка = Новый Массив;
        Блокировка.Добавить(ДокументСсылка);
        ЗаблокироватьДанныеДляРедактирования(
            РегистрНакопления.ТоварыНаСкладах,, Блокировка);

        // 2. Проверяем остатки (после блокировки!)
        Запрос = Новый Запрос;
        Запрос.Текст = "ВЫБРАТЬ КоличествоОстаток
            |ИЗ РегистрНакопления.ТоварыНаСкладах.Остатки
            |ГДЕ Склад = &Склад";

        // 3. Создаём движения
        Движение = Движения.ТоварыНаСкладах.Добавить();
        Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
        Движение.Склад = ЦелевойСклад;
        Движения.Записать();

        ЗафиксироватьТранзакцию();

    Исключение
        ОтменитьТранзакцию();
        ЗаписьЖурналаРегистрации("Ошибка перемещения: " + ОписаниеОшибки());
    КонецПопытки;

КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 4:** НачатьТранзакцию() — открываем явную транзакцию. Все записи БД будут атомарными.
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования — управляемая блокировка в КЛАСТЕРЕ 1С, не в СУБД.
- **Строка 13:** Проверяем остатки ПОСЛЕ блокировки — гарантируем консистентность данных.
- **Строка 20:** Движения.Добавить() — создаём запись движения в регистре накопления.
- **Строка 25:** ЗафиксироватьТранзакцию() — COMMIT. Все изменения в БД становятся постоянными.
- **Строка 28:** При ошибке — откатываем транзакцию. БД возвращается к состоянию ДО НачатьТранзакции().

**Квиз:** Что будет, если между строками 10 и 20 другой пользователь изменит остатки?

- ⬜ **A.** Данные будут противоречивыми
- ✅ **B.** Он получит ошибку таймаута — мы заблокировали данные
- ⬜ **C.** Его изменения пройдут, наши откатятся
- ⬜ **D.** Оба успешно запишут — управляемые блокировки не мешают

*Объяснение: Управляемая блокировка не позволит ему редактировать те же данные. Он будет ждать или получит ошибку.*

### Код: Предотвращение deadlock: порядок блокировки

*Как избежать взаимной блокировки — единый порядок блокировки ресурсов*

```bsl
// DEADLOCK: разный порядок блокировки!
// Сессия А: Блокирует Контрагента, потом Склад
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);

// Сессия Б: Блокирует Склад, потом Контрагента
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);
// -> DEADLOCK! А ждёт Б, Б ждёт А

// РЕШЕНИЕ: единый порядок блокировки
// ПРАВИЛО: Сначала Склады, потом Контрагенты

// Сессия А:
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);

// Сессия Б:
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
// -> Б ждёт, пока А не освободит Склады. Нет deadlock!
```

**Пояснения к строкам:**

- **Строка 2:** АНТИПАТТЕРН: каждый блокирует в своём порядке — прямой путь к deadlock.
- **Строка 4:** А заблокировал Контрагента, ждёт Склад — который уже заблокирован Б.
- **Строка 7:** Б заблокировал Склад, ждёт Контрагента — который уже заблокирован А. Классический deadlock.
- **Строка 12:** РЕШЕНИЕ: договоритесь о едином порядке блокировки ресурсов в команде!
- **Строка 20:** При одинаковом порядке: один из них будет ждать, но deadlock не возникнет.

**Квиз:** Как предотвратить взаимную блокировку между двумя сеансами?

- ⬜ **A.** Увеличить таймаут ожидания
- ✅ **B.** Использовать один и тот же порядок блокировки ресурсов
- ⬜ **C.** Переключиться на автоматический режим блокировок
- ⬜ **D.** Отключить управляемые блокировки

*Объяснение: Единый порядок блокировки — главный метод предотвращения deadlock. Все сеансы блокируют ресурсы в одном порядке.*

---

# День 4: День 4: Кластер и безопасность

> Архитектура кластера, RLS, аутентификация, производительность, ТЖ

**Фаза:** Фаза 4 | **Категорий:** 3 | **Карточек:** 15

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Кластер 1С | ~35мин | 5 | ✅ | ✅ | — |
| 2 | Безопасность и RLS | ~40мин | 5 | ✅ | ✅ | — |
| 3 | Производительность и ТЖ | ~30мин | 5 | — | — | — |

---

## Карточки

### Кластер 1С

#### 🔴 d4-fc-1: Из чего состоит кластер 1С:Предприятие?

**Ответ:** Центральный сервер (менеджер кластера) + рабочие серверы + серверы СУБД. Центральный сервер координирует блокировки, сеансы, фоновые задания. Рабочие серверы выполняют код. Рабочие процессы = отдельные процессы на рабочем сервере.

*Сложность: Сложно | Категория: Кластер*

#### 🟡 d4-fc-2: Что такое рабочий процесс и сколько их нужно?

**Ответ:** Рабочий процесс (working process) — отдельный процесс 1С, обрабатывающий пользовательские запросы. Рекомендация: 1 процесс на ядро CPU + 1-2 запасных. Минимум 2 для отказоустойчивости. Перегрузка = все процессы заняты = новые сеансы ждут.

*Сложность: Средне | Категория: Кластер*

#### 🟡 d4-fc-3: Что такое фоновые задания и регламентные задания?

**Ответ:** Фоновое задание: выполняется на сервере НЕ синхронно с клиентом (ФоновыеЗадания.Выполнить). Регламентное задание: выполняется по РАСПИСАНИЮ (КаждыйЧас, КаждуюМинуту). Регламентные задания создаются через конфигуратор, фоновые — из кода.

*Сложность: Средне | Категория: Кластер*

#### 🟡 d4-fc-4: Что такое ЦУП (Центр управления производством)?

**Ответ:** ЦУП — встроенный инструмент для мониторинга производительности. Показывает: планы запросов, взаимоблокировки, время ключевых операций. Позволяет разорвать deadlock напрямую из интерфейса. Нужен для диагностики проблем в продакшене.

*Сложность: Средне | Категория: Кластер*

#### 🔴 d4-fc-5: Как безопасно завершить зависший сеанс?

**Ответ:** Через консоль кластера 1С: Администрирование → Кластер → Сеансы. Удалить зависший сеанс. Также можно через код: ПолучитьСеансыИнформационнойБазы() → Удалить(). Сначала отключить от БД, потом завершить. Зависший сеанс держит транзакцию и блокировки!

*Сложность: Сложно | Категория: Кластер*

### Безопасность и RLS

#### 🔴 d4-fc-6: Что такое RLS и как работает?

**Ответ:** Record Level Security — ограничение доступа на уровне записей. Пользователь видит не все строки, а только разрешённые. Реализуется через параметры сеанса и условия в запросах. Влияет на производительность: добавляет WHERE к каждому запросу.

*Сложность: Сложно | Категория: Безопасность*

#### 🟡 d4-fc-7: Виды аутентификации в 1С?

**Ответ:** 1С-аутентификация (по списку пользователей 1С), ОС (AD/LDAP — через Windows/Active Directory), OIDC (OpenID Connect — современный стандарт, OAuth2). SSL/TLS шифрование клиент-серверного соединения. Для Big Tech чаще LDAP или OIDC.

*Сложность: Средне | Категория: Безопасность*

#### 🟡 d4-fc-8: Что такое профили групп доступа?

**Ответ:** Набор ограничений, которые применяются к группе пользователей. Профиль ограничивает: чтение/добавление/изменение/удаление по конкретным объектам метаданных. Позволяет гибко настраивать права без создания отдельных ролей. Типовые профили в БСП.

*Сложность: Средне | Категория: Безопасность*

#### 🔴 d4-fc-9: Разделение данных: общее vs по параметрам?

**Ответ:** Общее: все пользователи видят все данные (или через RLS). По параметрам: данные разделяются по значению (например, Организация). Пользователь видит только данные своей организации. Работает на уровне платформы, не требует RLS.

*Сложность: Сложно | Категория: Безопасность*

#### 🔴 d4-fc-10: Что такое разделение доступа на уровне записей?

**Ответ:** Это RLS с использованием параметров сеанса. Устанавливается в конфигураторе. Пример: пользователь видит только документы своего подразделения. Реализуется через УстановитьПараметрыСеанса() и условие в запросах.

*Сложность: Сложно | Категория: Безопасность*

### Производительность и ТЖ

#### 🔴 d4-fc-11: Как собрать Технологический журнал (ТЖ) и что там искать?

**Ответ:** ТЖ настраивается: Администрирование → Технологический журнал. События: фоновые задания, запросы, вызовы СУБД, ошибки. Ищем: длительные запросы (>1 сек), взаимоблокировки, частые вызовы сервера. Анализ через ЦУП или через GetQuoteTJ.xlsm.

*Сложность: Сложно | Категория: Производительность*

#### 🟡 d4-fc-12: Что такое профилировщик и когда его использовать?

**Ответ:** Инструмент в 1С для анализа производительности. Профилировщик CPU: какие функции потребляют процессор. Профилировщик вызовов DBMS: какие запросы к СУБД самые тяжёлые. Использовать при долгих операциях (>5 сек). Результат: метод → время → процент от общего.

*Сложность: Средне | Категория: Производительность*

#### 🔴 d4-fc-13: Как пакетно загружать большие объёмы данных?

**Ответ:** 1. Пакетная запись: НаборЗаписей вместо по одному. 2. НачатьТранзакцию → запись N записей → ЗафиксироватьТранзакцию. 3. batch_size = 5000 строк. 4. Очищать НаборЗаписей после каждого пакета. 5. Следить за размером журнала транзакций!

*Сложность: Сложно | Категория: Производительность*

#### 🔴 d4-fc-14: Что делать если 1С тормозит при 100+ пользователях?

**Ответ:** 1) ТЖ → определить узкие места. 2) Проверить блокировки (ЦУП). 3) Оптимизировать тяжёлые запросы. 4) Добавить рабочие процессы (при CPU 100%). 5) Проверить СУБД (индексы, статистика). 6) Масштабирование: добавить серверов в кластер.

*Сложность: Сложно | Категория: Производительность*

#### 🟡 d4-fc-15: Что такое GetQuoteTJ и как им пользоваться?

**Ответ:** GetQuoteTJ — внешний инструмент для чтения ТЖ. Получает данные из ТЖ через COM-соединение. Показывает: план запроса, время выполнения, контекст вызова. Альтернатива: читать ТЖ вручную через консоль кластера.

*Сложность: Средне | Категория: Производительность*

### Диаграмма: Структура кластера 1С:Предприятие

*Как устроен кластер серверов 1С и где выполняется код*

#### Шаг 1: Центральный сервер (Менеджер кластера)

Координирует все компоненты кластера. Хранит информацию о сеансах, блокировках, фоновых заданиях.
- Главный компонент кластера
- Хранит таблицу блокировок
- Координирует распределение нагрузки
- Без него кластер не работает

#### Шаг 2: Рабочие серверы

Серверы, на которых выполняется код 1С. Каждый содержит 1+ рабочих процессов.
- Код 1С выполняется ТОЛЬКО здесь
- 1 рабочий процесс = отдельный процесс в памяти
- Рекомендация: процессов = ядер CPU + запас
- Можно добавить серверов для масштабирования

#### Шаг 3: Серверы СУБД (PostgreSQL / SQL Server)

Хранят сами данные. Принимают SQL-запросы от рабочих процессов 1С.
- PostgreSQL или MS SQL Server
- Выполняют SQL от платформы 1С
- Индексы, статистика, autovacuum критичны
- Размер RAM влияет на shared_buffers

#### Шаг 4: Клиентские приложения

Тонкий клиент, веб-клиент, толстый клиент. Подключаются к рабочим серверам через центральный.
- Толстый: устарел, но встречается
- Тонкий: основной, лёгкий
- Веб-клиент: без установки, браузер
- Клиент отправляет запросы на рабочий сервер

### Диаграмма: Путь диагностики: от жалобы к решению

*Как найти и исправить узкое место в производительности 1С*

#### Шаг 1: Жалоба: '1С тормозит!'

Пользователь сообщает о медленной работе. Первый вопрос: конкретная операция или всё?
- Уточнить: какая операция медленная?
- Когда началось (всегда или после обновления)?
- Сколько пользователей затронуто?

#### Шаг 2: Сбор Технологического журнала

Включаем ТЖ для проблемного сеанса. События: запросы, вызовы СУБД, блокировки.
- Консоль кластера → ТЖ
- Фильтр по сеансу пользователя
- Минимальный набор событий для скорости
- Сохранить лог для анализа

#### Шаг 3: Анализ: ЦУП или GetQuoteTJ

Открываем ТЖ в инструменте анализа. Ищем: долгие запросы (>1 сек), блокировки, частые серверные вызовы.
- ЦУП: встроенный, мгновенный
- GetQuoteTJ: детальный анализ
- Ищем топ-N самых тяжёлых операций
- Проверяем план запроса

#### Шаг 4: Исправление и верификация

Оптимизируем: индексы, переписываем запрос, сокращаем транзакцию. Проверяем результат.
- Добавить индексы по фильтруемым полям
- Заменить запрос в цикле на пакетный
- Убрать избыточные блокировки
- Перезапустить и замерить время

### Код: Завершение зависших сеансов: серверный код

*Как найти и безопасно завершить зависшие сеансы через код*

```bsl
&НаСервере
Процедура ЗавершитьЗависшиеСеансы(МаксВремяМинут = 30)

    // 1. Получаем все активные сеансы
    Сеансы = ПолучитьСеансыИнформационнойБазы();
    Завершено = 0;

    Для Каждого Сеанс Из Сеансы Цикл
        Длительность = ТекущаяДата() - Сеанс.НачалоСеанса;

        Если Длительность > МаксВремяМинут * 60 Тогда
            Попытка
                // Отключаем от БД ПЕРВЫМ
                Сеанс.Завершить(Ложь); // отключить, не завершить
                Завершено = Завершено + 1;
            Исключение
                ЗаписьЖурналаРегистрации("Ошибка завершения: " + ОписаниеОшибки());
            КонецПопытки;
        КонецЕсли;
    КонецЦикла;

    Сообщить("Завершено сеансов: " + Завершено);
КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 5:** ПолучитьСеансыИнформационнойБазы() возвращает массив всех активных сеансов.
- **Строка 11:** Фильтруем по длительности — зависшие сеансы висят >30 минут бездействия.
- **Строка 14:** Сеанс.Завершить(Ложь) — отключает сеанс от БД, но не завершает процесс. Это безопаснее.
- **Строка 17:** Обязательно обрабатываем исключение — другие процессы могут зависеть от этого сеанса.

**Квиз:** Что делает Сеанс.Завершить(Ложь)?

- ⬜ **A.** Убивает процесс без вопросов
- ✅ **B.** Отключает сеанс от БД, но не завершает рабочий процесс
- ⬜ **C.** Блокирует сеанс на 5 минут
- ⬜ **D.** Ничего — параметр игнорируется

*Объяснение: Сеанс.Завершить(Истина) — полный kill. Сеанс.Завершить(Ложь) — только отключение от БД. Безопаснее для продакшена.*

### Код: RLS: настройка и влияние на запросы

*Как Record Level Security работает внутри запросов*

```bsl
// В конфигураторе: Справочник.Контрагенты
// Свойство: Ограничение доступа на уровне записей
// Тип: По параметру сеанса (Организация)

// Что происходит в запросе (автоматически):
ВЫБРАТЬ
    Контрагенты.Ссылка КАК Ссылка,
    Контрагенты.Наименование КАК Наименование
ИЗ Справочник.Контрагенты КАК Контрагенты
ГДЕ Контрагенты.Владелец В (&ДоступныеОрганизации)

// Платформа ДОБАВИЛА условие ГДЕ автоматически!
// &ДоступныеОрганизации — параметр сеанса RLS
// Устанавливается при старте сеанса

// Установка параметров сеанса:
УстановитьПараметрыСеанса(
    "ДоступныеОрганизации", МассивОрганизаций);
```

**Пояснения к строкам:**

- **Строка 3:** Разделение по параметру сеанса — самый простой и эффективный вид RLS.
- **Строка 10:** Платформа автоматически добавляет ГДЕ к КАЖДОМУ запросу к этому объекту!
- **Строка 12:** Это происходит прозрачно — разработчик не пишет это условие вручную.
- **Строка 18:** УстановитьПараметрыСеанса вызывается при старте приложения (например, в МодулеСеанса).

**Квиз:** Как RLS влияет на производительность запросов?

- ⬜ **A.** Никак не влияет
- ✅ **B.** Добавляет условие WHERE к каждому запросу — может замедлить
- ⬜ **C.** Только на вставку данных
- ⬜ **D.** Пересоздаёт индексы при каждом запросе

*Объяснение: RLS добавляет WHERE ко всем запросам. Без индекса по полю фильтрации — полный скан таблицы!*

---

# День 5: День 5: ACID и инфраструктура

> ACID, уровни изоляции, глубокие блокировки, кластер 1С, CAP

**Фаза:** Фаза 5 | **Категорий:** 3 | **Карточек:** 15

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | ACID и изоляция | ~40мин | 5 | ✅ | — | — |
| 2 | Глубокие блокировки | ~45мин | 5 | ✅ | ✅ | — |
| 3 | Инфраструктура и кластер | ~35мин | 5 | ✅ | ✅ | — |

---

## Карточки

### ACID и изоляция

#### 🟡 d5-fc-1: Что такое ACID?

**Ответ:** Atomicity: транзакция выполняется целиком или откатывается. Consistency: база всегда в корректном состоянии. Isolation: параллельные транзакции не мешают друг другу. Durability: зафиксированные данные сохраняются навсегда. Пример: банковский перевод — если снятие прошло, а зачисление нет, всё откатывается.

*Сложность: Средне | Категория: ACID и изоляция*

#### 🔴 d5-fc-2: 4 уровня изоляции SQL?

**Ответ:** Read Uncommitted: грязное чтение (почти не используется). Read Committed: неповторяемое чтение, фантомное возможно (по умолчанию в PostgreSQL). Repeatable Read: фантомное чтение, неповторяемое исключено (по умолчанию в MySQL InnoDB). Serializable: полная изоляция, все 3 проблемы исключены (уровень 1С).

*Сложность: Сложно | Категория: ACID и изоляция*

#### 🟡 d5-fc-3: Что такое грязное чтение (dirty read)?

**Ответ:** Чтение данных, изменённых транзакцией, которая ЕЩЁ НЕ зафиксирована. Если та транзакция откатится — прочитанные данные никогда не существовали. Пример: читаешь баланс 1000, другой переводит 500 и откатывает — ты видел 500, которых нет.

*Сложность: Средне | Категория: ACID и изоляция*

#### 🔴 d5-fc-4: Фантомное чтение (phantom read) vs неповторяемое (non-repeatable)?

**Ответ:** Неповторяемое: одна и та же строка меняется между чтениями (цена была 100, стала 0). Фантомное: при повторном запросе ПОЯВЛЯЮТСЯ новые строки (кто-то добавил товар за 60). Phantom = новые строки, Non-repeatable = изменённые существующие.

*Сложность: Сложно | Категория: ACID и изоляция*

#### 🟡 d5-fc-5: Правило 20 секунд для транзакций?

**Ответ:** Таймаут ожидания блокировки по умолчанию = 20с. Транзакция не должна превышать это время. Внутри транзакции: ТОЛЬКО запись и ответственное чтение. СНАРУЖИ: проверки заполнения, сложные расчёты, внешние вызовы, запросы без блокировки. Нарушение = каскадные ожидания.

*Сложность: Средне | Категория: ACID и изоляция*

### Глубокие блокировки

#### 🔴 d5-fc-6: Исключительная vs Разделяемая управляемая блокировка?

**Ответ:** Исключительная (РежимБлокировкиДанных.Исключительный): запрещает и читать, и писать. Используется при изменении остатков. Разделяемая (РежимБлокировкиДанных.Разделяемый): разрешает читать другим, запрещает писать. Совместима с другими разделяемыми. Платформа ставит разделяемые неявно при чтении наборов записей.

*Сложность: Сложно | Категория: Глубокие блокировки*

#### 🔴 d5-fc-7: Что такое пространство блокировки?

**Ответ:** Как измерения регистра сведений, по которым ставится блокировка. При блокировке по Ссылке документа блокируются ВСЕ поля: Склад, Организация и др. Можно сузить через УстановитьЗначение(). По полю = блокировать все документы с этим значением поля.

*Сложность: Сложно | Категория: Глубокие блокировки*

#### 🟡 d5-fc-8: Достаточные vs Избыточные блокировки?

**Ответ:** Достаточные: минимум необходимый для бизнес-логики. Избыточные: захватывают лишнее, снижая параллельность. Главная ошибка новичка: блокировать всё подряд. Пример избыточной: блокировать справочник при проведении документа, когда достаточно блокировать регистр.

*Сложность: Средне | Категория: Глубокие блокировки*

#### 🔴 d5-fc-9: 2 вида deadlock в 1С?

**Ответ:** 1) Разный порядок ресурсов: поток1 берёт X потом Y, поток2 берёт Y потом X. Решение: единый порядок захвата. 2) Повышение уровня: оба потока взяли разделяемую блокировку, потом оба пытаются повысить до исключительной — взаимный deadlock.

*Сложность: Сложно | Категория: Глубокие блокировки*

#### 🟡 d5-fc-10: Каскадное ожидание блокировок?

**Ответ:** Поток 2 ждёт поток 1, поток 3 ждёт поток 1, поток 4 ждёт поток 3 — цепочка. Увеличение таймаута НЕ помогает: пользователи воспринимают как зависание. Решение: синхронная очередь, изменение архитектуры, оптимизация кода (убрать лишние блокировки).

*Сложность: Средне | Категория: Глубокие блокировки*

### Инфраструктура и кластер

#### 🔴 d5-fc-11: 4 процесса кластера 1С?

**Ответ:** rphost: рабочий процесс, выполняет код 1С и запросы к СУБД. rmngr: менеджер кластера, распределяет нагрузку по rphost. ragent: агент сервера, запускает/останавливает rmngr и rphost. licsrv: сервер лицензирования, выдаёт программные лицензии. ragent НЕ входит в кластер!

*Сложность: Сложно | Категория: Инфраструктура*

#### 🔴 d5-fc-12: Как клиент подключается к кластеру?

**Ответ:** Клиент → rmngr (определяет наименее загруженный rphost) → rphost (выполняет код и запросы к СУБД) → СУБД (возвращает данные). rmngr использует алгоритмы балансировки: Round-Robin, Least Connections и др. Падение rphost не влияет на другие.

*Сложность: Сложно | Категория: Инфраструктура*

#### 🟡 d5-fc-13: Алгоритмы балансировки нагрузки кластера?

**Ответ:** Round-Robin: циклический по порядку (простой, но не учитывает загрузку). Weighted Round-Robin: с весом по производительности сервера. Least Connections: запрос на наименее загруженный rphost. Least Response Time: на самый быстрый сервер.

*Сложность: Средне | Категория: Инфраструктура*

#### 🟡 d5-fc-14: Установка 1С сервера на Ubuntu 22.04?

**Ответ:** 1) Локали: ru_RU.UTF-8, таймзона Europe/Moscow. 2) PostgreSQL 1C-ветки (зависимости + пакет). 3) Платформа: setup-full --mode unattended --enable-components. 4) systemd: srv1c.service + ras.service. 5) crserver для хранилища конфигураций. 6) Apache для веб-клиента (wsap24.so).

*Сложность: Средне | Категория: Инфраструктура*

#### 🔴 d5-fc-15: CAP-теорема: 3 свойства, только 2 одновременно?

**Ответ:** Consistency (согласованность): все видят одинаковые данные. Availability (доступность): всегда отвечает. Partition tolerance (разделение): работает при потере сети. Из 3 можно гарантировать только 2. При разделении сети 1С-кластер: либо перестаёт отвечать (CP), либо даёт устаревшие данные (AP).

*Сложность: Сложно | Категория: Инфраструктура*

### Диаграмма: ACID: 4 гарантии транзакций

*Как ACID обеспечивает целостность данных в 1С и любой СУБД*

#### Шаг 1: A — Atomicity (Атомарность)

Транзакция — неделимая единица. Все операции выполняются целиком или ни одной.
- Либо ВСЕ изменения зафиксированы (COMMIT)
- Либо ни одно (ROLLBACK)
- Пример: перевод денег — списание + зачисление вместе
- В 1С: ЗафиксироватьТранзакцию() или ОтменитьТранзакцию()

#### Шаг 2: C — Consistency (Согласованность)

База всегда переходит из одного корректного состояния в другое.
- Все правила и ограничения соблюдены
- Ссылочная целостность не нарушена
- Бизнес-правила конфигурации выполнены
- В 1С: проверка ПередЗаписью() + проводки

#### Шаг 3: I — Isolation (Изоляция)

Параллельные транзакции не мешают друг другу.
- 1С использует SERIALIZABLE — самый строгий уровень
- Грязное чтение, неповторяемое, фантомное — исключены
- Управляемые блокировки помогают 1С это гарантировать
- Цена: снижение параллелизма при высокой нагрузке

#### Шаг 4: D — Durability (Долговечность)

Зафиксированные данные сохраняются навсегда, даже при сбое питания.
- COMMIT = данные на диске (WAL в PostgreSQL)
- После подтверждения — данные не пропадут
- WAL (Write-Ahead Log) гарантирует это
- Crash recovery: СУБД восстанавливает по WAL

### Диаграмма: Два вида взаимных блокировок (deadlock)

*Как возникает deadlock и как его предотвратить*

#### Шаг 1: Вид 1: Разный порядок ресурсов

Поток 1 берёт ресурс X, потом хочет Y. Поток 2 взял Y, потом хочет X.
- Поток 1: Заблокировать(X) → ждёт Y
- Поток 2: Заблокировать(Y) → ждёт X
- Оба висят навсегда!
- Решение: единый порядок захвата всегда!

#### Шаг 2: Решение: Единый порядок захвата

Все потоки захватывают ресурсы в ОДНОМ порядке.
- Поток 1: Заблокировать(X) → Заблокировать(Y) → работает
- Поток 2: Заблокировать(X) → ждёт (или тоже Y) → работает
- Конфликт возможен, но НЕ взаимоблокировка
- Правило: сортируй ключи перед блокировкой!

#### Шаг 3: Вид 2: Повышение уровня изоляции

Оба потока взяли разделяемую блокировку (чтение), потом оба пытаются повысить до исключительной.
- Поток 1: Разделяемая(X) → хочет Исключительную(X)
- Поток 2: Разделяемая(X) → хочет Исключительную(X)
- Оба не могут повысить — deadlock!
- Решение: сразу ставить исключительную, если планируешь запись

#### Шаг 4: Как обнаружить deadlock

СУБД и 1С обнаруживают взаимоблокировку автоматически
- ТЖ: событие TLOCK, контекст DEADLOCK
- СУБД убивает одну из транзакций
- Побеждённая получает исключение
- ЦУП позволяет разорвать deadlock вручную

### Диаграмма: Путь запроса: Клиент → rmngr → rphost → СУБД

*Как запрос проходит через все процессы кластера 1С*

#### Шаг 1: Клиент подключается

Тонкий/веб-клиент отправляет запрос к центральному серверу кластера.
- Клиент знает адрес rmngr
- Запрос: 'Мне нужен рабочий процесс'
- Поддержание постоянного соединения

#### Шаг 2: rmngr — Менеджер кластера

Получает запрос и выбирает наименее загруженный рабочий процесс.
- Алгоритмы: Round-Robin, Least Connections
- Проверяет здоровье каждого rphost
- Если rphost упал — перенаправляет на другой
- Хранит таблицу сеансов и блокировок

#### Шаг 3: rphost — Рабочий процесс

Выполняет код 1С и генерирует SQL-запросы к СУБД.
- Один процесс = один поток выполнения
- Толстый слой: интерпретатор 1С
- Генерирует SQL из запросов 1С
- Контролирует транзакции и блокировки

#### Шаг 4: СУБД — PostgreSQL / SQL Server

Выполняет SQL, возвращает данные. rphost передаёт результат клиенту.
- Индексы, статистика, autovacuum
- СУБД ставит свои автоматические блокировки
- WAL для долговечности
- Результат: набор строк → rphost → клиент

### Код: Исключительная vs разделяемая: когда что ставить?

*Как правильно выбирать режим управляемой блокировки*

```bsl
&НаСервере
Процедура ПеремещениеОстатков(Склад, Номенклатура)
    // НУЖНО изменять остатки = исключительная!
    Блокировка = Новый Массив;
    ЭлементБлокировки = Новый Массив;
    ЭлементБлокировки.Добавить(Склад);
    Блокировка.Добавить(ЭлементБлокировки);

    // РежимБлокировкиДанных.ИсключительныйДляИзменения
    ЗаблокироватьДанныеДляРедактирования(
        РегистрНакопления.ОстаткиТоваров,
        РежимБлокировкиДанных.ИсключительныйДляИзменения,,
        Блокировка);

    // Теперь читаем и записываем остатки
    Набор = РегистрыНакопления.ОстаткиТоваров.СоздатьНаборЗаписей();
    Набор.Отбор.Склад.Установить(Склад);
    Для Каждого Запись Из Набор Цикл
        Запись.Количество = Запись.Количество - 1;
    КонецЦикла;
    Набор.Записать();
КонецПроцедуры

// Разделяемая = когда только ЧИТАЕМ данные
// Исключительная = когда ИЗМЕНЯЕМ данные
// Платформа сама ставит разделяемую при чтении наборов
```

**Пояснения к строкам:**

- **Строка 3:** Если планируешь ИЗМЕНЯТЬ остатки — нужна исключительная блокировка! Разделяемая не защитит от одновременного изменения.
- **Строка 9:** ИсключительныйДляИзменения — запрещает другим и читать, и писать. Гарантирует, что никто не изменит данные пока мы работаем.
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования — вызов в кластере 1С, не в СУБД. Кластер проверяет конфликты.
- **Строка 12:** ИсключительныйДляИзменения — правильный режим при записи движений регистра накопления.
- **Строка 24:** Разделяемая блокировка: разрешает другим читать, запрещает писать. Платформа ставит её НЕЯВНО при чтении наборов записей.

**Квиз:** Какой режим блокировки выбрать, если нужно ТОЛЬКО прочитать остатки для отчёта?

- ⬜ **A.** ИсключительныйДляИзменения — надёжнее
- ✅ **B.** Разделяемый — достаточно для чтения
- ⬜ **C.** Ничего — платформа сама разберётся
- ⬜ **D.** РежимБлокировкиДанных.Автоматический

*Объяснение: Для чтения достаточно разделяемой блокировки (платформа ставит её неявно). Исключительная нужна только при ИЗМЕНЕнии.*

### Код: CAP-теорема: что происходит при потере сети

*Как 1С-кластер ведёт себя при разделении сети*

```bsl
// CAP-теорема: из 3 свойств можно гарантировать только 2
// C = Consistency  (согласованность)
// A = Availability   (доступность)
// P = Partition     (устойчивость к разделению)

// СЦЕНАРИЙ: Потеря сети между серверами 1С

// Вариант CP (Consistency + Partition):
// Сервер Б отказывает отвечать, т.к. не может
// синхронизировать данные с Сервером А
// Результат: пользователи видят ошибку соединения

// Вариант AP (Availability + Partition):
// Сервер Б отвечает СТАРЫМИ данными из кэша
// Результат: пользователи работают, но данные
// могут быть НЕактуальными

// 1С выбирает CP: согласованность важнее доступности
// Данные всегда правильные, но может быть простои
```

**Пояснения к строкам:**

- **Строка 1:** CAP-теорема (Brewer, 2000): распределённая система не может гарантировать все 3 свойства одновременно при потере сети.
- **Строка 6:** Разделение сети (P) — неизбежно в реальных системах. Поэтому выбор всегда между C и A.
- **Строка 9:** CP-система: лучше unavailable, чем inconsistent. Пример: 1С, PostgreSQL.
- **Строка 14:** AP-система: лучше stale data, чем downtime. Пример: Cassandra, MongoDB.
- **Строка 18:** 1С-кластер выбирает CP. При разделении сети часть серверов перестаёт отвечать, но данные всегда согласованные.

**Квиз:** Что выбирает 1С при потере сети между серверами кластера?

- ⬜ **A.** Отвечает старыми данными (AP)
- ✅ **B.** Перестаёт отвечать до восстановления сети (CP)
- ⬜ **C.** Автоматически переключается на другой кластер
- ⬜ **D.** Перезапускает все рабочие процессы

*Объяснение: 1С — CP-система. При разделении сети: согласованность важнее доступности. Серверы перестают отвечать, пока сеть не восстановится.*

---

# День 6: День 6: Git, Web, хранилище

> Git, DevOps, REST API, временное хранилище, гарантии доставки

**Фаза:** Фаза 6 | **Категорий:** 3 | **Карточек:** 15

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Git для 1С | ~40мин | 5 | ✅ | — | — |
| 2 | Web/HTTP-сервисы | ~45мин | 5 | ✅ | ✅ | — |
| 3 | Временное хранилище | ~35мин | 5 | ✅ | ✅ | — |

---

## Карточки

### Git для 1С

#### 🔴 d6-fc-1: git reset --soft vs --hard vs --mixed?

**Ответ:** --soft: сбрасывает HEAD, но индекс и working directory НЕ меняются. Коммиты отменяются, изменения остаются в staged. --hard: сбрасывает HEAD + индекс + working directory. ВСЕ изменения теряются! Опасно. --mixed (по умолчанию): сбрасывает HEAD и индекс, но working directory сохраняет изменения в unstaged.

*Сложность: Сложно | Категория: Git*

#### 🔴 d6-fc-2: git reset vs git checkout — разница?

**Ответ:** reset перемещает HEAD (точку ветки) и обновляет индекс/working tree. checkout переключает ветки и обновляет файлы. reset = двигаешь коммит истории. checkout = переключаешься между ветками. git restore (новый) заменяет checkout для восстановления файлов.

*Сложность: Сложно | Категория: Git*

#### 🔴 d6-fc-3: git merge vs git rebase — когда что?

**Ответ:** merge: создаёт коммит-слияние с двумя родителями, сохраняет полную историю. Нечистая история, но безопасно. rebase: переписывает историю, перемещает твои коммиты поверх целевой ветки. Чистая история, но ПЕРЕПИСЫВАЕТ коммиты (не делай rebase на shared ветках!). В 1С-командах чаще merge.

*Сложность: Сложно | Категория: Git*

#### 🟡 d6-fc-4: git cherry-pick — что делает?

**Ответ:** Берёт конкретный коммит из одной ветки и применяет к текущей. Полезно: взять исправление бага из feature-ветки в master без слияния. Порядок: git cherry-pick abc123. Создаёт НОВЫЙ коммит с другим хешем. Если конфликт — нужно разрешить вручную.

*Сложность: Средне | Категория: Git*

#### 🔴 d6-fc-5: git reflog — что это и зачем?

**Ответ:** Журнал ВСЕХ движений HEAD, включая коммиты, которые 'потеряны' после reset --hard. reflog показывает, где был HEAD на каждом шаге. Спасение: после git reset --hard потерял коммит — найди хеш в reflog и сделай git reset --hard <хеш>. reflog хранит ~90 дней.

*Сложность: Сложно | Категория: Git*

### Web/HTTP-сервисы

#### 🟡 d6-fc-6: GET vs POST — отличия?

**Ответ:** GET: параметры в URL, кэшируемый, идемпотентный, ограничение длины URL. POST: тело запроса, не кэшируется, не идемпотентный, без ограничения размера. GET для получения данных, POST для изменения. В 1С: HTTPСервис.GET/POST или Автоматически для REST.

*Сложность: Средне | Категория: Web/HTTP*

#### 🔴 d6-fc-7: Веб-сервис vs HTTP-сервис в 1С?

**Ответ:** Веб-сервис (SOAP): XML-протокол, WSDL-описание, строго типизированный. Используется для интеграций по стандарту (1С ↔ 1С, 1С ↔ SAP). HTTP-сервис (REST): любой формат (JSON, XML, текст), гибкие URL-шаблоны (/customers/{id}), проще и быстрее. В Big Tech чаще HTTP-сервис + JSON.

*Сложность: Сложно | Категория: Web/HTTP*

#### 🟡 d6-fc-8: Что такое RESTful интерфейс?

**Ответ:** REST: Resource-based архитектура. 4 принципа: (1) Ресурсы = URL (/users, /orders/123). (2) HTTP-методы = действия (GET=читать, POST=создать, PUT=обновить, DELETE=удалить). (3) Статусы ответов (200, 201, 404, 500). (4) Без состояния (stateless). В 1С: HTTPСервис с GET/POST/PUT/DELETE.

*Сложность: Средне | Категория: Web/HTTP*

#### 🟡 d6-fc-9: Коды ответов HTTP: какие знать?

**Ответ:** 2xx: Успех (200 OK, 201 Created, 204 No Content). 3xx: Редирект (301 Moved, 302 Found). 4xx: Ошибка клиента (400 Bad Request, 401 Unauthorized, 403 Forbidden, 404 Not Found, 405 Method Not Allowed, 429 Too Many Requests). 5xx: Ошибка сервера (500 Internal Error, 502 Bad Gateway, 503 Service Unavailable).

*Сложность: Средне | Категория: Web/HTTP*

#### 🔴 d6-fc-10: EDT — что это и зачем 1С-разработчику?

**Ответ:** 1C:Enterprise Development Tools — плагин для Eclipse/Git. Хранение конфигурации в формате файлов (src/) — каждый объект = папка с XML-файлами. Позволяет использовать Git для версионирования 1С-конфигураций. Альтернатива: хранилище конфигураций 1С (которое НЕ поддерживает diff/merge нормально).

*Сложность: Сложно | Категория: Web/HTTP*

### Временное хранилище

#### 🟡 d6-fc-11: Что такое временное хранилище в 1С?

**Ответ:** Механизм для хранения данных на сервере между серверными вызовами. Данные сериализуются и живут на сервере, пока не удалены или не истёк таймаут. Нужен для передачи данных между разными формами, обработчиками и фоновыми заданиями. Ограничение: только сериализуемые типы!

*Сложность: Средне | Категория: Временное хранилище*

#### 🔴 d6-fc-12: Что можно и нельзя хранить во временном хранилище?

**Ответ:** МОЖНО: примитивы (Число, Строка, Дата), ТаблицыЗначений (после преобразования), Массивы, Структуры, Фиксированные структуры. НЕЛЬЗЯ: объекты 1С (СправочникОбъект, ДокументОбъект), Формы, РеквизитыФормы. Нужно сериализовать: ЗначениеВДвоичныеДанные() → ПоместитьВоВременноеХранилище().

*Сложность: Сложно | Категория: Временное хранилище*

#### 🟡 d6-fc-13: Когда очищается временное хранилище?

**Ответ:** 1) Явно: УдалитьИзВоВременногоХранилища(Адрес). 2) При завершении сеанса пользователя. 3) Через таймаут (настраивается в сервере 1С). 4) При перезапуске сервера. Если не удалять — растёт память сервера! Всегда удаляйте после использования.

*Сложность: Средне | Категория: Временное хранилище*

#### 🔴 d6-fc-14: Как передать данные между формами через временное хранилище?

**Ответ:** Форма А (клиент): Вызвать сервер, сервер кладёт данные в хранилище, возвращает Адрес. Форма А передаёт Адрес в Форму Б (через параметры открытия или глобальную переменную). Форма Б: ПолучитьИзВоВременногоХранилища(Адрес). Сервер десериализует и возвращает объект.

*Сложность: Сложно | Категория: Временное хранилище*

#### 🔴 d6-fc-15: Гарантии доставки: 3 модели?

**Ответ:** At-most-once: сообщение может быть доставлено 0 или 1 раз (возможна потеря). At-least-once: минимум 1 раз, возможен дубль. Exactly-once: ровно 1 раз — идеал. 1С + Kafka может обеспечить exactly-once через транзакционный producer. RabbitMQ: at-least-once по умолчанию.

*Сложность: Сложно | Категория: Временное хранилище*

### Диаграмма: Git reset: 3 режима работы

*Как reset --soft, --mixed и --hard влияют на коммиты, индекс и рабочую директорию*

#### Шаг 1: Три области Git

HEAD (указатель на коммит) → Index (staged) → Working Directory (файлы на диске). Reset двигает HEAD и опционально сбрасывает остальные.
- HEAD = текущий коммит ветки
- Index = 'git add' staging area
- Working Directory = реальные файлы
- reset двигает HEAD, а дальше зависит от флага

#### Шаг 2: --soft: только HEAD

Сдвигает HEAD на указанный коммит. Индекс и рабочая директория НЕ меняются.
- Коммиты отменяются, но изменения остаются staged
- Можно 'переделать' последний коммит: reset --soft HEAD~1
- Безопасный режим: ничего не теряется
- Полезно: squash коммитов без потери

#### Шаг 3: --mixed (по умолчанию)

Сдвигает HEAD и сбрасывает индекс. Рабочая директория сохраняется.
- Изменения становятся unstaged
- Файлы на диске НЕ меняются
- Можно сделать git add заново
- Самый частый сценарий: 'снять изменения из staged'

#### Шаг 4: --hard: всё сбрасывается

Сдвигает HEAD, сбрасывает индекс И рабочую директорию. Данные теряются!
- ВСЕ несохранённые изменения теряются!
- Используй git stash ПЕРЕД reset --hard
- Спасение: git reflog покажет старый HEAD
- git reset --hard <старый хеш> для восстановления

### Диаграмма: REST API в 1С: HTTP-сервис

*Как создать RESTful API через HTTP-сервис 1С*

#### Шаг 1: HTTP-сервис в конфигураторе

Создаём HTTP-сервис с шаблоном URL: /orders/{id}
- Корень: /ext/ords (настраивается)
- Шаблон: /orders/{OrderID}
- Методы: GET, POST, PUT, DELETE
- Каждый метод = отдельная функция

#### Шаг 2: GET /orders/{id} — получить заказ

Читаем заказ по ID из URL и возвращаем JSON.
- OrderID = ПараметрыЗапроса.Получить('OrderID')
- Запрос в регистры по OrderID
- Ответ: HTTP-ответ с Content-Type: application/json
- Статус 200 или 404 если не найден

#### Шаг 3: POST /orders — создать заказ

Получаем JSON тело, создаём документ, записываем.
- Тело запроса = строка JSON
- Читаем через Документ.ПолучитьТекст() в 1С
- JSON-парсинг: ПрочитатьJSON(), ТекстJSON
- Создаём и записываем документ, возврат 201

#### Шаг 4: DELETE /orders/{id} — удалить

Находим документ по ID, помечаем на удаление, записываем.
- Находим документ по UUID из OrderID
- УстановитьПометкуУдаления(Истина)
- Записать(). Возврат 204 No Content
- Или 403 если запрещено правами

### Диаграмма: Передача данных между формами через сервер

*Как временное хранилище связывает две разные формы через сервер*

#### Шаг 1: Форма А: пользователь нажимает кнопку

Клиентский обработчик готовит данные и вызывает серверный метод.
- Данные собираются на клиенте
- Вызываем серверный метод
- Передаём сериализуемые данные

#### Шаг 2: Сервер: сериализация и помещение в хранилище

Сервер преобразует данные в двоичный формат и сохраняет во временное хранилище.
- ЗначениеВДвоичныеДанные(Данные)
- Адрес = ПоместитьВоВременноеХранилище()
- Возвращаем Адрес (строка) клиенту

#### Шаг 3: Форма А открывает Форму Б с Адресом

Форма Б получает Адрес через параметр открытия формы.
- ОткрытьФорму('Форма.ФормаБ', , , Адрес)
- Адрес = строка-идентификатор в хранилище
- Форма Б получает его в параметрах

#### Шаг 4: Форма Б: извлечение из хранилища

Серверная процедура формы Б извлекает данные по Адресу и десериализует.
- ПолучитьИзВоВременногоХранилища(Адрес)
- ЗначениеИзДвоичныхДанных(ДвоичныеДанные)
- НЕ ЗАБЫТЬ: УдалитьИзВоВременногоХранилища(Адрес)!

### Код: HTTP-сервис: обработка GET-запроса

*Пример GET-обработчика HTTP-сервиса в 1С*

```bsl
Функция ЗаказыGET(Запрос)
    // Получаем ID из URL шаблона: /orders/{id}
    OrderID = Запрос.ПараметрыЗапроса.Получить('id');

    // Ищем документ
    Документ = Документы.Заказ.НайтиПоРеквизиту(
        'UUID', OrderID);

    Если Документ.Пустая() Тогда
        // Возвращаем 404 Not Found
        Ответ = Новый HTTPСервисОтвет(404);
        Ответ.УстановитьТелоСтрокой('Not Found');
        Возврат Ответ;
    КонецЕсли;

    // Формируем JSON ответ
    JSON = Новый ЗаписьJSON;
    JSON.Значение.Вставить('id', Документ.Ссылка.УникальныйИдентификатор());
    JSON.Значение.Вставить('number', Документ.Номер);
    JSON.Значение.Вставить('total', Документ.Сумма);

    // Возвращаем 200 OK с JSON
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.УстановитьТелоСтрокой(ЗаписьJSON.ВСтроку());
    Возврат Ответ;
```

**Пояснения к строкам:**

- **Строка 3:** ПараметрыЗапроса.Получить('id') — извлекает параметр из URL шаблона /orders/{id}. Имя совпадает с шаблоном.
- **Строка 11:** HTTPСервисОтвет(404) — создаём ответ со статусом 404. Правильный REST: если ресурс не найден — 404, не 200 с пустым телом.
- **Строка 18:** Ссылка.УникальныйИдентификатор() — UUID документа. Используем для внешних идентификаторов, а не внутренний номер.
- **Строка 23:** HTTPСервисОтвет(200) — статус 200 OK. В REST: GET успешен = 200, создание = 201, удаление = 204.

**Квиз:** Какой HTTP-статус нужно вернуть, если ресурс не найден?

- ⬜ **A.** 200 OK с пустым телом
- ⬜ **B.** 400 Bad Request
- ✅ **C.** 404 Not Found
- ⬜ **D.** 500 Internal Server Error

*Объяснение: 404 Not Found — стандартный REST ответ для несуществующего ресурса. 200 = успех (ресурс есть), 400 = ошибка в запросе клиента.*

### Код: Передача ТаблицыЗначений через временное хранилище

*Как правильно сериализовать и передать ТаблицуЗначений между формами*

```bsl
&НаСервере
Функция ПодготовитьДанныеДляДругойФормы(ТаблицаДанных)
    // 1. Сериализуем ТаблицуЗначений
    ДвоичныеДанные = ЗначениеВДвоичныеДанные(ТаблицаДанных);

    // 2. Помещаем во временное хранилище
    Адрес = ПоместитьВоВременноеХранилище(ДвоичныеДанные);

    // 3. Возвращаем АДРЕС (строку!) клиенту
    Возврат Адрес;
КонецФункции

&НаСервере
Функция ПрочитатьДанныеИзХранилища(Адрес)
    // 4. Извлекаем из хранилища
    ДвоичныеДанные = ПолучитьИзВоВременногоХранилища(Адрес);

    // 5. Десериализуем обратно
    ТаблицаДанных = ЗначениеИзДвоичныхДанных(ДвоичныеДанные);

    // 6. ОБЯЗАТЕЛЬНО удаляем!
    УдалитьИзВоВременногоХранилища(Адрес);

    Возврат ТаблицаДанных;
КонецФункции
```

**Пояснения к строкам:**

- **Строка 4:** ЗначениеВДвоичныеДанные() — сериализует ЛЮБОЙ сериализуемый объект в двоичные данные. Работает с ТаблицамиЗначений, Массивами, Структурами.
- **Строка 7:** ПоместитьВоВременноеХранилище() — сохраняет данные на сервере. Возвращает уникальный Адрес (строка).
- **Строка 10:** Адрес — это СТРОКА, её можно передать на клиент и в другую форму! Сама ТаблицуЗначений на клиент не передаётся.
- **Строка 16:** ПолучитьИзВоВременногоХранилища() — извлекает данные по адресу. Возвращает ДвоичныеДанные.
- **Строка 21:** КРИТИЧЕСКИ ВАЖНО:如果不 удалить, данные будут висеть в памяти сервера до таймаута. Утечка памяти!

**Квиз:** Почему нельзя напрямую передать ТаблицуЗначений на клиент?

- ⬜ **A.** Потому что она слишком большая
- ✅ **B.** Потому что ТаблицуЗначений — серверный объект, не сериализуется автоматически
- ⬜ **C.** Потому что платформа не поддерживает таблицы
- ⬜ **D.** Можно, просто используй(Return)

*Объяснение: ТаблицуЗначений — серверный контекст. Для передачи нужно: сериализовать (ЗначениеВДвоичныеДанные), поместить в хранилище, передать адрес, десериализовать.*

---

# День 7: День 7: Паттерны и практика

> Паттерны разработки, стандарты кода, обмен данными, типовые задачи

**Фаза:** Фаза 7 | **Категорий:** 3 | **Карточек:** 15

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Паттерны разработки 1С | ~40мин | 5 | ✅ | ✅ | — |
| 2 | Стандарты кода | ~35мин | 5 | ✅ | — | — |
| 3 | Обмен данными и типовые задачи | ~40мин | 5 | ✅ | ✅ | — |

---

## Карточки

### Паттерны разработки 1С

#### 🟡 d7-fc-1: Паттерн 'Обработка заполнения' — когда использовать?

**Ответ:** Когда у объекта есть логика автозаполнения при создании или изменении. Пример: заполнение реквизитов по умолчанию, копирование данных из другого объекта. Вызывается через ОбработкаЗаполнения() в модуле объекта. Параметр: ДанныеЗаполнения (может быть строка, ссылка, ТЗ). В БСП есть стандартный механизм.

*Сложность: Средне | Категория: Паттерны*

#### 🟡 d7-fc-2: Паттерн 'Подписки на события' — в чём польза?

**Ответ:** Позволяет реагировать на события объектов БЕЗ изменения их модулей. Подписка = обработчик события в отдельном модуле. Пример: при записи ЛЮБОГО документа — записать лог. Плюс: не нужно менять код конфигурации при обновлении. Минус: сложно отладить — не видно из модуля объекта.

*Сложность: Средне | Категория: Паттерны*

#### 🟡 d7-fc-3: Паттерн 'Обработчики ожидания' — что это?

**Ответ:** Фоновые задания, которые запускаются ПЕРИОДИЧЕСКИ на сервере. Пример: каждый час проверять новые заказы и отправлять уведомления. АвтоОжидание = True/False. ОстановитьОбработкуОжидания() для отмены. Работает в фоновом процессе, НЕ блокирует пользователей.

*Сложность: Средне | Категория: Паттерны*

#### 🟡 d7-fc-4: Паттерн 'Ввод на основании' — как реализовать?

**Ответ:** 1. В конфигураторе: добавить реквизит 'Основание' (тип любой документ). 2. Создать команду 'Ввести на основании'. 3. В обработчике: СоздатьДокумент().Основание = ТекущийДокумент. Заполнить реквизиты из основания. Платформа автоматически создаёт связь 'Документ-Основание'.

*Сложность: Средне | Категория: Паттерны*

#### 🔴 d7-fc-5: Что такое Расширения (Extensions) в 1С?

**Ответ:** Механизм модификации конфигурации БЕЗ изменения основного кода. Добавляемые объекты: формы, обработчики, реквизиты, подписки. Преимущества: (1) Безопасное обновление. (2) Можно отключить/удалить. (3) Множественные расширения одновременно. Используется для кастомизации типовых конфигураций.

*Сложность: Сложно | Категория: Паттерны*

### Стандарты кода

#### 🟡 d7-fc-6: Соглашения по именованию в 1С: основные правила?

**Ответ:** Переменные: camelCase (текущийЭлемент, массивДанных). Процедуры/Функции: CadaPalabra (ЗаписатьДокумент, ПолучитьОстатки). Реквизиты: CadaPalabra (Наименование, Контрагент). Локальные переменные: нижнийРегистр или camelCase. Методы БСП: СтандартнаяОбработка(), ЗначениеЗаполнено().

*Сложность: Средне | Категория: Стандарты кода*

#### 🔴 d7-fc-7: Почему нельзя использовать Попытка/Исключение для управления потоком?

**Ответ:** Попытка/Исключение — ТОЛЬКО для обработки ИСКЛЮЧИТЕЛЬНЫХ ситуаций (ошибок). НЕ для: проверки существования элемента, управления бизнес-логикой, возврата значений. Ловушка: в Исключении нет доступа к локальным переменным Попытки (только если объявлены ДО). Альтернатива: НайтиПоРеквизиту() и проверка Пустая().

*Сложность: Сложно | Категория: Стандарты кода*

#### 🔴 d7-fc-8: Как правильно организовать структуру модулей?

**Ответ:** МодульОбъекта: бизнес-логика объекта. МодульФормы: только UI-логика. МодульМенеджера: общие методы для работы с объектом (ПодготовитьПечать, ЗаполнитьНаОсновании). ОбщиеМодули: разделяемые (сервер+клиент) и серверные. Не ставьте &НаСервере в серверных общих модулях!

*Сложность: Сложно | Категория: Стандарты кода*

#### 🔴 d7-fc-9: Разделяемые общие модули vs серверные — разница?

**Ответ:** Разделяемые: вызываются и с клиента, и с сервера. Компилируются в обеих контекстах. Ограничения: нельзя использовать серверные объекты напрямую. Серверные: ТОЛЬКО сервер. Можно любые объекты БД. Правило: если метод нужен на клиенте — разделяемый. Если только для расчётов на сервере — серверный.

*Сложность: Сложно | Категория: Стандарты кода*

#### 🟡 d7-fc-10: Что такое УсловноеОформление и как влияет на производительность?

**Ответ:** Механизм раскраски ячеек формы. Правила задаются в конфигураторе или программно. ПРИНЦИП: платформа формирует ОДИН запрос к БД для получения данных оформления. Но если правил >10-15 — запрос усложняется, замедляет открытие формы. Лучшая практика: не более 10 правил, группировать по условиям.

*Сложность: Средне | Категория: Стандарты кода*

### Обмен данными и типовые задачи

#### 🟡 d7-fc-11: Что такое план обмена в 1С?

**Ответ:** Объект для настройки обмена данными между узлами (информационными базами). Каждый узел = отдельная ИБ. План обмена определяет: какие объекты участвуют, какие реквизиты передаются. Автоматическая регистрация изменений. NodeCode и NodeName — идентификаторы узла.

*Сложность: Средне | Категория: Обмен данными*

#### 🔴 d7-fc-12: Как работает распределённая информация (РИБ)?

**Ответ:** РИБ = Распределённая Информационная База. Основана на планах обмена. Механизм: (1) Создаём план обмена с узлами. (2) Записи регистрации изменений отслеживают изменения. (3) Выгрузка = XML/JSON файл с изменениями. (4) Загрузка в приёмном узле. Поддержка приоритетов узлов и авторегламентация.

*Сложность: Сложно | Категория: Обмен данными*

#### 🔴 d7-fc-13: Формат обмена: XML vs JSON vs XDTO?

**Ответ:** XML: классический формат 1С, универсальный, но тяжёлый. JSON: лёгкий, популярен в web-интеграциях, 1С поддерживает с 8.3.6+. XDTO (XML Data Type Object): фабрика типов, строгая типизация, используется внутри 1С для SOAP. Для REST API: JSON. Для 1С-1С: XML. Для SOAP: XDTO.

*Сложность: Сложно | Категория: Обмен данными*

#### 🔴 d7-fc-14: Как сделать массовую загрузку данных эффективно?

**Ответ:** (1) Загрузить Excel/CSV в ТаблицуЗначений. (2) Очистить регистр остатков одним НаборЗаписей. (3) Загрузить данные пакетами по 1000 записей. (4) Использовать ОбменДанными.Загрузить() для быстрой загрузки. (5) Отключить регламентные задания на время загрузки. Никогда не записывать по одной записи в цикле!

*Сложность: Сложно | Категория: Типовые задачи*

#### 🟡 d7-fc-15: Как организовать печать документов в 1С?

**Ответ:** (1) Макет = ТабличныйДокумент с областями. (2) Область = Макет.ПолучитьОбласть('Заголовок'). (3) Заполняем параметры области. (4) НомерСтроки = ТД.Вывести(Область). (5) Вывод секций: ВерхнийКолонтитул, НижнийКолонтитул. УправлениеПечатью.НоваяСтраница(). БСП имеет стандартный механизм печати.

*Сложность: Средне | Категория: Типовые задачи*

### Диаграмма: Расширения: безопасная модификация типовой конфигурации

*Как расширения позволяют кастомизировать без изменения основного кода*

#### Шаг 1: Создание расширения

Добавляем расширение в конфигурацию. Выбираем, что расширяем.
- Конфигуратор: Расширения → Добавить
- Привязка к конфигурации (по имени или по составу)
- Можно: формы, реквизиты, табличные части
- Можно: подписки, обработчики событий

#### Шаг 2: Модификация форм

Расширяем форму типового объекта: добавляем элементы, обработчики.
- Добавляем реквизиты и элементы на форму
- Создаём обработчики &НаКлиенте
- Переопределяем существующие процедуры
- Можно скрывать стандартные элементы

#### Шаг 3: Расширение модулей

Подключаем свои обработчики к событиям типовых модулей.
- &ИзменениеИКонтроль() — внедрение в метод
- &Вместо() — полная замена метода
- &Перед() / &После() — до и после метода
- Безопасно при обновлении типовой!

#### Шаг 4: Управление и доставка

Расширения можно включать/выключать без остановки работы.
- Включить/Отключить в меню поддержки
- Несколько расширений одновременно
- Обновление типовой не затрагивает расширения
- cf-файлы расширений хранятся отдельно

### Диаграмма: Архитектура модулей 1С: кто за что отвечает

*Правильное разделение логики по модулям объекта*

#### Шаг 1: Модуль менеджера

Общие методы для работы с объектом, не зависящие от конкретного экземпляра.
- ПодготовитьПечать(), ЗаполнитьНаОсновании()
- НайтиПоРеквизиту(), СоздатьЭлемент()
-  НЕ имеет контекста конкретного объекта
- Вызывается: Справочники.Номенклатура.Метод()

#### Шаг 2: Модуль объекта

Бизнес-логика конкретного экземпляра. Обработчики событий объекта.
- ПередЗаписью(), ПриЗаписи(), ОбработкаЗаполнения()
- ОбработкаПроведения() — формирует движения
- this = текущий объект (ДокументОбъект)
- Хранит логику проверки и расчётов

#### Шаг 3: Модуль формы

ТОЛЬКО UI-логика: открытие, закрытие, взаимодействие с элементами.
- &НаКлиенте и &НаСервере обработчики
- ПриСозданииНаСервере() — инициализация
- НЕ ставьте бизнес-логику сюда!
- События элементов: ПриИзменении, ПриВыборе

#### Шаг 4: Общие модули

Переиспользуемые функции для всей конфигурации.
- Серверные: только сервер, любые объекты БД
- Разделяемые: клиент + сервер, ограничения!
- Глобальные: доступны без уточнения
- Правило: один модуль = одна ответственность

### Диаграмма: Эффективная массовая загрузка: правильный подход

*Как загрузить 100 000 записей без блокировки базы на часы*

#### Шаг 1: Чтение исходных данных

Загружаем Excel/CSV в ТаблицуЗначений или Массив. НЕ читаем построчно!
- ТЗ = ЗначениеИзФайла('путь') или CSV чтение
- Всё в памяти — быстрый доступ
- Валидация данных ДО записи в БД
- Фильтруем дубли и некорректные строки

#### Шаг 2: Очистка существующих данных

Очищаем регистры и справочники ОДНИМ набором, не по одной записи.
- НаборЗаписей = Регистр.СоздатьНаборЗаписей()
- НаборЗаписей.Записать() — одна операция!
- Справочники: пометка удаления + Записать()
- НЕ удаляйте по одной записи в цикле!

#### Шаг 3: Загрузка пакетами

Записываем данные пакетами по 500-1000 записей.
- НачатьТранзакцию() перед пакетом
- Записываем 500 элементов
- ЗафиксироватьТранзакцию()
- Повторяем для следующего пакета

#### Шаг 4: Финализация

Включаем регламентные задания, обновляем статистику СУБД.
- Анализ().ОбновитьСтатистику() в PostgreSQL
- Включаем регламентные задания обратно
- Проверяем количество загруженных записей
- Логируем результат в ЖурналРегистрации

### Код: Расширение: добавление обработчика в типовую форму

*Как через расширение добавить свою логику в типовую конфигурацию*

```bsl
// Расширение: модуль формы документа
&НаКлиенте
Процедура Расш1_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

    // &Перед — наш код выполнится ДО типового
    // СтандартнаяОбработка = Ложь; // чтобы отменить типовой

    // Добавляем свой элемент на форму
    Элемент = Форма.Элементы.Добавить('ДопПоле', Тип('ПолеФормы'));
    Элемент.Вид = ВидПоляФормы.ПолеВвода;
    Элемент.Заголовок = 'Дополнительный комментарий';
    Элемент.ПутьКДанным = 'Объект.КомментарийРасш1';

КонецПроцедуры

// &Вместо — ПОЛНАЯ замена типового метода
Процедура Расш1_Записать(Отказ, ПараметрыЗаписи)
    // Наш код вместо типового
    Объект.КомментарийРасш1 = 'Из расширения';

    // ПродолжитьВызов() — вызовет типовой метод
    ПродолжитьВызов(Отказ, ПараметрыЗаписи);
КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 3:** Расш1_ — префикс расширения. Платформа связывает метод по префиксу и имени.
- **Строка 5:** &Перед = выполнить ДО типового. Можно подготовить данные или отменить (СтандартнаяОбработка = Ложь).
- **Строка 9:** Элементы.Добавить() — программное создание элемента формы в расширении.
- **Строка 12:** ПутьКДанным связывает элемент с реквизитом (который добавлен через расширение конфигурации).
- **Строка 16:** &Вместо = полная замена типового метода. Но можно вызвать ПродолжитьВызов() для оригинального.
- **Строка 22:** ПродолжитьВызов() — вызывает типовой метод, который мы заменили. Позволяет добавить логику ДО и ПОСЛЕ.

**Квиз:** Что делает ПродолжитьВызов() в расширении?

- ⬜ **A.** Завершает текущий метод
- ✅ **B.** Вызывает типовой метод, который был заменён через &Вместо
- ⬜ **C.** Создаёт новый фоновый процесс
- ⬜ **D.** Перезапускает форму

*Объяснение: ПродолжитьВызов() вызывает оригинальный типовой метод. Используется с &Вместо для добавления логики до/после типового кода.*

### Код: Печать документа: макеты и области

*Как организовать печать через ТабличныйДокумент и области макета*

```bsl
&НаСервере
Функция ПечатьДокумента(ДокументСсылка)

    // 1. Получаем макет из менеджера
    Макет = Документы.Заказ.ПолучитьМакет('ПечатьЗаказа');
    ТД = Новый ТабличныйДокумент;

    // 2. Получаем области макета
    Заголовок = Макет.ПолучитьОбласть('Заголовок');
    Заголовок.Параметры.Номер = ДокументСсылка.Номер;
    Заголовок.Параметры.Дата = ДокументСсылка.Дата;
    ТД.Вывести(Заголовок);

    // 3. Выводим строки табличной части
    ОбластьСтрока = Макет.ПолучитьОбласть('Строка');
    НомерСтроки = 0;
    Для Каждого СтрокаТЧ Из ДокументСсылка.Товары Цикл
        НомерСтроки = НомерСтроки + 1;
        ОбластьСтрока.Параметры.Заполнить(СтрокаТЧ);
        ТД.Вывести(ОбластьСтрока);
    КонецЦикла;

    // 4. Подвал с итогами
    Подвал = Макет.ПолучитьОбласть('Подвал');
    Подвал.Параметры.Итого = ДокументСсылка.СуммаДокумента;
    ТД.Вывести(Подвал);

    Возврат ТД;
КонецФункции
```

**Пояснения к строкам:**

- **Строка 5:** ПолучитьМакет() возвращает ТабличныйДокумент-шаблон. Имя макета задаётся в конфигураторе.
- **Строка 9:** ПолучитьОбласть('Имя') — извлекает именованную область из макета. Области задаются в макете.
- **Строка 15:** Получаем область шаблона ОДИН раз, потом заполняем параметры в цикле. Это эффективнее!
- **Строка 19:** Заполнить() автоматически заполняет параметры области по совпадению имён с свойствами объекта.

**Квиз:** Что возвращает ПолучитьОбласть() из макета?

- ⬜ **A.** ТабличныйДокумент (копию макета)
- ✅ **B.** ОбластьЯчеекТабличногоДокумента (шаблон строки)
- ⬜ **C.** Строку с именем области
- ⬜ **D.** Массив ячеек

*Объяснение: ПолучитьОбласть() возвращает ОбластьЯчеекТабличногоДокумента — это шаблон, в который можно подставить параметры.*

---

# День 8: День 8: Структура и регистры

> Организации, подразделения, РегистрСведений, типовые задачи, собеседование

**Фаза:** Фаза 8 | **Категорий:** 4 | **Карточек:** 30

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Организационная структура | ~50мин | 5 | ✅ | ✅ | 2 |
| 2 | Регистры сведений | ~60мин | 6 | ✅ | ✅ | 1 |
| 3 | Типовые задачи | ~40мин | 5 | — | — | — |
| 4 | Собеседование: типовые вопросы | ~90мин | 12 | — | — | — |

---

## Карточки

### Организационная структура

#### 🔴 d8-fc-1: Задача: Организация имеет подразделения, у сотрудника должность. Как связать Организация<->Подразделение<->Сотрудник<->Должность?

**Ответ:** РегистрСведений с уникальностью измерений. Измерения: Организация, Подразделение, Сотрудник, Должность. Уникальность измерений гарантирует, что одна связка не повторится. НЕЗависимый справочник не даёт такой гарантии!

*Сложность: Сложно | Категория: Орг. структура*

#### 🔴 d8-fc-2: Почему НЕ независимый справочник для связки Организация<->Подразделение?

**Ответ:** Независимый справочник позволяет создавать несколько записей с одинаковыми реквизитами. Нет гарантии уникальности связки. Если нужно чтобы на одну дату у одного сотрудника в одном подразделении была только одна должность - только РегистрСведений с уникальностью измерений решает это на уровне платформы.

*Сложность: Сложно | Категория: Орг. структура*

#### 🟡 d8-fc-3: Что такое РегистрСведений с уникальностью измерений?

**Ответ:** Регистр сведений, у которого установлен флаг 'Уникальность измерений' в конфигураторе. Комбинация значений измерений должна быть уникальной. При попытке записать дублирующую комбинацию - платформа выдаст ошибку. Используется для хранения связей многие-ко-многим с ограничением уникальности.

*Сложность: Средне | Категория: Орг. структура*

#### 🟡 d8-fc-4: РегистрСведений: какой режим записи по умолчанию?

**Ответ:** Независимый - записи можно добавлять/удалять в любой момент. Подчинён регистратору - записи привязаны к документу и управляются через движения. Для задачи Организация<->Подразделение нужен НЕЗАВИСИМЫЙ режим записи, т.к. связь не привязана к конкретному документу.

*Сложность: Средне | Категория: Орг. структура*

#### 🟡 d8-fc-5: Когда использовать РегистрСведений, а когда Справочник?

**Ответ:** Справочник - для объектов реального мира (Контрагенты, Номенклатура). РегистрСведений - для связей и дополнительной информации (цены, курсы валют, принадлежность к подразделению). Если данные описывают СВОЙСТВО или СВЯЗЬ - РегистрСведений. Если самостоятельная сущность - Справочник.

*Сложность: Средне | Категория: Орг. структура*

### Регистры сведений

#### 🟡 d8-fc-6: Виды РегистровСведений по периодичности?

**Ответ:** Непериодический - данные не зависят от времени (принадлежность к группе). Периодический - данные меняются во времени (цены, курсы). Период: День, Месяц, Квартал, Год. Периодический с уникальностью = только одна запись на комбинацию в пределах периода.

*Сложность: Средне | Категория: Регистры сведений*

#### 🔴 d8-fc-7: Как записать РегистрСведений independently?

**Ответ:** НаборЗаписей = РегистрыСведений.МойРегистр.СоздатьНаборЗаписей();
НаборЗаписей.Отбор.Организация.Установить(Орг);
НаборЗаписей.Прочитать();
НоваяЗапись = НаборЗаписей.Добавить();
НоваяЗапись.Организация = Орг;
НоваяЗапись.Записать();

*Сложность: Сложно | Категория: Регистры сведений*

#### 🔴 d8-fc-8: Как efficiently обновить записи РегистраСведений?

**Ответ:** 1. СоздатьНаборЗаписей() + установить отбор. 2. Прочитать() - получить текущие записи. 3. Добавить() новые / удалить() ненужные. 4. Записать() - атомарно. ОДИН НаборЗаписей = одна транзакция. НЕ делайте отдельные Записать() для каждой записи!

*Сложность: Сложно | Категория: Регистры сведений*

#### 🔴 d8-fc-9: СрезПоследних в РегистреСведений - что это?

**Ответ:** Виртуальная таблица, которая показывает ПОСЛЕДНИЕ записанные значения измерений на указанную дату. Для периодических регистров: СрезПоследних(Период, Условие). Возвращает самые свежие данные. Аналог СрезПервых - первые записанные значения.

*Сложность: Сложно | Категория: Регистры сведений*

#### 🟡 d8-fc-10: Что такое РегистрСведений 'Цены номенклатуры' в типовых?

**Ответ:** Периодический РегистрСведений. Измерения: Номенклатура, ТипЦен. Ресурс: Цена. Период: День. Используется в БСП. Уникальность измерений: одна цена на номенклатуру+тип цен на дату.

*Сложность: Средне | Категория: Регистры сведений*

#### 🟡 d8-fc-11: РегистрСведений vs ТабличнаяЧасть - разница?

**Ответ:** РС - независимый объект, данные существуют сами по себе. ТЧ - принадлежит конкретному объекту (документу/справочнику), не существует без него. РС доступен из любого места кода. ТЧ только через владельца.

*Сложность: Средне | Категория: Регистры сведений*

### Типовые задачи

#### 🟡 d8-fc-12: Задача: у Контрагента может быть несколько договоров. Как реализовать?

**Ответ:** Справочник 'Договоры' с реквизитом 'Владелец' (тип СправочникСсылка.Контрагенты). В конфигураторе: Владелец = Контрагенты. Платформа автоматически фильтрует договоры по владельцу. Это стандартный паттерн - подчинённый справочник.

*Сложность: Средне | Категория: Типовые задачи*

#### 🔴 d8-fc-13: Задача: Сотрудник может работать в нескольких подразделениях одновременно. Как?

**Ответ:** РегистрСведений. Измерения: Сотрудник, Подразделение, Должность, ДатаНачала, ДатаОкончания. Без уникальности - можно несколько записей. Периодический: можно отслеживать историю перевода.

*Сложность: Сложно | Категория: Типовые задачи*

#### 🟡 d8-fc-14: Задача: Нужна иерархия подразделений (отдел->секция->группа). Как?

**Ответ:** Справочник 'Подразделения' с иерархией (Группы и элементы). Родитель = ссылка на этот же справочник. Код = произвольный. Это стандартный механизм - иерархический справочник.

*Сложность: Средне | Категория: Типовые задачи*

#### 🟡 d8-fc-15: Как выбрать Структуру или Соответствие для хранения данных?

**Ответ:** Структура - фиксированный набор ключей, известен заранее. Соответствие - динамический набор ключей, неизвестен заранее. Для конфигурируемых параметров - Соответствие. Для known keys - Структура.

*Сложность: Средне | Категория: Типовые задачи*

#### 🟡 d8-fc-16: Что такое РегистрНакопленияОбороты и когда его использовать?

**Ответ:** РН типа 'Обороты' - для подсчёта оборотов за период. Пример: продажи за месяц. НЕ имеет остатков. Виртуальные таблицы: Обороты. РН типа 'Остатки' - для остатков на дату. Выбор типа зависит от задачи.

*Сложность: Средне | Категория: Типовые задачи*

### Собеседование: типовые вопросы

#### 🔴 d8-fc-17: Вопрос на собеседовании: 'Как вы оптимизируете запросы?'

**Ответ:** (1) Проверяю план запросов - есть ли Table Scan. (2) Использую УСЛОВИЕ для виртуальных таблиц. (3) Избегаю запросы в цикле - собираю в ТЗ. (4) Проверяю индексы. (5) Использую временные таблицы. (6) Профилирую через ТЖ.

*Сложность: Сложно | Категория: Собеседование*

#### 🟡 d8-fc-18: Вопрос: 'Какую платформу 1С считаете лучшей?'

**Ответ:** Ловушка! Правильный ответ: '1С 8.3 - актуальная платформа, рекомендуется для всех новых разработок'. 8.2 устарела. 8.3: управляемые формы, http-сервисы, расширения.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d8-fc-19: Вопрос: 'Что делаете, если типовая конфигурация не подходит?'

**Ответ:** (1) Расширения (безопасно). (2) Общие модули + подписки. (3) В крайнем случае - расширение конфигурации. (4) НИКОГДА не меняю типовой код напрямую - при обновлении всё затрётся.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d8-fc-20: Вопрос: 'Расскажите про РегистрСведений'

**Ответ:** Регистр для хранения произвольной информации. Периодический/непериодический, подчинённый/независимый. Уникальность измерений - ограничение. Используется: цены, курсы, принадлежность к группам.

*Сложность: Средне | Категория: Собеседование*

#### 🔴 d8-fc-21: Вопрос: 'Как работает LEFT JOIN в запросах 1С?'

**Ответ:** LEFT JOIN = все записи ЛЕВОЙ + совпадающие из ПРАВОЙ. Нет совпадения = NULL. Проверяется ЕСТЬ NULL, не = NULL! Частая ошибка: писать ГДЕ ПравоеПоле = NULL - всегда ЛОЖЬ!

*Сложность: Сложно | Категория: Собеседование*

#### 🔴 d8-fc-22: Вопрос: 'Что такое транзакция в 1С?'

**Ответ:** Атомарная единица работы с БД. НачатьТранзакцию() / ЗафиксироватьТранзакцию() / ОтменитьТранзакцию(). Неявная при записи объекта. Вложенные НЕ поддерживаются. Уровень: Serializable.

*Сложность: Сложно | Категория: Собеседование*

#### 🔴 d8-fc-23: Вопрос: 'В чём разница HTTP-сервиса и Web-сервиса?'

**Ответ:** HTTP-сервис: REST/JSON, проще, быстрее. Web-сервис: SOAP/XML, WSDL, сложнее. Для мобильных - HTTP-сервис. Для enterprise - Web-сервис.

*Сложность: Сложно | Категория: Собеседование*

#### 🔴 d8-fc-24: Вопрос: 'Какие HTTP-методы знаете?'

**Ответ:** GET - получение (безопасен, идемпотентен). POST - создание (не идемпотентен). PUT - полное обновление. PATCH - частичное. DELETE - удаление. HEAD - заголовки. OPTIONS - доступные методы.

*Сложность: Сложно | Категория: Собеседование*

#### 🟡 d8-fc-25: Вопрос: 'Что такое WebSocket?'

**Ответ:** Протокол полнодуплексной связи поверх TCP. Постоянное двустороннее соединение. Обе стороны отправляют WHEN THEY WANT. В 1С: не встроен, но можно через внешние компоненты.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d8-fc-26: Вопрос: 'Какие методы оптимизации форм знаете?'

**Ответ:** (1) Динамический список. (2) Отложенная загрузка. (3) УСЛОВИЕ оформления - не более 10 правил. (4) Пагинация. (5) Кэширование на клиенте.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d8-fc-27: Вопрос: 'Расскажите про расширения в 1С'

**Ответ:** Расширение = модификация конфигурации БЕЗ изменения основного кода. Можно: добавлять формы, реквизиты, обработчики. Безопасно при обновлении типовой.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d8-fc-28: Вопрос: 'Как работать с Git в 1С?'

**Ответ:** (1) EDT - хранит конфигурацию как XML. (2) Каждый функционал = отдельная ветка. (3) Code Review через PR. (4) НЕ хранить .cf в бинарном виде.

*Сложность: Средне | Категория: Собеседование*

### Диаграмма: diag-19 (НЕ НАЙДЕНА)

### Диаграмма: diag-20 (НЕ НАЙДЕНА)

### Код: ic-16 (НЕ НАЙДЕН)

### Код: ic-17 (НЕ НАЙДЕН)

### Сценарии: Организационная структура

- **sc-11** (НЕ НАЙДЕН)

- **sc-12** (НЕ НАЙДЕН)

### Сценарии: Регистры сведений

- **sc-13** (НЕ НАЙДЕН)

---

# День 9: День 9: NULL, HTTP, WebSocket, собеседование

> NULL в запросах, транзакции ИТС, HTTP/Web сервисы, WebSocket, методы, позиционирование

**Фаза:** Фаза 9 | **Категорий:** 5 | **Карточек:** 40

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | NULL в запросах 1С | ~80мин | 8 | ✅ | ✅ | 2 |
| 2 | Транзакции (ИТС) | ~60мин | 6 | ✅ | ✅ | — |
| 3 | HTTP vs Web-сервис, WebSocket | ~80мин | 8 | ✅ | ✅ | 2 |
| 4 | HTTP-методы | ~70мин | 8 | ✅ | — | — |
| 5 | Собеседование: позиционирование | ~90мин | 10 | ✅ | — | 1 |

---

## Карточки

### NULL в запросах 1С

#### 🔴 d9-fc-1: Откуда появляется NULL в 1С?

**Ответ:** ТОЛЬКО из запросов! Конкретно - при LEFT/RIGHT/FULL JOIN, когда нет совпадения. В объектной модели 1С нет NULL - все значения либо заполнены, либо Неопределено. NULL - концепция ТОЛЬКО языка запросов.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d9-fc-2: Почему при LEFT JOIN появляются NULL?

**Ответ:** LEFT JOIN берёт ВСЕ записи левой таблицы. Для каждой пытается найти совпадение в правой. Если НЕ НАШЁЛ - подставляет NULL во все поля правой. Пример: LEFT JOIN Контрагенты - все документы, даже без контрагента = NULL.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d9-fc-3: Как проверять NULL в запросах 1С?

**Ответ:** ТОЛЬКО через 'ЕСТЬ NULL' и 'НЕ ЕСТЬ NULL'. НИКОГДА через '= NULL' - это всегда ЛОЖЬ! NULL не равен NULL по определению SQL. Правильно: ГДЕ Контрагент ЕСТЬ NULL.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d9-fc-4: Что вернёт 'Неопределено' vs NULL в запросе?

**Ответ:** Неопределено - значение встроенного языка 1С. NULL - значение языка запросов. При передаче Неопределено в запрос - преобразуется. При получении NULL из запроса - преобразуется в Неопределено.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d9-fc-5: Как NULL влияет на агрегатные функции?

**Ответ:** СУММА, СРЕДНЕЕ, КОЛИЧЕСТВО игнорируют NULL. КОЛИЧЕСТВО(*) учитывает ВСЕ (включая NULL). МИН, МАКС - игнорируют NULL. Пример: 10, NULL, 30 = СУММА 40, СЧЁТ 2, СЧЁТ(*) 3.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d9-fc-6: NULL в условии ГДЕ vs УСЛОВИЕ - в чём разница?

**Ответ:** ГДЕ с NULL: 'ГДЕ Контрагент ЕСТЬ NULL' - фильтрует ПОСЛЕ получения. УСЛОВИЕ: работает внутри виртуальной таблицы. Для LEFT JOIN NULL появляется ПОСЛЕ соединения, ГДЕ работает корректно.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🟡 d9-fc-7: Функция ЕСТЬNULL() - зачем нужна?

**Ответ:** ЕСТЬNULL(Выражение, ПодставляемоеЗначение) - если выражение = NULL, возвращает подставляемое. Пример: ЕСТЬNULL(Контрагент.Наименование, 'Не указан'). Полезна для замены NULL.

*Сложность: Средне | Категория: NULL в запросах*

#### 🔴 d9-fc-8: ЗначенияПолей() и NULL - как работают вместе?

**Ответ:** ЗначенияПолей(Таблица.*) возвращает ТЗ. Если RIGHT JOIN не совпал = NULL = Неопределено в ТЗ. Нельзя проверить на NULL - нужно ЗначениеЗаполнено() или сравнение с Неопределено.

*Сложность: Сложно | Категория: NULL в запросах*

### Транзакции (ИТС)

#### 🔴 d9-fc-9: Официальное определение транзакции 1С (с ИТС)

**Ответ:** Транзакция - последовательность операций, рассматриваемая как единое целое. Все операции выполняются успешно и фиксируются (COMMIT), или при ошибке все отменяются (ROLLBACK). Гарантирует целостность данных.

*Сложность: Сложно | Категория: Транзакции*

#### 🔴 d9-fc-10: Что гарантирует транзакция в 1С (ACID)?

**Ответ:** А (Атомарность) - все или ни одного. C (Согласованность) - данные валидны. I (Изоляция) - параллельные не мешают (Serializable). D (Долговечность) - после фиксации сохранены.

*Сложность: Сложно | Категория: Транзакции*

#### 🔴 d9-fc-11: Явная транзакция - синтаксис (ИТС)

**Ответ:** НачатьТранзакцию();
Попытка
  // операции
  ЗафиксироватьТранзакцию();
Исключение
  ОтменитьТранзакцию();
КонецПопытки;
Вложенные НЕ поддерживаются!

*Сложность: Сложно | Категория: Транзакции*

#### 🟡 d9-fc-12: Неявная транзакция - когда работает?

**Ответ:** Платформа автоматически при Записать(), Провести(). Разработчик НЕ вызывал НачатьТранзакцию(). Если исключение внутри - платформа сама откатит. Код в обработчиках уже в транзакции!

*Сложность: Средне | Категория: Транзакции*

#### 🔴 d9-fc-13: Правило 20 секунд для транзакций

**Ответ:** Транзакция НЕ БОЛЕЕ 20 секунд. Блокировки дольше = другие ждут. Растёт риск deadlock. Растёт журнал СУБД. Читай данные ДО, внутри - только пиши.

*Сложность: Сложно | Категория: Транзакции*

#### 🔴 d9-fc-14: Почему переменные НЕ откатываются при ОтменитьТранзакцию()?

**Ответ:** Откатываются только ИЗМЕНЕНИЯ В БД, а не в памяти. Статус = 'Записано'; ОтменитьТранзакцию(); - Статус ВСЁ ЕЩЁ 'Записано', но в БД reverted. Критическая ловушка!

*Сложность: Сложно | Категория: Транзакции*

### HTTP vs Web-сервис, WebSocket

#### 🟡 d9-fc-15: HTTP-сервис в 1С - что это?

**Ответ:** RESTful API внутри 1С. Шаблоны URL: GET/POST/PUT/DELETE. Формат: JSON. Корень: /hs/ИмяСервиса/Метод. Плюсы: простой, быстрый, мобильные клиенты.

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🟡 d9-fc-16: Web-сервис (SOAP) в 1С - что это?

**Ответ:** SOAP-сервис с WSDL-описанием. Формат: XML. Операции через SOAP envelope. WSDL = контракт. Плюсы: строгая типизация, enterprise. Минусы: тяжёлый XML.

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🔴 d9-fc-17: 5 ключевых отличий HTTP-сервиса от Web-сервиса

**Ответ:** 1. Формат: JSON vs XML (SOAP). 2. URL: /hs/... vs /ws/... + WSDL. 3. Простота: меньше кода vs больше конфигурации. 4. Клиенты: мобильные vs enterprise. 5. Описание: OpenAPI vs WSDL.

*Сложность: Сложно | Категория: HTTP/Web сервисы*

#### 🟡 d9-fc-18: Когда выбрать HTTP-сервис, а когда Web-сервис?

**Ответ:** HTTP-сервис: мобильное приложение, SPA, modern API, простой обмен. Web-сервис: enterprise интеграция (SAP, Oracle), строгая типизация, клиент требует WSDL.

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🟡 d9-fc-19: Что такое WebSocket?

**Ответ:** Протокол двусторонней связи поверх TCP. В отличие от HTTP (запрос-ответ): обе стороны отправляют в ЛЮБОЙ момент. Начинается как HTTP с заголовком Upgrade: websocket.

*Сложность: Средне | Категория: WebSocket*

#### 🟡 d9-fc-20: WebSocket vs HTTP - в чём разница?

**Ответ:** HTTP: клиент запрашивает -> сервер отвечает. Односторонний. WebSocket: постоянное соединение. Обе стороны отправляют WHEN THEY WANT. Меньше latency. HTTP = письмо, WebSocket = телефон.

*Сложность: Средне | Категория: WebSocket*

#### 🟡 d9-fc-21: Где используется WebSocket?

**Ответ:** Чат-приложения. Real-time уведомления. Онлайн-игры. Торговые терминалы. Совместное редактирование. В 1С: push-уведомления через внешние компоненты.

*Сложность: Средне | Категория: WebSocket*

#### 🔴 d9-fc-22: Есть ли WebSocket в 1С?

**Ответ:** НЕТ встроенной поддержки. Обходные пути: (1) Внешние компоненты. (2) HTTP-сервис + long polling. (3) Внешнее соединение через COM. (4) .NET DLL.

*Сложность: Сложно | Категория: WebSocket*

### HTTP-методы

#### 🟡 d9-fc-23: GET - метод HTTP

**Ответ:** Получение данных. Без тела запроса. Параметры в URL. Безопасный, идемпотентный, кэшируемый. Пример: /hs/catalog/items?category=food.

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d9-fc-24: POST - метод HTTP

**Ответ:** Создание ресурса или отправка данных. Тело содержит данные. НЕ идемпотентный. НЕ кэшируемый. Пример: /hs/orders/create. Content-Type: application/json.

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d9-fc-25: PUT vs PATCH - в чём разница?

**Ответ:** PUT - ПОЛНОЕ обновление. Отправляешь ВСЕ поля. PATCH - ЧАСТИЧНОЕ. Только ИЗМЕНЁННЫЕ поля. Оба идемпотентные. PUT = перезапись всего, PATCH = обновление части.

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d9-fc-26: DELETE - метод HTTP

**Ответ:** Удаление ресурса. Идемпотентный. 200 OK или 204 No Content. В 1С: /hs/orders/123 -> удаление заказа. Проверять права перед удалением!

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d9-fc-27: HEAD и OPTIONS - для чего?

**Ответ:** HEAD - как GET, но ТОЛЬКО заголовки (без тела). Проверка: существует ли ресурс. OPTIONS - допустимые методы для ресурса. CORS preflight: браузер спрашивает можно ли отправить POST.

*Сложность: Средне | Категория: HTTP-методы*

#### 🔴 d9-fc-28: Что такое идемпотентность HTTP-методов?

**Ответ:** Идемпотентный = повторный запрос даёт тот же результат. GET, PUT, DELETE, HEAD, OPTIONS - идемпотентные. POST - НЕ идемпотентный (создаёт ресурс). Важно для retry.

*Сложность: Сложно | Категория: HTTP-методы*

#### 🟡 d9-fc-29: HTTP-статусы: какие нужно знать для 1С?

**Ответ:** 200 OK. 201 Created. 204 No Content. 400 Bad Request. 401 Unauthorized. 403 Forbidden. 404 Not Found. 500 Internal Server Error. HTTPСервисОтвет.УстановитьКодСостояния(200).

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d9-fc-30: Content-Type - какие значения использовать?

**Ответ:** application/json - JSON (рекомендуется). application/xml - XML. application/x-www-form-urlencoded - форма. multipart/form-data - файлы. По умолчанию 1С = application/json.

*Сложность: Средне | Категория: HTTP-методы*

### Собеседование: позиционирование

#### 🟡 d9-fc-31: Как позиционировать себя на собеседовании 1С?

**Ответ:** (1) НЕ преувеличивай. (2) ПОКАЗЫВАЙ знание стандартов - БСП, EDT, Git. (3) ГОВОРИ о конкретных задачах. (4) УПОМИНАЙ profiling - ТЖ, APDEX. (5) ПОКАЖЫ evolve.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-32: Как отвечать на вопросы, ответа на которые не знаешь?

**Ответ:** (1) Скажи честно: 'Я это не использовал, но знаю теорию...' (2) Расскажи что знаешь ПО ТЕМЕ. (3) Предложи как БЫ РЕШИЛ. (4) НЕ молчи и НЕ выдумывай.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-33: Топ-5 вопросов, которые ТОЧНО спросят на собесе 1С

**Ответ:** (1) Клиент-серверное взаимодействие. (2) Регистры: РН vs РС, проведения. (3) Транзакции: явные/неявные, ACID. (4) Запросы: JOIN, виртуальные таблицы, NULL. (5) Оптимизация: план запросов, ТЖ.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-34: Как подготовиться к собеседованию за 1 день?

**Ответ:** (1) Повтори все темы Study Lab. (2) Проговори ответы вслух. (3) Погугли реальные вопросы. (4) Попрактикуй код. (5) Отдохни - свежая голова важнее.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-35: Как рассказать о своём опыте (даже если мало)?

**Ответ:** (1) Фокус на ПРОЕКТАХ, не на времени. (2) Конкретика: 'Сделал HTTP-сервис для обмена с складом'. (3) Покажи КАК решал проблемы. (4) Упомяни обучаемость.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-36: Красные флаги на собеседовании - чего НЕ делать

**Ответ:** (1) НЕ говори 'я всё знаю'. (2) НЕ критикуй бывших. (3) НЕ отвечай односложно. (4) НЕ спрашивай про зарплату в первые 5 минут. (5) НЕ игнорируй вопросы.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-37: Какие вопросы ЗАДАВАТЬ интервьюеру в конце?

**Ответ:** (1) 'Какой стек?' (2) 'Какая методология?' (3) 'Какие задачи первые 3 месяца?' (4) 'Есть ли onboarding?' (5) 'Как организован код-ревью?' Показывает интерес!

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-38: Как оценить уровень позиции?

**Ответ:** Junior: знает основы, нужно ведение. Middle: сам решает типовые, знает БСП. Senior: проектирует архитектуру, менторит. Честно оцени по темам Study Lab.

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-39: Что такое Behavioural Interview (STAR)?

**Ответ:** Вопросы 'Расскажи о случае когда...'. STAR: Situation (ситуация) -> Task (задача) -> Action (действие) -> Result (результат). Пример: 'Был дедлайн, нужно оптимизировать запрос, переписал через УСЛОВИЕ, ускорил в 5 раз.'

*Сложность: Средне | Категория: Собеседование*

#### 🟡 d9-fc-40: Техническое задание на собеседовании - как решать?

**Ответ:** (1) ВНИМАТЕЛЬНО прочитай задачу. (2) Спроси уточняющие вопросы - зачитывается! (3) Обдумай архитектуру. (4) Комментируй что пишешь. (5) Если застрял - скажи о чём думаешь. (6) Проверь код.

*Сложность: Средне | Категория: Собеседование*

### Диаграмма: diag-21 (НЕ НАЙДЕНА)

### Диаграмма: diag-22 (НЕ НАЙДЕНА)

### Диаграмма: diag-23 (НЕ НАЙДЕНА)

### Диаграмма: diag-24 (НЕ НАЙДЕНА)

### Диаграмма: diag-25 (НЕ НАЙДЕНА)

### Код: ic-18 (НЕ НАЙДЕН)

### Код: ic-20 (НЕ НАЙДЕН)

### Код: ic-19 (НЕ НАЙДЕН)

### Сценарии: NULL в запросах 1С

- **sc-14** (НЕ НАЙДЕН)

- **sc-15** (НЕ НАЙДЕН)

### Сценарии: HTTP vs Web-сервис, WebSocket

- **sc-16** (НЕ НАЙДЕН)

- **sc-18** (НЕ НАЙДЕН)

### Сценарии: Собеседование: позиционирование

- **sc-17** (НЕ НАЙДЕН)

### Теория: HTTP-методы: руководство

GET (безопасный, идемпотентный, кэшируемый), POST (НЕ идемпотентный), PUT (полное обновление), PATCH (частичное), DELETE (идемпотентный), HEAD (заголовки), OPTIONS (CORS). Идемпотентность важна для retry.

**Ключевые моменты:**

- GET: безопасный, идемпотентный
- POST: НЕ идемпотентный
- PUT: полное, PATCH: частичное
- DELETE: идемпотентный
- Статусы: 200, 201, 204, 400, 401, 403, 404, 500
- Content-Type: application/json

**Советы:**

- Идемпотентность важна для retry
- GET + URL параметры, POST + тело
- HTTP-статусы = часть API контракта

---

# День 10: День 10: Мок-собеседование Магнит

> Все темы повтор, JSON, EDT+Git, розничная специфика, готовые ответы

**Фаза:** Фаза 10 | **Категорий:** 6 | **Карточек:** 43

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Задача Магнит (Орг-Подразделение) | ~60мин | 5 | ✅ | ✅ | 1 |
| 2 | Транзакции ИТС (повтор) | ~70мин | 7 | ✅ | — | 1 |
| 3 | ЕСТЬ NULL vs ЕСТЬNULL (повтор) | ~40мин | 4 | — | — | 1 |
| 4 | HTTP/Web сервисы + JSON | ~100мин | 12 | ✅ | ✅ | 1 |
| 5 | EDT + Git | ~40мин | 4 | ✅ | — | 1 |
| 6 | Мок-собеседование: готовые ответы | ~100мин | 11 | — | — | — |

---

## Карточки

### Задача Магнит (Орг-Подразделение)

#### 🔴 d10-fc-1: Дано: Организация, подразделения, сотрудник, должность. К одной организации - несколько подразделений, к одному подразделению - одна организация. Данные меняются раз в день. Как реализовать связку?

**Ответ:** РегистрСведений с уникальностью измерений + периодический (период = День). Измерения: Организация, Подразделение, Сотрудник, Должность. Уникальность гарантирует что связка не повторится. Периодический т.к. данные меняются раз в день - нужен СрезПоследних.

*Сложность: Сложно | Категория: Задача Магнит*

#### 🔴 d10-fc-2: Почему НЕ справочник для связки Организация-Подразделение?

**Ответ:** Справочник НЕ гарантирует уникальность. Можно создать два элемента с одинаковыми реквизитами. РегистрСведений с уникальностью измерений = платформа сама отклонит дубликат. Также: если нужна история изменений - РС периодический даёт СрезПоследних.

*Сложность: Сложно | Категория: Задача Магнит*

#### 🔴 d10-fc-3: Паттерн записи в РегистрСведений с уникальностью

**Ответ:** НаборЗаписей = РС.СоздатьНаборЗаписей();
НаборЗаписей.Отбор.Ключ.Установить(Значение);
НаборЗаписей.Прочитать();
Если НаборЗаписей.Количество() = 0 Тогда
  Запись = НаборЗаписей.Добавить();
Иначе
  НаборЗаписей[0].Ресурс = НовоеЗначение;
КонецЕсли;
НаборЗаписей.Записать();

*Сложность: Сложно | Категория: Задача Магнит*

#### 🔴 d10-fc-4: Как получить актуальную связку на дату?

**Ответ:** ВЫБРАТЬ СвязиСрезПоследних.* ИЗ РегистрСведений.Связи.СрезПоследних(&ТекущаяДата, Сотрудник = &Сотрудник) КАК СвязиСрезПоследних. СрезПоследних = виртуальная таблица, оптимизирована платформой. Параметры передаются внутрь - используется индекс.

*Сложность: Сложно | Категория: Задача Магнит*

#### 🟡 d10-fc-5: Что если данные НЕ меняются часто (не раз в день)?

**Ответ:** Непериодический РегистрСведений. Нет виртуальных таблиц СрезПервых/СрезПоследних. Подходит если: данные стабильные, история изменений не нужна, просто текущая привязка. Но для задачи Магнит (раз в день) нужен периодический!

*Сложность: Средне | Категория: Задача Магнит*

### Транзакции ИТС (повтор)

#### 🔴 d10-fc-6: Определение транзакции 1С по ИТС — дословно

**Ответ:** Транзакция - это неделимая последовательность операций, которая рассматривается как единое целое. В результате выполнения транзакции либо все входящие в неё операции будут выполнены, либо ни одна из них. Обеспечивает ACID: Атомарность, Согласованность, Изоляция, Долговечность.

*Сложность: Сложно | Категория: Транзакции ИТС*

#### 🟡 d10-fc-7: Что такое Атомарность транзакции простыми словами?

**Ответ:** Атомарность = ВСЁ или НИЧЕГО. Пример: проводим документ - движения в 3 регистрах. Если хотя бы один регистр упал - ВСЕ движения откатываются. Документ либо проведён полностью, либо не проведён совсем. Нет промежуточного состояния.

*Сложность: Средне | Категория: Транзакции ИТС*

#### 🔴 d10-fc-8: Что такое Изоляция (Serializable)?

**Ответ:** Параллельные транзакции не видят изменений друг друга до завершения. Как будто они выполняются последовательно. 1С использует Serializable - самый строгий уровень. Это гарантирует целостность, но снижает параллелизм и требует управляемых блокировок.

*Сложность: Сложно | Категория: Транзакции ИТС*

#### 🔴 d10-fc-9: Явная транзакция: полный код с комментариями

**Ответ:** НачатьТранзакцию(); // 1. Открытие
Попытка
  ЗаблокироватьДанныеДляРедактирования(); // 2. Блокировка ДО чтения
  // 3. Читаем данные
  // 4. Модифицируем и записываем
  ЗафиксироватьТранзакцию(); // 5. COMMIT
Исключение
  ОтменитьТранзакцию(); // 6. ROLLBACK
КонецПопытки;

*Сложность: Сложно | Категория: Транзакции ИТС*

#### 🔴 d10-fc-10: Критическая ловушка: что откатывает ОтменитьТранзакцию()?

**Ответ:** ТОЛЬКО изменения в БД откатываются. Локальные переменные в памяти НЕ откатываются! Пример: Статус = 'Записано'; ОтменитьТранзакцию(); // Статус всё ещё 'Записано'! Но данные в БД - reverted. Это частая причина багов у начинающих.

*Сложность: Сложно | Категория: Транзакции ИТС*

#### 🟡 d10-fc-11: Неявная транзакция - когда платформа сама открывает?

**Ответ:** При вызове: Документ.Записать(), Документ.Провести(), НаборЗаписей.Записать(), Объект.Записать(). Разработчик НЕ вызывал НачатьТранзакцию(). Если внутри обработчика исключение - платформа сама откатит. Важно: код уже в транзакции!

*Сложность: Средне | Категория: Транзакции ИТС*

#### 🔴 d10-fc-12: Правило 20 секунд - почему?

**Ответ:** Транзакция > 20 секунд = проблема. Причины: (1) Блокировки держатся дольше - другие пользователи ждут. (2) Растёт журнал транзакций СУБД. (3) Растёт риск deadlock. Решение: читаем данные ДО транзакции, внутри - только запись.

*Сложность: Сложно | Категория: Транзакции ИТС*

### ЕСТЬ NULL vs ЕСТЬNULL (повтор)

#### 🔴 d10-fc-13: ЕСТЬ NULL vs = NULL — в чём разница?

**Ответ:** ЕСТЬ NULL - проверяет, является ли значение NULL. РАБОТАЕТ. = NULL - сравнение с NULL. НЕ РАБОТАЕТ (всегда ложь). Почему: NULL не равен NULL по определению SQL. Любое сравнение с NULL через = или <> возвращает NULL (ложь). Пример: ГДЕ Контрагент ЕСТЬ NULL - найдёт. ГДЕ Контрагент = NULL - пустой результат.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🔴 d10-fc-14: ЕСТЬNULL() vs ЕСТЬ NULL — разные функции?

**Ответ:** ДА, разные! ЕСТЬ NULL - оператор проверки (возвращает Истина/Ложь). ЕСТЬNULL(Выражение, Значение) - функция подстановки (возвращает Значение если NULL). Пример: ВЫБРАТЬ ЕСТЬNULL(Контрагент.Наименование, 'Не указан'). Используй ЕСТЬ NULL в ГДЕ, ЕСТЬNULL() в выборке.

*Сложность: Сложно | Категория: NULL в запросах*

#### 🟡 d10-fc-15: НЕ ЕСТЬ NULL — когда использовать?

**Ответ:** Когда нужно НАЙТИ записи где поле ЗАПОЛНЕНО (не NULL). Пример после LEFT JOIN: ГДЕ Контрагент НЕ ЕСТЬ NULL - только документы У которых есть контрагент. Это фильтрует строки где LEFT JOIN нашёл совпадение.

*Сложность: Средне | Категория: NULL в запросах*

#### 🔴 d10-fc-16: NULL и агрегатные функции: что нужно знать?

**Ответ:** СУММА, СРЕДНЕЕ, МИНИМУМ, МАКСИМУМ - игнорируют NULL. СЧЁТ() - тоже игнорирует NULL. СЧЁТ(*) - учитывает ВСЕ строки включая NULL. Пример: значения 10, NULL, 30. СУММА = 40, СЧЁТ = 2, СЧЁТ(*) = 3, СРЕДНЕЕ = 20.

*Сложность: Сложно | Категория: NULL в запросах*

### HTTP/Web сервисы + JSON

#### 🟡 d10-fc-17: HTTP-сервис 1С: что это, как создать?

**Ответ:** RESTful API внутри 1С. Конфигуратор -> HTTP-сервисы -> Добавить. Шаблоны URL: /hs/ИмяСервиса/Метод. Методы GET/POST/PUT/DELETE. Обработчик = функция с именем МетодGet(). Формат: JSON (по умолчанию). Ответ: HTTPСервисОтвет с кодом состояния.

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🟡 d10-fc-18: Web-сервис (SOAP) 1С: что это?

**Ответ:** SOAP-сервис с WSDL-описанием. Конфигуратор -> Web-сервисы -> Добавить. Операции = методы сервиса. Формат: XML + SOAP envelope. WSDL = контракт (описание типов, методов). URL: /ws/ИмяСервиса?wsdl. Для enterprise-интеграций (SAP, Oracle).

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🔴 d10-fc-19: 7 отличий HTTP-сервиса от Web-сервиса

**Ответ:** (1) Формат: JSON vs XML. (2) URL: /hs/... vs /ws/...?wsdl. (3) Описание: нет vs WSDL. (4) Протокол: HTTP vs SOAP поверх HTTP. (5) Сложность: просто vs сложно. (6) Размер данных: маленький vs большой. (7) Использование: мобильные, SPA vs enterprise. Для новых проектов - HTTP-сервис!

*Сложность: Сложно | Категория: HTTP/Web сервисы*

#### 🟡 d10-fc-20: WebSocket: что это, есть ли в 1С?

**Ответ:** Протокол полнодуплексной связи поверх TCP. Начинается как HTTP с заголовком Upgrade: websocket. Обе стороны отправляют сообщения WHEN THEY WANT. В 1С НЕ встроен. Обходные пути: внешние компоненты, HTTP-сервис + long polling, COM-соединение, .NET DLL.

*Сложность: Средне | Категория: HTTP/Web сервисы*

#### 🟡 d10-fc-21: GET, POST, PUT, DELETE — кратко и точно

**Ответ:** GET - получить данные. Безопасный, идемпотентный, кэшируемый. POST - создать ресурс. НЕ идемпотентный. PUT - полное обновление. Идемпотентный. DELETE - удалить. Идемпотентный. В 1С: имя функции = метод + шаблон (OrdersGet, OrdersPost, OrdersPut, OrdersDelete).

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d10-fc-22: PATCH vs PUT — разница с примерами

**Ответ:** PUT = ПОЛНОЕ обновление. Клиент -> сервер: 'вот ВСЯ новая версия ресурса'. Идемпотентный. PATCH = ЧАСТИЧНОЕ обновление. Клиент -> сервер: 'измени ТОЛЬКО эти поля'. Идемпотентный в теории. Пример PUT: {name:'Иван',age:25,city:'Москва'}. PATCH: {age:26}. Для частичного обновления - PATCH.

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d10-fc-23: HEAD и OPTIONS — зачем нужны?

**Ответ:** HEAD = GET без тела ответа. Используется для проверки: существует ли ресурс, размер, дата изменения. OPTIONS = возвращает допустимые методы для URL. Используется CORS preflight: браузер спрашивает 'можно ли отправить POST сюда?'. В 1С: обрабатываются в HTTP-сервисе.

*Сложность: Средне | Категория: HTTP-методы*

#### 🔴 d10-fc-24: Идемпотентность — что это и почему важно?

**Ответ:** Идемпотентный = повторный запрос даёт тот же результат. Важно для retry при ошибках сети. Если запрос не дошёл - можно повторить БЕЗ риска. GET/PUT/DELETE: повторение безопасно. POST: повторение = второй ресурс! Retry для POST = проблема.

*Сложность: Сложно | Категория: HTTP-методы*

#### 🟡 d10-fc-25: HTTP-статусы: что нужно знать для собеседования

**Ответ:** 200 OK - успех. 201 Created - ресурс создан. 204 No Content - успех без тела. 400 Bad Request - ошибка в запросе клиента. 401 Unauthorized - не авторизован. 403 Forbidden - нет прав. 404 Not Found - не найден. 500 Internal Server Error - ошибка сервера.

*Сложность: Средне | Категория: HTTP-методы*

#### 🟡 d10-fc-26: Как работать с JSON в 1С - основные функции?

**Ответ:** ЗаписьJSON = записать объект в JSON-строку. ЧтениеJSON = прочитать JSON-строку. Для записи: создать ЗаписьJSON, записать объект (Структура/Массив). Для чтения: создать ЧтениеJSON, прочитать строку, ParseJSON() в 8.3.6+. Структура = {}, Массив = [].

*Сложность: Средне | Категория: JSON в 1С*

#### 🟡 d10-fc-27: ParseJSON() в 1С 8.3.6+ — как использовать?

**Ответ:** РезультатЧтения = ЗаписьJSON.Прочитать(СтрокаJSON);
// Или новый способ (8.3.6+):
Результат = Json.Прочитать(СтрокаJSON);
// Обращение: Результат.ИмяСвойства, Результат[0] для массива.
// Проверка типа: Результат.Тип() = 'Объект' или 'Массив'.

*Сложность: Средне | Категория: JSON в 1С*

#### 🔴 d10-fc-28: HTTP-сервис: как вернуть JSON клиенту?

**Ответ:** Ответ = Новый HTTPСервисОтвет(200);
Ответ.Заголовки.Вставить('Content-Type', 'application/json; charset=utf-8');
ОбъектJSON = Новый Структура('id', 'name');
ОбъектJSON.id = Ссылка;
ОбъектJSON.name = Наименование;
Ответ.УстановитьТелоИзСтроки(ЗаписьJSON(ОбъектJSON));
Возврат Ответ;

*Сложность: Сложно | Категория: JSON в 1С*

### EDT + Git

#### 🟡 d10-fc-29: Что такое EDT и зачем он нужен?

**Ответ:** 1C:Enterprise Development Tools = новая IDE от 1С для разработки в Eclipse. Хранит конфигурацию как XML-файлы (не бинарный .cf). Это позволяет использовать Git для версионирования. Преимущества: diff между версиями, merge веток, code review, CI/CD.

*Сложность: Средне | Категория: EDT + Git*

#### 🟡 d10-fc-30: Git workflow для 1С-разработчика

**Ответ:** (1) Feature branch: git checkout -b feature/my-task. (2) Разработка в EDT. (3) Commit: git commit. (4) Push: git push. (5) Pull Request / Merge Request. (6) Code Review. (7) Merge в main/master. (8) Конфликт? Разрешить через merge tool EDT.

*Сложность: Средне | Категория: EDT + Git*

#### 🔴 d10-fc-31: Git: reset --soft vs --hard vs --mixed

**Ответ:** --soft: отменяет коммит, но оставляет изменения в индексе. --mixed (default): отменяет коммит + убирает из индекса, но оставляет файлы. --hard: отменяет коммит + индекс + изменения в файлах. ОПАСНО: --hard удалит незакоммиченные изменения!

*Сложность: Сложно | Категория: EDT + Git*

#### 🟡 d10-fc-32: Почему НЕЛЬЗЯ хранить .cf в Git?

**Ответ:** .cf = бинарный файл конфигурации. Diff между версиями не работает - Git показывает 'изменён весь файл'. Merge веток невозможен - бинарный конфликт. EDT хранит XML-файлы - каждый объект = отдельный файл. Diff работает, merge работает, code review возможен.

*Сложность: Средне | Категория: EDT + Git*

### Мок-собеседование: готовые ответы

#### 🔴 d10-fc-33: Вопрос Магнит: 'Расскажите про клиент-серверное взаимодействие в 1С'

**Ответ:** Тонкий клиент <-> Сервер 1С <-> СУБД. Клиент: только UI-логика, формы, диалоги. Сервер: бизнес-логика, работа с БД. Вызов сервера через &НаСервере или &НаКлиентеНаСервере. С сервера можно вернуть: примитивы, массивы, ФиксированнаяСтруктура. НЕЛЬЗЯ вернуть объекты 1С.

*Сложность: Сложно | Категория: Мок-собес*

#### 🔴 d10-fc-34: Вопрос Магнит: 'Чем управляемые блокировки отличаются от автоматических?'

**Ответ:** Управляемые: ставятся разработчиком ЗАРАНЕЕ через ЗаблокироватьДанныеДляРедактирования(), проверяются в кластере 1С, упреждающие. Автоматические: ставятся СУБД при реальном UPDATE/INSERT, реактивные, могут вызвать deadlock. Для регистров - управляемые!

*Сложность: Сложно | Категория: Мок-собес*

#### 🟡 d10-fc-35: Вопрос Магнит: 'Какие расширения вы создавали?'

**Ответ:** Конкретный пример: 'Создавал расширение для типовой БСП - добавил поле КомментарийHR в форму ПриказКадровый. Через расширение добавил реквизит, элемент формы и обработчик заполнения. Расширение безопасно при обновлении типовой - не затирается.'

*Сложность: Средне | Категория: Мок-собес*

#### 🟡 d10-fc-36: Вопрос Магнит: 'Как оптимизировать форму которая тормозит?'

**Ответ:** (1) Проверить условное оформление - не более 10 правил. (2) Отложенное заполнение для справочников. (3) Динамический список вместо ручной загрузки. (4) Проверить ТЖ - какие запросы при открытии. (5) Кэшировать постоянные значения на клиенте. (6) Пагинация в списках.

*Сложность: Средне | Категория: Мок-собес*

#### 🟡 d10-fc-37: Вопрос Магнит: 'Опыт работы с JSON и HTTP-сервисами?'

**Ответ:** Конкретный пример: 'Создавал HTTP-сервис для обмена с мобильным приложением склада. GET /hs/inventory/items - получение остатков. POST /hs/inventory/update - обновление остатков. JSON формат, Content-Type: application/json. Обрабатывал ошибки через HTTP-статусы 400/500.'

*Сложность: Средне | Категория: Мок-собес*

#### 🔴 d10-fc-38: Вопрос Магнит: 'РегистрНакопления vs РегистрСведений?'

**Ответ:** РН - связан с документами. Записывается ТОЛЬКО через проведение. Имеет виртуальные таблицы: Остатки, Обороты. РС - независимый. Можно записать в любой момент через НаборЗаписей. Периодический: СрезПервых/СрезПоследних. Для связей и справочной информации - РС. Для количественного учёта - РН.

*Сложность: Сложно | Категория: Мок-собес*

#### 🔴 d10-fc-39: Вопрос Магнит: 'Какой уровень изоляции использует 1С?'

**Ответ:** SERIALIZABLE (сериализуемый) - самый строгий уровень. Транзакции выполняются так, будто последовательно. Реализуется через управляемые блокировки + СУБД. Гарантирует целостность, но снижает параллелизм. Правило 20 секунд для минимизации блокировок.

*Сложность: Сложно | Категория: Мок-собес*

#### 🔴 d10-fc-40: Вопрос Магнит: 'Как работает проведение документа?'

**Ответ:** 1. Пользователь нажимает 'Провести'. 2. Платформа открывает (не)явную транзакцию. 3. Вызывается ОбработкаПроведения(). 4. В модуле объекта: Движения.Остатки.Записывать = Истина. 5. РегистрНакопленияОбороты.Добавить().Заполнить(). 6. Записать(). ЗафиксироватьТранзакцию().

*Сложность: Сложно | Категория: Мок-собес*

#### 🔴 d10-fc-41: Вопрос Магнит: 'Что такоеdeadlock и как его избежать?'

**Ответ:** Deadlock = взаимная блокировка. Транзакция A ждёт ресурс B, B ждёт ресурс A. Обе зависают. СУБД убивает одну. Как избежать: (1) Единый порядок блокировки ресурсов. (2) Короткие транзакции. (3) Управляемые блокировки. (4) Таймаут ожидания.

*Сложность: Сложно | Категория: Мок-собес*

#### 🟡 d10-fc-42: Как отвечать на вопрос 'Расскажите о себе' для Магнит?

**Ответ:** STAR-подход: 'У меня опыт разработки на 1С X лет. Работал с конфигурациями (перечислить). Последний проект: (конкретный пример с результатом). Знаком с БСП, EDT, Git. Опыт интеграций через HTTP-сервисы (JSON). Интересуюсь оптимизацией производительности. Хочу развиваться в direction вашей компании.'

*Сложность: Средне | Категория: Мок-собес*

#### 🟡 d10-fc-43: Что спросит Магнит про розничную специфику?

**Ответ:** Магнит = розничная торговля. Возможные вопросы: (1) Документы: ЧекККМ, Реализация, Возврат. (2) РН: Остатки товаров, Продажи. (3) Интеграция: кассовое оборудование, штрихкодирование. (4) Печатные формы: этикетки, чеки. (5) Обмен: центральная БД <-> магазины. (6) БСП: стандартные механизмы.

*Сложность: Средне | Категория: Мок-собес*

### Диаграмма: diag-27 (НЕ НАЙДЕНА)

### Диаграмма: diag-28 (НЕ НАЙДЕНА)

### Диаграмма: diag-29 (НЕ НАЙДЕНА)

### Диаграмма: diag-30 (НЕ НАЙДЕНА)

### Код: ic-21 (НЕ НАЙДЕН)

### Код: ic-22 (НЕ НАЙДЕН)

### Сценарии: Задача Магнит (Орг-Подразделение)

- **sc-19** (НЕ НАЙДЕН)

### Сценарии: Транзакции ИТС (повтор)

- **sc-20** (НЕ НАЙДЕН)

### Сценарии: ЕСТЬ NULL vs ЕСТЬNULL (повтор)

- **sc-21** (НЕ НАЙДЕН)

### Сценарии: HTTP/Web сервисы + JSON

- **sc-22** (НЕ НАЙДЕН)

### Сценарии: EDT + Git

- **sc-23** (НЕ НАЙДЕН)

---

# День 11: День 11: HTTP-интеграция Магнит

> Заявка на расход, JSON, РС/РН движения, ЭДО, HTTP-сервис, мок-собес

**Фаза:** Фаза 11 | **Категорий:** 7 | **Карточек:** 40

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Архитектура интеграции | ~60мин | 5 | ✅ | ✅ | 1 |
| 2 | JSON и парсинг | ~40мин | 5 | — | — | — |
| 3 | Создание документа | ~50мин | 5 | — | ✅ | — |
| 4 | Регистры: РН и РС | ~70мин | 7 | ✅ | — | 1 |
| 5 | Проведение: движения | ~60мин | 4 | ✅ | ✅ | — |
| 6 | HTTP-сервис: полная реализация | ~60мин | 4 | — | ✅ | 1 |
| 7 | Мок-собеседование: интеграция | ~100мин | 10 | — | — | 2 |

---

## Карточки

### Архитектура интеграции

#### 🔴 d11-fc-1: Общая схема интеграции: ЭДО → 1С КИС

**Ответ:** Смета создаётся в ЭДО (внешняя система). Там согласовывается. После согласования ЭДО отправляет JSON в HTTP-сервис 1С КИС (на базе 1С:БП, сильно доработана). HTTP-сервис парсит JSON и создаёт нетиповой документ 'Заявка на расход'. Документ формирует движения по РН и РС.

*Сложность: Сложно | Категория: Интеграция Магнит*

#### 🟡 d11-fc-2: Почему HTTP-сервис, а не Web-сервис (SOAP)?

**Ответ:** HTTP-сервис = JSON, легковесный, RESTful. ЭДО работает с JSON. Web-сервис = SOAP/XML, тяжёлый, нужен WSDL. Для современных интеграций — HTTP-сервис. URL: /hs/magnit/expense. Формат: JSON. Быстрее разрабатывать, проще отлаживать через Postman/cURL.

*Сложность: Средне | Категория: Интеграция Магнит*

#### 🟡 d11-fc-3: 1С КИС Магнит — что это?

**Ответ:** Корпоративная информационная система Магнит на базе 1С:Бухгалтерия предприятия. Сильно доработана под розничную сеть. Включает: бюджетирование, закупки, склад, финансы. Нетиповые объекты: ЗаявкаНаРасход, свои регистры для бюджетного контроля. HTTP-интеграция с ЭДО для автоматизации.

*Сложность: Средне | Категория: Интеграция Магнит*

#### 🟡 d11-fc-4: Какой HTTP-метод использовать для создания заявки?

**Ответ:** POST — создание нового ресурса. ЭДО отправляет POST на /hs/magnit/expense с JSON-телом. Содержит: номер сметы, дату, статьи бюджета, сумму, организацию, подразделение, ответственного. HTTP-сервис создаёт документ ЗаявкаНаРасход и возвращает 201 Created или 400/500 при ошибке.

*Сложность: Средне | Категория: Интеграция Магнит*

#### 🟢 d11-fc-5: Что такое ЭДО и зачем оно в этой схеме?

**Ответ:** ЭДО = Электронный документооборот. Внешняя система для создания и согласования смет. Workflow: создание → согласование → отправка в 1С. Преимущества: разгрузка 1С от UI согласования, гибкие маршруты, аудит. 1С получает уже готовые согласованные данные.

*Сложность: Легко | Категория: Интеграция Магнит*

### JSON и парсинг

#### 🔴 d11-fc-6: Как получить JSON из тела HTTP-запроса в 1С?

**Ответ:** Функция ExpensesPost(Запрос)
  ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
  ЧтениеJSON = Новый ЧтениеJSON();
  ЧтениеJSON.УстановитьСтроку(ТелоЗапроса);
  Результат = ЧтениеJSON.ПрочитатьЗначение();
  // Результат = Соответствие или Структура
КонецФункции
Или через ParseJSON() в 8.3.6+: Результат = Json.Прочитать(ТелоЗапроса);

*Сложность: Сложно | Категория: JSON парсинг*

#### 🟡 d11-fc-7: Структура JSON для Заявки на расход — пример

**Ответ:** {
  'externalId': 'SM-2025-001',
  'date': '2025-08-06',
  'organization': 'ООО Магнит',
  'department': 'IT-отдел',
  'responsible': 'Иванов И.И.',
  'items': [
    {'article': 'Канцтовары', 'amount': 15000},
    {'article': 'Оборудование', 'amount': 250000}
  ],
  'totalAmount': 265000
}

*Сложность: Средне | Категория: JSON парсинг*

#### 🔴 d11-fc-8: Как перебрать массив items из JSON в 1С?

**Ответ:** // После ParseJSON():
МассивСтрок = Результат.items;
Для Каждого Элемент Из МассивСтрок Цикл
  Статья = Элемент.article;
  Сумма = Элемент.amount;
  // Добавить строку в ТЧ документа
  НоваяСтрока = Документ.Товары.Добавить();
  НоваяСтрока.СтатьяРасхода = Статья;
  НоваяСтрока.Сумма = Сумма;
КонецЦикла;

*Сложность: Сложно | Категория: JSON парсинг*

#### 🔴 d11-fc-9: Как обработать ошибки парсинга JSON в 1С?

**Ответ:** Попытка
  Результат = Json.Прочитать(ТелоЗапроса);
Исключение
  Ответ = Новый HTTPСервисОтвет(400);
  Ответ.УстановитьТелоИзСтроки('{"error": "Invalid JSON"}');
  Возврат Ответ;
КонецПопытки;
// Проверить обязательные поля:
Если НЕ Результат.externalId Тогда
  Ответ = Новый HTTPСервисОтвет(400);
  Ответ.УстановитьТелоИзСтроки('{"error": "Missing externalId"}');
  Возврат Ответ;
КонецЕсли;

*Сложность: Сложно | Категория: JSON парсинг*

#### 🟡 d11-fc-10: ParseJSON() vs ЧтениеJSON.ПрочитатьЗначение() — разница?

**Ответ:** ParseJSON() (8.3.6+): возвращает объект JSON. Обращение через точку: Результат.field. ЧтениеJSON.ПрочитатьЗначение(): возвращает Соответствие или Массив. Обращение через [] или ЧтениеЗначения. ParseJSON() удобнее и современнее. Рекомендуется для новых разработок.

*Сложность: Средне | Категория: JSON парсинг*

### Создание документа

#### 🔴 d11-fc-11: Как создать документ ЗаявкаНаРасход из JSON?

**Ответ:** Документ = Документы.ЗаявкаНаРасход.СоздатьДокумент();
Документ.Дата = Дата(Результат.date);
Документ.Организация = НайтиОрганизацию(Результат.organization);
Документ.Подразделение = НайтиПодразделение(Результат.department);
Документ.Ответственный = НайтиСотрудника(Результат.responsible);
Документ.ВнешнийИдентификатор = Результат.externalId;
// Заполнить ТЧ items...
Документ.Записать(); // или Провести()

*Сложность: Сложно | Категория: Создание документа*

#### 🔴 d11-fc-12: Где размещать логику создания документа: в HTTP-сервисе или в модуле менеджера?

**Ответ:** В МОДУЛЕ МЕНЕЖЕРА документа! HTTP-сервис = только приёмка JSON и вызов. Модуль менеджера: Документы.ЗаявкаНаРасход.СоздатьИзJSON(СтруктураJSON). Это паттерн разделения ответственности. HTTP-сервис не должен знать про внутреннюю структуру документа. Модуль менеджера инкапсулирует логику создания.

*Сложность: Сложно | Категория: Создание документа*

#### 🔴 d11-fc-13: Как избежать дублирования при повторной отправке JSON?

**Ответ:** Перед созданием: проверить по externalId. РС.СопоставлениеДокументовЭДО. Если запись существует — вернуть 200 с UUID существующего документа (идемпотентность). Если нет — создать, записать в РС, вернуть 201.
// Проверка:
ЗаписьЭДО = РС.НайтиПоВнешнемуИдентификатору(externalId);
Если ЗаписьЭДО Тогда Возврат СуществующийДокумент КонецЕсли;

*Сложность: Сложно | Категория: Создание документа*

#### 🔴 d11-fc-14: Как найти справочник по строке из JSON?

**Ответ:** // По наименованию:
Элемент = Справочники.Подразделения.НайтиПоНаименованию(ИмяИзJSON);
// По коду (если в JSON передают код):
Элемент = Справочники.СтатьиРасходов.НайтиПоКоду(КодИзJSON);
// По реквизиту:
Запрос = Новый Запрос(
  'ВЫБРАТЬ Первый 1 КАК Ссылка ИЗ Справочник.Организации'
  + ' ГДЕ ИНН = &ИНН');
Запрос.УстановитьПараметр('ИНН', ИННИзJSON);
Ссылка = Запрос.Выполнить().Ссылка;

*Сложность: Сложно | Категория: Создание документа*

#### 🟡 d11-fc-15: Что делать если справочник не найден по JSON?

**Ответ:** Варианты: (1) Вернуть 400 Bad Request с описанием ошибки. (2) Создать новый элемент справочника автоматически (автозаполнение). (3) Записать в журнал ошибок интеграции. Для Магнит: вероятно вариант (1) — ЭДО должен передавать корректные данные. Добавить в ответ: {'error': 'Подразделение IT-отдел не найдено', 'code': 'DEPARTMENT_NOT_FOUND'}.

*Сложность: Средне | Категория: Создание документа*

### Регистры: РН и РС

#### 🔴 d11-fc-16: ЗаявкаНаРасход: какие движения по РН нужны?

**Ответ:** ДВА регистра накопления:
(1) РН.БюджетныеЛимиты (Остатки) — Расход при проведении заявки. Уменьшает доступный лимит по статье/подразделению/периоду.
(2) РН.ФактическиеРасходы (Обороты) — Приход при проведении. Фиксирует факт расхода для аналитики оборотов за период.

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-17: РН.БюджетныеЛимиты — структура и движение

**Ответ:** Измерения: Организация, Подразделение, СтатьяРасхода, Период (дата).
Ресурс: Лимит (число).
Движение при проведении ЗаявкиНаРасход: РАСХОД.
Движение при УтвержденииБюджета: ПРИХОД.
Запрос остатков: Остатки на дату по подразделению/статье.
Проверка: если Остаток < 0 — бюджет превышен!

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-18: РН.ФактическиеРасходы — структура и движение

**Ответ:** Измерения: Организация, Подразделение, СтатьяРасхода.
Ресурсы: Сумма (число).
Движение: ПРИХОД при проведении ЗаявкиНаРасход.
Тип: Обороты (не Остатки!). Для аналитики: Обороты за месяц/квартал/год.
Виртуальная таблица: Обороты(Начало, Конец). Группировка по подразделению, статье.

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-19: ЗаявкаНаРасход: какие движения по РС нужны?

**Ответ:** ДВА регистра сведений:
(1) РС.СопоставлениеДокументовЭДО — связка externalId из ЭДО с ссылкой документа в 1С. Периодический, подчинён регистратору.
(2) РС.СостоянияСогласования — статус: Черновик/На согласовании/Согласовано/Отклонено. Периодический, подчинён регистратору.

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-20: РС.СопоставлениеДокументовЭДО — зачем и структура?

**Ответ:** Ресурс: Документ1С (документ). Измерения: ВнешнийИдентификатор (строка). Подчинён регистратору. Позволяет: (1) При повторной отправке — найти документ в 1С по externalId (идемпотентность). (2) В 1С — показать номер сметы в ЭДО. (3) Отладка интеграции: по какому документу из ЭДО создан.

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-29: Как работает РС.СопоставлениеДокументовЭДО?

**Ответ:** Структура: Измерение = ВнешнийИдентификатор (строка 100). Ресурс = Документ1С (Документ.ЗаявкаНаРасход). Периодический, подчинён регистратору. При создании документа из JSON: НаборЗаписей = РС.СоздатьНаборЗаписей(); Запись = НаборЗаписей.Добавить(); Запись.ВнешнийИдентификатор = externalId; Запись.Документ1С = Документ.Ссылка; НаборЗаписей.Записать();

*Сложность: Сложно | Категория: Регистры: РН и РС*

#### 🔴 d11-fc-30: Запрос: найти документ 1С по externalId из ЭДО

**Ответ:** ВЫБРАТЬ
  Сопоставление.Документ1С КАК Документ,
  Сопоставление.ВнешнийИдентификатор КАК ExternalId
ИЗ
  РегистрСведений.СопоставлениеДокументовЭДО КАК Сопоставление
ГДЕ
  Сопоставление.ВнешнийИдентификатор = &ExternalId

// Используется в HTTP-сервисе для проверки идемпотентности.
// Если найден — повторная отправка, вернуть существующий документ.

*Сложность: Сложно | Категория: Регистры: РН и РС*

### Проведение: движения

#### 🔴 d11-fc-21: ОбработкаПроведения ЗаявкиНаРасход: полный код

**Ответ:** Процедура ОбработкаПроведения(Отказ, РежимПроведения)
  Движения.БюджетныеЛимиты.Записывать = Истина;
  Движения.ФактическиеРасходы.Записывать = Истина;
  Для Каждого СтрокаТЧ Из Товары Цикл
    ЗаписьБюджет = Движения.БюджетныеЛимиты.Добавить();
    ЗаписьБюджет.Период = Дата;
    ЗаписьБюджет.Организация = Организация;
    ЗаписьБюджет.Подразделение = Подразделение;
    ЗаписьБюджет.СтатьяРасхода = СтрокаТЧ.Статья;
    ЗаписьБюджет.Лимит = СтрокаТЧ.Сумма * (-1); // РАСХОД
    
    ЗаписьРасход = Движения.ФактическиеРасходы.Добавить();
    ЗаписьРасход.Период = Дата;
    ЗаписьРасход.Организация = Организация;
    ЗаписьРасход.Подразделение = Подразделение;
    ЗаписьРасход.СтатьяРасхода = СтрокаТЧ.Статья;
    ЗаписьРасход.Сумма = СтрокаТЧ.Сумма; // ПРИХОД
  КонецЦикла;
КонецПроцедуры

*Сложность: Сложно | Категория: Проведение*

#### 🔴 d11-fc-22: Как проверить бюджет перед проведением заявки?

**Ответ:** В ОбработкаПроведения (или ПередЗаписью при Проведении):
Запрос = Новый Запрос;
Запрос.Текст = 'ВЫБРАТЬ Остатки.Лимит ИЗ'
  + ' РегистрНакопления.БюджетныеЛимиты.Остатки(&Дата, ...)' ;
Для Каждого Стр Из Товары Цикл
  Запрос.УстановитьПараметр('Статья', Стр.Статья);
  Остаток = Запрос.Выполнить().Лимит;
  Если Остаток < Стр.Сумма Тогда
    Сообщить('Бюджет превышен: ' + Стр.Статья);
    Отказ = Истина;
  КонецЕсли;
КонецЦикла;

*Сложность: Сложно | Категория: Проведение*

#### 🔴 d11-fc-23: Модуль менеджера: где создавать РС-записи при интеграции?

**Ответ:** В обработчике HTTP-сервиса, ПОСЛЕ создания и записи документа:
// В модуле менеджера ЗаявкиНаРасход:
Функция СоздатьИзJSON(ДанныеJSON) Экспорт
  Документ = СоздатьИЗаполнитьДокумент(ДанныеJSON);
  Документ.Записать(РежимЗаписиДокумента.Проведение);
  ЗаписатьСопоставлениеЭДО(Документ, ДанныеJSON.externalId);
  Возврат Документ.Ссылка;
КонецФункции
РС записывается через НаборЗаписей, не через движения документа!

*Сложность: Сложно | Категория: Проведение*

#### 🔴 d11-fc-24: РН.Остатки vs РН.Обороты: когда какой?

**Ответ:** РН.Остатки = баланс. Есть измерения + ресурсы. Виртуальные таблицы: Остатки, Обороты. Для бюджетного контроля: нужен остаток лимита на текущий момент = Остатки.
РН.Обороты = только движения. Виртуальная таблица: Обороты. Для аналитики 'сколько потрачено за период' = Обороты.
БюджетныеЛимиты = Остатки (контроль лимита). ФактическиеРасходы = Обороты (аналитика).

*Сложность: Сложно | Категория: Проведение*

### HTTP-сервис: полная реализация

#### 🔴 d11-fc-25: Полный HTTP-сервис: POST /hs/magnit/expense

**Ответ:** Функция ExpensePost(Запрос)
  Попытка
    ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку();
    Данные = Json.Прочитать(ТелоЗапроса);
    ПроверитьОбязательныеПоля(Данные);
    // Проверка идемпотентности
    Существующий = РС.СопоставлениеДокументовЭДО.Найти(Данные.externalId);
    Если Существующий Тогда
      Ответ = Новый HTTPСервисОтвет(200);
      Возврат Ответ;
    КонецЕсли;
    Ссылка = Документы.ЗаявкаНаРасход.СоздатьИзJSON(Данные);
    Ответ = Новый HTTPСервисОтвет(201);
    Ответ.УстановитьТелоИзСтроки('{"id": "' + Ссылка.УникальныйИдентификатор() + '"}');
    Возврат Ответ;
  Исключение
    Ответ = Новый HTTPСервисОтвет(500);
    Возврат Ответ;
  КонецПопытки;
КонецФункции

*Сложность: Сложно | Категория: HTTP-сервис*

#### 🔴 d11-fc-26: GET-метод: получить статус заявки по externalId

**Ответ:** Функция ExpenseGet(Запрос)
  ExternalId = Запрос.ПараметрыURL['externalId'];
  Если НЕ ExternalId Тогда
    Ответ = Новый HTTPСервисОтвет(400);
    Возврат Ответ;
  КонецЕсли;
  Запись = РС.СопоставлениеДокументовЭДО.Найти(ExternalId);
  Если НЕ Запись Тогда
    Ответ = Новый HTTPСервисОтвет(404);
    Возврат Ответ;
  КонецЕсли;
  Документ = Запись.Документ1С.ПолучитьОбъект();
  Статус = Документ.Статус;
  Ответ = Новый HTTPСервисОтвет(200);
  Ответ.УстановитьТелоИзСтроки('{"status": "' + Статус + '"}');
  Возврат Ответ;
КонецФункции

*Сложность: Сложно | Категория: HTTP-сервис*

#### 🔴 d11-fc-27: Обработка ошибок HTTP-сервиса: какие статусы вернуть?

**Ответ:** 200 OK — GET успешен (или POST при повторе — идемпотентность).
201 Created — POST, новый документ создан.
400 Bad Request — JSON невалиден или отсутствуют обязательные поля.
401 Unauthorized — нет аутентификации (если нужен Basic Auth).
404 Not Found — документ по externalId не найден.
409 Conflict — бюджет превышен, документ не проведён.
500 Internal Server Error — непредвиденная ошибка на сервере.
Важно: ВСЕГДА возвращать JSON с описанием ошибки.

*Сложность: Сложно | Категория: HTTP-сервис*

#### 🟡 d11-fc-28: Шаблоны URL в HTTP-сервисе: как настроить?

**Ответ:** Конфигуратор -> HTTP-сервисы -> Добавить.
Имя: MagnitExpenseAPI.
Шаблоны URL:
(1) /expense/{externalId} — GET, PUT (получить/обновить одну заявку)
(2) /expense — POST (создать новую заявку)
(3) /expense/status — POST (обновить статус из ЭДО)
Корневой URL: /hs/MagnitExpenseAPI/
Полный путь: /hs/MagnitExpenseAPI/expense?externalId=SM-001

*Сложность: Средне | Категория: HTTP-сервис*

### Мок-собеседование: интеграция

#### 🔴 d11-fc-31: Вопрос Магнит: 'Опишите архитектуру HTTP-интеграции с ЭДО'

**Ответ:** ЭДО (создание и согласование сметы) -> POST JSON -> HTTP-сервис 1С (/hs/magnit/expense) -> ParseJSON() -> Создание документа ЗаявкаНаРасход -> Движения: РН.БюджетныеЛимиты (Расход), РН.ФактическиеРасходы (Приход) -> РС.СопоставлениеДокументовЭДО (связка externalId с документом) -> Ответ 201 с UUID.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-32: Вопрос Магнит: 'Какие регистры нужны для бюджетной заявки?'

**Ответ:** РН.БюджетныеЛимиты (Остатки): Контроль лимитов. Расход при проведении. Измерения: Организация, Подразделение, СтатьяРасхода, Период. Ресурс: Лимит.
РН.ФактическиеРасходы (Обороты): Аналитика. Приход при проведении. Измерения: Организация, Подразделение, СтатьяРасхода. Ресурс: Сумма.
РС.СопоставлениеДокументовЭДО: Связка с ЭДО. РС.СостоянияСогласования: Статусы.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-33: Вопрос Магнит: 'Как обеспечить идемпотентность?'

**Ответ:** При повторной отправке JSON из ЭДО (сеть, retry): перед созданием документа проверяем РС.СопоставлениеДокументовЭДО по externalId. Если запись существует — возвращаем 200 OK с UUID существующего документа, НЕ создаём дубликат. Это стандартный паттерн для POST — превращаем неидемпотентный POST в идемпотентный через проверку уникальности.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-34: Вопрос Магнит: 'Где разместить код создания документа?'

**Ответ:** РАЗДЕЛЕНИЕ ОТВЕТСТВЕННОСТИ:
HTTP-сервис ( ExpensePost ): приёмка JSON -> ParseJSON -> проверка обязательных полей -> вызов модуля менеджера.
Модуль менеджера ( Документы.ЗаявкаНаРасход.СоздатьИзJSON ): создание объекта -> заполнение реквизитов -> Записать(Проведение) -> запись в РС.
Модуль объекта ( ОбработкаПроведения ): движения по РН.
Печать: модуль менеджера.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-35: Вопрос Магнит: 'Что произойдёт если бюджет превышен?'

**Ответ:** В ОбработкаПроведения: перед записью движений проверяем остаток по РН.БюджетныеЛимиты.Остатки(&Дата, Статья=...). Если остаток < сумма заявки — Отказ = Истина + Сообщить. HTTP-сервис получает отказ, возвращает 409 Conflict с описанием: {'error': 'Budget exceeded', 'article': 'Оборудование', 'available': 100000, 'requested': 250000}.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🟡 d11-fc-36: Вопрос Магнит: 'Как протестировать HTTP-сервис без ЭДО?'

**Ответ:** (1) Postman: отправить JSON вручную на /hs/magnit/expense. (2) cURL: curl -X POST -H 'Content-Type: application/json' -d @test.json http://localhost/hs/magnit/expense. (3) EDT: точка останова в обработчике HTTP-сервиса. (4) Фикстуры: файлы test.json с типовыми сценариями (нормальный, без обязательного поля, дубль). (5) Логирование: записывать входящий JSON в РС.ЛогИнтеграции.

*Сложность: Средне | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-37: Вопрос Магнит: 'Как обработать ситуацию когда 1С недоступна?'

**Ответ:** В ЭДО нужен механизм retry с backoff: при ошибке 500 или таймауте — повторить через 1 мин, 5 мин, 15 мин, 1 час. Также: очередь сообщений (RabbitMQ/Kafka) для гарантированной доставки. Журнал отправок в ЭДО: Success/Failed/Retry. Код ответа 503 Service Unavailable. 1С: при восстановлении — повторная отправка по логу.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🔴 d11-fc-38: Вопрос Магнит: 'Нужна ли транзакция при создании документа из HTTP?'

**Ответ:** ДА! НЕЯВНАЯ транзакция открывается при Документ.Записать(). Внутри неё: запись документа + запись движений по РН + запись РС. Если хоть что-то упадёт — ВСЕ откатится. Документ не создастся 'половинчато'. Платформа гарантирует атомарность. Дополнительно: НачатьТранзакцию() если нужно записать ещё объекты.

*Сложность: Сложно | Категория: Мок-собес: интеграция*

#### 🟡 d11-fc-39: Вопрос Магнит: 'Как логировать входящие JSON для отладки?'

**Ответ:** РС.ЛогИнтеграцииЭДО: Измерения: ExternalId, ДатаОтправки. Ресурсы: ТелоJSON (строка), СтатусОтвета (число), ОписаниеОшибки (строка), Документ1С. Периодический. Записывается ДО создания документа. Позволяет: отладить проблемы, перепровести после исправления, аудит всех интеграций.

*Сложность: Средне | Категория: Мок-собес: интеграция*

#### 🟡 d11-fc-40: Вопрос Магнит: 'Где разместить процедуру печати заявки?'

**Ответ:** В МОДУЛЕ МЕНЕЖЕРА (не модуль объекта, не форма!). Модуль менеджера = общие операции для типа документов. Печать = общая операция. Вызов: из модуля формы -> Документы.ЗаявкаНаРасход.Печать(Объект). Модуль менеджера генерирует ТабличныйДокумент. Форма вызывает Показать().

*Сложность: Средне | Категория: Мок-собес: интеграция*

### Диаграмма: Полная схема: ЭДО → HTTP → 1С → Регистры

*Как данные сметы проходят от ЭДО до регистров 1С*

#### Шаг 1: ЭДО: Создание сметы

Пользователь создаёт смету в системе ЭДО. Заполняет статьи, суммы, подразделение. Смета проходит внутреннее согласование.
- Смета в ЭДО — первичный документ
- Содержит: externalId, дата, организация, подразделение, статьи, суммы
- Проходит workflow согласования в ЭДО

#### Шаг 2: ЭДО: Согласовано → POST JSON

После согласования ЭДО отправляет HTTP POST на /hs/MagnitExpenseAPI/expense с JSON-телом
- Метод: POST
- Content-Type: application/json
- Заголовки: Authorization Basic (если нужен)
- Тело: {externalId, date, organization, department, items, totalAmount}

#### Шаг 3: 1С: HTTP-сервис — приёмка

HTTP-сервис получает запрос. ParseJSON(). Проверка обязательных полей. Проверка идемпотентности через РС.
- ParseJSON() — разбор тела
- Проверка: externalId, date, organization есть?
- РС.СопоставлениеДокументовЭДО — есть ли уже?
- 400 если JSON невалиден, 200 если дубль

#### Шаг 4: 1С: Создание документа

Модуль менеджера создаёт ЗаявкаНаРасход. Заполняет реквизиты из JSON. Записывает с проведением.
- Документы.ЗаявкаНаРасход.СоздатьИзJSON()
- Дата, Организация, Подразделение, Ответственный
- ТЧ Товары: СтатьяРасхода, Сумма
- Записать(РежимЗаписиДокумента.Проведение)

#### Шаг 5: 1С: Движения по регистрам

ОбработкаПроведения формирует движения: РН.БюджетныеЛимиты (Расход), РН.ФактическиеРасходы (Приход)
- РН.БюджетныеЛимиты: Расход = -Сумма по каждой статье
- РН.ФактическиеРасходы: Приход = +Сумма
- Проверка: остаток лимита >= 0 (иначе Отказ)
- Все движения в одной неявной транзакции

#### Шаг 6: 1С: РС + Ответ

Запись в РС.СопоставлениеДокументовЭДО. Возврат HTTP 201 Created с UUID документа.
- РС: externalId <-> Документ1С
- Ответ: 201 Created
- Тело: {id: 'UUID документа'}
- ЭДО получает UUID для дальнейшей работы

### Диаграмма: Структура регистров для ЗаявкиНаРасход

*Какие регистры нужны и какие движения формирует документ*

#### Шаг 1: РН.БюджетныеЛимиты (Остатки)

Контроль бюджетных лимитов. Приход = утверждение бюджета. Расход = проведение заявки.
- Измерения: Организация, Подразделение, СтатьяРасхода, Период
- Ресурс: Лимит (число)
- Приход: документ УтверждениеБюджета
- Расход: документ ЗаявкаНаРасход
- Виртуальная таблица: Остатки(&Дата)

#### Шаг 2: РН.ФактическиеРасходы (Обороты)

Аналитика фактических расходов за период. Только Приход при проведении заявки.
- Измерения: Организация, Подразделение, СтатьяРасхода
- Ресурс: Сумма (число)
- Движение: Приход при проведении ЗаявкиНаРасход
- Виртуальная таблица: Обороты(&Начало, &Конец)
- Аналитика по подразделениям и статьям за период

#### Шаг 3: РС.СопоставлениеДокументовЭДО

Связка внешнего ID из ЭДО с документом 1С. Идемпотентность интеграции.
- Измерение: ВнешнийИдентификатор (строка)
- Ресурс: Документ1С (Документ.ЗаявкаНаРасход)
- Подчинён регистратору
- Записывается после создания документа
- Используется для проверки дублей при повторе

#### Шаг 4: РС.СостоянияСогласования

Статусы согласования сметы из ЭДО: Черновик, На согласовании, Согласовано, Отклонено.
- Измерение: ВнешнийИдентификатор
- Ресурс: Статус (Перечисление)
- Периодический, подчинён регистратору
- Обновляется через GET от ЭДО при изменении статуса
- Позволяет отслеживать статус без запроса в ЭДО

### Диаграмма: Жизненный цикл HTTP-запроса в 1С

*Что происходит когда ЭДО отправляет POST на HTTP-сервис 1С*

#### Шаг 1: Приём запроса

Сервер 1С получает HTTP POST. Платформа определяет HTTP-сервис и вызывает обработчик ExpensePost.
- URL: /hs/MagnitExpenseAPI/expense
- Платформа маршрутизирует запрос по URL
- Вызывается функция ExpensePost(Запрос)
- Запрос = HTTPСервисЗапрос

#### Шаг 2: Получение тела JSON

Запрос.ПолучитьТелоКакСтроку() → Строка JSON. Json.Прочитать() → Объект.
- ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку()
- Проверка Content-Type
- ParseJSON / ЧтениеJSON
- Результат = объект JSON (Структура)

#### Шаг 3: Валидация данных

Проверка обязательных полей, типов данных, бизнес-правил.
- externalId не пустой?
- date корректная дата?
- items не пустой массив?
- Каждый item: article + amount?
- 400 Bad Request при ошибке

#### Шаг 4: Бизнес-логика

Создание документа через модуль менеджера. Проведение. Запись РС.
- Документы.ЗаявкаНаРасход.СоздатьИзJSON()
- Заполнение реквизитов из JSON
- Записать(Проведение) = транзакция
- РС.СопоставлениеДокументовЭДО

#### Шаг 5: Формирование ответа

HTTPСервисОтвет с кодом состояния и JSON-телом.
- 201 Created при успехе
- 200 OK при дубле (идемпотентность)
- 400/409/500 при ошибках
- Content-Type: application/json
- UUID документа в теле ответа

### Код: ic-23 (НЕ НАЙДЕН)

### Код: ic-25 (НЕ НАЙДЕН)

### Код: ic-24 (НЕ НАЙДЕН)

### Код: ic-26 (НЕ НАЙДЕН)

### Сценарии: Архитектура интеграции

- **sc-24** (НЕ НАЙДЕН)

### Сценарии: Регистры: РН и РС

- **sc-25** (НЕ НАЙДЕН)

### Сценарии: HTTP-сервис: полная реализация

- **sc-26** (НЕ НАЙДЕН)

### Сценарии: Мок-собеседование: интеграция

- **sc-27** (НЕ НАЙДЕН)

- **sc-28** (НЕ НАЙДЕН)

---

# День 12: День 12: СКД и сложные запросы

> СхемаКомпоновки, многопакетные запросы, План/Факт, сводная таблица, оптимизация

**Фаза:** Фаза 12 | **Категорий:** 6 | **Карточек:** 40

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | СКД основы | ~70мин | 8 | ✅ | ✅ | 1 |
| 2 | Временные таблицы и ВТ | ~60мин | 6 | ✅ | ✅ | 1 |
| 3 | План/Факт и бюджетирование | ~60мин | 5 | ✅ | — | 2 |
| 4 | Группировки, Итоги, Сводная | ~40мин | 4 | — | — | 1 |
| 5 | Оптимизация СКД | ~30мин | 3 | — | — | — |
| 6 | Мок-собеседование: СКД | ~90мин | 14 | — | — | — |

---

## Карточки

### СКД основы

#### 🟢 d12-fc-1: Что такое СКД и для чего нужна?

**Ответ:** СхемаКомпоновкиДанных (СКД) = встроенный механизм 1С для создания отчётов без написания SQL. Описывает: наборы данных (источники), группировки (иерархия), параметры (фильтры), поля, условное оформление. Платформа сама генерирует SQL из описания СКД.

*Сложность: Легко | Категория: СКД*

#### 🔴 d12-fc-2: Какой цикл выполнения СКД от схемы до экрана?

**Ответ:** 4 этапа: (1) СхемаКомпоновкиДанных — описание отчёта (исходник). (2) КомпоновщикМакетаКомпоновкиДанных — компилирует схему с учётом настроек в макет. (3) ПроцессорКомпоновкиДанных — выполняет запросы к БД, получает данные. (4) ПроцессорВыводаРезультатаКомпоновкиДанных — рендерит в ТабличныйДокумент или диаграмму.

*Сложность: Сложно | Категория: СКД*

#### 🟡 d12-fc-3: Где выполняется СКД — на клиенте или на сервере?

**Ответ:** ВСЁ на СЕРВЕРЕ! СхемаКомпоновкиДанных, Компоновщик, Процессор, ТабличныйДокумент — все серверные объекты. На клиент передаётся готовый ТабличныйДокумент через ЗначениеВРеквизитФормы() или временное хранилище.

*Сложность: Средне | Категория: СКД*

#### 🟡 d12-fc-4: Что такое 'Набор данных' в СКД и какие типы бывают?

**Ответ:** Набор данных = источник данных для СКД. Типы: (1) Запрос — основной, выполняет запрос к регистрам/справочникам. (2) Объект — уже существующая ТаблицаЗначений. (3) Объединение — объединяет несколько наборов. (4) РегистрСведений — прямой доступ к РС без запроса. Для отчётов в 90% случаев — Запрос.

*Сложность: Средне | Категория: СКД*

#### 🟡 d12-fc-5: Что такое вычисляемое поле в СКД?

**Ответ:** Поле, значение которого вычисляется по выражению из других полей. Пример: Вычислить('СуммаПлан - СуммаФакт') для отклонения. Вычислить('СуммаФакт / СуммаПлан * 100') для процента. Можно ссылаться на другие поля текущей строки. Не требует изменения запроса.

*Сложность: Средне | Категория: СКД*

#### 🟢 d12-fc-6: Условное оформление в СКД — как работает?

**Ответ:** Правила визуального оформления строк/ячеек по условию. Пример: если ПроцентВыполнения > 100% → красный фон (перерасход). Если < 80% → жёлтый. Настраивается в конструкторе СКД или программно. Условие: Поле > Значение. Формат: цвет фона, цвет текста, шрифт.

*Сложность: Легко | Категория: СКД*

#### 🔴 d12-fc-7: Параметры СКД — почему это критически важно?

**Ответ:** Параметры = значения, которые подставляются в запрос перед выполнением. Пример: &ПериодНачало, &ПериодКонец, &Организация. Если параметр НЕ ЗАПОЛНЕН — виртуальные таблицы вернут ПУСТОЙ результат! Это причина №1 проблем с отчётами: 'Отчёт пустой, а в консоли запросов данные есть' = параметры не заполнены.

*Сложность: Сложно | Категория: СКД*

#### 🔴 d12-fc-8: Как программно установить параметры СКД?

**Ответ:** Настройки = Компоновщик.Настройки;
Настройки.ПараметрыДанных.Элементы.Найти('ПериодНачало').Значение = НачалоМесяца(ТекущаяДата());
Настройки.ПараметрыДанных.Элементы.Найти('ПериодКонец').Значение = КонецМесяца(ТекущаяДата());
Настройки.ПараметрыДанных.Элементы.Найти('Организация').Значение = Объект.Организация;
// Потом: Компоновщик.Выполнить(Схема, Настройки);

*Сложность: Сложно | Категория: СКД*

### Временные таблицы и ВТ

#### 🟡 d12-fc-9: Что такое временная таблица (ВТ) в запросе 1С?

**Ответ:** ВТ = таблица, созданная внутри запроса через ПОМЕСТИТЬ. Живёт только внутри одного выполнения запроса. Материализуется на стороне СУБД (физическая таблица в temp). Последующие пакеты запроса могут ссылаться на ВТ. Удаляется автоматически после выполнения запроса.

*Сложность: Средне | Категория: Временные таблицы*

#### 🔴 d12-fc-10: ВТ vs вложенный запрос — что быстрее и почему?

**Ответ:** ВТ быстрее при многократном использовании! ВТ материализуется ОДИН раз, потом читается из temp-таблицы (Index Scan). Вложенный запрос = подзапрос выполняется для КАЖДОЙ строки основного запроса. Правило: если результат нужен 2+ раз в разных местах запроса → ВТ. Если один раз → можно вложенный.

*Сложность: Сложно | Категория: Временные таблицы*

#### 🟡 d12-fc-11: Можно ли использовать ВТ из одного пакета в другом?

**Ответ:** ДА! Это основное предназначение ВТ. Порядок: Пакет 1 создаёт ВТ_Движения через ПОМЕСТИТЬ. Пакет 2 читает ИЗ ВТ_Движения, обогащает, создаёт ВТ_Обогащённые. Пакет 3 — финальная выборка из ВТ_Обогащённые. Порядок пакетов = порядок выполнения!

*Сложность: Средне | Категория: Временные таблицы*

#### 🟡 d12-fc-12: Может ли ВТ ссылаться на ВТ из следующего (будущего) пакета?

**Ответ:** НЕТ! Пакеты выполняются последовательно. Нельзя использовать ВТ, которая ещё не создана. Порядок пакетов = порядок выполнения. Это частая ошибка: переставить пакеты местами = ошибка 'Таблица не найдена'.

*Сложность: Средне | Категория: Временные таблицы*

#### 🟡 d12-fc-13: ОБЪЕДИНИТЬ ВСЕ — создаёт ли ВТ?

**Ответ:** НЕТ! ОБЪЕДИНИТЬ ВСЕ = объединяет результаты пакетов в один набор данных. Это НЕ создаёт ВТ. Для ВТ нужен оператор ПОМЕСТИТЬ. ОБЪЕДИНИТЬ ВСЕ полезен для объединения данных из разных источников в одну плоскую структуру.

*Сложность: Средне | Категория: Временные таблицы*

#### 🟢 d12-fc-14: Сколько ВТ можно создать в одном запросе?

**Ответ:** Технически — без ограничения. Практически — не более 5-7 ВТ. Причины: (1) Читаемость падает. (2) Каждая ВТ = создание temp-таблицы в СУБД (overhead). (3) Отладка становится сложной. Если нужно больше — пересмотри архитектуру.

*Сложность: Легко | Категория: Временные таблицы*

### План/Факт и бюджетирование

#### 🔴 d12-fc-15: Как спроектировать План/Факт отчёт в 1С?

**Ответ:** ДВА регистра накопления: (1) РН.БюджетныеЛимиты (Остатки) — хранит план. Приход при утверждении бюджета. (2) РН.ФактическиеРасходы (Обороты) — хранит факт. Приход при проведении ЗаявкиНаРасход. В СКД — два набора данных, связанных через Статья+Подразделение. Вычисляемые поля для отклонений.

*Сложность: Сложно | Категория: План/Факт*

#### 🔴 d12-fc-16: Почему План = РН.Остатки, а Факт = РН.Обороты?

**Ответ:** План = лимит на период. Нужен ТЕКУЩИЙ остаток лимита (сколько осталось) → Остатки. Факт = зафиксированные расходы. Нужна аналитика ЗА ПЕРИОД (сколько потрачено в месяце) → Обороты. Разные бизнес-задачи → разные типы регистров.

*Сложность: Сложно | Категория: План/Факт*

#### 🔴 d12-fc-17: Как объединить План и Факт в одном запросе?

**Ответ:** Три пакета: (1) ПОМЕСТИТЬ ВТ_План из РН.БюджетныеЛимиты.Остатки. (2) ПОМЕСТИТЬ ВТ_Факт из РН.ФактическиеРасходы.Обороты с СУММА(). (3) ЛЕВОЕ СОЕДИНЕНИЕ ВТ_План и ВТ_Факт по Статья+Подразделение. ЕСТЬNULL(Факт, 0) для статей без расходов.

*Сложность: Сложно | Категория: План/Факт*

#### 🔴 d12-fc-18: Почему ЛЕВОЕ СОЕДИНЕНИЕ, а не ВНУТРЕННЕЕ при объединении План/Факт?

**Ответ:** ЛЕВОЕ = все статьи из плана, даже без факта. ВНУТРЕННЕЕ = только статьи с фактом. Если по статье 'Аренда' не было расходов — ВНУТРЕННЕЕ потеряет эту строку. Руководитель не увидит что лимит не использован. ЛЕВОЕ + ЕСТЬNULL(Факт, 0) = полная картина.

*Сложность: Сложно | Категория: План/Факт*

#### 🔴 d12-fc-19: Как вычислить процент выполнения бюджета в запросе/СКД?

**Ответ:** ВЫРАЗИТЬ(ЕСТЬNULL(СуммаФакт, 0) / СуммаПлан * 100 КАК ПроцентВыполнения). ВЫРАЗИТЬ нужен для корректного типа числа. ЕСТЬNULL — если факт = NULL (нет расходов). В СКД: вычисляемое поле Вычислить('СуммаФакт / СуммаПлан * 100').

*Сложность: Сложно | Категория: План/Факт*

### Группировки, Итоги, Сводная

#### 🟡 d12-fc-20: ИТОГИ ПО в запросе — как работает с регистрами?

**Ответ:** ИТОГИ ПО = виртуальные итоги платформы. При ИТОГИ ПО Подразделение: 1С группирует записи по подразделению и считает SUM/МAX/MIN. В СКД = аналогично, группировки задают иерархию. ОбщийИтог = итог по всему запросу.

*Сложность: Средне | Категория: Группировки*

#### 🟡 d12-fc-21: Что такое СрезПоследних и когда использовать?

**Ответ:** Виртуальная таблица РегистраСведений. Возвращает ПОСЛЕДНЮЮ запись на указанную дату для каждого сочетания измерений. Пример: текущая должность сотрудника на сегодня. Используется для периодических РС где нужно актуальное значение.

*Сложность: Средне | Категория: Группировки*

#### 🟡 d12-fc-22: Остатки() vs Обороты() — когда какой вызывать?

**Ответ:** Остатки(&Дата) — возвращает ТЕКУЩИЙ баланс на дату. Для РН.Остатки. Пример: сколько денег на счёте на 01.08. Обороты(&Начало, &Конец) — возвращает движения за период. Для РН.Оборот и РН.Остатки. Пример: сколько поступило за квартал.

*Сложность: Средне | Категория: Группировки*

#### 🔴 d12-fc-23: Сводная таблица в СКД — как создать?

**Ответ:** В СКД: (1) Добавить группировку по строкам (Подразделение). (2) Добавить вложенную группировку по колонкам (Месяц). (3) В свойствах группировки 'Месяц' → 'Отображать в заголовках колонок'. Платформа автоматически создаст сводную таблицу: строки = подразделения, колонки = месяцы.

*Сложность: Сложно | Категория: Группировки*

### Оптимизация СКД

#### 🟡 d12-fc-24: Почему отчёт СКД работает медленно — основные причины?

**Ответ:** (1) Не заполнены параметры → виртуальные таблицы сканируют ВСЮ историю. (2) Нет индексов на полях группировки. (3) Слишком много условного оформления. (4) Запрос в СКД — вложенные вместо ВТ. (5) Группировки по полям без индексов. (6) Итоги по большому количеству записей без иерархии.

*Сложность: Средне | Категория: Оптимизация*

#### 🟡 d12-fc-25: Как ускорить медленный СКД-отчёт?

**Ответ:** (1) Заполнить параметры периода! Ограничить диапазон. (2) Индексы на полях измерений и ресурсов. (3) Заменить вложенные запросы на ВТ. (4) Уменьшить глубину итогов. (5) Проверить план запроса в консоли. (6) Убрать лишние поля из запроса.

*Сложность: Средне | Категория: Оптимизация*

#### 🟡 d12-fc-26: Сгруппировать vs АвтоПорядок в СКД — в чём разница?

**Ответ:** Сгруппировать = 按 указанным полю группировать (Aggregate). АвтоПорядок = упорядочить внутри группы. При ИТОГИ ПО Подразделение СГРУППИРОВАТЬ ПО Статья = группы внутри подразделения. УПОРЯДОЧИТЬ ПО = сортировка результата (не агрегация).

*Сложность: Средне | Категория: Оптимизация*

### Мок-собеседование: СКД

#### 🟡 d12-fc-27: Вопрос: 'В СКД пустой отчёт. В консоли запросов тот же запрос возвращает данные. В чём причина?'

**Ответ:** 90% — не заполнены параметры СКД! &ПериодНачало и &ПериодКонец = Неопределено → виртуальные таблицы Обороты() вернут пустой результат. В консоли ты подставляешь даты вручную. Решение: программно заполнить Настройки.ПараметрыДанных.Элементы.

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-28: Вопрос: 'Как вывести в СКД 4 колонки: План, Факт, Отклонение, % выполнения?'

**Ответ:** 2 набора данных в СКД: (1) Запрос к РН.БюджетныеЛимиты.Остатки. (2) Запрос к РН.ФактическиеРасходы.Обороты. Связь через общие поля (Статья+Подразделение). 2 вычисляемых поля: Отклонение = Вычислить('План - Факт'), % = Вычислить('Факт/План*100'). Условное оформление: красный если > 100%.

*Сложность: Сложно | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-29: Вопрос: 'Где должен храниться макет СКД — в справочнике или в обработке?'

**Ответ:** В макете обработки или отчёта! Макет СКД = СхемаКомпоновкиДанных. Хранится в конфигурации (внутри объекта Обработка или Отчёт). Не в справочнике! Справочник = данные для пользователей. Макет СКД = технический объект для разработчика.

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🟢 d12-fc-30: Вопрос: 'Можно ли изменить настройки СКД пользователем и сохранить их?'

**Ответ:** Да! Это встроенная функция СКД. Пользователь может: отобрать, сгруппировать, изменить порядок, условное оформление. Сохранить варианты — ВариантыОтчёта. Схема = стандартный набор, Вариант = пользовательская настройка поверх схемы.

*Сложность: Легко | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-31: Вопрос: 'Что делать если нужно вывести данные в диаграмму через СКД?'

**Ответ:** В настройках СКД → тип вывода = Диаграмма. Выбрать тип: гистограмма, круговая, линейная. Серия = группировка по измерению. Точка = ресурс. ПроцессорВыводаРезультатаКомпоновкиДанныхВДиаграмму. Можно переключать между табличным и графическим видом.

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-32: Вопрос: 'Временные таблицы — где физически создаются?'

**Ответ:** На стороне СУБД! В PostgreSQL — в pg_temp (temp schema). В SQL Server — в tempdb. 1С отправляет ПОМЕСТИТЬ как CREATE TEMP TABLE + INSERT. Последующие пакеты = SELECT FROM temp_table. Автоматически удаляются при завершении запроса.

*Сложность: Сложно | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-33: Вопрос: 'Почему в СКД нельзя написать прямой SQL к таблицам БД?'

**Ответ:** Потому что СКД = абстракция поверх SQL. 1С управляет физической структурой таблиц. Имена полей в БД != имена в конфигураторе. Виртуальные таблицы = оптимизированный SQL, который платформа генерирует сама. Прямой SQL = нарушит переносимость между СУБД и версиями платформы.

*Сложность: Сложно | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-34: Вопрос: 'Как программно добавить отбор в СКД?'

**Ответ:** Отбор = Настройки.Отбор.Элементы.Добавить(Тип('ЭлементОтбораКомпоновкиДанных'));
Отбор.ЛевоеЗначение = Новый ПолеКомпоновкиДанных('Подразделение');
Отбор.ВидСравнения = ВидСравнения.Равно;
Отбор.ПравоеЗначение = Объект.Подразделение;
// Потом: Компоновщик.Выполнить(Схема, Настройки);

*Сложность: Сложно | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-35: Вопрос: 'Как вывести СКД в табличный документ на форме?'

**Ответ:** 4 объекта: КомпоновщикМакетаКомпоновкиДанных → ПроцессорКомпоновкиДанных → ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент → ТабличныйДокумент. Финал: ЗначениеВРеквизитФормы(ДокументРезультат, 'Результат'). Всё на сервере!

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-36: Вопрос: 'ЕСТЬ NULL vs ЕСТЬNULL() — где что использовать?'

**Ответ:** ЕСТЬ NULL — оператор в запросе. Проверяет: значение является NULL. Пример: ГДЕ Контрагент ЕСТЬ NULL. ЕСТЬNULL(Поле, Значение) — функция в запросе. Заменяет NULL на значение. Пример: ЕСТЬNULL(СуммаФакт, 0). Первый — проверка (в WHERE/ГДЕ). Второй — подстановка (в SELECT/ВЫБРАТЬ).

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-37: Вопрос: 'Сводная таблица в 1С — это СКД или отдельный механизм?'

**Ответ:** Сводные таблицы 1С = построены НА СКД. В СКД есть группировки, и если одну из них переключить в режим 'Заголовки колонок' — получится сводная таблица. Например: строки = Подразделение, колонки = Месяцы, ячейки = Сумма.

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-38: Вопрос: 'Как отладить СКД если она ничего не выводит?'

**Ответ:** Пошагово: (1) Скопируй запрос из СКД в консоль запросов — проверь данные. (2) Заполни параметры вручную. (3) Если в консоли есть данные — проблема в настройках СКД. (4) Проверь: параметры заполнены? отборы не противоречат? группировки правильные? (5) Добавь точку останова перед Компоновщик.Выполнить().

*Сложность: Сложно | Категория: Мок-собес: СКД*

#### 🟡 d12-fc-39: Вопрос: 'Как запретить пользователю изменять настройки СКД?'

**Ответ:** Настройки.ПользовательскиеНастройки.Элементы.Очистить() — убрать пользовательские настройки. Или установить флаг: Компоновщик.ИспользоватьПользовательскиеНастройки = Ложь. Тогда пользователь видит только стандартный вид, не может менять отборы и группировки.

*Сложность: Средне | Категория: Мок-собес: СКД*

#### 🔴 d12-fc-40: Вопрос: 'Магнит: как вывести отчёт 'Исполнение бюджета' для руководителя?'

**Ответ:** СКД-отчёт с 2 наборами данных: (1) РН.БюджетныеЛимиты.Остатки — план. (2) РН.ФактическиеРасходы.Обороты — факт. Группировка: Подразделение → СтатьяРасхода. Вычисляемые поля: Отклонение, % выполнения. Условное оформление: красный если >100% (перерасход), зелёный если <90% (экономия). Параметры: Период, Организация.

*Сложность: Сложно | Категория: Мок-собес: СКД*

### Диаграмма: Жизненный цикл отчёта СКД: от схемы до экрана

*Как СКД преобразует схему компоновки в видимый пользователю отчёт*

#### Шаг 1: СхемаКомпоновкиДанных

Исходник: наборы данных (запросы), поля, группировки, ресурсы, параметры, условия, условное оформление
- Содержит ВСЁ описание отчёта
- Запросы к регистрам и справочникам
- Иерархия группировок (ПоПодразделению -> ПоСтатее)
- Параметры для фильтрации

#### Шаг 2: КомпоновщикМакета

Платформа компилирует СКД в макет на основе настроек пользователя
- Применяет пользовательские настройки (отбор, группировки)
- Генерирует программный код макета
- Определяет: какие поля выводить, как группировать
- Получает ТабличныйДокумент

#### Шаг 3: ПроцессорКомпоновкиДанных

Выполняет запросы к БД, получает данные и формирует результат
- Выполняет запросы из наборов данных СКД
- Обрабатывает итоги и группировки
- Заполняет ресурсы и вычисляемые поля
- Работает на сервере — может быть тяжёлым

#### Шаг 4: ПроцессорВыводаРезультата

Рендерит результат в ТабличныйДокумент или ДокументHTML
- Применяет условное оформление
- Формирует итоги и заголовки
- Выводит в табличный документ / диаграмму
- Пользователь видит готовый отчёт

### Диаграмма: Многопакетный запрос: архитектура данных для СКД

*Как запрос из 3-5 пакетов собирает данные для сложного отчёта*

#### Шаг 1: Пакет 1: Временная таблица (ВТ)

Загружаем исходные данные в ВТ_Движения из регистров
- ВЫБРАТЬ ... ПОМЕСТИТЬ ВТ_Движения
- Движения регистра за период
- Фильтр по организации, подразделению
- ВТ материализуется — быстрый доступ

#### Шаг 2: Пакет 2: ВТ с аналитикой

Обогащаем ВТ_Движения данными из справочников
- ЛЕВОЕ СОЕДИНЕНИЕ с ВТ_Статьи, ВТ_Подразделения
- Добавляем наименования, коды
- Рассчитываем дополнительные поля
- ВЫБРАТЬ ... ПОМЕСТИТЬ ВТ_Обогащённые

#### Шаг 3: Пакет 3: Итоговый запрос

Финальная выборка с группировками и итогами
- СГРУППИРОВАТЬ ПО Статья, Подразделение
- СУММА(Сумма), МАКСИМУМ(Дата)
- ОБЪЕДИНИТЬ ВСЕ если нужно несколько вариантов
- Сортировка для СКД

#### Шаг 4: СКД использует запрос как источник

СКД берёт результат последнего пакета как данные для отчёта
- СКД выполняет запрос целиком
- Платформа управляет параметрами
- Пользовательские настройки применяются поверх
- Быстрый рендеринг благодаря ВТ

### Диаграмма: Архитектура План/Факт в 1С: два регистра → один отчёт

*Как план и факт из разных регистров объединяются в СКД*

#### Шаг 1: РН.БюджетныеЛимиты (Остатки)

Хранит утверждённый бюджет. Приход — при утверждении бюджета. Расход — при проведении заявки
- Тип: Остатки (баланс)
- Измерения: Статья, Подразделение, Период
- Ресурс: Лимит (сумма плана)
- Виртуальная таблица: Остатки(&Дата)

#### Шаг 2: РН.ФактическиеРасходы (Обороты)

Хранит фактические расходы. Приход — при проведении ЗаявкиНаРасход
- Тип: Обороты (движения за период)
- Измерения: Статья, Подразделение
- Ресурс: Сумма (факт)
- Виртуальная таблица: Обороты(&Начало, &Конец)

#### Шаг 3: СКД: объединение двух наборов данных

Два набора данных в СКД, связанных через общие поля Статья+Подразделение
- Набор 1: запрос к РН.БюджетныеЛимиты.Остатки
- Набор 2: запрос к РН.ФактическиеРасходы.Обороты
- Связь: СтатьяРасхода + Подразделение
- Вычисляемые поля: Отклонение, % выполнения

#### Шаг 4: Отчёт: План/Факт/Отклонение

Готовый отчёт с условным оформлением: зелёный = экономия, красный = перерасход
- Колонки: Статья | План | Факт | Отклонение | %
- Условное оформление: красный если Факт > План
- Группировки: Подразделение → Статья
- Итоги по каждому подразделению

### Код: Программный вывод отчёта СКД на форму

*Как сгенерировать и вывести СКД-отчёт в табличный документ на форме*

```bsl
&НаСервере
Процедура СформироватьОтчётНаСервере()
    // 1. Загружаем схему СКД из макета
    СхемаСКД = ПолучитьМакет('ОсновнаяСхемаКомпоновкиДанных');
    Компоновщик = Новый КомпоновщикМакетаКомпоновкиДанных;
    Настройки = Компоновщик.Настройки;

    // 2. Устанавливаем параметры периода
    Настройки.ПараметрыДанных.Элементы.Найти('ПериодНачало').Значение = НачалоМесяца(ТекущаяДата());
    Настройки.ПараметрыДанных.Элементы.Найти('ПериодКонец').Значение = КонецМесяца(ТекущаяДата());
    Настройки.ПараметрыДанных.Элементы.Найти('Организация').Значение = Объект.Организация;

    // 3. Применяем пользовательский отбор
    Если ЗначениеЗаполнено(Объект.Подразделение) Тогда
        ОтборСтатья = Настройки.Отбор.Элементы.Добавить(Тип('ЭлементОтбораКомпоновкиДанных'));
        ОтборСтатья.ВидСравнения = ВидСравнения.Равно;
        ОтборСтатья.ЛевоеЗначение = Новый ПолеКомпоновкиДанных('Подразделение');
        ОтборСтатья.ПравоеЗначение = Объект.Подразделение;
    КонецЕсли;

    // 4. Формируем макет
    МакетКомпоновки = Компоновщик.Выполнить(СхемаСКД, Настройки);
    Процессор = Новый ПроцессорКомпоновкиДанных;
    Процессор.Инициализировать(МакетКомпоновки);

    // 5. Вывод в ТабличныйДокумент
    ДокументРезультат = Новый ТабличныйДокумент;
    ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
    ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
    ПроцессорВывода.Вывести();

    ЗначениеВРеквизитФормы(ДокументРезультат, 'Результат');
КонецПроцедуры
```

**Пояснения к строкам:**

- **Строка 1:** &НаСервере — ВСЯ работа с СКД выполняется ТОЛЬКО на сервере. ТабличныйДокумент — серверный объект.
- **Строка 4:** Схема компоновки обычно хранится в макете конфигурации (в обработке или отчёте).
- **Строка 9:** Параметры — критично! Если не заполнить &ПериодНачало/Конец, виртуальные таблицы вернут пустой результат.
- **Строка 15:** Программный отбор = аналог пользовательского отбора. Добавляется в коллекцию Настройки.Отбор.
- **Строка 22:** Компоновщик.Выполнить() — ключевой метод. Генерирует макет, учитывая настройки и параметры.
- **Строка 27:** ТабличныйДокумент = результат. Через ЗначениеВРеквизитФормы передаётся на форму для отображения.
- **Строка 32:** ЗначениеВРеквизитФормы() — передаёт серверный объект в реквизит формы для отображения на клиенте.

**Квиз:** Где формируется макет СКД и где выводится результат?

- ⬜ **A.** Всё на клиенте — СКД работает на клиенте
- ✅ **B.** Формирование на сервере, вывод тоже на сервере, передача на форму через ЗначениеВРеквизитФормы
- ⬜ **C.** Формирование макета на клиенте, вывод на сервере
- ⬜ **D.** Всё в фоновом задании, результат в временном хранилище

*Объяснение: Всё на сервере! СхемаКомпоновкиДанных, Компоновщик, Процессор — серверные объекты. ТабличныйДокумент тоже серверный. На клиент передаётся готовый для отображения объект через ЗначениеВРеквизитФормы().*

### Код: Многопакетный запрос: План/Факт с ВТ

*Как написать запрос из 3 пакетов для объединения плана и факта в одном отчёте*

```bsl
ВЫБРАТЬ
    БюджетныеЛимитыОстатки.Подразделение КАК Подразделение,
    БюджетныеЛимитыОстатки.СтатьяРасхода КАК СтатьяРасхода,
    БюджетныеЛимитыОстатки.ЛимитОстаток КАК СуммаПлан
ПОМЕСТИТЬ ВТ_План
ИЗ РегистрНакопления.БюджетныеЛимиты.Остатки(
    &КонецПериода, ,
    Подразделение В (&МассивПодразделений),
    Организация = &Организация) КАК БюджетныеЛимитыОстатки;

ВЫБРАТЬ
    ФактОбороты.Подразделение КАК Подразделение,
    ФактОбороты.СтатьяРасхода КАК СтатьяРасхода,
    СУММА(ФактОбороты.СуммаОборот) КАК СуммаФакт
ПОМЕСТИТЬ ВТ_Факт
ИЗ РегистрНакопления.ФактическиеРасходы.Обороты(
    &НачалоПериода, &КонецПериода, 
    Подразделение В (&МассивПодразделений),
    Организация = &Организация) КАК ФактОбороты
СГРУППИРОВАТЬ ПО
    ФактОбороты.Подразделение,
    ФактОбороты.СтатьяРасхода;

ВЫБРАТЬ
    ВТ_План.Подразделение,
    ВТ_План.СтатьяРасхода,
    ВТ_План.СуммаПлан,
    ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) КАК СуммаФакт,
    ВТ_План.СуммаПлан - ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) КАК Отклонение,
    ВЫРАЗИТЬ(ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0)
        / ВТ_План.СуммаПлан * 100 КАК ПроцентВыполнения)
ИЗ ВТ_План
    ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Факт
    ПО ВТ_План.Подразделение = ВТ_Факт.Подразделение
    И ВТ_План.СтатьяРасхода = ВТ_Факт.СтатьяРасхода
УПОРЯДОЧИТЬ ПО Подразделение, СтатьяРасхода;
```

**Пояснения к строкам:**

- **Строка 5:** ПОМЕСТИТЬ ВТ_План — создаёт временную таблицу. Материализуется один раз — быстрый доступ для последующих пакетов.
- **Строка 6:** Остатки(&КонецПериода, ) — виртуальная таблица регистра Остатков. Пустой второй параметр = без отбора по измерениям.
- **Строка 14:** СУММА() — агрегатная функция. В регистре Обороты ресурс уже суммирован, но СГРУППИРОВАТЬ ПО требует явного SUM().
- **Строка 28:** ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) — LEFT JOIN может вернуть NULL для статей без расходов. Заменяем на 0.
- **Строка 29:** Отклонение = План - Факт. Отрицательное = перерасход, положительное = экономия.
- **Строка 33:** ЛЕВОЕ СОЕДИНЕНИЕ — берём ВСЕ статьи из плана, даже если по ним не было факта. Важно для полноты картины.

**Квиз:** Почему пакет 3 использует ЛЕВОЕ СОЕДИНЕНИЕ, а не ВНУТРЕННЕЕ?

- ⬜ **A.** ВНУТРЕННЕЕ быстрее — СУБД оптимизирует лучше
- ✅ **B.** ЛЕВОЕ нужно чтобы показать ВСЕ плановые статьи, даже без факта
- ⬜ **C.** Без разницы, можно использовать любое
- ⬜ **D.** ЛЕВОЕ СОЕДИНЕНИЕ работает только с ВТ

*Объяснение: ЛЕВОЕ СОЕДИНЕНИЕ сохраняет ВСЕ записи из ВТ_План. Если по статье бюджета не было расходов — СуммаФакт = NULL (заменяю на 0 через ЕСТЬNULL). ВНУТРЕННЕЕ потеряло бы статьи без факта — пользователь не увидел бы их в отчёте.*

### Сценарии: СКД основы

#### sc-29: Где исправить ошибку в отчёте СКД

**Сложность:** Сложно

**Теги:** `СКД`, `Отладка`, `Виртуальные таблицы`

**Ситуация:** Пользователь жалуется: в отчёте СКД 'Исполнение бюджета за квартал' колонка 'СуммаФакт' показывает нули для всех подразделений. В СКД поле Ресурса 'СуммаФакт' настроено, в запросе оборотов ресурс есть. При этом в консоли запросов запрос возвращает правильные числа. Что проверишь?

**Варианты ответа:**

- ✅ Проверю заполнение параметров СКД — период, организация. Возможно параметр &ПериодНачало не заполняется
  > *Параметры СКД — частая причина пустых данных. Если &ПериодНачало/Конец не заполнены, виртуальная таблица Обороты вернёт пустой результат. В консоли запросов ты подставляешь вручную, а СКД берёт из параметров формы.*
- ❌ Перепишу запрос, заменив виртуальную таблицу на прямую к таблице регистра
  > *Не используй прямые запросы к таблицам БД! Виртуальные таблицы — единственный правильный способ работы с регистрами. Проблема в параметрах, не в запросе.*
- ❌ Удалю поле и добавлю заново в настройках СКД
  > *Если поле есть в запросе и настроено в СКД — проблема не в настройке поля. Пересоздание поля не поможет.*
- ❌ Добавлю условие ГДЕ СуммаФакт > 0
  > *Фильтрация в запросе не исправит проблему с параметрами. Если параметры не заполнены — запрос вообще ничего не вернёт.*

### Сценарии: Временные таблицы и ВТ

#### sc-30: Медленный отчёт с вложенными запросами

**Сложность:** Сложно

**Теги:** `Оптимизация`, `Временные таблицы`, `Индексы`

**Ситуация:** Отчёт 'Анализ расходов по центрам ответственности' работает 25 секунд. В нём 4 вложенных запроса с ВТ, JOIN к 5 справочникам, группировки по 3 измерениям. База — 200 000 движений регистра. Как ускоришь?

**Варианты ответа:**

- ❌ Замени временные таблицы на вложенные запросы — СУБД сама оптимизирует
  > *ВТ быстрее вложенных запросов в 1С! СУБД выполняет подзапросы для КАЖДОЙ строки основного, а ВТ материализуются один раз. Замена наоборот замедлит.*
- ✅ Добавлю индексы на поля измерений регистра (СтатьяРасхода, Подразделение, Период)
  > *Индексы на измерениях — основной способ ускорения запросов к регистрам. 200 000 записей с группировкой по 3 полям = полным сканированием без индексов. Индексы дадут Index Scan.*
- ❌ Перенесу вычисления на клиент — у клиента быстрее процессор
  > *На клиенте нельзя работать с запросами и регистрами. Все вычисления с данными — только на сервере.*
- ❌ Уменьшу период: за месяц вместо года
  > *Это не оптимизация отчёта — это изменение бизнес-требования. Если пользователю нужен год — нужно оптимизировать для года, а не урезать функциональность.*

### Сценарии: План/Факт и бюджетирование

#### sc-31: Как вывести отклонение от бюджета в СКД

**Сложность:** Сложно

**Теги:** `СКД`, `ВычисляемыеПоля`, `Регистры`

**Ситуация:** Нужно вывести в отчёте СКД 4 колонки: 'План', 'Факт', 'Абсолютное отклонение', 'Процент выполнения'. План и Факт — из разных регистров накопления. Как реализуешь процент выполнения?

**Варианты ответа:**

- ✅ Вычисляемое поле СКД с выражением: Вычислить('Факт / План * 100')
  > *Вычисляемые поля СКД — правильный механизм. Можно ссылаться на другие поля текущей строки. Формат: Вычислить('СуммаФакт / СуммаПлан * 100'). Дополнительно — условное оформление для значений < 80% (красный).*
- ❌ Добавлю ресурс Процент прямо в запросе к регистрам
  > *В одном запросе нельзя делить ресурсы из разных источников (РН.БюджетныеЛимиты vs РН.ФактическиеРасходы). Нужны ВТ или отдельные пакеты. СКД сделает это сама через вычисляемые поля.*
- ❌ Создам регистр сведений для хранения процента, буду заполнять при проведении
  > *Вычисляемый показатель не нужно хранить! Он всегда = Факт/План. Хранение избыточных данных = рассинхрон при изменении.*
- ❌ Использую условное оформление для разных цветов колонки
  > *Условное оформление — это визуализация, не вычисление. Сначала нужно посчитать процент, потом через УО показать цветом.*

#### sc-33: Как отразить плановые и фактические данные в одном отчёте

**Сложность:** Сложно

**Теги:** `СКД`, `Бюджетирование`, `Архитектура`

**Ситуация:** Руководитель хочет один отчёт: по каждой статье бюджета — план на год, факт за прошедшие месяцы, прогноз на оставшиеся. Данные: РН.БюджетныеЛимиты (план) и РН.ФактическиеРасходы (факт). Как спроектируешь СКД?

**Варианты ответа:**

- ✅ Два отдельных источника данных в СКД, связанных через общие измерения (Статья, Подразделение)
  > *В СКД можно использовать несколько наборов данных. Первый — запрос к РН.БюджетныеЛимиты (Остатки). Второй — запрос к РН.ФактическиеРасходы (Обороты). Связь через общие поля. Вычисляемые поля для отклонений.*
- ❌ Один запрос с UNION ALL двух виртуальных таблиц
  > *UNION ALL объединяет результаты с одинаковой структурой. План и Факт имеют РАЗНЫЕ поля (Лимит vs Сумма). Нужны отдельные источники СКД или ВТ.*
- ❌ Создам один регистр, где будут и план и факт в разных ресурсах
  > *План и факт — разные по смыслу величины. План вводится ДО периода (утверждение бюджета), Факт формируется В течение периода (проведение документов). Разные документы = разные регистры.*
- ❌ Два отдельных отчёта, пользователь смотрит рядом
  > *Раздельные отчёты = неудобно, нет возможности сравнения и расчёта отклонений. СКД создана именно для объединения разных источников.*

### Сценарии: Группировки, Итоги, Сводная

#### sc-32: Срез последних: почему данные не актуальные

**Сложность:** Средне

**Теги:** `СрезПоследних`, `Регистр сведений`, `Отладка`

**Ситуация:** Отчёт 'Текущие должности сотрудников' показывает должность за прошлый месяц, хотя сотрудники уже переведены. Используется запрос к РегистрСведений.СрезПоследних(&ТекущаяДата, ...). Что не так?

**Варианты ответа:**

- ✅ &ТекущаяДата не заполняется — передаётся Неопределено
  > *Если параметр не заполнен, СрезПоследних может использовать дату по умолчанию или пустую дату. Всегда проверяй заполнение параметров: &ТекущаяДата = ТекущаяДата() на сервере.*
- ❌ Регистр нужно переключить с периода 'День' на 'Год'
  > *Периодичность влияет на гранулярность, но СрезПоследних корректно работает с любым периодом. Проблема в другом.*
- ❌ Нужно использовать Обороты вместо СрезПоследних
  > *Обороты — для регистров накопления. Регистр сведений не имеет Оборотов. СрезПоследних — правильная виртуальная таблица.*
- ❌ Добавлю ГДЕ Регистратор ССЫЛКА Документ.Перевод
  > *СрезПоследних автоматически находит последнюю запись по дате. Фильтрация по регистратору ограничит выборку, а не исправит дату.*

---

# День 13: День 13: Алгоритмы и задачи на собеседовании

> Два указателя, хэш-таблица, скользящее окно, оптимизация, типовые задачи

**Фаза:** Фаза 13 | **Категорий:** 6 | **Карточек:** 40

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Стратегии собеса | ~40мин | 5 | — | — | — |
| 2 | Паттерн 'Два указателя' | ~60мин | 5 | ✅ | ✅ | 1 |
| 3 | Хэш-таблица (Соответствие) | ~60мин | 4 | ✅ | ✅ | 2 |
| 4 | Оптимизация на практике | ~50мин | 4 | — | — | 1 |
| 5 | Типовые задачи | ~50мин | 5 | — | — | 1 |
| 6 | Мок-собеседование: алгоритмы | ~120мин | 17 | — | — | — |

---

## Карточки

### Стратегии собеса

#### 🟢 d13-fc-1: Как решать алгоритмические задачи на собеседовании?

**Ответ:** 4 шага: (1) Повтори задачу своими словами — покажи что понял. (2) Предложи простое решение (O(n^2)). (3) Спроси можно ли оптимизировать. (4) Предложи оптимальное (O(n)). Главное — НЕ МОЛЧИ! Рассуждай вслух. Интервьюер оценивает ход мысли, а не только ответ.

*Сложность: Легко | Категория: Стратегии собеса*

#### 🟢 d13-fc-2: Что говорить если не знаешь ответ?

**Ответ:** НЕ говори 'не знаю'! Скажи: 'Я не уверен, но я бы попробовал...' и предложи вариант. Или: 'Я не сталкивался, но по аналогии с похожей задачей...' Рассуждай. Назови что знаешь по смежной теме. Покажи способность мыслить.

*Сложность: Легко | Категория: Стратегии собеса*

#### 🟡 d13-fc-3: Какие структуры данных в 1С использовать для алгоритмов?

**Ответ:** (1) Массив — упорядоченный список, индекс O(1), поиск O(n). (2) Соответствие — хэш-таблица, вставка O(1), поиск по ключу O(1). (3) ТаблицаЗначений — таблица с колонками, поиск линейный O(n). (4) СписокЗначений — неупорядоченный массив с доп. методами. (5) ФиксированныйМассив/Структура — неизменяемые.

*Сложность: Средне | Категория: Стратегии собеса*

#### 🟡 d13-fc-4: Что такое O(n), O(n^2), O(log n) — объясни простыми словами

**Ответ:** O(n) = пропорционально количеству данных. 10 элементов = 10 шагов. O(n^2) = квадрат. 10 элементов = 100 шагов. O(log n) = логарифм. 1000 элементов = 10 шагов (деление пополам). O(1) = константа, всегда одинаково быстро. Примеры: линейный поиск O(n), вложенный цикл O(n^2), бинарный поиск O(log n), хэш-поиск O(1).

*Сложность: Средне | Категория: Стратегии собеса*

#### 🟡 d13-fc-5: O(n) по времени и O(n) по памяти — что это значит?

**Ответ:** O(n) по времени = алгоритм делает пропорционально N операций. O(n) по памяти = использует дополнительно N ячеек памяти (массив, Соответствие). Пример: поиск дубликатов через Соответствие = O(n) время (один проход), O(n) память (храню каждый элемент). Идеально: O(n) время + O(1) память.

*Сложность: Средне | Категория: Стратегии собеса*

### Паттерн 'Два указателя'

#### 🟡 d13-fc-6: Паттерн 'два указателя' — когда использовать?

**Ответ:** Когда нужно обработать последовательность с двух концов. Примеры: (1) Палиндром — сравнить символы с обоих концов. (2) Сумма двух чисел = target в отсортированном массиве. (3) Сортировка двумя проходами. (4) Поиск подстроки. Суть: Левый = начало, Правый = конец, двигаются навстречу. O(n) время, O(1) память.

*Сложность: Средне | Категория: Два указателя*

#### 🔴 d13-fc-7: Палиндром в 1С — алгоритм с двумя указателями

**Ответ:** Функция ЭтоПалиндром(Стр):
Левый = 1; Правый = СтрДлина(Стр);
Пока Левый < Правый Цикл
  Если Сред(Стр, Левый, 1) <> Сред(Стр, Правый, 1) Тогда Возврат Ложь КонецЕсли;
  Левый = Левый + 1; Правый = Правый - 1;
КонецЦикла;
Возврат Истина;
Сложность: O(n) время, O(1) память.

*Сложность: Сложно | Категория: Два указателя*

#### 🔴 d13-fc-8: Сумма двух чисел в отсортированном массиве = target

**Ответ:** Дано: отсортированный массив [1, 3, 5, 7, 9], target = 8.
Левый = 0, Правый = 4 (индексы массива, с 0).
arr[0] + arr[4] = 1+9 = 10 > 8 → Правый--.
arr[0] + arr[3] = 1+7 = 8 = 8 → Нашли!
O(n) время, O(1) память. Не нужен вложенный цикл!

*Сложность: Сложно | Категория: Два указателя*

#### 🔴 d13-fc-13: Паттерн 'скользящее окно' — когда использовать?

**Ответ:** Когда нужно найти подмассив/подстроку удовлетворяющую условию. Примеры: (1) Максимальная сумма подмассива из K элементов. (2) Самая длинная подстрока без повторяющихся символов. (3) Подстрока содержащая все буквы алфавита. Суть: два указателя, правый расширяет окно, левый сужает.

*Сложность: Сложно | Категория: Скользящее окно*

#### 🔴 d13-fc-14: Максимальная сумма K подряд идущих элементов

**Ответ:** Дано: [2, 1, 5, 1, 3, 2], K = 3. Найти max сумму 3 подряд.
Окно = 2+1+5 = 8. Сдвиг: -2 + 3 = 9. Сдвиг: -1 + 1 = 9. Сдвиг: -5 + 2 = 6.
Max = 9 (подмассив [1,5,3] или [5,1,3]).
Алгоритм: ТекСумма - arr[Левый] + arr[Правый+1]. O(n).

*Сложность: Сложно | Категория: Скользящее окно*

### Хэш-таблица (Соответствие)

#### 🟡 d13-fc-9: Соответствие в 1С = хэш-таблица. Как работает?

**Ответ:** Соответствие хранит пары Ключ → Значение. Доступ по ключу за O(1) в среднем (внутри — хэш-функция). Вставка: Уник[Ключ] = Значение. Поиск: Если Уник[Ключ] <> Неопределено. Удаление: Уник.Удалить(Ключ). Идеально для замены вложенных циклов.

*Сложность: Средне | Категория: Хэш-таблица*

#### 🔴 d13-fc-10: Поиск дубликатов через Соответствие — код

**Ответ:** Уник = Новый Соответствие;
Дубли = Новый Массив;
Для Каждого Эл Из Массив Цикл
  Если Уник[Эл] <> Неопределено Тогда
    Дубли.Добавить(Эл);  // Дубль!
  Иначе
    Уник[Эл] = Истина;  // Первый раз
  КонецЕсли;
КонецЦикла;
Сложность: O(n) время, O(n) память.

*Сложность: Сложно | Категория: Хэш-таблица*

#### 🟡 d13-fc-11: Частотный словарь через Соответствие

**Ответ:** Частота = Новый Соответствие;
Для Каждого Эл Из Массив Цикл
  Если Частота[Эл] <> Неопределено Тогда
    Частота[Эл] = Частота[Эл] + 1;
  Иначе
    Частота[Эл] = 1;
  КонецЕсли;
КонецЦикла;
// Частота['apple'] = 3 — слово 'apple' встретилось 3 раза.

*Сложность: Средне | Категория: Хэш-таблица*

#### 🟡 d13-fc-12: Соответствие vs ТаблицаЗначений для поиска — что быстрее?

**Ответ:** Соответствие = O(1) поиск по ключу. ТЗ.НайтиСтроки() = O(n) линейный поиск. Для частого поиска — Соответствие однозначно быстрее. ТЗ нужна когда нужны колонки, сортировка, вывод на форму. Для маппинга Ключ→Значение — Соответствие.

*Сложность: Средне | Категория: Хэш-таблица*

### Оптимизация на практике

#### 🔴 d13-fc-15: Задача: заменить цикл с НайтиПоРеквизиту на пакетный запрос

**Ответ:** БЫЛО: Для Каждого Стр Из Список Цикл Найденная = Справочники.Номенклатура.НайтиПоРеквизиту('Артикул', Стр.Артикул) КонецЦикла;
СТАЛО: Запрос.УстановитьПараметр('МассивАртикулов', СписокАртикулов);
Результат = Запрос.Выполнить();
Маппинг = Новый Соответствие; Пока Выборка.Следующий() Цикл Маппинг[Выборка.Артикул] = Выборка.Ссылка КонецЦикла;
Замена 10 000 SQL на 1 SQL + 10 000 обращений к памяти.

*Сложность: Сложно | Категория: Оптимизация*

#### 🟡 d13-fc-16: Как посчитать сколько SQL-запросов делает код?

**Ответ:** Правило: каждый вызов НайтиПоРеквизиту, НайтиПоКоду, Выполнить() внутри цикла = SQL-запрос. Если цикл от 1 до N и внутри есть запрос = N запросов. Если вложенный цикл = N*M запросов. Считай! > 100 запросов = нужно оптимизировать через пакетный запрос.

*Сложность: Средне | Категория: Оптимизация*

#### 🔴 d13-fc-17: Массовая запись: как записать 1000 документов за один проход?

**Ответ:** НаборЗаписей = РегистрыНакопления.Пример.СоздатьНаборЗаписей();
Для Каждого Док Из МассивДокументов Цикл
  Запись = НаборЗаписей.Добавить();
  Запись.Регистратор = Док.Ссылка;
  Запись.Период = Док.Дата;
  // ... заполнить измерения и ресурсы
КонецЦикла;
НаборЗаписей.Записать();
Один вызов Записать() = одна транзакция.

*Сложность: Сложно | Категория: Оптимизация*

#### 🟡 d13-fc-18: Пакетная обработка данных — правило 500-1000

**Ответ:** При массовой обработке: разбивай на пакеты по 500-1000 записей. Причина: (1) Транзакция > 20 сек = таймаут. (2) Память СУБД ограничена. (3) Управляемые блокировки захватывают ресурсы. Паттерн: Пока ЕстьДанные Цикл Пакет = Взять500Записей(); Обработать(Пакет); ОбработатьБлокировку(); КонецЦикла;

*Сложность: Средне | Категория: Оптимизация*

### Типовые задачи

#### 🟢 d13-fc-19: FizzBuzz на 1С — классическая задача

**Ответ:** Для Число = 1 По 100 Цикл
  Если Число % 15 = 0 Тогда Сообщить('FizzBuzz')
  Иначеесли Число % 3 = 0 Тогда Сообщить('Fizz')
  Иначеесли Число % 5 = 0 Тогда Сообщить('Buzz')
  Иначе Сообщить(Число)
  КонецЕсли;
КонецЦикла;
В 1С: остаток от деления = Число % Делитель. Нет отдельной функции.

*Сложность: Легко | Категория: Типовые задачи*

#### 🟢 d13-fc-20: Подсчёт слов в строке на 1С

**Ответ:** // Вариант 1: через СтрРазделить
МассивСлов = СтрРазделить(Текст, ' ', Ложь);
КоличествоСлов = МассивСлов.Количество();
// Вариант 2: подсчёт пробелов
КоличествоСлов = СтрЧислоВхождений(Текст, ' ') + 1;
СтрРазделить — проще и надёжнее. Ложь = не удалять пустые строки.

*Сложность: Легко | Категория: Типовые задачи*

#### 🟡 d13-fc-21: Реверс массива в 1С

**Ответ:** Н = Массив.ВГраница(); // Последний индекс
Для i = 0 По Н/2 Цикл
  Временный = Массив[i];
  Массив[i] = Массив[Н - i];
  Массив[Н - i] = Временный;
КонецЦикла;
Обмен элементов через временную переменную. O(n/2) = O(n). O(1) память. Н/2 обменов.

*Сложность: Средне | Категория: Типовые задачи*

#### 🔴 d13-fc-22: Flatten вложенного массива в 1С

**Ответ:** Функция Flatten(Массив)
  Результат = Новый Массив;
  Для Каждого Эл Из Массив Цикл
    Если ТипЗнч(Эл) = Тип('Массив') Тогда
      Вложенный = Flatten(Эл); // Рекурсия!
      Для Каждого В Из Вложенный Цикл Результат.Добавить(В) КонецЦикла;
    Иначе
      Результат.Добавить(Эл);
    КонецЕсли;
  КонецЦикла;
  Возврат Результат;
КонецФункции
// Рекурсия безопасна для небольшой вложенности.

*Сложность: Сложно | Категория: Типовые задачи*

#### 🟡 d13-fc-23: Пузырьковая сортировка на 1С

**Ответ:** Для i = 0 По Массив.ВГраница() - 1 Цикл
  Для j = 0 По Массив.ВГраница() - 1 - i Цикл
    Если Массив[j] > Массив[j + 1] Тогда
      Вр = Массив[j]; Массив[j] = Массив[j+1]; Массив[j+1] = Вр;
    КонецЕсли;
  КонецЦикла;
КонецЦикла;
O(n^2). В 1С есть Массив.Сортировать() — но на собесе просят реализовать алгоритм.

*Сложность: Средне | Категория: Типовые задачи*

### Мок-собеседование: алгоритмы

#### 🟡 d13-fc-24: Вопрос: 'Оптимизируй этот код: цикл с НайтиПоРеквизиту внутри'

**Ответ:** Ответ: 'Этот код делает N SQL-запросов. Я бы заменил на один пакетный запрос: соберу все артикулы в Массив, сделаю Запрос с ГДЕ Артикул В(&Массив), результат выгружу в Соответствие для O(1) маппинга. Замена N SQL-запросов на 1 запрос.'

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-25: Вопрос: 'Есть отсортированный массив. Найти элемент за O(log n)'

**Ответ:** Бинарный поиск: Левый = 0, Правый = N-1. Пока Левый <= Правый: Середина = (Левый + Правый) / 2. Если Массив[Середина] = Искомый → нашли. Если Массив[Середина] < Искомый → Левый = Середина + 1. Иначе Правый = Середина - 1. O(log n) — каждый шаг отбрасывает половину.

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-26: Вопрос: 'Сколько SQL-запросов делает этот код?'
Для Каждого Стр Из ТЧ Цикл
  Док = Документы.Реализация.НайтиПоНомеру(Стр.Номер);
КонецЦикла

**Ответ:** N запросов, где N = количество строк ТЧ. НайтиПоНомеру = SQL-запрос. Решение: один Запрос с В(&МассивНомеров). Важно: на собеседовании СЧИТАЙ запросы. Правило: любой поиск/выполнение внутри цикла = SQL на каждой итерации.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🟢 d13-fc-27: Вопрос: 'Что сложнее: O(n log n) или O(n^2)?'

**Ответ:** O(n^2) сложнее. Сравнение при n=1000: n log n = 1000 * 10 = 10 000. n^2 = 1 000 000. Разница в 100 раз! При n=10 000: n log n = 40 000, n^2 = 100 000 000 (в 2500 раз). Пример: быстрая сортировка O(n log n) vs пузырьковая O(n^2).

*Сложность: Легко | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-28: Вопрос: 'Какие структуры данных есть в 1С для алгоритмов?'

**Ответ:** Массив (упорядоченный, O(1) индекс, O(n) поиск), Соответствие (хэш-таблица, O(1) поиск по ключу), ТаблицаЗначений (колонки, O(n) поиск), СписокЗначений (неупорядоченный), ФиксированныйМассив (неизменяемый), ФиксированноеСоответствие (неизменяемый), Структура (названные поля). Нет: HashMap/HashSet/LinkedList напрямую — используем аналоги.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-29: Вопрос: 'Сделать реверс строки. Без встроенных функций.'

**Ответ:** Результат = '';
Для i = СтрДлина(Стр) По 1 Шаг -1 Цикл
  Результат = Результат + Сред(Стр, i, 1);
КонецЦикла;
O(n) время, O(n) память (новая строка). Паттерн: обратный проход. На 1С конкатенация строк в цикле НЕ оптимальна (создаёт новую строку каждый раз), но для собеседования — корректный ответ.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-30: Вопрос Магнит: 'В ЗаявкаНаРасход 10 000 строк. Нужно найти справочник по артикулу для каждой. Как оптимизируешь?'

**Ответ:** (1) Собрать все артикулы в Массив. (2) Запрос: ВЫБРАТЬ Артикул, Ссылка ИЗ Справочник.Номенклатура ГДЕ Артикул В(&Массив). (3) Выгрузить в Соответствие: Маппинг[Артикул] = Ссылка. (4) Обойти 10 000 строк, брать Ссылку из Маппинг за O(1). Замена 10 000 SQL на 1 SQL.

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-31: Вопрос: 'Разделяй и властвуй — пример в 1С'

**Ответ:** Быстрая сортировка (Quicksort): (1) Выбрать опорный элемент. (2) Разделить массив: меньше опорного влево, больше вправо. (3) Рекурсивно отсортировать обе части. В 1С есть Массив.Сортировать() но на собесе просят реализовать. Merge sort, Binary search — тоже примеры.

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-32: Вопрос: Магнит — нужно загрузить 50 000 цен. Как?

**Ответ:** НЕ по одной! Три подхода: (1) Запрос с ВЫБРАТЬ из Excel (COM-объект) → ТЗ → загрузить пакетами по 1000. (2) ОбменДанными.Загрузить() для быстрой загрузки. (3) Из внешнего файла: ПостроитьXML, прочитать в ТЗ, записать пакетами через НаборЗаписей. Ключ: МИНИМИЗИРУЙ количество транзакций.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-33: Вопрос: 'Что такое стабильная сортировка и почему это важно?'

**Ответ:** Стабильная сортировка сохраняет порядок равных элементов. Пример: отсортируем по подразделению, потом по ФИО. Если вторая сортировка стабильна — внутри одного подразделения ФИО будут в алфавитном порядке. Массив.Сортировать() в 1С — стабильная. Нестабильная: быстрая сортировка без модификации.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-34: Вопрос: 'Как найти пересечение двух массивов (общие элементы)?'

**Ответ:** Соответствие как множество:
Хэш1 = Новый Соответствие;
Для Каждого Эл Из Массив1 Цикл Хэш1[Эл] = Истина КонецЦикла;
Общие = Новый Массив;
Для Каждого Эл Из Массив2 Цикл
  Если Хэш1[Эл] <> Неопределено Тогда Общие.Добавить(Эл) КонецЕсли;
КонецЦикла;
O(n+m) время, O(min(n,m)) память.

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-35: Вопрос: 'Сортировка: пузырьковая vs быстрая — какая лучше?'

**Ответ:** Быстрая (Quicksort): O(n log n) средний, O(n^2) худший. Пузырьковая: всегда O(n^2). На 10 000 элементов: быстрая = ~130 000 операций, пузырьковая = 100 000 000 (в 770 раз медленнее!). 1С: Массив.Сортировать() использует быструю сортировку. На собесе — знай обе.

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-36: Вопрос: 'Как эффективно объединить два отсортированных массива?'

**Ответ:** Слияние (merge): два указателя, по одному на каждый массив. Сравниваем текущие элементы, меньший идёт в результат. Продвигаем указатель. O(n+m) время. Если массивы не отсортированы — сначала отсортируй O(n log n + m log m), потом слияние. Не нужно объединять и потом сортировать — это O((n+m) log(n+m)).

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-37: Вопрос: 'Сколько swap-операций в пузырьковой сортировке для [5,3,1,4,2]?'

**Ответ:** Проход 1: [3,1,4,2,5] — 4 swap. Проход 2: [1,3,2,4,5] — 3 swap. Проход 3: [1,2,3,4,5] — 1 swap. Проход 4: без swap — выход. Итого: 8 swap. Каждый swap = 3 присваивания. n=5, макс = n(n-1)/2 = 10 swap.

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-38: Вопрос: 'Магнит: отчёт тормозит. В коде — цикл с Запрос.Выполнить() внутри. Что посоветуешь?'

**Ответ:** (1) Вынести запрос ДО цикла. (2) Параметры → Массив для В(). (3) Один запрос с вложенными ВТ. (4) Если нужна итерация по результатам — СКД! Если внутри цикла Запрос к одним и тем же данным → кэшировать в Соответствие. Правило: запросы НЕ в цикле!

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

#### 🟡 d13-fc-39: Вопрос: 'Магнит: как проверить что JSON валиден перед парсингом?'

**Ответ:** Попытка Парсинг = Json.Прочитать(СтрJSON); Исключение Вернуть Ложь; КонецПопытки; Возврат Истина;
В 1С нет отдельной функции валидации JSON. Единственный способ — попытка парсинга. Это нормальный паттерн: 'try-parse' = Попытка/Исключение. Из JSON можно проверить: наличие ключа через Свойство().

*Сложность: Средне | Категория: Мок-собес: алгоритмы*

#### 🔴 d13-fc-40: Вопрос: 'Дан массив чисел и число K. Найти два числа, сумма = K. Решение?'

**Ответ:** ОПТ: Соответствие:
Хэш = Новый Соответствие;
Для Каждого Число Из Массив Цикл
  Нужный = K - Число;
  Если Хэш[Нужный] <> Неопределено Тогда Возврат [Нужный, Число] КонецЕсли;
  Хэш[Число] = Истина;
КонецЦикла;
O(n) время, O(n) память. Проще: два указателя для отсортированного массива O(n).

*Сложность: Сложно | Категория: Мок-собес: алгоритмы*

### Диаграмма: Паттерн 'Два указателя': подход к задачам на строках и массивах

*Как работает классический алгоритм с двумя указателями на примере поиска палиндрома*

#### Шаг 1: Инициализация: два указателя

Левый = начало строки (индекс 1). Правый = конец строки (индекс Длина). Оба начинают с краёв.
- ЛевыйУказатель = 1
- ПравыйУказатель = СтрДлина(Стр)
- Двигаются навстречу друг другу
- O(1) дополнительной памяти

#### Шаг 2: Сравнение символов

Сред(Стр, Левый, 1) vs Сред(Стр, Правый, 1). Если равны — двигаем оба указателя к центру.
- КодСимв(Стр, Левый) = КодСимв(Стр, Правый)?
- Если да: Левый++, Правый--
- Если нет: НЕ палиндром, выходим
- Каждое сравнение O(1)

#### Шаг 3: Двигаем указатели

Левый++ и Правый--. Проверяем: Левый < Правый? Если да — продолжаем. Если нет — палиндром.
- ЛевыйУказатель = ЛевыйУказатель + 1
- ПравыйУказатель = ПравыйУказатель - 1
- Цикл пока Левый < Правый
- Если цикл завершился — все пары совпали

#### Шаг 4: Результат

Если все пары символов совпали — строка палиндром. O(n) по времени, O(1) по памяти.
- Время: O(n/2) = O(n)
- Память: O(1) — только 2 переменных
- Паттерн: скользящее окно, два указателя
- Применение: палиндром, сумма двух чисел, разворот

### Диаграмма: Хэш-таблица: Соответствие как замена вложенных циклов

*Как Соответствие ускоряет поиск дубликатов с O(n^2) до O(n)*

#### Шаг 1: Проблема: вложенный цикл O(n^2)

10 000 элементов × 10 000 сравнений = 100 000 000 операций. Медленно!
- Для каждого элемента — поиск по всему массиву
- O(n^2) — квадратичная сложность
- На 10 000 элементах: ~10 секунд
- При 100 000: ~16 минут!

#### Шаг 2: Решение: Соответствие как множество

Соответствие = хэш-таблица. Вставка и поиск по ключу = O(1) в среднем.
- Создаём пустое Соответствие
- Обходим массив один раз
- Каждый элемент — проверяем в ключах
- Есть ключ = дубль. Нет = добавляем

#### Шаг 3: Итерация за O(n)

Один проход по массиву. Каждая проверка O(1). Итого O(n).
- 10 000 элементов = 10 000 проверок
- Каждая проверка мгновенная (хэш)
- Результат за миллисекунды
- O(n) время, O(n) память

#### Шаг 4: Шаблон кода 1С

Хэш = Новый Соответствие; Для Каждого Эл Из Массив Цикл Если Хэш[Эл] <> Неопределено Тогда Дубли.Добавить(Эл) Иначе Хэш[Эл] = Истина КонецЕсли КонецЦикла
- Соответствие[Ключ] = Неопределено если ключа нет
- Вставка: Соответствие[Ключ] = Значение
- Проверка: Если Соответствие[Ключ] <> Неопределено
- Альтернатива: Хэш.Вставить(Ключ) не ругается на дубль

### Код: Два указателя: проверка палиндрома на 1С

*Классический алгоритм собеседования — определение палиндрома без разворота строки*

```bsl
Функция ЭтоПалиндром(Стр) Экспорт
    // Паттерн: два указателя
    Левый = 1;
    Правый = СтрДлина(Стр);
    Пока Левый < Правый Цикл
        ЛевойСимвол = Сред(Стр, Левый, 1);
        ПравыйСимвол = Сред(Стр, Правый, 1);
        Если ЛевойСимвол <> ПравыйСимвол Тогда
            Возврат Ложь; // Не палиндром
        КонецЕсли;
        Левый = Левый + 1;
        Правый = Правый - 1;
    КонецЦикла;
    Возврат Истина; // Палиндром
КонецФункции

// Примеры:
// ЭтоПалиндром('abcba')  = Истина
// ЭтоПалиндром('abcde')  = Ложь
// ЭтоПалиндром('123321') = Истина
```

**Пояснения к строкам:**

- **Строка 1:** Функция Экспорт = доступна из других модулей. Встроенный язык 1С.
- **Строка 3:** Левый указатель = начало строки (индексация в 1С с 1).
- **Строка 5:** Пока Левый < Правый — не нужно сравнивать середину, она совпадает сама с собой.
- **Строка 8:** Если хоть одна пара символов не совпала — сразу выход. Ранний возврат = оптимизация.
- **Строка 11:** Двигаем оба указателя к центру. Каждый шаг = одно сравнение O(1).
- **Строка 14:** Если цикл завершился без выхода — все пары совпали = палиндром.

**Квиз:** Какова сложность этого алгоритма?

- ⬜ **A.** O(n^2) — вложенный цикл по строке
- ✅ **B.** O(n) по времени, O(1) по дополнительной памяти
- ⬜ **C.** O(log n) — бинарный поиск
- ⬜ **D.** O(n) по времени, O(n) по памяти — нужен массив символов

*Объяснение: O(n) время: каждый символ сравнивается максимум 1 раз (n/2 сравнений). O(1) память: только 2 переменных-указателя. Не создаём дополнительных массивов или строк.*

### Код: Соответствие как хэш-множество: поиск дубликатов за O(n)

*Как заменить вложенный цикл O(n^2) на линейный проход O(n) через Соответствие*

```bsl
Функция НайтиДубликаты(Массив) Экспорт
    // Соответствие = хэш-множество
    Уник = Новый Соответствие;
    Дубли = Новый Массив;

    Для Каждого Элемент Из Массив Цикл
        // Если элемент уже в ключах = дубль
        Если Уник[Элемент] <> Неопределено Тогда
            Дубли.Добавить(Элемент);
        Иначе
            // Первый раз видим — добавляем
            Уник[Элемент] = Истина;
        КонецЕсли;
    КонецЦикла;

    Возврат Дубли;
КонецФункции

// Альтернатива: Использовать .Вставить()
// Уник.Вставить(Ключ, Значение) — НЕ заменяет если ключ есть
// Можно проверить: Уник.Получить(Элемент) <> Неопределено

// Вариант с количеством:
// Если Уник[Элемент] <> Неопределено Тогда
//     Уник[Элемент] = Уник[Элемент] + 1; // Считаем
// Иначе
//     Уник[Элемент] = 1;
// КонецЕсли;
```

**Пояснения к строкам:**

- **Строка 3:** Соответствие = хэш-таблица. Доступ к ключу за O(1). Идеально как множество.
- **Строка 6:** Один проход по массиву = O(n) итераций.
- **Строка 8:** Соответствие[Ключ] возвращает Неопределено если ключа нет. Если значение есть — значит элемент уже встречался.
- **Строка 12:** Значение = Истина — любое непустое значение помечает ключ как 'видели'. Можно использовать счётчик.
- **Строка 25:** Расширение: вместо флага Истина можно считать количество. Тогда Соответствие = частотный словарь.

**Квиз:** Почему Соответствие быстрее вложенного цикла для поиска дубликатов?

- ⬜ **A.** Соответствие хранит данные эффективнее
- ✅ **B.** Доступ к ключу в Соответствие = O(1) в среднем (хэш-таблица), а поиск в цикле = O(n)
- ⬜ **C.** Соответствие использует индексы СУБД
- ⬜ **D.** Вложенный цикл не работает в 1С

*Объяснение: Соответствие реализовано как хэш-таблица: вычисляет хэш ключа и напрямую обращается к ячейке памяти = O(1). Вложенный цикл каждый раз сканирует массив заново = O(n) за итерацию = O(n^2) всего.*

### Сценарии: Паттерн 'Два указателя'

#### sc-34: Задача: определение является ли строка палиндромом

**Сложность:** Средне

**Теги:** `Два указателя`, `Строки`, `Алгоритмы`

**Ситуация:** На собеседовании дают задачу: 'Напишите функцию, которая определяет, является ли строка палиндромом. Без использования обратной строки. 1С не имеет встроенной функции разворота строки.' Как решите?

**Варианты ответа:**

- ✅ Цикл по индексам: сравнить первый символ с последним, второй с предпоследним и т.д.
  > *Паттерн 'два указателя': начало и конец строки двигаются к центру. O(n) по времени, O(1) по памяти. Стр(Найти(Строка, КодСимвола(Строка, ЛевыйУказатель))) = ЛевыйУказатель + ПравыйУказатель - 1 значит палиндром.*
- ❌ Вызываю СтрЗаменить для каждого символа и сравниваю с оригиналом
  > *СтрЗаменить не меняет порядок символов. Этот подход не решает задачу палиндрома — нужно сравнивать символы с разных концов строки.*
- ❌ Записываю строку в массив, сортирую, сравниваю
  > *Сортировка меняет порядок символов — палиндром превратится в другой порядок. 'abcba' после сортировки = 'abbca' — это не палиндром. Подход не работает.*
- ❌ Создаю вторую строку в обратном порядке через цикл и сравниваю
  > *Формально работает, но задача сказана 'без обратной строки'. Дополнительно: O(n) памяти для второй строки. Паттерн двух указателей — O(1) памяти.*

### Сценарии: Хэш-таблица (Соответствие)

#### sc-35: Задача: найти дубли в массиве без сортировки

**Сложность:** Средне

**Теги:** `Хэш-таблица`, `Соответствие`, `Алгоритмы`

**Ситуация:** Дан массив из N элементов (числа). Есть ли дубли? Нельзя использовать сортировку (алгоритмическая задача на собеседовании). Доступны: Массив, Соответствие, ТаблицаЗначений. Как решите в 1С?

**Варианты ответа:**

- ❌ Вложенный цикл: для каждого элемента проверить все остальные. O(n^2)
  > *Работает, но O(n^2) — медленно для 100 000 элементов = 10 млрд операций. На собеседовании скажут 'можно ли быстрее?'.*
- ✅ Соответствие как хэш-множество: обойти массив, если элемента нет в ключах — добавить, если есть — дубль найден. O(n)
  > *Соответствие в 1С = хэш-таблица с O(1) поиском. Обходим массив один раз. Если элемента нет в ключах — добавляем. Если есть — нашли дубль. O(n) время, O(n) память.*
- ❌ Записать в ТаблицуЗначений и использовать НайтиСтроки
  > *ТЗ.НайтиСтроки() — линейный поиск внутри таблицы. В сумме: O(n*m) где m = размер ТЗ на момент поиска. Хуже чем Соответствие.*
- ❌ Запрос с ГРУППИРОВАТЬ ПО и ВЫБРАТЬ С Число > 1
  > *Для массива в памяти нельзя написать запрос. Запрос работает с БД (справочники, регистры). Массив = объект встроенного языка, не источник данных для запросов.*

#### sc-38: Задача: подсчёт уникальных элементов в массиве за O(n)

**Сложность:** Средне

**Теги:** `Соответствие`, `Хэш-таблица`, `O(n)`

**Ситуация:** Дан массив с дубликатами: [5, 3, 5, 2, 3, 7, 2, 5]. Нужно посчитать сколько уникальных чисел. На собеседовании просят O(n). Как?

**Варианты ответа:**

- ✅ Соответствие: обойти массив, добавлять элементы как ключи. Количество ключей = количество уникальных
  > *Соответствие = хэш-множество. Каждая вставка O(1). Дубли не создают новые ключи. Итого: O(n) время, O(k) память где k = кол-во уникальных. Ответ: Соответствие.Количество().*
- ❌ Отсортировать массив, потом посчитать уникальные (отличные от предыдущего)
  > *Сортировка O(n log n), а нужно O(n). Хотя это правильный подход если нельзя использовать доп. память (in-place), но на собеседовании сказали O(n).*
- ❌ Вложенный цикл: для каждого проверить, встречался ли раньше
  > *O(n^2). Не удовлетворяет условию O(n). На 100 000 элементов будет слишком медленно.*
- ❌ Создать ТаблицуЗначений с колонкой 'Значение' и использовать НоваяСтрока()
  > *ТЗ.НайтиСтроку() = линейный поиск. В сумме O(n^2). Соответствие с O(1) доступом по ключу — правильный выбор.*

### Сценарии: Оптимизация на практике

#### sc-36: Задача: оптимизировать медленный цикл с поиском

**Сложность:** Сложно

**Теги:** `Оптимизация`, `Соответствие`, `Пакетные операции`

**Ситуация:** Код работает медленно:
Для Каждого Стр Из Список Цикл
  Найденная = Справочники.Номенклатура.НайтиПоРеквизиту('Артикул', Стр.Артикул);
  // ...
КонецЦикла
Список = 10 000 элементов. Как оптимизируешь?

**Варианты ответа:**

- ✅ Сделаю Запрос с ВЫБРАТЬ из справочника ГДЕ Артикул В Массиве
  > *Один запрос к БД вместо 10 000 отдельных поисков! СУБД выполнит за миллисекунды используя индекс. Параметр: МассивАртикулов. Результат в Соответствие для быстрого доступа.*
- ❌ Добавлю индекс на реквизит Артикул
  > *Индекс ускорит отдельный НайтиПоРеквизиту, но 10 000 вызовов = 10 000 SQL-запросов к СУБД. Индекс не решит проблему количества вызовов. Нужен ОДИН запрос.*
- ❌ Сгруппирую артикулы и вызову НайтиПоРеквизиту один раз для каждой группы
  > *Сгруппировка артикулов уменьшит количество вызовов, но всё равно будет много SQL-запросов. Один запрос с В Массиве = оптимально.*
- ❌ Использую Справочники.Номенклатура.СоздатьДокументНаСервере()
  > *Этот метод не существует. Наверное имелось в виду СоздатьЭлемент(), но это не решает проблему поиска — нужен поиск по реквизиту.*

### Сценарии: Типовые задачи

#### sc-37: Задача: реверс строки на собеседовании

**Сложность:** Легко

**Теги:** `Строки`, `Базовый алгоритм`, `Собеседование`

**Ситуация:** Напишите алгоритм разворота строки без встроенных функций (если бы их не было). 1С: СтрПолучитьСтроку() и СтрДлина() доступны. '12345' → '54321'.

**Варианты ответа:**

- ✅ Цикл от конца строки к началу, собираю символы в новый массив, потом СтрСоединить()
  > *Классический алгоритм: для i = СтрДлина(Стр) по 1 шаг -1. Добавляю Сред(Стр, i, 1) в Массив. Потом СтрСоединить(Массив, ''). O(n) время, O(n) память. Паттерн: обратный проход.*
- ❌ Рекурсивно: первый символ + реверс(остальная строка)
  > *Рекурсия в 1С = переполнение стека при длинных строках. 1С не оптимизирует хвостовую рекурсию. Для 10 000 символов = 10 000 вложенных вызовов = ошибка стека.*
- ❌ Использую Сред(Стр, СтрДлина(Стр), 1) для первого символа
  > *Сред(Стр, СтрДлина(Стр), 1) = последний символ. Это лишь первый шаг — нужен цикл по всем символам. Неполное решение.*
- ❌ СтрЗаменить(Стр, '', ' обратный порядок')
  > *СтрЗаменить заменяет подстроку, не разворачивает. Нет встроенной функции для реверса строки в 1С — нужно писать алгоритм.*

---

# День 14: День 14: Расширения конфигурации

> Расширения, подписки на события, формы, &Вместо/&Перед/&После, интеграция через расширение

**Фаза:** Фаза 14 | **Категорий:** 6 | **Карточек:** 40

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Основы расширений | ~50мин | 5 | ✅ | — | 1 |
| 2 | События и обработчики | ~80мин | 7 | ✅ | ✅ | 2 |
| 3 | Формы и интерфейс | ~50мин | 4 | — | ✅ | 1 |
| 4 | Контекст и практика | ~80мин | 9 | ✅ | — | 1 |
| 5 | Ограничения и лучшие практики | ~50мин | 8 | — | — | — |
| 6 | Мок-собеседование: расширения | ~60мин | 7 | — | — | — |

---

## Карточки

### Основы расширений

#### 🟢 d14-fc-1: Что такое расширение в 1С?

**Ответ:** Расширение = отдельный объект конфигурации, который ДОРАБАТЫВАЕТ типовую конфигурацию БЕЗ изменения основного кода. Текст расширения хранится отдельно, обновления типовой проходят без конфликтов. Это альтернатива прямому изменению модулей и форм. Появилось в платформе 8.3.6.

*Сложность: Легко | Категория: Основы расширений*

#### 🟢 d14-fc-2: Расширение vs прямая доработка — плюсы расширения

**Ответ:** (1) Безопасное обновление типовой — расширение не затирается. (2) Включение/выключение одним флагом. (3) Несколько расширений на одной конфигурации. (4) Отдельное хранение кода — легко передать заказчику. (5) Откат = отключил расширение. Минусы: не всё можно сделать через расширение.

*Сложность: Легко | Категория: Основы расширений*

#### 🟡 d14-fc-3: Когда расширение НЕ подходит?

**Ответ:** (1) Нужно изменить СТРУКТУРУ метаданных (добавить реквизит на форму, которой нет в расширении). (2) Нужна новая роль или привилегированный режим. (3) Нужно изменить бизнес-логику внутри модуля объекта (не через подписку). (4) Нужен новый регистрационный объект. (5) Нужна модификация схемы компоновки данных программно.

*Сложность: Средне | Категория: Основы расширений*

#### 🟢 d14-fc-4: Как создать расширение в EDT?

**Ответ:** Конфигурация → Расширения → Добавить → указать имя. Появится папка в дереве конфигурации. Внутри можно создавать: формы, обработки, справочники, общие модули, подписки, обработчики событий. В .cf расширение = отдельный файл или внутри .cfe.

*Сложность: Легко | Категория: Основы расширений*

#### 🟡 d14-fc-5: Типы расширений конфигурации: какие бывают?

**Ответ:** (1) Расширение конфигурации — основной тип. (2) Расширение рабочей области — модификация интерфейса (команды, панели, навигация). (3) Расширения можно подключать к другим расширениям. Нельзя: расширение самого расширения в старых платформах, в 8.3.20+ можно.

*Сложность: Средне | Категория: Основы расширений*

### События и обработчики

#### 🟡 d14-fc-6: Подписка на событие в расширении — как работает?

**Ответ:** (1) В расширении создаёшь ПодпискаНаСобытие. (2) Выбираешь событие: ПередЗаписью, ПриЗаписи, ОбработкаПроведения и т.д. (3) Выбираешь объект: Документ.РеализацияТоваровУслуг. (4) Указываешь обработчик — процедуру в модуле расширения. Порядок: типовой обработчик выполняется первым, потом расширение (или наоборот через флаг).

*Сложность: Средне | Категория: События и обработчики*

#### 🟡 d14-fc-7: Аннотация &Вместо — что делает?

**Ответ:** &Вместо = ЗАМЕНА типового обработчика. Платформа вызывает ТВОЮ процедуру ВМЕСТО типовой. Если хочешь вызвать типовой внутри: ПродолжитьВызов(). Пример: &Вместо procedura ПередЗаписью(Отказ). Внутри пишешь свою логику + ПродолжитьВызов() для типового кода.

*Сложность: Средне | Категория: События и обработчики*

#### 🟡 d14-fc-8: Аннотация &Перед — что делает?

**Ответ:** &Перед = выполнить код ДО типового обработчика. Порядок: расширение(&Перед) → типовой обработчик → расширение(&После). Используется когда нужно подготовить данные или отменить действие (установить Отказ = Истина до того как типовой код выполнится).

*Сложность: Средне | Категория: События и обработчики*

#### 🟡 d14-fc-9: Аннотация &После — что делает?

**Ответ:** &После = выполнить код ПОСЛЕ типового обработчика. Порядок: типовой → расширение(&После). Используется для: логирования, дополнительных проверок, отправки уведомлений, записи в дополнительные регистры. Частый паттерн: логировать все проведённые документы.

*Сложность: Средне | Категория: События и обработчики*

#### 🟡 d14-fc-10: ПродолжитьВызов() — зачем нужен?

**Ответ:** Вызывает типовой обработчик из расширения. Без него типовой код НЕ выполнится! Пример: &Вместо процедура ПриЗаписи(Отказ). Твоя логика... ПродолжитьВызов(). Ещё твоя логика... Если забыл ПродолжитьВызов() = типовой код потерян = баг.

*Сложность: Средне | Категория: События и обработчики*

#### 🔴 d14-fc-11: &Вместо + ПродолжитьВызов() — порядок вызовов

**Ответ:** Расширение 1: &Перед → Расширение 1: &Вместо → ПродолжитьВызов() → Расширение 2: &Перед → Расширение 2: &Вместо → ПродолжитьВызов() → Типовой обработчик → Расширение 2: &После → Расширение 1: &После. Цепочка! Важно для нескольких расширений.

*Сложность: Сложно | Категория: События и обработчики*

#### 🟡 d14-fc-12: Какие события можно перехватить через расширение?

**Ответ:** Обработка заполнения, ПередЗаписью, ПриЗаписи, ОбработкаПроведения, ОбработкаУдаленияПроведения, ПередУдалением, ОбработкаПроверкиЗаполнения, ОбработкаОповещения, ПриИзменении (для реквизитов формы), ПриСозданииНаСервере. Почти все события объекта!

*Сложность: Средне | Категория: События и обработчики*

### Формы и интерфейс

#### 🟡 d14-fc-13: Расширение формы — что можно добавить?

**Ответ:** (1) Реквизиты формы (не объекта!). (2) Элементы формы: поля, кнопки, группы, декорации. (3) Команды формы и обработчики. (4) Автокоманды: подбор, печать. (5) Условное оформление. Нельзя через расширение: добавить реквизит ОБЪЕКТА (например, новый реквизит справочника). Для этого — расширение конфигурации с правкой объекта.

*Сложность: Средне | Категория: Формы и интерфейс*

#### 🟢 d14-fc-14: Как добавить кнопку на форму через расширение?

**Ответ:** В расширении формы: (1) Команды → Добавить команду. (2) Элементы формы → Добавить → Кнопка. (3) Указать команду для кнопки. (4) В модуле формы расширения написать обработчик: &НаКлиенте процедура МояКнопка(). Платформа сама подставит кнопку на форму при открытии.

*Сложность: Легко | Категория: Формы и интерфейс*

#### 🟡 d14-fc-15: Расширение управляемой формы: программная работа

**Ответ:** Методы в модуле формы расширения: ЭтаФорма = форма объекта. Можно обращаться к Элементы.МоеПоле. Можно читать и писать реквизиты формы расширения. Нельзя напрямую читать реквизиты ТИПОВОГО объекта — через контекст или дополнительные реквизиты.

*Сложность: Средне | Категория: Формы и интерфейс*

#### 🔴 d14-fc-16: Машина состояний в расширении формы

**Ответ:** Расширение формы может менять доступность элементов, видимость, заголовки через Элементы.МояГруппа.Доступность = Ложь. Работает параллельно с типовой логикой. Важно: не конфликтовать с типовой обработкой ИзменитьПроверкуДоступности() — нужно тестировать.

*Сложность: Сложно | Категория: Формы и интерфейс*

### Контекст и практика

#### 🟡 d14-fc-17: Дополнительные реквизиты и свойства — в чём разница?

**Ответ:** ДополнительныеРеквизиты = реквизиты, которые добавляются к ОБЪЕКТУ через расширение. Хранятся в БД, отображаются в типовых формах. ДополнительныеСвойства = характеристика (вид, значение). Хранятся в плане видов характеристик. Реквизиты = фиксированный набор полей. Свойства = справочник-характеристика с гибкими значениями.

*Сложность: Средне | Категория: Контекст*

#### 🟡 d14-fc-18: Безопасный режим расширения — что это?

**Ответ:** Расширение может работать в безопасном режиме = ограничение прав. В безопасном режиме нельзя: напрямую обращаться к БД, использовать COM-объекты, загружать внешние компоненты. Включается: УстановитьБезопасныйРежим(Истина). Полезно для расширений от сторонних разработчиков.

*Сложность: Средне | Категория: Контекст*

#### 🔴 d14-fc-19: Как расширение получает доступ к данным объекта?

**Ответ:** Через параметры обработчиков! В &Вместо процедура ПередЗаписью(Отказ, ПараметрыЗаписи) — объект доступен как ЭтотОбъект. В расширении формы — ЭтаФорма.Объект. Через ДополнительныеРеквизиты — можно хранить свои данные. Через СохраняемыеЗначения — общие данные между сеансами.

*Сложность: Сложно | Категория: Контекст*

#### 🟡 d14-fc-20: СохраняемыеЗначения в расширении — зачем?

**Ответ:** СохраняемыеЗначения = хранилище ключ-значение в расширении. Данные общие для всех сеансов. Пример: хранить настройки расширения (URL интеграции, флаги). Установить / Получить через методы. Автоматически сохраняется в БД. Не нужно создавать отдельный регистр сведений.

*Сложность: Средне | Категория: Контекст*

#### 🔴 d14-fc-21: Магнит: через расширение добавить проверку бюджета при проведении ЗаявкиНаРасход

**Ответ:** ПодпискаНаСобытие → ПередЗаписью (для Проведение). &Перед или &Вместо. В обработчике: проверить бюджет через РН.БюджетныеЛимиты.Остатки. Если превышен → Отказ = Истина, Сообщить. ПродолжитьВызов() если не отказано. Логировать попытку превышения.

*Сложность: Сложно | Категория: Практика*

#### 🔴 d14-fc-22: Магнит: через расширение логировать все HTTP-запросы от ЭДО

**Ответ:** Подписка на событие после записи РС.СопоставлениеЭДО. &После обработчик ПриЗаписи. Запись = новый объект (РегистрСведенийЗапись.СопоставлениеЭДО). Получить JSON-строку, записать в РС.ЖурналИнтеграции (создать через расширение). Включить/выключить логирование флагом.

*Сложность: Сложно | Категория: Практика*

#### 🟡 d14-fc-23: Магнит: добавить автоподбор номенклатуры по артикулу в ЗаявкуНаРасход

**Ответ:** Расширение формы ЗаявкиНаРасход. Добавить кнопку Автоподбор. Обработчик на клиенте: получить таблицу формы, собрать артикулы, запрос на сервер. На сервере: НайтиПоАртикулу → вернуть Соответствие. Обойти строки, заполнить Номенклатуру из Соответствия. Обновить форму.

*Сложность: Средне | Категория: Практика*

#### 🔴 d14-fc-24: Магнит: создать панель бизнес-мониторинга на главной

**Ответ:** Расширение рабочей области (или формы). Добавить: (1) Общую форму с динамическим списком. (2) Команду в навигацию. (3) Подсказки: Бюджет на месяц, Исполнение, Топ расходов. (4) СКД-отчёт с диаграммой. Всё в расширении — без изменения типовой главной формы!

*Сложность: Сложно | Категория: Практика*

#### 🟡 d14-fc-25: Через расширение изменить условное оформление типового списка

**Ответ:** В расширении формы списка: (1) УстановитьОтображениеУсловногоОформления(). (2) Создать элемент условного оформления. (3) Указать отбор (СтатьяРасхода = Оплата). (4) Настроить оформление (красный фон). Платформа применит ТВОЙ стиль поверх типового.

*Сложность: Средне | Категория: Практика*

### Ограничения и лучшие практики

#### 🟡 d14-fc-26: Что НЕЛЬЗЯ сделать через расширение?

**Ответ:** (1) Добавить реквизит объекта в расширении. (2) Изменить структуру модуля (добавить переменные уровня модуля). (3) Изменить права доступа. (4) Добавить новые регистры/справочники (можно в расширении конфигурации, не формы). (5) Изменить подписки типовой конфигурации. (6) Модифицировать макеты СКД.

*Сложность: Средне | Категория: Ограничения*

#### 🟡 d14-fc-27: Расширение конфигурации vs расширение формы — разница

**Ответ:** Расширение конфигурации: можно добавлять НОВЫЕ объекты (справочники, регистры, обработки), общие модули, подписки, роли. Расширение формы: можно доработать только ФОРМУ существующих объектов (кнопки, поля, обработчики). Расширение конфигурации = мощнее, расширение формы = безопаснее.

*Сложность: Средне | Категория: Ограничения*

#### 🟡 d14-fc-28: Как отладить расширение в EDT?

**Ответ:** (1) Поставить точку останова в модуле расширения. (2) Запустить отладку конфигурации с подключённым расширением. (3) Если расширение не сработало — проверить: включено ли в свойствах конфигурации? Правильный ли порядок обработчиков? (4) ТЖ — записать в технологический журнал событие из расширения.

*Сложность: Средне | Категория: Ограничения*

#### 🔴 d14-fc-29: Конфликт двух расширений — как решить?

**Ответ:** Если оба расширения подписаны на одно событие — порядок определяет приоритет. Настройка в свойствах конфигурации: Расширения → стрелки вверх/вниз. Можно отключить одно из расширений. Нельзя: два &Вместо на одно событие от разных расширений — платформа вызовет оба по порядку.

*Сложность: Сложно | Категория: Ограничения*

#### 🟡 d14-fc-30: Когда выбрать расширение, а когда типовую доработку?

**Ответ:** Расширение если: (1) Нужно сохранить обновляемость. (2) Доработка = добавочная логика. (3) Доработка включается/выключается. Типовая доработка если: (1) Нужен новый реквизит объекта. (2) Нужна новая роль. (3) Нужно изменить структуру БД. (4) Доработка = фундаментальная, не отдельная. Лучше: комбинация!

*Сложность: Средне | Категория: Лучшие практики*

#### 🟡 d14-fc-31: Как организовать код в расширении?

**Ответ:** (1) Общие модули расширения для бизнес-логики. (2) Модули форм расширения для UI. (3) Модули объектов расширения (&Вместо, &Перед, &После). (4) Папка в дереве: расширение = отдельный контур. (5) Префикс методов: *_Расш1_ для избежания конфликтов.

*Сложность: Средне | Категория: Лучшие практики*

#### 🔴 d14-fc-32: Миграция расширения на новую версию типовой

**Ответ:** (1) Обновить типовую конфигурацию. (2) Открыть расширение в EDT. (3) Проверить: все ссылки на объекты актуальны? (4) Пересоединить обработчики событий — иногда имена меняются. (5) Протестировать все функции. Если типовой метод переименовали — обновить &Вместо/&Перед/&После.

*Сложность: Сложно | Категория: Лучшие практики*

#### 🟡 d14-fc-33: Расширение и хранилище настроек

**Ответ:** Хранить настройки расширения в: (1) СохраняемыеЗначения — простые флаги и строки. (2) РегистрСведений (в расширении конфигурации) — структурированные настройки. (3) ХранилищеНастроек — пользовательские настройки. Идеально: РС с измерением Пользователь + Настройка + Значение.

*Сложность: Средне | Категория: Лучшие практики*

### Мок-собеседование: расширения

#### 🟢 d14-fc-34: Вопрос: Что такое расширение и зачем оно нужно?

**Ответ:** Расширение = отдельный объект конфигурации для доработки типовой БЕЗ изменения основного кода. Нужен для: сохранения обновляемости типовой, включения/выключения доработок, разделения кода. Появилось в 8.3.6. Поддерживает: формы, подписки, обработчики событий, общие модули, объекты.

*Сложность: Легко | Категория: Мок-собес: расширения*

#### 🟢 d14-fc-35: Вопрос: В чём разница &Вместо, &Перед, &После?

**Ответ:** &Вместо = ЗАМЕНЯЕТ типовой обработчик. Без ПродолжитьВызов() типовой код НЕ выполнится. &Перед = ДО типового (подготовка, отмена). &После = ПОСЛЕ типового (логирование, уведомления). Порядок: &Перед → Типовой → &После. В &Вместо: ПродолжитьВызов() вызывает типовой.

*Сложность: Легко | Категория: Мок-собес: расширения*

#### 🔴 d14-fc-36: Вопрос: Магнит использует типовую УТ. Вам нужно добавить проверку лимитов при проведении. Как сделаете?

**Ответ:** Расширение конфигурации + ПодпискаНаСобытие. Событие: ОбработкаПроведения для Документ.ЗаявкаНаРасход. &Вместо процедура ОбработкаПроведения(Отказ). Проверить лимит через РН.БюджетныеЛимиты. Если превышен → Отказ = Истина, Сообщить. ПродолжитьВызов() если лимит в норме. Логировать в РС.ЖурналИнтеграции.

*Сложность: Сложно | Категория: Мок-собес: расширения*

#### 🟡 d14-fc-37: Вопрос: Можно ли через расширение добавить новый реквизит справочника?

**Ответ:** НЕЛЬЗЯ напрямую через расширение формы или подписку! Но можно через расширение конфигурации: (1) Создать РегистрСведений для хранения доп. данных. (2) Использовать ДополнительныеРеквизиты (если типовая их поддерживает). (3) Только прямая доработка конфигурации добавляет реквизит объекта.

*Сложность: Средне | Категория: Мок-собес: расширения*

#### 🟡 d14-fc-38: Вопрос: Как протестировать что расширение не ломает типовой функционал?

**Ответ:** (1) Написать автотесты: создать документ, провести, проверить регистры. (2) Тестировать ВКЛЮЧИВ и ВЫКЛЮЧИВ расширение — результат должен совпадать (кроме новых функций). (3) Нагрузочный тест на большом объёме данных. (4) Проверить все типовые сценарии проведения.

*Сложность: Средне | Категория: Мок-собес: расширения*

#### 🔴 d14-fc-39: Вопрос: Magnit: расширение для интеграции ЭДО — архитектура

**Ответ:** Расширение конфигурации: (1) Общий модуль ЭДО_Расш — логика парсинга JSON, HTTP. (2) РегистрСведений СопоставлениеЭДО_Расш. (3) HTTP-сервис POST /orders (в расширении). (4) Подписка на событие ПередЗаписью ЗаявкиНаРасход — проверка и доп. логика. (5) Отчёт СКД в расширении. Всё отключается одним флагом!

*Сложность: Сложно | Категория: Мок-собес: расширения*

#### 🔴 d14-fc-40: Вопрос: Что будет если два расширения используют &Вместо на одно событие?

**Ответ:** Платформа выстроит цепочку: Расш1(&Вместо) → ПродолжитьВызов() → Расш2(&Вместо) → ПродолжитьВызов() → Типовой. Порядок определяется настройкой в свойствах конфигурации. Если оба вызовут ПродолжитьВызов() — типовой код выполнится ОДИН раз. Не будет бесконечной рекурсии.

*Сложность: Сложно | Категория: Мок-собес: расширения*

### Диаграмма: Расширение vs типовая доработка: матрица решений

*Как выбрать между расширением и прямой модификацией конфигурации*

#### Шаг 1: Нужен новый реквизит объекта?

Доработка конфигурации! Расширение не может добавить реквизит к объекту

#### Шаг 2: Нужна дополнительная проверка при проведении?

Расширение! Подписка + &Вместо + ПродолжитьВызов()

#### Шаг 3: Нужен новый HTTP-сервис или отчёт?

Расширение! Создаёшь объекты внутри расширения

#### Шаг 4: Нужна новая роль или изменение прав?

Доработка конфигурации! Расширение не управляет ролями

#### Шаг 5: Нужна логика включения/выключения?

Расширение! Флаг включения = мгновенное включение/выключение

#### Шаг 6: Нужно хранить доп. данные?

Расширение конфигурации: РС, Доп. реквизиты, СохраняемыеЗначения

### Диаграмма: Жизненный цикл вызова расширения: &Вместо + ПродолжитьВызов

*Как платформа 1С выстраивает цепочку вызовов между расширением и типовым кодом*

#### Шаг 1: Событие возникает

Пользователь нажимает Записать → платформа генерирует событие ПередЗаписью

#### Шаг 2: Расширение: &Перед

Если есть &Перед — выполняется ПЕРЕД типовым кодом

#### Шаг 3: Расширение: &Вместо

Если есть &Вместо — вызывается ВМЕСТО типового обработчика

#### Шаг 4: ПродолжитьВызов()

Если вызываешь ПродолжитьВызов() — выполняется типовой обработчик

#### Шаг 5: Типовой обработчик

Платформа вызывает стандартный обработчик объекта

#### Шаг 6: Расширение: &После

После типового кода выполняется &После обработчик

### Диаграмма: Архитектура расширения для ЭДО-интеграции в Магните

*Как расширение конфигурации инкапсулирует всю логику интеграции*

#### Шаг 1: HTTP-запрос от ЭДО

POST /edo/orders с JSON-телом заявки

#### Шаг 2: HTTP-сервис расширения

POST обработчик в расширении: парсинг JSON, валидация

#### Шаг 3: Поиск/создание объекта

Запрос к Справочнику.Номенклатура по артикулу, поиск контрагента

#### Шаг 4: Создание документа

Документы.ЗаявкаНаРасход.СоздатьДокумент(). Заполнение реквизитов

#### Шаг 5: Подписка: проверка

&Перед подписка на ПередЗаписью → проверка лимитов бюджета

#### Шаг 6: Подписка: проведение

&После подписка → движения по РН, запись в СопоставлениеЭДО

### Код: Расширение: проверка лимита бюджета при проведении

*Полный пример подписки на событие &Вместо с ПродолжитьВызов() для проверки бюджетных лимитов в Магните*

```bsl
// Общий модуль расширения: ПроверкаЛимитов_Расш

&Вместо
Процедура ОбработкаПроведения_Расш(Отказ, РежимПроведения)
  Запрос = Новый Запрос;
  Запрос.Текст =
    "ВЫБРАТЬ
    |   БюджетныеЛимитыОстатки.СтатьяРасхода КАК Статья,
    |   БюджетныеЛимитыОстатки.Подразделение КАК Подразделение,
    |   СУММА(БюджетныеЛимитыОстатки.СуммаЛимита) КАК Лимит
    |ИЗ
    |   РегистрНакопления.БюджетныеЛимиты.Остатки(
    |       &Месяц, ,
    |       Подразделение В (&Подразделения),
    |       СтатьяРасхода В (&Статьи)
    |   ) КАК БюджетныеЛимитыОстатки
    |СГРУППИРОВАТЬ ПО
    |   СтатьяРасхода, Подразделение

  // Собираем параметры из документа
  МассивПодр = Новый Массив;
  МассивСтатей = Новый Массив;
  Для Каждого Стр Из Объект.Товары Цикл
    МассивПодр.Добавить(Стр.Подразделение);
    МассивСтатей.Добавить(Стр.СтатьяРасхода);
  КонецЦикла;

  Запрос.УстановитьПараметр("Месяц", НачалоМесяца(Объект.Дата));
  Запрос.УстановитьПараметр("Подразделения", МассивПодр);
  Запрос.УстановитьПараметр("Статьи", МассивСтатей);

  Результат = Запрос.Выполнить();
  МаппингЛимитов = Новый Соответствие;
  Для Каждого Стр Из Результат Цикл
    Ключ = Стр.Подразделение + Стр.СтатьяРасхода;
    МаппингЛимитов[Ключ] = Стр.Лимит;
  КонецЦикла;

  // Проверяем каждую строку документа
  Для Каждого Стр Из Объект.Товары Цикл
    Ключ = Стр.Подразделение + Стр.СтатьяРасхода;
    Лимит = МаппингЛимитов[Ключ];
    Если Лимит <> Неопределено И Стр.Сумма > Лимит Тогда
      Сообщить("Превышен лимит: " + Стр.СтатьяРасхода);
      Отказ = Истина;
    КонецЕсли;
  КонецЦикла;

  Если НЕ Отказ Тогда
    ПродолжитьВызов(); // Вызываем типовой код!
  КонецЕсли;

&После
Процедура ОбработкаПроведения_После_Расш(Отказ)
  // Логируем результат в РС.ЖурналИнтеграции
  Запись = РегистрыСведений.ЖурналИнтеграции.СоздатьЗапись();
  Запись.Документ = Объект.Ссылка;
  Запись.Статус = ?(Отказ, "Отклонён", "Проведён");
  Запись.Записать();
КонецПроцедуры
```

### Код: Расширение формы: автоподбор номенклатуры по артикулу

*Как добавить кнопку и обработчик на форму документа через расширение формы*

```bsl
// Модуль формы расширения ЗаявкиНаРасход

&НаКлиенте
Процедура АвтоподборНоменклатуры_Расш(Команда)

  // Собираем артикулы из строк
  Артикулы = Новый Массив;
  Для Каждого Стр Из Объект.Товары Цикл
    Если ЗначениеЗаполнено(Стр.Артикул) И НЕ ЗначениеЗаполнено(Стр.Номенклатура) Тогда
      Артикулы.Добавить(Стр.Артикул);
    КонецЕсли;
  КонецЦикла;

  Если Артикулы.Количество() = 0 Тогда
    ПоказатьПредупреждение(, "Нет строк для подбора");
    Возврат;
  КонецЕсли;

  // Вызываем сервер
  Маппинг = ПодобратьНоменклатуруНаСервере_Расш(Артикулы);

  // Заполняем номенклатуру
  Счётчик = 0;
  Для Каждого Стр Из Объект.Товары Цикл
    Если Маппинг[Стр.Артикул] <> Неопределено Тогда
      Стр.Номенклатура = Маппинг[Стр.Артикул];
      Счётчик = Счётчик + 1;
    КонецЕсли;
  КонецЦикла;

  ПоказатьОповещениеПользователя("ПиктограммаИнформация",,
    "Заполнено: " + Счётчик + " из " + Артикулы.Количество());

КонецПроцедуры

&НаСервере
Функция ПодобратьНоменклатуруНаСервере_Расш(Артикулы)

  Запрос = Новый Запрос;
  Запрос.Текст = "ВЫБРАТЬ Артикул, Ссылка ИЗ Справочник.Номенклатура ГДЕ Артикул В(&Массив)";
  Запрос.УстановитьПараметр("Массив", Артикулы);

  Результат = Запрос.Выполнить();
  Маппинг = Новый Соответствие;
  Для Каждого Стр Из Результат Цикл
    Маппинг[Стр.Артикул] = Стр.Ссылка;
  КонецЦикла;

  Возврат Маппинг;

КонецФункции
```

### Сценарии: Основы расширений

#### sc-42: Миграция расширения на новую версию типовой

**Сложность:** Средне

**Теги:** 

**Контекст:** Вышло обновление типовой УТ. В расширении Magnit_Расш есть подписка на ОбработкаПроведения и расширение формы. Нужно мигрировать.

**Пошаговый план:**

**Шаг 1:** Обновить типовую конфигурацию в хранилище
> 💡 Выгрузить типовую, сравнить с текущей, принять изменения. Расширение останется подключённым.

**Шаг 2:** Открыть расширение в EDT и проверить ошибки компиляции
> 💡 Платформа покажет: ссылки на несуществующие объекты, переименованые методы, изменённые параметры обработчиков.

**Шаг 3:** Обновить подписки: проверить что события и объекты не переименовали
> 💡 Если типовая переименовала Документ.ЗаявкаНаРасход → ЗаявкаНаРасходНовый — обновить в подписке.

**Шаг 4:** Обновить расширение формы: переустановить расширение формы
> 💡 Удалить старое расширение формы, создать новое на базе обновлённой типовой формы. Перенести доработки.

**Шаг 5:** Запустить все автотесты и ручное тестирование
> 💡 Провести ЗаявкуНаРасход. Проверить: (1) Проверка лимита работает. (2) Логирование записывается. (3) Автоподбор работает.

### Сценарии: События и обработчики

#### sc-39: Создать расширение для проверки лимитов

**Сложность:** Средне

**Теги:** 

**Контекст:** В Магните на типовой УТ нужно добавить автоматическую проверку бюджетных лимитов при проведении ЗаявкиНаРасход. Требование: не ломать обновления типовой.

**Пошаговый план:**

**Шаг 1:** Создать расширение конфигурации в EDT: Конфигурация → Расширения → Добавить
> 💡 Имя: Магнит_Расш. Платформа 8.3.6+ поддерживает расширения.

**Шаг 2:** Создать РегистрСведений ЖурналПроверокЛимитов для логирования
> 💡 Измерения: Документ, Подразделение, СтатьяРасхода. Ресурс: СтатусПроверки (перечисление). Периодический: Да.

**Шаг 3:** Создать ПодпискаНаСобытие на ОбработкаПроведения ЗаявкиНаРасход
> 💡 В расширении: Подписки → Добавить. Событие: ОбработкаПроведения. Объект: Документ.ЗаявкаНаРасход. Обработчик: процедуру в общем модуле расширения.

**Шаг 4:** Написать обработчик: &Вместо процедура ОбработкаПроведения(Отказ)
> 💡 Проверить бюджет через РН.БюджетныеЛимиты.Остатки. Если превышен → Отказ = Истина, Сообщить пользователю. Если норма → ПродолжитьВызов().

**Шаг 5:** Добавить запись в ЖурналПроверокЛимитов после проверки
> 💡 В &После обработчике — записать результат: пройти/превышен/недостаточно данных. Для аналитики руководителя.

**Шаг 6:** Протестировать: включить/выключить расширение и провести документ
> 💡 С расширением: проверка работает, отклоняет превышение. Без расширения: типовой функционал без изменений.

#### sc-40: Конфликт двух расширений на одном событии

**Сложность:** Средне

**Теги:** 

**Контекст:** В Магните работают два разработчика: один создал расширение для логирования (Расш1), второй — для проверки лимитов (Расш2). Оба подписались на ОбработкаПроведения ЗаявкиНаРасход. Возник конфликт.

**Пошаговый план:**

**Шаг 1:** Определить текущий порядок расширений в свойствах конфигурации
> 💡 Конфигурация → Расширения → порядок списка = порядок вызовов. Первый в списке = вызывается первым.

**Шаг 2:** Проверить: оба используют &Вместо? Оба вызывают ПродолжитьВызов()?
> 💡 Если оба &Вместо с ПродолжитьВызов() — цепочка сработает корректно. Если кто-то забыл → типовой код потерян.

**Шаг 3:** Установить правильный порядок: логирование(&После) → проверка(&Вместо)
> 💡 Логирование лучше поставить &После — не мешает проверке. Проверка &Вместо может отменить проведение.

**Шаг 4:** Протестировать все сценарии: нормальное проведение, превышение лимита, ошибка данных
> 💡 Проверить: (1) Оба расширения вызываются. (2) Проверка лимита работает. (3) Логирование записывает все попытки. (4) Типовой код выполняется.

**Шаг 5:** Добавить префиксы методов для избежания конфликтов имён
> 💡 Расш1: ЛогированиеЗаявок_Расш1_ОбработкаПроведения. Расш2: ПроверкаЛимитов_Расш2_ОбработкаПроведения.

### Сценарии: Формы и интерфейс

#### sc-41: Расширение формы: автоподбор номенклатуры

**Сложность:** Средне

**Теги:** 

**Контекст:** Менеджер Магнита просит: добавить кнопку Автоподбор в форму ЗаявкиНаРасход. По кнопке — заполнить номенклатуру по артикулу из массива строк.

**Пошаговый план:**

**Шаг 1:** Создать расширение формы ЗаявкиНаРасход
> 💡 В расширении: Формы → Добавить → выбрать типовую форму документа. Платформа создаст расширение формы.

**Шаг 2:** Добавить команду формы Автоподбор и кнопку
> 💡 Команды → Добавить. Элементы → Кнопка → связать с командой. Разместить в командной панели.

**Шаг 3:** Написать &НаКлиенте обработчик кнопки Автоподбор
> 💡 Собрать артикулы из строк таблицы формы. Передать на сервер. На сервере: Запрос к Справочник.Номенклатура ГДЕ Артикул В(&Массив). Вернуть Соответствие.

**Шаг 4:** Заполнить Номенклатуру в строках из Соответствия
> 💡 Для Каждого Строка Из Объект.Товары Цикл: Если Маппинг[Строка.Артикул] <> Неопределено Тогда Строка.Номенклатура = Маппинг[Строка.Артикул] КонецЕсли.

**Шаг 5:** Протестировать: ввести артикулы, нажать кнопку, проверить заполнение
> 💡 Проверить: неизвестные артикулы пропускаются без ошибки. Найденные — заполняются. Форма обновляется.

### Сценарии: Контекст и практика

#### sc-43: Архитектура: всё расширение для интеграции ЭДО

**Сложность:** Средне

**Теги:** 

**Контекст:** В Магните нужно реализовать интеграцию с ЭДО для ЗаявокНаРасход. Всё решение — в расширении, чтобы при удалении не осталось следов в типовой.

**Пошаговый план:**

**Шаг 1:** Спроектировать структуру расширения
> 💡 Общие модули: ЭДО_Расш (логика), HTTP_Расш (запросы). Объекты: РС.СопоставлениеЭДО, РС.ЖурналИнтеграции. HTTP-сервис. СКД-отчёт.

**Шаг 2:** Создать HTTP-сервис POST /edo/orders в расширении
> 💡 Шаблон URL: /edo/* Обработчик: POST → СоздатьИзJSON. Принимать JSON, создавать ЗаявкуНаРасход, возвращать HTTP 201.

**Шаг 3:** Создать РегистрСведений СопоставлениеЭДО в расширении
> 💡 Ресурсы: IDЭДО, ИДДокумента1С, Статус (строка), ДатаОтправки, ДатаОтвета. Измерение: UUID (уникальный идентификатор).

**Шаг 4:** Подписка на ПередЗаписью ЗаявкиНаРасход: доп. проверка и заполнение
> 💡 &Перед: проверить что сумма > 0, заполнить Статус = Черновик. Записать в СопоставлениеЭДО.

**Шаг 5:** СКД-отчёт ИсполнениеБюджета_Расш в расширении
> 💡 Создать Отчёт в расширении. Макет СКД. 2 набора: план и факт. Диаграмма. Автономно от типовой.

**Шаг 6:** Тест: отключить расширение и проверить что типовая работает без изменений
> 💡 Всё что добавлено — исчезает. HTTP-сервис недоступен. СопоставлениеЭДО не существует. Типовая УТ = чистая.

---

# День 15: День 15: Оптимизация и рефакторинг (Middle → Senior)

> План запросов, ВТ, чистая архитектура API, тестирование, разбор реального кода Скаенг

**Фаза:** Фаза 15 | **Категорий:** 6 | **Карточек:** 35

## Обзор категорий

| # | Категория | Время | Карточек | Диаграмма | Код | Сценарии |
|---|-----------|-------|----------|-----------|-----|----------|
| 1 | Оптимизация запросов | ~60мин | 6 | ✅ | — | 1 |
| 2 | Рефакторинг кода | ~80мин | 7 | ✅ | ✅ | 1 |
| 3 | Архитектура HTTP-сервисов | ~50мин | 4 | — | — | — |
| 4 | Тестирование и транзакции | ~60мин | 6 | — | ✅ | 1 |
| 5 | Скаенг: разбор реального кода | ~60мин | 3 | — | — | 2 |
| 6 | Мок-собеседование: оптимизация | ~80мин | 9 | — | — | — |

---

## Карточки

### Оптимизация запросов

#### 🟢 d15-fc-1: Главное правило оптимизации запросов 1С

**Ответ:** НЕ ДОБАВЛЯТЬ индексы без плана запроса! Порядок: (1) Получить план запроса. (2) Найти узкое место. (3) Добавить индекс ТОЛЬКО если план показывает сканирование таблицы. (4) Проверить что индекс помогает. Лишние индексы замедляют запись!

*Сложность: Легко | Категория: Оптимизация запросов*

#### 🟡 d15-fc-2: План запросов: как читать и что искать

**Ответ:** Консоль запросов → Запрос → Показать план. Ищи: (1) Временные таблицы (зелёные) — ОК. (2) Индексные сканирования (Index Scan) — ОК. (3) Полные сканирования таблиц (Table Scan) — ПЛОХО, нужен индекс. (4) Nested Loop vs Hash Join — Hash лучше для больших данных. (5) Сортировка (Sort) — лишняя если есть индекс.

*Сложность: Средне | Категория: Оптимизация запросов*

#### 🟡 d15-fc-3: ВТ vs вложенные запросы: когда что?

**Ответ:** ВТ = быстрее для сложных запросов с несколькими этапами. Запрос выполняется на СУБД за 1 вызов. Вложенные = медленнее: каждый вложенный = отдельный запрос к СУБД. Правило: если > 2 вложенных запроса → переделай на ВТ. Исключение: простые подзапросы в ВЫБРАТЬ (scalar subquery).

*Сложность: Средне | Категория: Оптимизация запросов*

#### 🟡 d15-fc-4: Почему LEFT JOIN в запросе может быть медленным?

**Ответ:** (1) Нет индекса по полям связи (Измерения регистра). (2) Большая таблица слева, маленькая справа → СУБД сканирует всю левую. (3) Много NULL-значений после JOIN → фильтрация потом. Решение: индекс на поля связи, пересмотреть структуру запроса, использовать ВТ.

*Сложность: Средне | Категория: Оптимизация запросов*

#### 🟡 d15-fc-5: СрезПоследних: почему может быть медленным?

**Ответ:** (1) Не заполнен параметр Периода → сканирует ВСЮ историю. (2) Нет индекса по регистратору. (3) Много измерений = много группировок. (4) Если выбираешь за месяц, а данных 5 лет. Решение: всегда передавать период, индекс на измерения, тестировать на реальном объёме.

*Сложность: Средне | Категория: Оптимизация запросов*

#### 🔴 d15-fc-6: СхемаЗапроса: зачем программно изменять запрос?

**Ответ:** СхемаЗапроса = запрос как объект. Можно: (1) Удалить/добавить группировки. (2) Изменить условия. (3) Добавить выражения итогов. Твой код из Скаенга: удалял ИтогиПакета по pricesetType и СТК, добавлял МАКСИМУМ. Это нужно когда запрос собирается динамически!

*Сложность: Сложно | Категория: Оптимизация запросов*

### Рефакторинг кода

#### 🟡 d15-fc-7: Функция > 50 строк — что делать?

**Ответ:** (1) Выделить логику в отдельные функции с осмысленными именами. (2) Извлечь константы (магические числа, строки). (3) Разделить на фазы: валидация → запрос → обработка → формирование ответа. Твой get-default-and-real-prices: 200+ строк → разбиение на 5-6 функций.

*Сложность: Средне | Категория: Рефакторинг*

#### 🟢 d15-fc-8: Магические числа и строки — паттерн рефакторинга

**Ответ:** Плохо: Если КодЛокализации = 'math' Тогда. Хорошо: Константы.КодЛокализации.Математика. Или Перечисление.КодыЛокализации.Математика. Плохо: Если expand >= 5 Тогда. Хорошо: Константы.МинимальныйExpandДляСкидок. Магические значения = сложнее менять и тестировать.

*Сложность: Легко | Категория: Рефакторинг*

#### 🟡 d15-fc-9: Дублирование кода — как устранить

**Ответ:** (1) Вынести общую логику в функцию. (2) Использовать общие модули. (3) Шаблонный метод: общая структура + вариации. В Скаенге:构建 Струк_Default и Струк_Real почти идентичны → одна функция СоздатьСтруктуруЦены(). Дубли = место для багов.

*Сложность: Средне | Категория: Рефакторинг*

#### 🟡 d15-fc-10: Попытка/Исключение: правильный паттерн

**Ответ:** Плохо: Попытка...Исключение вокруг всего кода. Хорошо: (1) Минимальная секция Попытка. (2) Специфический обработчик ошибки. (3) Логирование ошибки для диагностики. (4) Возврат корректного ответа. Твой код: 2 Попытки — одна для JSON, одна для логики — это правильный подход!

*Сложность: Средне | Категория: Рефакторинг*

#### 🟡 d15-fc-24: Паттерн 'Строитель ответа' для HTTP-сервиса

**Ответ:** Функция СоздатьОтвет200(Данные) Экспорт. Внутри: создаёт структуру с code=200, metadata, data. Функция СоздатьОтвет490(Ошибки). Функция СоздатьОтвет590(ТекстОшибки). В каждом обработчике: только бизнес-логика + вызов строителя. Убирает дублирование кода ответов.

*Сложность: Средне | Категория: Паттерны*

#### 🟡 d15-fc-25: Паттерн 'Фабрика запросов' для динамических запросов

**Ответ:** Функция СоздатьЗапросЦен(СекцияЦен, Expand) Экспорт. Внутри: СхемаЗапроса, настройка итогов, параметры. Фабрика = не строить запрос строкой, а собирать объект. Плюсы: переиспользование, типобезопасность, тестирование. Твой код с СхемаЗапроса = это уже фабрика!

*Сложность: Средне | Категория: Паттерны*

#### 🟡 d15-fc-26: Модульная структура обработки API

**Ответ:** (1) ОбщийМодуль.ОбработкаЗапросовСервиса_Billing — общие функции. (2) ОбщийМодуль.МаппингЦен — создание структур JSON. (3) ОбщийМодуль.ВалидацияЗапросов — проверка входящих данных. (4) Обработчики HTTP-сервиса — только маршрутизация. Каждый модуль < 200 строк.

*Сложность: Средне | Категория: Паттерны*

### Архитектура HTTP-сервисов

#### 🔴 d15-fc-11: Чистая архитектура HTTP-сервиса 1С

**Ответ:** Слои: (1) Обработчик HTTP → валидация JSON. (2) Бизнес-логика → запросы, вычисления. (3) Маппинг → структура → JSON. Разделение: обработчик не содержит бизнес-логику! Твой get-default-and-real-prices смешивает все 3 слоя в одной функции. Цель: 3 функции, каждая < 50 строк.

*Сложность: Сложно | Категория: Архитектура API*

#### 🟡 d15-fc-12: HTTP-коды ответов: какой когда использовать?

**Ответ:** 200 = OK. 490 = бизнес-ошибка (невалидные данные). 590 = техническая ошибка (исключение). 401 = не авторизован. 403 = нет прав. 404 = ресурс не найден. 500 = ошибка сервера. Твой код: 200, 490, 590 — это кастомные коды Скаенга. Стандарт: 400 для бизнес-ошибок, 500 для технических.

*Сложность: Средне | Категория: Архитектура API*

#### 🟡 d15-fc-13: Как логировать API-вызовы правильно?

**Ответ:** (1) Зафиксировать: входящий запрос (тело, заголовки, URL). (2) Дата начала обработки. (3) Код ответа. (4) Исходящий ответ. (5) Время выполнения. (6) Ошибка если есть. РегистрСведений или отдельная таблица. Твой паттерн: ЗафиксироватьОбращениеАПИ — правильно! Но добавь: время выполнения.

*Сложность: Средне | Категория: Архитектура API*

#### 🟡 d15-fc-14: Валидация входящих данных: стратегия

**Ответ:** (1) Проверить JSON формат → Попытка/Исключение. (2) Проверить обязательные поля. (3) Проверить типы данных (число, строка). (4) Проверить допустимые значения (priceSection = eng|math). (5) Вернуть детальную ошибку (какое поле неверно, почему). Твой код: делает всё это! СтруктураОшибки = хороший паттерн.

*Сложность: Средне | Категория: Архитектура API*

### Тестирование и транзакции

#### 🟡 d15-fc-15: Как тестировать HTTP-сервис 1С без внешних инструментов

**Ответ:** (1) Создать обработку с полем JSON. (2) Сформировать структуру запроса. (3) Вызвать функцию обработчика напрямую (на сервере). (4) Сравнить результат с ожидаемым. (5) Для нескольких сценариев: валидные данные, невалидный JSON, отсутствующие поля. Автотест = сохранённая обработка.

*Сложность: Средне | Категория: Тестирование*

#### 🟢 d15-fc-16: Паттерн Arrange-Act-Assert для 1С

**Ответ:** Arrange: подготовить данные (создать ученика, заполнить цены). Act: вызвать тестируемую функцию. Assert: проверить результат (код 200, JSON содержит expected). В 1С: Обработка тестирования с кнопками: НастроитьДанные → Выполнить → ПроверитьРезультат.

*Сложность: Легко | Категория: Тестирование*

#### 🟡 d15-fc-17: Как обнаружить что API сломался после доработки?

**Ответ:** (1) Сохранить эталонные запросы и ответы. (2) Перед релизом прогнать все эталоны. (3) Сравнить фактический ответ с ожидаемым. (4) Использовать Массив.Сравнить() для JSON-структур. Твой опыт из Скаенга: фактический ответ != ожидаемый → нашли баг с пустым math!

*Сложность: Средне | Категория: Тестирование*

#### 🔴 d15-fc-18: Попытка/Исключение внутри транзакции — ловушка

**Ответ:** Если Исключение внутри транзакции → транзакция откатывается, НО переменные НЕ откатываются! Пример: Статус = 'Ошибка' — останется. Решение: после Исключения восстанавливать переменные. Или: не использовать переменные для контроля между Попытка и КонецПопытки.

*Сложность: Сложно | Категория: Транзакции и ошибки*

#### 🟡 d15-fc-19: Откат документа в 1С — когда это правильно?

**Ответ:** Откат документа = удалить запись регистра и перепровести. Это НОРМАЛЬНО если: (1) Обнаружены некорректные движения. (2) Нужна перерегистрация. (3) Изменилась структура регистра. НЕ нормально: откат как способ исправить баг в коде! Сначала исправить код, потом откатить и перепровести.

*Сложность: Средне | Категория: Транзакции и ошибки*

#### 🔴 d15-fc-20: Как безопасно обрабатывать внешние вызовы в 1С?

**Ответ:** (1) Получить данные ДО транзакции. (2) Начать транзакцию. (3) Записать/провести. (4) Если Исключение → откат + логирование. (5) Внешнему сервису вернуть статус. ПРАВИЛО: никогда не делать HTTP-запросы ВНУТРИ транзакции! Отключение сети = висячая транзакция.

*Сложность: Сложно | Категория: Транзакции и ошибки*

### Скаенг: разбор реального кода

#### 🔴 d15-fc-21: Скаенг: почему get-default-and-real-prices возвращает пустой math?

**Ответ:** Анализ твоего кода: (1) Запрос получает данные из Справочник.Номенклатура по СТК. (2) Если ВидыНоменклатуры не загружены → запрос возвращает пусто. (3) Решение: проверить что ПолучениеВидовНоменклатурыЕдиничек() возвращает 'mathematics_kids'. (4) Факт: перенесли виды номенклатуры — это исправило проблему!

*Сложность: Сложно | Категория: Скаенг: разбор*

#### 🔴 d15-fc-22: Скаенг: finance_gate.proxy.1c — зачем префикс?

**Ответ:** Префикс distinguishes: какие сообщения через какой шлюз. finance_gate.proxy.1c.event.X = отправляется через прокси 1С. Без префикса = напрямую через FinanceGate. При переносе 12→13: все exchange names нужно обновить. Пропущенный префикс = сообщение не дойдёт до шлюза!

*Сложность: Сложно | Категория: Скаенг: разбор*

#### 🔴 d15-fc-23: Скаенг: расширение для автозагрузки — зачем?

**Ответ:** Проблема: после проведения документа поломались значения в external credit. Решение: создали расширение с обработчиком ПриЗаписи. Расширение загружает данные из API автоматически. Плюсы: (1) Не трогаем типовой код. (2) Включение/выключение. (3) Отдельно тестируется. Минусы: ещё один слой абстракции.

*Сложность: Сложно | Категория: Скаенг: разбор*

### Мок-собеседование: оптимизация

#### 🟡 d15-fc-27: Вопрос: Отчёт работает 30 секунд. Ваши действия?

**Ответ:** (1) Получить план запроса. (2) Найти Table Scan. (3) Проверить параметры периода — заполнены? (4) Проверить индексы по полям группировки и отбора. (5) Заменить вложенные запросы на ВТ. (6) Проверить: не выбираются ли лишние поля. (7) Уменьшить глубину итогов. (8) Проверить СУБД: автовариум, статистика.

*Сложность: Средне | Категория: Мок-собес: оптимизация*

#### 🟡 d15-fc-28: Вопрос: В цикле 1000 раз вызывается Запрос.Выполнить(). Что делать?

**Ответ:** (1) Собрать все параметры в массивы. (2) Один запрос с В() вместо =. (3) Выгрузить результат в Соответствие. (4) Обойти цикл и брать данные из Соответствия за O(1). Замена 1000 SQL → 1 SQL + 1000 O(1). Паттерн хэш-таблицы из Day 13!

*Сложность: Средне | Категория: Мок-собес: оптимизация*

#### 🔴 d15-fc-29: Вопрос: Как ускорить загрузку 50 000 строк из Excel?

**Ответ:** (1) COM-объект Excel → читать в ТЗ. (2) Пакетами по 1000 строк → НаборЗаписей.Записать(). (3) ОбменДанными.Загрузить() для быстрой загрузки. (4) Включить автотранзакцию: НачатьТранзакцию(), цикл пакетов, ЗафиксироватьТранзакцию(). Ключ: МИНИМИЗИРОВАТЬ количество транзакций!

*Сложность: Сложно | Категория: Мок-собес: оптимизация*

#### 🔴 d15-fc-30: Вопрос: Скаенг: API возвращает пустой массив вместо данных. Диагностика?

**Ответ:** Шаги: (1) Вызвать функцию в отладчике с теми же параметрами. (2) Проверить: Запрос.Выполнить() возвращает данные? (3) Если в консоли запросов есть данные → проблема в обработке результата. (4) Если запрос пустой → проверить параметры, виды номенклатуры, СТК. (5) Проверить что СхемаЗапроса не удалила нужные итоги.

*Сложность: Сложно | Категория: Мок-собес: оптимизация*

#### 🟡 d15-fc-31: Вопрос: Функция обработчика 200+ строк. Как аргументировать рефакторинг?

**Ответ:** (1) Трудно тестировать — нет единичных точек проверки. (2) Дублирование — Струк_Default и Струк_Real идентичны. (3) Смешивание слоёв — валидация + бизнес + маппинг. (4) При изменении логики цен — нужно менять 2 места. (5) Новому разработчику сложно понять. Цель: < 50 строк на функцию, 1 ответственность.

*Сложность: Средне | Категория: Мок-собес: оптимизация*

#### 🔴 d15-fc-32: Вопрос: Как выглядит идеальный HTTP-обработчик в 1С?

**Ответ:** Функция Обработать(Запрос) Экспорт. Валидация = Валидатор.Проверить(Запрос). Если ошибку → Возврат Ответ.490(). Данные = СервисПрайсов.Получить(Параметры). Ответ = Маппер.ВJSON(Данные). Логирование = Лог.API(Запрос, Ответ). Возврат Ответ. Всего 10 строк, вся логика в модулях.

*Сложность: Сложно | Категория: Мок-собес: оптимизация*

#### 🔴 d15-fc-33: Вопрос: Ваш HTTP-сервис возвращает 1С-формат JSON (#type). Как исправить?

**Ответ:** Проблема: используется ОбъектВJson с дефолтными параметрами. Решение: se_КоннекторHTTP.ОбъектВJson(Структура, ПараметрыЗаписиJSON). Параметры: без типа #type, формат ISO даты, без BOM. Проверить: se_ОбработкаЗапросовСервиса_billing_ОбщийСервер.ПолучитьПараметрыЗаписиJSONПоУмолчанию() — уже настроен?

*Сложность: Сложно | Категория: Мок-собес: оптимизация*

#### 🔴 d15-fc-34: Вопрос: Скаенг: после обновления виды номенклатуры не загрузились. Почему?

**Ответ:** Причины: (1) Внешняя обработка загрузки не перезапускали после изменения. (2) Варианты рассрочки = отдельный регистр, не загрузили. (3) Виды номенклатуры были перенесены вручную с ошибками. (4) ProductKit изменил структуру — старые виды не подходят. Решение: автозагрузка при запуске через расширение + подписка.

*Сложность: Сложно | Категория: Мок-собес: оптимизация*

#### 🟡 d15-fc-35: Вопрос: Как тестировать что refactoring не сломал API?

**Ответ:** (1) До рефакторинга: сохранить 3-5 запросов и ожидаемых ответов. (2) После рефакторинга: прогнать те же запросы. (3) Сравнить фактические ответы с ожидаемыми. (4) Автоматизация: обработка с кнопкой ЗапуститьТесты. (5) Для регрессии: запускать перед каждым релизом. Из твоего опыта: факт. != ожид. = нашли баг!

*Сложность: Средне | Категория: Мок-собес: оптимизация*

### Диаграмма: Путь диагностики: от жалобы к оптимизации запроса

*Пошаговый процесс поиска и устранения узкого места в медленном запросе 1С*

#### Шаг 1: Получить жалобу

Пользователь: отчёт/API работает медленно (30 секунд)

#### Шаг 2: Воспроизвести

Вызвать с теми же параметрами в отладчике

#### Шаг 3: План запроса

Скопировать текст запроса в консоль → Показать план

#### Шаг 4: Добавить индекс

Создать индекс по полям группировки + отборов

#### Шаг 5: ВТ вместо вложенных

Заменить вложенные запросы на временные таблицы

#### Шаг 6: Проверить результат

Запустить снова, сравнить время

### Диаграмма: Рефакторинг: от монолита к модулям

*Как разбить функцию 200+ строк на чистую архитектуру HTTP-сервиса*

#### Шаг 1: Было: монолит

Одна функция 200 строк: валидация + логика + маппинг + логирование

#### Шаг 2: Шаг 1: извлечь валидацию

Модуль ВалидаторЗапросов: проверка JSON, обязательных полей, типов, допустимых значений

#### Шаг 3: Шаг 2: извлечь бизнес-логику

Модуль СервисПрайсов: запрос к БД, вычисление цен, скидок, кредитов

#### Шаг 4: Шаг 3: извлечь маппинг

Модуль МапперЦен: преобразование ТЗ → структура для JSON

#### Шаг 5: Шаг 4: обработчик = маршрутизатор

HTTP-обработчик: 10-15 строк, только вызовы модулей

#### Шаг 6: Результат: чистая архитектура

3 модуля + обработчик. Тестируемость, переиспользование, читаемость

### Код: Рефакторинг: от монолита к модулям (Скаенг пример)

*Как разбить функцию get-default-and-real-prices на чистую архитектуру из 3 модулей*

```bsl
// БЫЛО: 200+ строк в одной функции
Функция Обработка_api_internal_v1_price_get...
  // Валидация, запрос, обработка, JSON — всё тут
КонецФункции // 200+ строк!

// ═══ СТАЛО: Модуль 1 — Валидатор ═══
Функция ПроверитьЗапросЦен(ЗапросСтруктуры) Экспорт
  РезультатВалидации = Новый Структура;
  РезультатВалидации.Валидно = Истина;
  РезультатВалидации.Ошибки = Неопределено;
  РезультатВалидации.ИДУченика = Неопределено;
  РезультатВалидации.СекцияЦен = Неопределено;
  РезультатВалидации.Expand = Ложь;

  Если НЕ ЗапросСтруктуры.Свойство(`studentId`)
    ИЛИ ЗначениеЗаполнено(ЗапросСтруктуры.studentId) = Ложь Тогда
    РезультатВалидации.Валидно = Ложь;
    РезультатВалидации.Ошибки = Новый Структура;
    РезультатВалидации.Ошибки.Вставить(`studentId`,
      `ID студента не может быть пустым`);
    Возврат РезультатВалидации;
  КонецЕсли;

  РезультатВалидации.ИДУченика = ЗапросСтруктуры.studentId;
  ЗапросСтруктуры.Свойство(`priceSection`,
    РезультатВалидации.СекцияЦен);
  ЗапросСтруктуры.Свойство(`expand`,
    РезультатВалидации.Expand);

  Возврат РезультатВалидации;
КонецФункции

// ═══ Модуль 2 — Сервис (бизнес-логика) ═══
Функция ПолучитьЦены(ИДУченик, СекцияЦен, Expand)
  Запрос = СоздатьЗапросЦен(СекцияЦен);
  Запрос.УстановитьПараметр(`Ученик`, Ученик);
  Запрос.УстановитьПараметр(`МассивСТК`,
    ПолучитьМассивСТК(СекцияЦен));
  РезЗап = Запрос.Выполнить();
  Возврат РезЗап;
КонецФункции

// ═══ Модуль 3 — Обработчик = 15 строк! ═══
Функция Обработать(Запрос) Экспорт
  ДатаСтарта = ТекущаяУниверсальнаяДатаВМиллисекундах();

  // 1. Валидация
  Валид = ВалидаторЗапросовBilling.Проверить(
    JsonВОбъект(Запрос.ПолучитьТелоКакПоток()));
  Если НЕ Валид.Валидно Тогда
    Возврат Ответ_490(Валид.Ошибки, ДатаСтарта);
  КонецЕсли;

  // 2. Бизнес-логика
  РезЗап = СервисПрайсов.ПолучитьЦены(
    Валид.ИДУченика, Валид.СекцияЦен, Валид.Expand);

  // 3. Маппинг + ответ
  ТелоОтвета = МапперЦен.ВJSON(РезЗап, Валид.Expand);
  Лог.API(`/price/get-default-and-real`, ДатаСтарта, ТелоОтвета);
  Возврат Ответ_200(ТелоОтвета);
КонецФункции
```

### Код: Автотест HTTP-сервиса: обработка с 3 тестами

*Как создать простую обработку для тестирования API без внешних инструментов*

```bsl
// Обработка АвтотестAPI

&НаСервере
Процедура ЗапуститьВсеТесты()
  ТЗ = Объект.Тесты; // Таблица значений на форме
  ТЗ.Очистить();
  Результат = ``;

  // Тест 1: валидный запрос math
  ДобавитьТест(ТЗ, `Математика: валидный запрос`,
    "{"studentId": 44006323, "priceSection": "math", "expand": 5}",
    200);

  // Тест 2: без studentId → 490
  ДобавитьТест(ТЗ, `Отсутствует studentId`, "{"priceSection": "math"}", 490);

  // Тест 3: невалидный JSON → 490
  ДобавитьТест(ТЗ, `Невалидный JSON`, "{не_json}", 490);

  // Выполнить все тесты
  Для Каждого СтрТест Из ТЗ Цикл
    Попытка
      ЗапросСервис = Новый HTTPСервисЗапрос;
      ЗапросСервис.УстановитьТелоИзСтроки(СтрТест.ТелоЗапроса);
      Ответ = Обработать(ЗапросСервис);
      ФактическийКод = Ответ.КодСостояния;
    Исключение
      ФактическийКод = 590; // Техническая ошибка
    КонецПопытки;

    // Assert: проверить результат
    СтрТест.Прошёл = (ФактическийКод = СтрТест.ОжидаемыйКод);
    СтрТест.ФактическийКод = ФактическийКод;
    Если НЕ СтрТест.Прошёл Тогда
      Результат = Результат + Символы.ПС
        + `[ПРОВАЛ] ` + СтрТест.Описание;
    КонецЕсли;
  КонецЦикла;

  Если Результат = `` Тогда
    Результат = `Все тесты пройдены!`;
  КонецЕсли;
КонецПроцедуры
```

### Сценарии: Оптимизация запросов

#### sc-44: Диагностика медленного API-эндпоинта

**Сложность:** Средне

**Теги:** 

**Контекст:** В Скаенг эндпоинт /api/internal/v1/price/get-default-and-real-prices отвечает за 8 секунд вместо ожидаемых 500мс. Нужно найти причину и исправить.

**Пошаговый план:**

**Шаг 1:** Поставить точку останова в начале функции и измерить время каждого этапа
> 💡 ТекущаяУниверсальнаяДатаВМиллисекундах() — до и после каждого блока. Найти самый медленный.

**Шаг 2:** Проверить: Запрос.Выполнить() возвращает быстро или медленно?
> 💡 Если запрос медленный → проблема в SQL. Если быстрый → проблема в обработке результата.

**Шаг 3:** Получить план запроса в консоли
> 💡 Открыть консоль запросов, скопировать текст запроса, заполнить параметры, выполнить. Показать план.

**Шаг 4:** Найти Table Scan в плане запроса
> 💡 Полное сканирование = нет индекса. Добавить индекс по полям группировки: СТК + КодЛокализации.

**Шаг 5:** Проверить: выборка по группировкам занимает сколько времени?
> 💡 ОбходРезультатаЗапроса.ПоГруппировкам + внутренний цикл = N записей. Если N > 1000 → оптимизировать.

**Шаг 6:** Рефакторинг: вынести обработку результата в отдельную функцию
> 💡 Функция СформироватьОтветПоЦенам(Выборка, УказатьСкидки). Убрать дублирование кода.

### Сценарии: Рефакторинг кода

#### sc-45: Рефакторинг функции 200+ строк в 3 модуля

**Сложность:** Средне

**Теги:** 

**Контекст:** Функция Обработка_api_internal_v1_price_get_default_and_real_prices содержит 200+ строк с валидацией, бизнес-логикой и маппингом. Нужно разбить.

**Пошаговый план:**

**Шаг 1:** Выделить валидацию в отдельный модуль: ВалидаторЗапросов Billing
> 💡 Функция ПроверитьЗапросЦен(ЗапросСтруктуры). Возвращает Структура с ошибками или Неопределено.

**Шаг 2:** Выделить бизнес-логику в модуль: СервисПрайсов
> 💡 Функция ПолучитьЦеныДляУченика(ИДУченика, СекцияЦен, Expand). Возвращает ТаблицуЗначений с ценами.

**Шаг 3:** Выделить маппинг в модуль: МапперЦен
> 💡 Функция ПреобразоватьВJSON(ТаблицаЦен, Expand). Возвращает структуру для JSON.

**Шаг 4:** Обработчик HTTP-сервиса = только маршрутизация
> 💡 Валидация → Сервис → Маппинг → Логирование → Ответ. 10-15 строк вместо 200.

**Шаг 5:** Протестировать: прогнать 3 эталонных запроса до и после рефакторинга
> 💡 Результаты должны совпадать. Время выполнения должно уменьшиться или остаться тем же.

### Сценарии: Тестирование и транзакции

#### sc-46: Создание автотеста для HTTP-сервиса

**Сложность:** Средне

**Теги:** 

**Контекст:** Нужно создать обработку для автотестирования API-эндпоинтов Скаенга без внешних инструментов.

**Пошаговый план:**

**Шаг 1:** Создать обработку АвтотестAPI с реквизитами: ТестЗапрос, ТестОтвет, ФактическийОтвет, Результат
> 💡 Модуль формы: реквизиты + кнопки Запустить, Сравнить, Все тесты.

**Шаг 2:** Создать вкладку с эталонными тестами (3-5 сценариев)
> 💡 Таблица значений: Описание, URL, ТелоЗапроса, ОжидаемыйКод, ОжидаемыйОтвет.

**Шаг 3:** Написать кнопку Запустить: вызвать функцию обработчика напрямую
> 💡 На сервере: сформировать HTTPСервисЗапрос, вызвать функцию, получить HTTPСервисОтвет.

**Шаг 4:** Написать кнопку Сравнить: фактический vs ожидаемый
> 💡 Сравнить код ответа. Сравнить JSON: разобрать обе структуры, сравнить по ключам.

**Шаг 5:** Добавить кнопку Все тесты: прогнать все сценарии и показать результат
> 💡 Цикл по строкам ТЗ, запуск каждого теста, результат = зелёный/красный.

### Сценарии: Скаенг: разбор реального кода

#### sc-47: Исправление пустого ответа math в Скаенг

**Сложность:** Средне

**Теги:** 

**Контекст:** Реальный баг из твоего опыта: get-default-and-real-prices возвращает пустой math массив, хотя в БД данные есть.

**Пошаговый план:**

**Шаг 1:** Вызвать функцию в отладчике с параметрами из баг-репорта
> 💡 studentId: 44006323, priceSection: math, expand: 5. Точка останова в начале функции.

**Шаг 2:** Проверить: МассивСТКНоменклатуры содержит 'mathematics_kids'?
> 💡 Если МассивСТКНоменклатуры пустой или не содержит нужный СТК → запрос вернёт пусто.

**Шаг 3:** Скопировать текст запроса в консоль запросов и выполнить вручную
> 💡 Заполнить параметры: МассивСТКНоменклатуры, Ученик, ТекущаяДата. Проверить результат.

**Шаг 4:** Если запрос в консоли пустой — проверить ВидыНоменклатуры
> 💡 ПолучениеВидовНоменклатурыЕдиничек() возвращает все нужные виды? Математика загружена?

**Шаг 5:** Исправление: перенести виды номенклатуры и перезапустить автозагрузку
> 💡 Внешняя обработка загрузки видов номенклатуры. Или: расширение с подпиской на обновление.

#### sc-48: Настройка finance_gate.proxy.1c для новой среды

**Сложность:** Средне

**Теги:** 

**Контекст:** При переносе 12→13 нужно обновить все exchange names, добавив префикс finance_gate.proxy.1c

**Пошаговый план:**

**Шаг 1:** Найти все исходящие сообщения в конфигурации
> 💡 Поиск по конфигурации: event.billing, event.crm, trigger_gateway. Без префикса = нужно обновить.

**Шаг 2:** Обновить каждое имя: добавить finance_gate.proxy.1c. в начало
> 💡 event.billing_marketing.certificate.expired → finance_gate.proxy.1c.event.billing_marketing.certificate.expired

**Шаг 3:** Обновить очереди обмена и точки обмена для стейджинга
> 💡 Стейджинг = другой адрес RabbitMQ. Настройки = по аналогии из 13 базы, но с новым адресом.

**Шаг 4:** Проверить: если очередь заполнена — не добавлять finance_gate.proxy.1c
> 💡 Исключение: уже настроенные точки обмена. Не трогать — только новые и пустые.

**Шаг 5:** Обновить код: все обращения к прайсетченджд
> 💡 Поиск по коду: все строковые литералы с именами обменов. Заменить на переменные конфигурации.

### Теория: Массовые операции: замена циклов с вызовами к БД на пакетные запросы

Частая задача на собеседовании и в проде: нужно обработать N элементов, обращаясь к БД. Антипаттерн: цикл с НайтиПоРеквизиту/Записать() внутри = N отдельных SQL-запросов. Паттерн: один запрос с В Массиве + Соответствие для маппинга + пакетная запись. Шаблон: (1) Собрать все ключи поиска в Массив. (2) Один Запрос с ГДЕ Артикул В(&МассивАртикулов). (3) Результат в Соответствие: Соответствие[Артикул] = Ссылка. (4) Обойти исходный массив, брать из Соответствия. Замена 10 000 SQL-запросов на 1 запрос + 10 000 обращений к памяти.

**Ключевые моменты:**

- Цикл с НайтиПоРеквизиту внутри = N SQL-запросов = ОЧЕНЬ медленно
- Один запрос с В(&Массив) = 1 SQL-запрос = быстро
- Соответствие для маппинга: Ключ → Значение за O(1)
- Запрос.Выгрузить() в ТаблицуЗначений для удобства
- Для записи: НаборЗаписей + загрузка таблицы = пакетная запись
- Правило: минимальное количество переходов клиент ↔ сервер ↔ СУБД

**Советы:**

- Сначала считай сколько SQL-запросов делает код
- Если цикл внутри цикла — это точно O(n^2), нужно оптимизировать
- Пакетная обработка: по 500-1000 записей за транзакцию
- Соответствие = лучший друг для маппинга данных в 1С

---

# Все диаграммы (отдельный справочник)

## diag-1: Путь вызова: Клиент → Сервер → БД

*Категория: Клиент-сервер*

Как работает вызов серверной процедуры из клиентского кода

### Шаг 1: Клиент нажимает кнопку

Пользователь нажимает кнопку на форме. Срабатывает обработчик &НаКлиенте.

- Клиентский обработчик получает управление
- Подготавливает данные для отправки
- Вызывает серверную процедуру

### Шаг 2: Вызов сервера (сетевой запрос)

Клиент отправляет HTTP-запрос к серверу 1С. Это НЕ мгновенно!

- Данные сериализуются и передаются по сети
- Занимает 10-500мс в зависимости от нагрузки
- Минимизируйте количество серверных вызовов

### Шаг 3: Выполнение на сервере

Сервер создаёт объекты, выполняет запросы к СУБД, обрабатывает логику.

- Создание объектов — только здесь!
- Выполнение запросов к БД
- Бизнес-логика и валидация

### Шаг 4: Ответ клиенту

Сервер отправляет результат обратно. Только примитивные типы!

- Нельзя вернуть СправочникОбъект — только ФиксированнаяСтруктура
- Массивы, ТаблицыЗначений — можно через форму
- Ответ приходит как результат функции

---

## diag-2: Жизненный цикл проведения документа

*Категория: Регистры*

Что происходит шаг за шагом при проведении документа

### Шаг 1: Пользователь нажимает 'Провести'

Начинается процесс записи документа и формирования движений

- Срабатывает ПередЗаписью (можно отменить)
- Объект записывается в БД
- Запускается ОбработкаПроведения

### Шаг 2: Открытие транзакции

1С автоматически начинает транзакцию

- Все движения и запись — в одной транзакции
- При ошибке — полный откат

### Шаг 3: Формирование движений

В ОбработкаПроведения создаются записи в регистрах

- Движения.РегистрНакопления.Записать;
- Каждый регистр получает свой НаборЗаписей
- Заполняются измерения, ресурсы, реквизиты

### Шаг 4: Запись движений и фиксация

Все движения записываются, транзакция фиксируется

- Движения записываются в БД
- Транзакция фиксируется (COMMIT)
- Документ становится 'Проведён'

---

## diag-3: Механизм управляемых блокировок

*Категория: Блокировки*

Как 1С управляет блокировками в кластере

### Шаг 1: Код вызывает ЗаблокироватьДанныеДляРедактирования()

Программист явно запрашивает блокировку в кластере 1С

- Передаётся ключ: что блокируем
- Кластер проверяет: нет ли уже блокировки?
- Блокировка регистрируется в кластере 1С (НЕ в СУБД!)

### Шаг 2: Проверка в кластере 1С

Менеджер блокировок кластера проверяет конфликты

- Кластер 1С — центральный координатор блокировок
- Проверяются все активные блокировки
- При конфликте — ожидание или исключение

### Шаг 3: Редактирование данных

После успешной блокировки — можно безопасно редактировать

- Данные заблокированы для других пользователей
- Начинается транзакция
- Чтение и модификация данных

### Шаг 4: Запись и снятие блокировки

При фиксации транзакции блокировка автоматически снимается

- ЗафиксироватьТранзакцию() снимает блокировку
- При ОтменитьТранзакцию() — тоже снимается

---

## diag-4: Как запрос проходит через индекс

*Категория: Запросы и индексы*

От текста запроса к данным в СУБД — что происходит внутри

### Шаг 1: Парсер запроса

1С разбирает текст запроса и строит дерево синтаксического разбора

- Проверка синтаксиса
- Разрешение имён объектов
- Определение источников данных

### Шаг 2: Построитель плана запроса

1С генерирует план выполнения — какие операции и в каком порядке

- Выбор стратегии: Index Scan vs Seq Scan
- Определение порядка JOIN
- Оптимизация порядка фильтрации

### Шаг 3: Использование индекса (быстро)

Если есть подходящий индекс — СУБД ищет по B-дереву, O(log N)

- Поиск по ключу индекса
- Быстрый переход к нужной странице
- 10 записей из 1 000 000 за миллисекунды

### Шаг 4: Seq Scan (медленно)

Если индекса нет или он не подходит — СУБД сканирует ВСЮ таблицу

- Читает каждую строку.
- 1 000 000 строк = секунды или минуты.
- Именно поэтому важно создавать правильные индексы.

---

## diag-5: Виртуальные таблицы: Остатки vs Обороты

*Категория: Запросы и индексы*

Как платформа 1С преобразует виртуальные таблицы в реальные запросы

### Шаг 1: Запрос к виртуальной таблице Остатки

Вы пишете: ВЫБРАТЬ Из РегистрНакопления.Остатки()

- Платформа видит обращение к виртуальной таблице
- Определяет: какой тип регистра
- Формирует параметры: Период, Условие

### Шаг 2: Платформа генерирует СКРЫТЫЙ запрос

1С автоматически создаёт запрос с SUM и GROUP BY

- Для Остатков: SUM(Приход) - SUM(Расход) GROUP BY Измерения
- Для Оборотов: SUM(Ресурс) GROUP BY Измерения, Период
- Добавляет условие МЕЖДУ для периода

### Шаг 3: Запрос к СУБД

Сформированный запрос отправляется в PostgreSQL / SQL Server

- СУБД выполняет реальный SQL-запрос
- Использует индексы измерений
- Возвращает результат платформе

### Шаг 4: Агрегация на стороне платформы

Платформа может досчитать итоги сама, если СУБД не справляется

- ИТОГИ ПО часто считается платформой
- Виртуальные таблицы ИТОГИ — особый случай
- Балансировка нагрузки между 1С и СУБД

---

## diag-6: Жизненный цикл управляемой транзакции

*Категория: Транзакции*

Что происходит на каждом этапе явной транзакции в 1С

### Шаг 1: НачатьТранзакцию()

Разработчик явно открывает транзакцию. Платформа начинает отслеживать все изменения.

- Открывается транзакция в СУБД (BEGIN TRANSACTION)
- Все последующие записи попадут в эту транзакцию
- Вложенные транзакции НЕ поддерживаются — вызовет ошибку

### Шаг 2: Чтение и блокировка данных

Сначала блокируем, потом читаем. Управляемые блокировки проверяются в кластере 1С.

- ЗаблокироватьДанныеДляРедактирования() — упреждающая блокировка
- Чтение данных только ПОСЛЕ установки блокировки
- Кластер 1С координирует блокировки между сеансами

### Шаг 3: Модификация и запись в БД

Изменяем данные и записываем объекты. Всё в рамках одной транзакции.

- Объект.Записать() — отправка в СУБД
- Движения.Записать() — запись в регистры
- ВСЕ изменения — временные, пока не зафиксированы

### Шаг 4: ЗафиксироватьТранзакцию() или ОтменитьТранзакцию()

Финал: либо подтверждаем все изменения, либо откатываем.

- COMMIT: все изменения становятся постоянными
- ROLLBACK: БД возвращается к состоянию до транзакции
- ПЕРЕМЕННЫЕ НЕ ОТКАТЫВАЮТСЯ — только данные в БД!

---

## diag-7: Как возникает взаимная блокировка (Deadlock)

*Категория: Блокировки*

Визуализация классического сценария взаимной блокировки

### Шаг 1: Транзакция A: блокирует Ресурс 1

Пользователь А начинает редактирование Контрагента 'Озон'. Устанавливается управляемая блокировка.

- ЗаблокироватьДанныеДляРедактирования(Контрагент.Озон)
- Блокировка зарегистрирована в кластере 1С
- Теперь никто не может изменить Контрагента 'Озон'

### Шаг 2: Транзакция B: блокирует Ресурс 2

Пользователь Б начинает редактирование Контрагента 'Яндекс'. Тоже ставит блокировку.

- ЗаблокироватьДанныеДляРедактирования(Контрагент.Яндекс)
- Блокировка успешно установлена — ресурс свободен
- А не знает, что Б заблокировал ресурс

### Шаг 3: Транзакция A: ждёт Ресурс 2

Пользователь А пытается заблокировать Контрагента 'Яндекс'. Он уже занят Б!

- A ждёт освобождения Ресурса 2 (Контрагент.Яндекс)
- Блокировка Ресурса 2 удерживается Транзакцией B
- Ожидание...

### Шаг 4: DEADLOCK! СУБД убивает одну транзакцию

Обе транзакции ждут друг друга бесконечно. СУБД обнаруживает цикл и прерывает одну из них.

- B тоже пытается заблокировать Ресурс 1 — ожидание
- Цикл: A ждёт B, B ждёт A = DEADLOCK
- СУБД выбирает «жертву» и откатывает одну транзакцию
- Решение: единый порядок блокировки ресурсов!

---

## diag-8: Структура кластера 1С:Предприятие

*Категория: Кластер*

Как устроен кластер серверов 1С и где выполняется код

### Шаг 1: Центральный сервер (Менеджер кластера)

Координирует все компоненты кластера. Хранит информацию о сеансах, блокировках, фоновых заданиях.

- Главный компонент кластера
- Хранит таблицу блокировок
- Координирует распределение нагрузки
- Без него кластер не работает

### Шаг 2: Рабочие серверы

Серверы, на которых выполняется код 1С. Каждый содержит 1+ рабочих процессов.

- Код 1С выполняется ТОЛЬКО здесь
- 1 рабочий процесс = отдельный процесс в памяти
- Рекомендация: процессов = ядер CPU + запас
- Можно добавить серверов для масштабирования

### Шаг 3: Серверы СУБД (PostgreSQL / SQL Server)

Хранят сами данные. Принимают SQL-запросы от рабочих процессов 1С.

- PostgreSQL или MS SQL Server
- Выполняют SQL от платформы 1С
- Индексы, статистика, autovacuum критичны
- Размер RAM влияет на shared_buffers

### Шаг 4: Клиентские приложения

Тонкий клиент, веб-клиент, толстый клиент. Подключаются к рабочим серверам через центральный.

- Толстый: устарел, но встречается
- Тонкий: основной, лёгкий
- Веб-клиент: без установки, браузер
- Клиент отправляет запросы на рабочий сервер

---

## diag-9: Путь диагностики: от жалобы к решению

*Категория: Производительность*

Как найти и исправить узкое место в производительности 1С

### Шаг 1: Жалоба: '1С тормозит!'

Пользователь сообщает о медленной работе. Первый вопрос: конкретная операция или всё?

- Уточнить: какая операция медленная?
- Когда началось (всегда или после обновления)?
- Сколько пользователей затронуто?

### Шаг 2: Сбор Технологического журнала

Включаем ТЖ для проблемного сеанса. События: запросы, вызовы СУБД, блокировки.

- Консоль кластера → ТЖ
- Фильтр по сеансу пользователя
- Минимальный набор событий для скорости
- Сохранить лог для анализа

### Шаг 3: Анализ: ЦУП или GetQuoteTJ

Открываем ТЖ в инструменте анализа. Ищем: долгие запросы (>1 сек), блокировки, частые серверные вызовы.

- ЦУП: встроенный, мгновенный
- GetQuoteTJ: детальный анализ
- Ищем топ-N самых тяжёлых операций
- Проверяем план запроса

### Шаг 4: Исправление и верификация

Оптимизируем: индексы, переписываем запрос, сокращаем транзакцию. Проверяем результат.

- Добавить индексы по фильтруемым полям
- Заменить запрос в цикле на пакетный
- Убрать избыточные блокировки
- Перезапустить и замерить время

---

## diag-10: ACID: 4 гарантии транзакций

*Категория: ACID и изоляция*

Как ACID обеспечивает целостность данных в 1С и любой СУБД

### Шаг 1: A — Atomicity (Атомарность)

Транзакция — неделимая единица. Все операции выполняются целиком или ни одной.

- Либо ВСЕ изменения зафиксированы (COMMIT)
- Либо ни одно (ROLLBACK)
- Пример: перевод денег — списание + зачисление вместе
- В 1С: ЗафиксироватьТранзакцию() или ОтменитьТранзакцию()

### Шаг 2: C — Consistency (Согласованность)

База всегда переходит из одного корректного состояния в другое.

- Все правила и ограничения соблюдены
- Ссылочная целостность не нарушена
- Бизнес-правила конфигурации выполнены
- В 1С: проверка ПередЗаписью() + проводки

### Шаг 3: I — Isolation (Изоляция)

Параллельные транзакции не мешают друг другу.

- 1С использует SERIALIZABLE — самый строгий уровень
- Грязное чтение, неповторяемое, фантомное — исключены
- Управляемые блокировки помогают 1С это гарантировать
- Цена: снижение параллелизма при высокой нагрузке

### Шаг 4: D — Durability (Долговечность)

Зафиксированные данные сохраняются навсегда, даже при сбое питания.

- COMMIT = данные на диске (WAL в PostgreSQL)
- После подтверждения — данные не пропадут
- WAL (Write-Ahead Log) гарантирует это
- Crash recovery: СУБД восстанавливает по WAL

---

## diag-11: Два вида взаимных блокировок (deadlock)

*Категория: Глубокие блокировки*

Как возникает deadlock и как его предотвратить

### Шаг 1: Вид 1: Разный порядок ресурсов

Поток 1 берёт ресурс X, потом хочет Y. Поток 2 взял Y, потом хочет X.

- Поток 1: Заблокировать(X) → ждёт Y
- Поток 2: Заблокировать(Y) → ждёт X
- Оба висят навсегда!
- Решение: единый порядок захвата всегда!

### Шаг 2: Решение: Единый порядок захвата

Все потоки захватывают ресурсы в ОДНОМ порядке.

- Поток 1: Заблокировать(X) → Заблокировать(Y) → работает
- Поток 2: Заблокировать(X) → ждёт (или тоже Y) → работает
- Конфликт возможен, но НЕ взаимоблокировка
- Правило: сортируй ключи перед блокировкой!

### Шаг 3: Вид 2: Повышение уровня изоляции

Оба потока взяли разделяемую блокировку (чтение), потом оба пытаются повысить до исключительной.

- Поток 1: Разделяемая(X) → хочет Исключительную(X)
- Поток 2: Разделяемая(X) → хочет Исключительную(X)
- Оба не могут повысить — deadlock!
- Решение: сразу ставить исключительную, если планируешь запись

### Шаг 4: Как обнаружить deadlock

СУБД и 1С обнаруживают взаимоблокировку автоматически

- ТЖ: событие TLOCK, контекст DEADLOCK
- СУБД убивает одну из транзакций
- Побеждённая получает исключение
- ЦУП позволяет разорвать deadlock вручную

---

## diag-12: Путь запроса: Клиент → rmngr → rphost → СУБД

*Категория: Инфраструктура*

Как запрос проходит через все процессы кластера 1С

### Шаг 1: Клиент подключается

Тонкий/веб-клиент отправляет запрос к центральному серверу кластера.

- Клиент знает адрес rmngr
- Запрос: 'Мне нужен рабочий процесс'
- Поддержание постоянного соединения

### Шаг 2: rmngr — Менеджер кластера

Получает запрос и выбирает наименее загруженный рабочий процесс.

- Алгоритмы: Round-Robin, Least Connections
- Проверяет здоровье каждого rphost
- Если rphost упал — перенаправляет на другой
- Хранит таблицу сеансов и блокировок

### Шаг 3: rphost — Рабочий процесс

Выполняет код 1С и генерирует SQL-запросы к СУБД.

- Один процесс = один поток выполнения
- Толстый слой: интерпретатор 1С
- Генерирует SQL из запросов 1С
- Контролирует транзакции и блокировки

### Шаг 4: СУБД — PostgreSQL / SQL Server

Выполняет SQL, возвращает данные. rphost передаёт результат клиенту.

- Индексы, статистика, autovacuum
- СУБД ставит свои автоматические блокировки
- WAL для долговечности
- Результат: набор строк → rphost → клиент

---

## diag-13: Git reset: 3 режима работы

*Категория: Git*

Как reset --soft, --mixed и --hard влияют на коммиты, индекс и рабочую директорию

### Шаг 1: Три области Git

HEAD (указатель на коммит) → Index (staged) → Working Directory (файлы на диске). Reset двигает HEAD и опционально сбрасывает остальные.

- HEAD = текущий коммит ветки
- Index = 'git add' staging area
- Working Directory = реальные файлы
- reset двигает HEAD, а дальше зависит от флага

### Шаг 2: --soft: только HEAD

Сдвигает HEAD на указанный коммит. Индекс и рабочая директория НЕ меняются.

- Коммиты отменяются, но изменения остаются staged
- Можно 'переделать' последний коммит: reset --soft HEAD~1
- Безопасный режим: ничего не теряется
- Полезно: squash коммитов без потери

### Шаг 3: --mixed (по умолчанию)

Сдвигает HEAD и сбрасывает индекс. Рабочая директория сохраняется.

- Изменения становятся unstaged
- Файлы на диске НЕ меняются
- Можно сделать git add заново
- Самый частый сценарий: 'снять изменения из staged'

### Шаг 4: --hard: всё сбрасывается

Сдвигает HEAD, сбрасывает индекс И рабочую директорию. Данные теряются!

- ВСЕ несохранённые изменения теряются!
- Используй git stash ПЕРЕД reset --hard
- Спасение: git reflog покажет старый HEAD
- git reset --hard <старый хеш> для восстановления

---

## diag-14: Передача данных между формами через сервер

*Категория: Временное хранилище*

Как временное хранилище связывает две разные формы через сервер

### Шаг 1: Форма А: пользователь нажимает кнопку

Клиентский обработчик готовит данные и вызывает серверный метод.

- Данные собираются на клиенте
- Вызываем серверный метод
- Передаём сериализуемые данные

### Шаг 2: Сервер: сериализация и помещение в хранилище

Сервер преобразует данные в двоичный формат и сохраняет во временное хранилище.

- ЗначениеВДвоичныеДанные(Данные)
- Адрес = ПоместитьВоВременноеХранилище()
- Возвращаем Адрес (строка) клиенту

### Шаг 3: Форма А открывает Форму Б с Адресом

Форма Б получает Адрес через параметр открытия формы.

- ОткрытьФорму('Форма.ФормаБ', , , Адрес)
- Адрес = строка-идентификатор в хранилище
- Форма Б получает его в параметрах

### Шаг 4: Форма Б: извлечение из хранилища

Серверная процедура формы Б извлекает данные по Адресу и десериализует.

- ПолучитьИзВоВременногоХранилища(Адрес)
- ЗначениеИзДвоичныхДанных(ДвоичныеДанные)
- НЕ ЗАБЫТЬ: УдалитьИзВоВременногоХранилища(Адрес)!

---

## diag-15: REST API в 1С: HTTP-сервис

*Категория: Web/HTTP*

Как создать RESTful API через HTTP-сервис 1С

### Шаг 1: HTTP-сервис в конфигураторе

Создаём HTTP-сервис с шаблоном URL: /orders/{id}

- Корень: /ext/ords (настраивается)
- Шаблон: /orders/{OrderID}
- Методы: GET, POST, PUT, DELETE
- Каждый метод = отдельная функция

### Шаг 2: GET /orders/{id} — получить заказ

Читаем заказ по ID из URL и возвращаем JSON.

- OrderID = ПараметрыЗапроса.Получить('OrderID')
- Запрос в регистры по OrderID
- Ответ: HTTP-ответ с Content-Type: application/json
- Статус 200 или 404 если не найден

### Шаг 3: POST /orders — создать заказ

Получаем JSON тело, создаём документ, записываем.

- Тело запроса = строка JSON
- Читаем через Документ.ПолучитьТекст() в 1С
- JSON-парсинг: ПрочитатьJSON(), ТекстJSON
- Создаём и записываем документ, возврат 201

### Шаг 4: DELETE /orders/{id} — удалить

Находим документ по ID, помечаем на удаление, записываем.

- Находим документ по UUID из OrderID
- УстановитьПометкуУдаления(Истина)
- Записать(). Возврат 204 No Content
- Или 403 если запрещено правами

---

## diag-16: Архитектура модулей 1С: кто за что отвечает

*Категория: Стандарты кода*

Правильное разделение логики по модулям объекта

### Шаг 1: Модуль менеджера

Общие методы для работы с объектом, не зависящие от конкретного экземпляра.

- ПодготовитьПечать(), ЗаполнитьНаОсновании()
- НайтиПоРеквизиту(), СоздатьЭлемент()
-  НЕ имеет контекста конкретного объекта
- Вызывается: Справочники.Номенклатура.Метод()

### Шаг 2: Модуль объекта

Бизнес-логика конкретного экземпляра. Обработчики событий объекта.

- ПередЗаписью(), ПриЗаписи(), ОбработкаЗаполнения()
- ОбработкаПроведения() — формирует движения
- this = текущий объект (ДокументОбъект)
- Хранит логику проверки и расчётов

### Шаг 3: Модуль формы

ТОЛЬКО UI-логика: открытие, закрытие, взаимодействие с элементами.

- &НаКлиенте и &НаСервере обработчики
- ПриСозданииНаСервере() — инициализация
- НЕ ставьте бизнес-логику сюда!
- События элементов: ПриИзменении, ПриВыборе

### Шаг 4: Общие модули

Переиспользуемые функции для всей конфигурации.

- Серверные: только сервер, любые объекты БД
- Разделяемые: клиент + сервер, ограничения!
- Глобальные: доступны без уточнения
- Правило: один модуль = одна ответственность

---

## diag-17: Эффективная массовая загрузка: правильный подход

*Категория: Типовые задачи*

Как загрузить 100 000 записей без блокировки базы на часы

### Шаг 1: Чтение исходных данных

Загружаем Excel/CSV в ТаблицуЗначений или Массив. НЕ читаем построчно!

- ТЗ = ЗначениеИзФайла('путь') или CSV чтение
- Всё в памяти — быстрый доступ
- Валидация данных ДО записи в БД
- Фильтруем дубли и некорректные строки

### Шаг 2: Очистка существующих данных

Очищаем регистры и справочники ОДНИМ набором, не по одной записи.

- НаборЗаписей = Регистр.СоздатьНаборЗаписей()
- НаборЗаписей.Записать() — одна операция!
- Справочники: пометка удаления + Записать()
- НЕ удаляйте по одной записи в цикле!

### Шаг 3: Загрузка пакетами

Записываем данные пакетами по 500-1000 записей.

- НачатьТранзакцию() перед пакетом
- Записываем 500 элементов
- ЗафиксироватьТранзакцию()
- Повторяем для следующего пакета

### Шаг 4: Финализация

Включаем регламентные задания, обновляем статистику СУБД.

- Анализ().ОбновитьСтатистику() в PostgreSQL
- Включаем регламентные задания обратно
- Проверяем количество загруженных записей
- Логируем результат в ЖурналРегистрации

---

## diag-18: Расширения: безопасная модификация типовой конфигурации

*Категория: Паттерны*

Как расширения позволяют кастомизировать без изменения основного кода

### Шаг 1: Создание расширения

Добавляем расширение в конфигурацию. Выбираем, что расширяем.

- Конфигуратор: Расширения → Добавить
- Привязка к конфигурации (по имени или по составу)
- Можно: формы, реквизиты, табличные части
- Можно: подписки, обработчики событий

### Шаг 2: Модификация форм

Расширяем форму типового объекта: добавляем элементы, обработчики.

- Добавляем реквизиты и элементы на форму
- Создаём обработчики &НаКлиенте
- Переопределяем существующие процедуры
- Можно скрывать стандартные элементы

### Шаг 3: Расширение модулей

Подключаем свои обработчики к событиям типовых модулей.

- &ИзменениеИКонтроль() — внедрение в метод
- &Вместо() — полная замена метода
- &Перед() / &После() — до и после метода
- Безопасно при обновлении типовой!

### Шаг 4: Управление и доставка

Расширения можно включать/выключать без остановки работы.

- Включить/Отключить в меню поддержки
- Несколько расширений одновременно
- Обновление типовой не затрагивает расширения
- cf-файлы расширений хранятся отдельно

---

## diag-31: Полная схема: ЭДО → HTTP → 1С → Регистры

*Категория: Интеграция Магнит*

Как данные сметы проходят от ЭДО до регистров 1С

### Шаг 1: ЭДО: Создание сметы

Пользователь создаёт смету в системе ЭДО. Заполняет статьи, суммы, подразделение. Смета проходит внутреннее согласование.

- Смета в ЭДО — первичный документ
- Содержит: externalId, дата, организация, подразделение, статьи, суммы
- Проходит workflow согласования в ЭДО

### Шаг 2: ЭДО: Согласовано → POST JSON

После согласования ЭДО отправляет HTTP POST на /hs/MagnitExpenseAPI/expense с JSON-телом

- Метод: POST
- Content-Type: application/json
- Заголовки: Authorization Basic (если нужен)
- Тело: {externalId, date, organization, department, items, totalAmount}

### Шаг 3: 1С: HTTP-сервис — приёмка

HTTP-сервис получает запрос. ParseJSON(). Проверка обязательных полей. Проверка идемпотентности через РС.

- ParseJSON() — разбор тела
- Проверка: externalId, date, organization есть?
- РС.СопоставлениеДокументовЭДО — есть ли уже?
- 400 если JSON невалиден, 200 если дубль

### Шаг 4: 1С: Создание документа

Модуль менеджера создаёт ЗаявкаНаРасход. Заполняет реквизиты из JSON. Записывает с проведением.

- Документы.ЗаявкаНаРасход.СоздатьИзJSON()
- Дата, Организация, Подразделение, Ответственный
- ТЧ Товары: СтатьяРасхода, Сумма
- Записать(РежимЗаписиДокумента.Проведение)

### Шаг 5: 1С: Движения по регистрам

ОбработкаПроведения формирует движения: РН.БюджетныеЛимиты (Расход), РН.ФактическиеРасходы (Приход)

- РН.БюджетныеЛимиты: Расход = -Сумма по каждой статье
- РН.ФактическиеРасходы: Приход = +Сумма
- Проверка: остаток лимита >= 0 (иначе Отказ)
- Все движения в одной неявной транзакции

### Шаг 6: 1С: РС + Ответ

Запись в РС.СопоставлениеДокументовЭДО. Возврат HTTP 201 Created с UUID документа.

- РС: externalId <-> Документ1С
- Ответ: 201 Created
- Тело: {id: 'UUID документа'}
- ЭДО получает UUID для дальнейшей работы

---

## diag-32: Структура регистров для ЗаявкиНаРасход

*Категория: Регистры*

Какие регистры нужны и какие движения формирует документ

### Шаг 1: РН.БюджетныеЛимиты (Остатки)

Контроль бюджетных лимитов. Приход = утверждение бюджета. Расход = проведение заявки.

- Измерения: Организация, Подразделение, СтатьяРасхода, Период
- Ресурс: Лимит (число)
- Приход: документ УтверждениеБюджета
- Расход: документ ЗаявкаНаРасход
- Виртуальная таблица: Остатки(&Дата)

### Шаг 2: РН.ФактическиеРасходы (Обороты)

Аналитика фактических расходов за период. Только Приход при проведении заявки.

- Измерения: Организация, Подразделение, СтатьяРасхода
- Ресурс: Сумма (число)
- Движение: Приход при проведении ЗаявкиНаРасход
- Виртуальная таблица: Обороты(&Начало, &Конец)
- Аналитика по подразделениям и статьям за период

### Шаг 3: РС.СопоставлениеДокументовЭДО

Связка внешнего ID из ЭДО с документом 1С. Идемпотентность интеграции.

- Измерение: ВнешнийИдентификатор (строка)
- Ресурс: Документ1С (Документ.ЗаявкаНаРасход)
- Подчинён регистратору
- Записывается после создания документа
- Используется для проверки дублей при повторе

### Шаг 4: РС.СостоянияСогласования

Статусы согласования сметы из ЭДО: Черновик, На согласовании, Согласовано, Отклонено.

- Измерение: ВнешнийИдентификатор
- Ресурс: Статус (Перечисление)
- Периодический, подчинён регистратору
- Обновляется через GET от ЭДО при изменении статуса
- Позволяет отслеживать статус без запроса в ЭДО

---

## diag-33: Жизненный цикл HTTP-запроса в 1С

*Категория: HTTP-сервис*

Что происходит когда ЭДО отправляет POST на HTTP-сервис 1С

### Шаг 1: Приём запроса

Сервер 1С получает HTTP POST. Платформа определяет HTTP-сервис и вызывает обработчик ExpensePost.

- URL: /hs/MagnitExpenseAPI/expense
- Платформа маршрутизирует запрос по URL
- Вызывается функция ExpensePost(Запрос)
- Запрос = HTTPСервисЗапрос

### Шаг 2: Получение тела JSON

Запрос.ПолучитьТелоКакСтроку() → Строка JSON. Json.Прочитать() → Объект.

- ТелоЗапроса = Запрос.ПолучитьТелоКакСтроку()
- Проверка Content-Type
- ParseJSON / ЧтениеJSON
- Результат = объект JSON (Структура)

### Шаг 3: Валидация данных

Проверка обязательных полей, типов данных, бизнес-правил.

- externalId не пустой?
- date корректная дата?
- items не пустой массив?
- Каждый item: article + amount?
- 400 Bad Request при ошибке

### Шаг 4: Бизнес-логика

Создание документа через модуль менеджера. Проведение. Запись РС.

- Документы.ЗаявкаНаРасход.СоздатьИзJSON()
- Заполнение реквизитов из JSON
- Записать(Проведение) = транзакция
- РС.СопоставлениеДокументовЭДО

### Шаг 5: Формирование ответа

HTTPСервисОтвет с кодом состояния и JSON-телом.

- 201 Created при успехе
- 200 OK при дубле (идемпотентность)
- 400/409/500 при ошибках
- Content-Type: application/json
- UUID документа в теле ответа

---

## diag-34: Жизненный цикл отчёта СКД: от схемы до экрана

*Категория: СКД*

Как СКД преобразует схему компоновки в видимый пользователю отчёт

### Шаг 1: СхемаКомпоновкиДанных

Исходник: наборы данных (запросы), поля, группировки, ресурсы, параметры, условия, условное оформление

- Содержит ВСЁ описание отчёта
- Запросы к регистрам и справочникам
- Иерархия группировок (ПоПодразделению -> ПоСтатее)
- Параметры для фильтрации

### Шаг 2: КомпоновщикМакета

Платформа компилирует СКД в макет на основе настроек пользователя

- Применяет пользовательские настройки (отбор, группировки)
- Генерирует программный код макета
- Определяет: какие поля выводить, как группировать
- Получает ТабличныйДокумент

### Шаг 3: ПроцессорКомпоновкиДанных

Выполняет запросы к БД, получает данные и формирует результат

- Выполняет запросы из наборов данных СКД
- Обрабатывает итоги и группировки
- Заполняет ресурсы и вычисляемые поля
- Работает на сервере — может быть тяжёлым

### Шаг 4: ПроцессорВыводаРезультата

Рендерит результат в ТабличныйДокумент или ДокументHTML

- Применяет условное оформление
- Формирует итоги и заголовки
- Выводит в табличный документ / диаграмму
- Пользователь видит готовый отчёт

---

## diag-35: Архитектура План/Факт в 1С: два регистра → один отчёт

*Категория: Бюджетирование*

Как план и факт из разных регистров объединяются в СКД

### Шаг 1: РН.БюджетныеЛимиты (Остатки)

Хранит утверждённый бюджет. Приход — при утверждении бюджета. Расход — при проведении заявки

- Тип: Остатки (баланс)
- Измерения: Статья, Подразделение, Период
- Ресурс: Лимит (сумма плана)
- Виртуальная таблица: Остатки(&Дата)

### Шаг 2: РН.ФактическиеРасходы (Обороты)

Хранит фактические расходы. Приход — при проведении ЗаявкиНаРасход

- Тип: Обороты (движения за период)
- Измерения: Статья, Подразделение
- Ресурс: Сумма (факт)
- Виртуальная таблица: Обороты(&Начало, &Конец)

### Шаг 3: СКД: объединение двух наборов данных

Два набора данных в СКД, связанных через общие поля Статья+Подразделение

- Набор 1: запрос к РН.БюджетныеЛимиты.Остатки
- Набор 2: запрос к РН.ФактическиеРасходы.Обороты
- Связь: СтатьяРасхода + Подразделение
- Вычисляемые поля: Отклонение, % выполнения

### Шаг 4: Отчёт: План/Факт/Отклонение

Готовый отчёт с условным оформлением: зелёный = экономия, красный = перерасход

- Колонки: Статья | План | Факт | Отклонение | %
- Условное оформление: красный если Факт > План
- Группировки: Подразделение → Статья
- Итоги по каждому подразделению

---

## diag-36: Многопакетный запрос: архитектура данных для СКД

*Категория: Сложные запросы*

Как запрос из 3-5 пакетов собирает данные для сложного отчёта

### Шаг 1: Пакет 1: Временная таблица (ВТ)

Загружаем исходные данные в ВТ_Движения из регистров

- ВЫБРАТЬ ... ПОМЕСТИТЬ ВТ_Движения
- Движения регистра за период
- Фильтр по организации, подразделению
- ВТ материализуется — быстрый доступ

### Шаг 2: Пакет 2: ВТ с аналитикой

Обогащаем ВТ_Движения данными из справочников

- ЛЕВОЕ СОЕДИНЕНИЕ с ВТ_Статьи, ВТ_Подразделения
- Добавляем наименования, коды
- Рассчитываем дополнительные поля
- ВЫБРАТЬ ... ПОМЕСТИТЬ ВТ_Обогащённые

### Шаг 3: Пакет 3: Итоговый запрос

Финальная выборка с группировками и итогами

- СГРУППИРОВАТЬ ПО Статья, Подразделение
- СУММА(Сумма), МАКСИМУМ(Дата)
- ОБЪЕДИНИТЬ ВСЕ если нужно несколько вариантов
- Сортировка для СКД

### Шаг 4: СКД использует запрос как источник

СКД берёт результат последнего пакета как данные для отчёта

- СКД выполняет запрос целиком
- Платформа управляет параметрами
- Пользовательские настройки применяются поверх
- Быстрый рендеринг благодаря ВТ

---

## diag-37: Паттерн 'Два указателя': подход к задачам на строках и массивах

*Категория: Алгоритмы*

Как работает классический алгоритм с двумя указателями на примере поиска палиндрома

### Шаг 1: Инициализация: два указателя

Левый = начало строки (индекс 1). Правый = конец строки (индекс Длина). Оба начинают с краёв.

- ЛевыйУказатель = 1
- ПравыйУказатель = СтрДлина(Стр)
- Двигаются навстречу друг другу
- O(1) дополнительной памяти

### Шаг 2: Сравнение символов

Сред(Стр, Левый, 1) vs Сред(Стр, Правый, 1). Если равны — двигаем оба указателя к центру.

- КодСимв(Стр, Левый) = КодСимв(Стр, Правый)?
- Если да: Левый++, Правый--
- Если нет: НЕ палиндром, выходим
- Каждое сравнение O(1)

### Шаг 3: Двигаем указатели

Левый++ и Правый--. Проверяем: Левый < Правый? Если да — продолжаем. Если нет — палиндром.

- ЛевыйУказатель = ЛевыйУказатель + 1
- ПравыйУказатель = ПравыйУказатель - 1
- Цикл пока Левый < Правый
- Если цикл завершился — все пары совпали

### Шаг 4: Результат

Если все пары символов совпали — строка палиндром. O(n) по времени, O(1) по памяти.

- Время: O(n/2) = O(n)
- Память: O(1) — только 2 переменных
- Паттерн: скользящее окно, два указателя
- Применение: палиндром, сумма двух чисел, разворот

---

## diag-38: Хэш-таблица: Соответствие как замена вложенных циклов

*Категория: Оптимизация*

Как Соответствие ускоряет поиск дубликатов с O(n^2) до O(n)

### Шаг 1: Проблема: вложенный цикл O(n^2)

10 000 элементов × 10 000 сравнений = 100 000 000 операций. Медленно!

- Для каждого элемента — поиск по всему массиву
- O(n^2) — квадратичная сложность
- На 10 000 элементах: ~10 секунд
- При 100 000: ~16 минут!

### Шаг 2: Решение: Соответствие как множество

Соответствие = хэш-таблица. Вставка и поиск по ключу = O(1) в среднем.

- Создаём пустое Соответствие
- Обходим массив один раз
- Каждый элемент — проверяем в ключах
- Есть ключ = дубль. Нет = добавляем

### Шаг 3: Итерация за O(n)

Один проход по массиву. Каждая проверка O(1). Итого O(n).

- 10 000 элементов = 10 000 проверок
- Каждая проверка мгновенная (хэш)
- Результат за миллисекунды
- O(n) время, O(n) память

### Шаг 4: Шаблон кода 1С

Хэш = Новый Соответствие; Для Каждого Эл Из Массив Цикл Если Хэш[Эл] <> Неопределено Тогда Дубли.Добавить(Эл) Иначе Хэш[Эл] = Истина КонецЕсли КонецЦикла

- Соответствие[Ключ] = Неопределено если ключа нет
- Вставка: Соответствие[Ключ] = Значение
- Проверка: Если Соответствие[Ключ] <> Неопределено
- Альтернатива: Хэш.Вставить(Ключ) не ругается на дубль

---

## diag-39: Жизненный цикл вызова расширения: &Вместо + ПродолжитьВызов

*Категория: Расширения*

Как платформа 1С выстраивает цепочку вызовов между расширением и типовым кодом

### Шаг 1: Событие возникает

Пользователь нажимает Записать → платформа генерирует событие ПередЗаписью


### Шаг 2: Расширение: &Перед

Если есть &Перед — выполняется ПЕРЕД типовым кодом


### Шаг 3: Расширение: &Вместо

Если есть &Вместо — вызывается ВМЕСТО типового обработчика


### Шаг 4: ПродолжитьВызов()

Если вызываешь ПродолжитьВызов() — выполняется типовой обработчик


### Шаг 5: Типовой обработчик

Платформа вызывает стандартный обработчик объекта


### Шаг 6: Расширение: &После

После типового кода выполняется &После обработчик


---

## diag-40: Архитектура расширения для ЭДО-интеграции в Магните

*Категория: Расширения*

Как расширение конфигурации инкапсулирует всю логику интеграции

### Шаг 1: HTTP-запрос от ЭДО

POST /edo/orders с JSON-телом заявки


### Шаг 2: HTTP-сервис расширения

POST обработчик в расширении: парсинг JSON, валидация


### Шаг 3: Поиск/создание объекта

Запрос к Справочнику.Номенклатура по артикулу, поиск контрагента


### Шаг 4: Создание документа

Документы.ЗаявкаНаРасход.СоздатьДокумент(). Заполнение реквизитов


### Шаг 5: Подписка: проверка

&Перед подписка на ПередЗаписью → проверка лимитов бюджета


### Шаг 6: Подписка: проведение

&После подписка → движения по РН, запись в СопоставлениеЭДО


---

## diag-41: Расширение vs типовая доработка: матрица решений

*Категория: Расширения*

Как выбрать между расширением и прямой модификацией конфигурации

### Шаг 1: Нужен новый реквизит объекта?

Доработка конфигурации! Расширение не может добавить реквизит к объекту


### Шаг 2: Нужна дополнительная проверка при проведении?

Расширение! Подписка + &Вместо + ПродолжитьВызов()


### Шаг 3: Нужен новый HTTP-сервис или отчёт?

Расширение! Создаёшь объекты внутри расширения


### Шаг 4: Нужна новая роль или изменение прав?

Доработка конфигурации! Расширение не управляет ролями


### Шаг 5: Нужна логика включения/выключения?

Расширение! Флаг включения = мгновенное включение/выключение


### Шаг 6: Нужно хранить доп. данные?

Расширение конфигурации: РС, Доп. реквизиты, СохраняемыеЗначения


---

## diag-42: Путь диагностики: от жалобы к оптимизации запроса

*Категория: Оптимизация*

Пошаговый процесс поиска и устранения узкого места в медленном запросе 1С

### Шаг 1: Получить жалобу

Пользователь: отчёт/API работает медленно (30 секунд)


### Шаг 2: Воспроизвести

Вызвать с теми же параметрами в отладчике


### Шаг 3: План запроса

Скопировать текст запроса в консоль → Показать план


### Шаг 4: Добавить индекс

Создать индекс по полям группировки + отборов


### Шаг 5: ВТ вместо вложенных

Заменить вложенные запросы на временные таблицы


### Шаг 6: Проверить результат

Запустить снова, сравнить время


---

## diag-43: Рефакторинг: от монолита к модулям

*Категория: Архитектура API*

Как разбить функцию 200+ строк на чистую архитектуру HTTP-сервиса

### Шаг 1: Было: монолит

Одна функция 200 строк: валидация + логика + маппинг + логирование


### Шаг 2: Шаг 1: извлечь валидацию

Модуль ВалидаторЗапросов: проверка JSON, обязательных полей, типов, допустимых значений


### Шаг 3: Шаг 2: извлечь бизнес-логику

Модуль СервисПрайсов: запрос к БД, вычисление цен, скидок, кредитов


### Шаг 4: Шаг 3: извлечь маппинг

Модуль МапперЦен: преобразование ТЗ → структура для JSON


### Шаг 5: Шаг 4: обработчик = маршрутизатор

HTTP-обработчик: 10-15 строк, только вызовы модулей


### Шаг 6: Результат: чистая архитектура

3 модуля + обработчик. Тестируемость, переиспользование, читаемость


---

# Все интерактивные коды (отдельный справочник)

## ic-1: Правильный вызов сервера из клиента

*Категория: Клиент-сервер*

Как правильно организовать клиент-серверное взаимодействие

```bsl
&НаКлиенте
Процедура СоздатьКонтрагента(Команда)
    // Подготавливаем данные на клиенте
    Имя = "ООО Ромашка";
    ИНН = "7712345678";

    // Вызываем сервер (ОДИН вызов!)
    Результат = СоздатьКонтрагентаНаСервере(Имя, ИНН);

    // Работаем с результатом на клиенте
    Сообщить("Создан: " + Результат.Ссылка);
КонецПроцедуры

&НаСервере
Функция СоздатьКонтрагентаНаСервере(Имя, ИНН)
    // Здесь МОЖНО создавать объекты!
    НовыйКонтрагент = Справочники.Контрагенты.СоздатьЭлемент();
    НовыйКонтрагент.Наименование = Имя;
    НовыйКонтрагент.ИНН = ИНН;
    НовыйКонтрагент.Записать();

    // Возвращаем ФиксированнуюСтруктуру
    Возврат Новый ФиксированнаяСтруктура("Ссылка", НовыйКонтрагент.Ссылка);
КонецФункции
```

**Пояснения:**

- **Строка 1:** &НаКлиенте — директива! Код работает на клиенте. Здесь нельзя создавать объекты 1С.
- **Строка 8:** Один серверный вызов вместо нескольких — паттерн. Минимизируйте round-trips.
- **Строка 14:** &НаСервере — код выполняется на сервере. Здесь можно ВСЁ.
- **Строка 17:** СоздатьЭлемент() — серверный вызов! Создаёт СправочникОбъект в памяти сервера.
- **Строка 20:** Записать() — физически записывает в БД. Только на сервере!
- **Строка 23:** ФиксированнаяСтруктура — можно передать на клиент! Обычную Структуру нельзя.

**Квиз:** Что будет, если убрать &НаСервере и оставить СоздатьЭлемент() в клиентской процедуре?

- ⬜ **A.** Будет ошибка компиляции
- ⬜ **B.** Код выполнится на клиенте (работает)
- ✅ **C.** Будет ошибка выполнения: 'Неверное использование серверного вызова'
- ⬜ **D.** Объект создастся локально на клиенте

*Объяснение: Правильно: Будет ошибка выполнения. Справочники.Контрагенты.СоздатьЭлемент() — серверный метод.*

---

## ic-2: Транзакция и переменные — ловушка!

*Категория: Транзакции*

Частая ошибка: переменные НЕ откатываются при ОтменитьТранзакцию()

```bsl
&НаСервере
Процедура ПримерТранзакции()
    НачатьТранзакцию();

    Документ = Документы.Приходная.НайтиПоНомеру(123);
    Документ.Комментарий = "Изменён";
    Документ.Записать();

    Статус = "Записано";

    Попытка
        // Какая-то операция, которая упадёт
        ВызватьИсключение("Ошибка!");
    Исключение
        ОтменитьТранзакцию();
    КонецПопытки;

    // Статус = "Записано", но в БД изменений НЕТ!
    // Переменная НЕ откатилась!
КонецПроцедуры
```

**Пояснения:**

- **Строка 3:** Начинаем транзакцию. Все последующие записи в БД будут в рамках одной транзакции.
- **Строка 7:** Документ записан в БД (в рамках транзакции). Если откатим — запись исчезнет.
- **Строка 9:** Локальная переменная Статус. Она НЕ откатится при ОтменитьТранзакцию()!
- **Строка 15:** ОтменитьТранзакцию() откатывает только записи в БД. Переменные в памяти НЕ меняются.
- **Строка 18:** КРИТИЧЕСКИЙ МОМЕНТ: Статус = 'Записано', но документ НЕ записан.

**Квиз:** Что содержит переменная Статус после ОтменитьТранзакцию()?

- ⬜ **A.** Пустая строка
- ✅ **B.** "Записано" — переменные НЕ откатываются
- ⬜ **C.** "Ошибка"
- ⬜ **D.** Будет ошибка

*Объяснение: Статус = 'Записано'. Переменные НЕ откатываются при откате транзакции.*

---

## ic-3: Управляемые блокировки — правильный паттерн

*Категория: Блокировки*

Как правильно ставить управляемые блокировки

```bsl
&НаСервере
Процедура ИзменитьКонтрагентаБезопасно(Ссылка, НовоеИмя)

    // 1. БЛОКИРУЕМ данные ПЕРЕД чтением
    Блокировка = Новый Массив;
    ЭлементБлокировки = Новый Массив;
    ЭлементБлокировки.Добавить(Ссылка);
    Блокировка.Добавить(ЭлементБлокировки);

    // Блокировка в КЛАСТЕРЕ 1С (не в СУБД!)
    ЗаблокироватьДанныеДляРедактирования(
        РегистрСведений.ЦеныНоменклатуры,,
        Блокировка);

    // 2. Читаем данные ПОСЛЕ блокировки
    Объект = Ссылка.ПолучитьОбъект();
    Объект.Наименование = НовоеИмя;

    // 3. Записываем — блокировка снимется автоматически
    Объект.Записать();

КонецПроцедуры
```

**Пояснения:**

- **Строка 4:** ПРАВИЛО: Блокировка -> Чтение -> Изменение -> Запись. Всегда в таком порядке!
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования ставит блокировку В КЛАСТЕРЕ 1С. Не в СУБД!
- **Строка 11:** Можно блокировать не только справочники, но и регистры, документы.
- **Строка 15:** Читаем данные ТОЛЬКО ПОСЛЕ блокировки. Иначе — потерянные обновления.
- **Строка 20:** При успешной записи транзакция фиксируется, блокировка снимается автоматически.

**Квиз:** Где регистрируется управляемая блокировка?

- ⬜ **A.** В SQL Server / PostgreSQL (в СУБД)
- ✅ **B.** В кластере серверов 1С
- ⬜ **C.** На клиенте приложения
- ⬜ **D.** В файловой базе

*Объяснение: Управляемая блокировка — механизм сервера 1С. Регистрируется в МЕНЕДЖЕРЕ БЛОКИРОВОК КЛАСТЕРА 1С.*

---

## ic-4: Запрос через индекс vs полный скан

*Категория: Запросы и индексы*

Видим разницу между эффективным и неэффективным запросом

```bsl
// БЫСТРО — есть индекс по Номенклатуре
Запрос = Новый Запрос;
Запрос.Текст =
    "ВЫБРАТЬ РегистрНакопления.Остатки"
    "ИЗ РегистрНакопления.Остатки КАК Остатки"
    "ГДЕ Остатки.Номенклатура = &Номенклатура";
Запрос.УстановитьПараметр("Номенклатура", Ссылка);
// -> СУБД использует Index Scan = O(log N)

// МЕДЛЕННО — нет индекса по Склад.Ответственный
Запрос2 = Новый Запрос;
Запрос2.Текст =
    "ВЫБРАТЬ * ИЗ Справочник.Склады КАК Склады"
    "ГДЕ Склады.Ответственный = &Ответственный";
// -> СУБД делает Seq Scan = читает ВСЮ таблицу
// -> 100 000 строк за 5 секунд!

// РЕШЕНИЕ: добавить индекс по Ответственный
// или использовать УСЛОВИЕ вместо ГДЕ
```

**Пояснения:**

- **Строка 6:** Номенклатура — это измерение регистра, по нему АВТОМАТИЧЕСКИ создан индекс. СУБД найдёт строку мгновенно.
- **Строка 14:** Ответственный — это РЕКВИЗИТ справочника. По реквизитам нет индексов по умолчанию!
- **Строка 15:** Seq Scan (Sequential Scan) — СУБД читает каждую строку и проверяет условие. Для 100К строк это медленно.
- **Строка 16:** Решение: добавить индекс по реквизиту или перепроектировать запрос.

**Квиз:** Почему запрос по полю Ответственный медленный?

- ⬜ **A.** Потому что поле текстовое
- ✅ **B.** Нет индекса по реквизиту — СУБД делает полный скан таблицы
- ⬜ **C.** Нужно использовать ВЫБРАТЬ РАЗЛИЧНЫЕ
- ⬜ **D.** Регистр не поддерживает поиск по реквизитам

*Объяснение: По реквизитам справочников нет автоматических индексов. СУБД сканирует всю таблицу. Решение: добавить индекс.*

---

## ic-5: Виртуальная таблица Остатки с УСЛОВИЕ

*Категория: Запросы и индексы*

Правильный запрос к виртуальной таблице — УСЛОВИЕ передаётся внутрь

```bsl
// ПРАВИЛЬНО — УСЛОВИЕ передаётся в виртуальную таблицу
ВЫБРАТЬ
    Остатки.Номенклатура КАК Номенклатура,
    Остатки.КоличествоОстаток КАК Остаток
ИЗ РегистрНакопления.Остатки КАК Остатки
ГДЕ Остатки.Номенклатура В ИЕРАРХИИ(&Группа)
    И Остатки.Склад = &Склад

// -> Платформа передаёт условия Склад в параметры
// -> СУБД фильтрует по индексу = O(log N)

// НЕПРАВИЛЬНО — фильтр через левое соединение
ВЫБРАТЬ Остатки.КоличествоОстаток
ИЗ РегистрНакопления.Остатки КАК Остатки
ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Номенклатура КАК Н
    ПО Остатки.Номенклатура = Н.Ссылка
ГДЕ Н.Статус = &Статус  // <- антипаттерн!
// -> СУБД читает ВСЕ остатки, потом фильтрует
```

**Пояснения:**

- **Строка 1:** УСЛОВИЕ — передаётся ПРЯМО в виртуальную таблицу как параметр. СУБД использует индексы.
- **Строка 6:** Условия ГДЕ по измерениям = параметры виртуальной таблицы. Платформа автоматически оптимизирует.
- **Строка 17:** АНТИПАТТЕРН: фильтрация по полям не измерений через JOIN. Виртуальная таблица вернёт ВСЕ данные.
- **Строка 18:** СУБД сначала получает все остатки (может быть миллионы строк), потом фильтрует. Очень медленно.

**Квиз:** Как правильно фильтровать виртуальную таблицу Остатков по Складу?

- ⬜ **A.** Через ГДЕ после выборки
- ✅ **B.** Через УСЛОВИЕ в параметрах виртуальной таблицы
- ⬜ **C.** Через LEFT JOIN со Справочником.Склады
- ⬜ **D.** Виртуальные таблицы не поддерживают фильтрацию

*Объяснение: УСЛОВИЕ в ГДЕ по измерениям автоматически передаётся в параметры виртуальной таблицы. СУБД использует индекс.*

---

## ic-6: Явная транзакция: правильная структура

*Категория: Транзакции*

Полный пример явной транзакции с блокировкой и обработкой ошибок

```bsl
&НаСервере
Процедура ПереместитьТоварНаСклад(ДокументСсылка, ЦелевойСклад)

    НачатьТранзакцию();
    Попытка

        // 1. Блокируем регистр остатков
        Блокировка = Новый Массив;
        Блокировка.Добавить(ДокументСсылка);
        ЗаблокироватьДанныеДляРедактирования(
            РегистрНакопления.ТоварыНаСкладах,, Блокировка);

        // 2. Проверяем остатки (после блокировки!)
        Запрос = Новый Запрос;
        Запрос.Текст = "ВЫБРАТЬ КоличествоОстаток
            |ИЗ РегистрНакопления.ТоварыНаСкладах.Остатки
            |ГДЕ Склад = &Склад";

        // 3. Создаём движения
        Движение = Движения.ТоварыНаСкладах.Добавить();
        Движение.ВидДвижения = ВидДвиженияНакопления.Расход;
        Движение.Склад = ЦелевойСклад;
        Движения.Записать();

        ЗафиксироватьТранзакцию();

    Исключение
        ОтменитьТранзакцию();
        ЗаписьЖурналаРегистрации("Ошибка перемещения: " + ОписаниеОшибки());
    КонецПопытки;

КонецПроцедуры
```

**Пояснения:**

- **Строка 4:** НачатьТранзакцию() — открываем явную транзакцию. Все записи БД будут атомарными.
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования — управляемая блокировка в КЛАСТЕРЕ 1С, не в СУБД.
- **Строка 13:** Проверяем остатки ПОСЛЕ блокировки — гарантируем консистентность данных.
- **Строка 20:** Движения.Добавить() — создаём запись движения в регистре накопления.
- **Строка 25:** ЗафиксироватьТранзакцию() — COMMIT. Все изменения в БД становятся постоянными.
- **Строка 28:** При ошибке — откатываем транзакцию. БД возвращается к состоянию ДО НачатьТранзакции().

**Квиз:** Что будет, если между строками 10 и 20 другой пользователь изменит остатки?

- ⬜ **A.** Данные будут противоречивыми
- ✅ **B.** Он получит ошибку таймаута — мы заблокировали данные
- ⬜ **C.** Его изменения пройдут, наши откатятся
- ⬜ **D.** Оба успешно запишут — управляемые блокировки не мешают

*Объяснение: Управляемая блокировка не позволит ему редактировать те же данные. Он будет ждать или получит ошибку.*

---

## ic-7: Предотвращение deadlock: порядок блокировки

*Категория: Блокировки*

Как избежать взаимной блокировки — единый порядок блокировки ресурсов

```bsl
// DEADLOCK: разный порядок блокировки!
// Сессия А: Блокирует Контрагента, потом Склад
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);

// Сессия Б: Блокирует Склад, потом Контрагента
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);
// -> DEADLOCK! А ждёт Б, Б ждёт А

// РЕШЕНИЕ: единый порядок блокировки
// ПРАВИЛО: Сначала Склады, потом Контрагенты

// Сессия А:
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
ЗаблокироватьДанныеДляРедактирования(Справочники.Контрагенты, , [А]);

// Сессия Б:
ЗаблокироватьДанныеДляРедактирования(Справочники.Склады, , [Б]);
// -> Б ждёт, пока А не освободит Склады. Нет deadlock!
```

**Пояснения:**

- **Строка 2:** АНТИПАТТЕРН: каждый блокирует в своём порядке — прямой путь к deadlock.
- **Строка 4:** А заблокировал Контрагента, ждёт Склад — который уже заблокирован Б.
- **Строка 7:** Б заблокировал Склад, ждёт Контрагента — который уже заблокирован А. Классический deadlock.
- **Строка 12:** РЕШЕНИЕ: договоритесь о едином порядке блокировки ресурсов в команде!
- **Строка 20:** При одинаковом порядке: один из них будет ждать, но deadlock не возникнет.

**Квиз:** Как предотвратить взаимную блокировку между двумя сеансами?

- ⬜ **A.** Увеличить таймаут ожидания
- ✅ **B.** Использовать один и тот же порядок блокировки ресурсов
- ⬜ **C.** Переключиться на автоматический режим блокировок
- ⬜ **D.** Отключить управляемые блокировки

*Объяснение: Единый порядок блокировки — главный метод предотвращения deadlock. Все сеансы блокируют ресурсы в одном порядке.*

---

## ic-8: Завершение зависших сеансов: серверный код

*Категория: Кластер*

Как найти и безопасно завершить зависшие сеансы через код

```bsl
&НаСервере
Процедура ЗавершитьЗависшиеСеансы(МаксВремяМинут = 30)

    // 1. Получаем все активные сеансы
    Сеансы = ПолучитьСеансыИнформационнойБазы();
    Завершено = 0;

    Для Каждого Сеанс Из Сеансы Цикл
        Длительность = ТекущаяДата() - Сеанс.НачалоСеанса;

        Если Длительность > МаксВремяМинут * 60 Тогда
            Попытка
                // Отключаем от БД ПЕРВЫМ
                Сеанс.Завершить(Ложь); // отключить, не завершить
                Завершено = Завершено + 1;
            Исключение
                ЗаписьЖурналаРегистрации("Ошибка завершения: " + ОписаниеОшибки());
            КонецПопытки;
        КонецЕсли;
    КонецЦикла;

    Сообщить("Завершено сеансов: " + Завершено);
КонецПроцедуры
```

**Пояснения:**

- **Строка 5:** ПолучитьСеансыИнформационнойБазы() возвращает массив всех активных сеансов.
- **Строка 11:** Фильтруем по длительности — зависшие сеансы висят >30 минут бездействия.
- **Строка 14:** Сеанс.Завершить(Ложь) — отключает сеанс от БД, но не завершает процесс. Это безопаснее.
- **Строка 17:** Обязательно обрабатываем исключение — другие процессы могут зависеть от этого сеанса.

**Квиз:** Что делает Сеанс.Завершить(Ложь)?

- ⬜ **A.** Убивает процесс без вопросов
- ✅ **B.** Отключает сеанс от БД, но не завершает рабочий процесс
- ⬜ **C.** Блокирует сеанс на 5 минут
- ⬜ **D.** Ничего — параметр игнорируется

*Объяснение: Сеанс.Завершить(Истина) — полный kill. Сеанс.Завершить(Ложь) — только отключение от БД. Безопаснее для продакшена.*

---

## ic-9: RLS: настройка и влияние на запросы

*Категория: Безопасность*

Как Record Level Security работает внутри запросов

```bsl
// В конфигураторе: Справочник.Контрагенты
// Свойство: Ограничение доступа на уровне записей
// Тип: По параметру сеанса (Организация)

// Что происходит в запросе (автоматически):
ВЫБРАТЬ
    Контрагенты.Ссылка КАК Ссылка,
    Контрагенты.Наименование КАК Наименование
ИЗ Справочник.Контрагенты КАК Контрагенты
ГДЕ Контрагенты.Владелец В (&ДоступныеОрганизации)

// Платформа ДОБАВИЛА условие ГДЕ автоматически!
// &ДоступныеОрганизации — параметр сеанса RLS
// Устанавливается при старте сеанса

// Установка параметров сеанса:
УстановитьПараметрыСеанса(
    "ДоступныеОрганизации", МассивОрганизаций);
```

**Пояснения:**

- **Строка 3:** Разделение по параметру сеанса — самый простой и эффективный вид RLS.
- **Строка 10:** Платформа автоматически добавляет ГДЕ к КАЖДОМУ запросу к этому объекту!
- **Строка 12:** Это происходит прозрачно — разработчик не пишет это условие вручную.
- **Строка 18:** УстановитьПараметрыСеанса вызывается при старте приложения (например, в МодулеСеанса).

**Квиз:** Как RLS влияет на производительность запросов?

- ⬜ **A.** Никак не влияет
- ✅ **B.** Добавляет условие WHERE к каждому запросу — может замедлить
- ⬜ **C.** Только на вставку данных
- ⬜ **D.** Пересоздаёт индексы при каждом запросе

*Объяснение: RLS добавляет WHERE ко всем запросам. Без индекса по полю фильтрации — полный скан таблицы!*

---

## ic-10: Исключительная vs разделяемая: когда что ставить?

*Категория: Глубокие блокировки*

Как правильно выбирать режим управляемой блокировки

```bsl
&НаСервере
Процедура ПеремещениеОстатков(Склад, Номенклатура)
    // НУЖНО изменять остатки = исключительная!
    Блокировка = Новый Массив;
    ЭлементБлокировки = Новый Массив;
    ЭлементБлокировки.Добавить(Склад);
    Блокировка.Добавить(ЭлементБлокировки);

    // РежимБлокировкиДанных.ИсключительныйДляИзменения
    ЗаблокироватьДанныеДляРедактирования(
        РегистрНакопления.ОстаткиТоваров,
        РежимБлокировкиДанных.ИсключительныйДляИзменения,,
        Блокировка);

    // Теперь читаем и записываем остатки
    Набор = РегистрыНакопления.ОстаткиТоваров.СоздатьНаборЗаписей();
    Набор.Отбор.Склад.Установить(Склад);
    Для Каждого Запись Из Набор Цикл
        Запись.Количество = Запись.Количество - 1;
    КонецЦикла;
    Набор.Записать();
КонецПроцедуры

// Разделяемая = когда только ЧИТАЕМ данные
// Исключительная = когда ИЗМЕНЯЕМ данные
// Платформа сама ставит разделяемую при чтении наборов
```

**Пояснения:**

- **Строка 3:** Если планируешь ИЗМЕНЯТЬ остатки — нужна исключительная блокировка! Разделяемая не защитит от одновременного изменения.
- **Строка 9:** ИсключительныйДляИзменения — запрещает другим и читать, и писать. Гарантирует, что никто не изменит данные пока мы работаем.
- **Строка 10:** ЗаблокироватьДанныеДляРедактирования — вызов в кластере 1С, не в СУБД. Кластер проверяет конфликты.
- **Строка 12:** ИсключительныйДляИзменения — правильный режим при записи движений регистра накопления.
- **Строка 24:** Разделяемая блокировка: разрешает другим читать, запрещает писать. Платформа ставит её НЕЯВНО при чтении наборов записей.

**Квиз:** Какой режим блокировки выбрать, если нужно ТОЛЬКО прочитать остатки для отчёта?

- ⬜ **A.** ИсключительныйДляИзменения — надёжнее
- ✅ **B.** Разделяемый — достаточно для чтения
- ⬜ **C.** Ничего — платформа сама разберётся
- ⬜ **D.** РежимБлокировкиДанных.Автоматический

*Объяснение: Для чтения достаточно разделяемой блокировки (платформа ставит её неявно). Исключительная нужна только при ИЗМЕНЕнии.*

---

## ic-11: CAP-теорема: что происходит при потере сети

*Категория: Инфраструктура*

Как 1С-кластер ведёт себя при разделении сети

```bsl
// CAP-теорема: из 3 свойств можно гарантировать только 2
// C = Consistency  (согласованность)
// A = Availability   (доступность)
// P = Partition     (устойчивость к разделению)

// СЦЕНАРИЙ: Потеря сети между серверами 1С

// Вариант CP (Consistency + Partition):
// Сервер Б отказывает отвечать, т.к. не может
// синхронизировать данные с Сервером А
// Результат: пользователи видят ошибку соединения

// Вариант AP (Availability + Partition):
// Сервер Б отвечает СТАРЫМИ данными из кэша
// Результат: пользователи работают, но данные
// могут быть НЕактуальными

// 1С выбирает CP: согласованность важнее доступности
// Данные всегда правильные, но может быть простои
```

**Пояснения:**

- **Строка 1:** CAP-теорема (Brewer, 2000): распределённая система не может гарантировать все 3 свойства одновременно при потере сети.
- **Строка 6:** Разделение сети (P) — неизбежно в реальных системах. Поэтому выбор всегда между C и A.
- **Строка 9:** CP-система: лучше unavailable, чем inconsistent. Пример: 1С, PostgreSQL.
- **Строка 14:** AP-система: лучше stale data, чем downtime. Пример: Cassandra, MongoDB.
- **Строка 18:** 1С-кластер выбирает CP. При разделении сети часть серверов перестаёт отвечать, но данные всегда согласованные.

**Квиз:** Что выбирает 1С при потере сети между серверами кластера?

- ⬜ **A.** Отвечает старыми данными (AP)
- ✅ **B.** Перестаёт отвечать до восстановления сети (CP)
- ⬜ **C.** Автоматически переключается на другой кластер
- ⬜ **D.** Перезапускает все рабочие процессы

*Объяснение: 1С — CP-система. При разделении сети: согласованность важнее доступности. Серверы перестают отвечать, пока сеть не восстановится.*

---

## ic-12: Передача ТаблицыЗначений через временное хранилище

*Категория: Временное хранилище*

Как правильно сериализовать и передать ТаблицуЗначений между формами

```bsl
&НаСервере
Функция ПодготовитьДанныеДляДругойФормы(ТаблицаДанных)
    // 1. Сериализуем ТаблицуЗначений
    ДвоичныеДанные = ЗначениеВДвоичныеДанные(ТаблицаДанных);

    // 2. Помещаем во временное хранилище
    Адрес = ПоместитьВоВременноеХранилище(ДвоичныеДанные);

    // 3. Возвращаем АДРЕС (строку!) клиенту
    Возврат Адрес;
КонецФункции

&НаСервере
Функция ПрочитатьДанныеИзХранилища(Адрес)
    // 4. Извлекаем из хранилища
    ДвоичныеДанные = ПолучитьИзВоВременногоХранилища(Адрес);

    // 5. Десериализуем обратно
    ТаблицаДанных = ЗначениеИзДвоичныхДанных(ДвоичныеДанные);

    // 6. ОБЯЗАТЕЛЬНО удаляем!
    УдалитьИзВоВременногоХранилища(Адрес);

    Возврат ТаблицаДанных;
КонецФункции
```

**Пояснения:**

- **Строка 4:** ЗначениеВДвоичныеДанные() — сериализует ЛЮБОЙ сериализуемый объект в двоичные данные. Работает с ТаблицамиЗначений, Массивами, Структурами.
- **Строка 7:** ПоместитьВоВременноеХранилище() — сохраняет данные на сервере. Возвращает уникальный Адрес (строка).
- **Строка 10:** Адрес — это СТРОКА, её можно передать на клиент и в другую форму! Сама ТаблицуЗначений на клиент не передаётся.
- **Строка 16:** ПолучитьИзВоВременногоХранилища() — извлекает данные по адресу. Возвращает ДвоичныеДанные.
- **Строка 21:** КРИТИЧЕСКИ ВАЖНО:如果不 удалить, данные будут висеть в памяти сервера до таймаута. Утечка памяти!

**Квиз:** Почему нельзя напрямую передать ТаблицуЗначений на клиент?

- ⬜ **A.** Потому что она слишком большая
- ✅ **B.** Потому что ТаблицуЗначений — серверный объект, не сериализуется автоматически
- ⬜ **C.** Потому что платформа не поддерживает таблицы
- ⬜ **D.** Можно, просто используй(Return)

*Объяснение: ТаблицуЗначений — серверный контекст. Для передачи нужно: сериализовать (ЗначениеВДвоичныеДанные), поместить в хранилище, передать адрес, десериализовать.*

---

## ic-13: HTTP-сервис: обработка GET-запроса

*Категория: Web/HTTP*

Пример GET-обработчика HTTP-сервиса в 1С

```bsl
Функция ЗаказыGET(Запрос)
    // Получаем ID из URL шаблона: /orders/{id}
    OrderID = Запрос.ПараметрыЗапроса.Получить('id');

    // Ищем документ
    Документ = Документы.Заказ.НайтиПоРеквизиту(
        'UUID', OrderID);

    Если Документ.Пустая() Тогда
        // Возвращаем 404 Not Found
        Ответ = Новый HTTPСервисОтвет(404);
        Ответ.УстановитьТелоСтрокой('Not Found');
        Возврат Ответ;
    КонецЕсли;

    // Формируем JSON ответ
    JSON = Новый ЗаписьJSON;
    JSON.Значение.Вставить('id', Документ.Ссылка.УникальныйИдентификатор());
    JSON.Значение.Вставить('number', Документ.Номер);
    JSON.Значение.Вставить('total', Документ.Сумма);

    // Возвращаем 200 OK с JSON
    Ответ = Новый HTTPСервисОтвет(200);
    Ответ.УстановитьТелоСтрокой(ЗаписьJSON.ВСтроку());
    Возврат Ответ;
```

**Пояснения:**

- **Строка 3:** ПараметрыЗапроса.Получить('id') — извлекает параметр из URL шаблона /orders/{id}. Имя совпадает с шаблоном.
- **Строка 11:** HTTPСервисОтвет(404) — создаём ответ со статусом 404. Правильный REST: если ресурс не найден — 404, не 200 с пустым телом.
- **Строка 18:** Ссылка.УникальныйИдентификатор() — UUID документа. Используем для внешних идентификаторов, а не внутренний номер.
- **Строка 23:** HTTPСервисОтвет(200) — статус 200 OK. В REST: GET успешен = 200, создание = 201, удаление = 204.

**Квиз:** Какой HTTP-статус нужно вернуть, если ресурс не найден?

- ⬜ **A.** 200 OK с пустым телом
- ⬜ **B.** 400 Bad Request
- ✅ **C.** 404 Not Found
- ⬜ **D.** 500 Internal Server Error

*Объяснение: 404 Not Found — стандартный REST ответ для несуществующего ресурса. 200 = успех (ресурс есть), 400 = ошибка в запросе клиента.*

---

## ic-14: Печать документа: макеты и области

*Категория: Типовые задачи*

Как организовать печать через ТабличныйДокумент и области макета

```bsl
&НаСервере
Функция ПечатьДокумента(ДокументСсылка)

    // 1. Получаем макет из менеджера
    Макет = Документы.Заказ.ПолучитьМакет('ПечатьЗаказа');
    ТД = Новый ТабличныйДокумент;

    // 2. Получаем области макета
    Заголовок = Макет.ПолучитьОбласть('Заголовок');
    Заголовок.Параметры.Номер = ДокументСсылка.Номер;
    Заголовок.Параметры.Дата = ДокументСсылка.Дата;
    ТД.Вывести(Заголовок);

    // 3. Выводим строки табличной части
    ОбластьСтрока = Макет.ПолучитьОбласть('Строка');
    НомерСтроки = 0;
    Для Каждого СтрокаТЧ Из ДокументСсылка.Товары Цикл
        НомерСтроки = НомерСтроки + 1;
        ОбластьСтрока.Параметры.Заполнить(СтрокаТЧ);
        ТД.Вывести(ОбластьСтрока);
    КонецЦикла;

    // 4. Подвал с итогами
    Подвал = Макет.ПолучитьОбласть('Подвал');
    Подвал.Параметры.Итого = ДокументСсылка.СуммаДокумента;
    ТД.Вывести(Подвал);

    Возврат ТД;
КонецФункции
```

**Пояснения:**

- **Строка 5:** ПолучитьМакет() возвращает ТабличныйДокумент-шаблон. Имя макета задаётся в конфигураторе.
- **Строка 9:** ПолучитьОбласть('Имя') — извлекает именованную область из макета. Области задаются в макете.
- **Строка 15:** Получаем область шаблона ОДИН раз, потом заполняем параметры в цикле. Это эффективнее!
- **Строка 19:** Заполнить() автоматически заполняет параметры области по совпадению имён с свойствами объекта.

**Квиз:** Что возвращает ПолучитьОбласть() из макета?

- ⬜ **A.** ТабличныйДокумент (копию макета)
- ✅ **B.** ОбластьЯчеекТабличногоДокумента (шаблон строки)
- ⬜ **C.** Строку с именем области
- ⬜ **D.** Массив ячеек

*Объяснение: ПолучитьОбласть() возвращает ОбластьЯчеекТабличногоДокумента — это шаблон, в который можно подставить параметры.*

---

## ic-15: Расширение: добавление обработчика в типовую форму

*Категория: Паттерны*

Как через расширение добавить свою логику в типовую конфигурацию

```bsl
// Расширение: модуль формы документа
&НаКлиенте
Процедура Расш1_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)

    // &Перед — наш код выполнится ДО типового
    // СтандартнаяОбработка = Ложь; // чтобы отменить типовой

    // Добавляем свой элемент на форму
    Элемент = Форма.Элементы.Добавить('ДопПоле', Тип('ПолеФормы'));
    Элемент.Вид = ВидПоляФормы.ПолеВвода;
    Элемент.Заголовок = 'Дополнительный комментарий';
    Элемент.ПутьКДанным = 'Объект.КомментарийРасш1';

КонецПроцедуры

// &Вместо — ПОЛНАЯ замена типового метода
Процедура Расш1_Записать(Отказ, ПараметрыЗаписи)
    // Наш код вместо типового
    Объект.КомментарийРасш1 = 'Из расширения';

    // ПродолжитьВызов() — вызовет типовой метод
    ПродолжитьВызов(Отказ, ПараметрыЗаписи);
КонецПроцедуры
```

**Пояснения:**

- **Строка 3:** Расш1_ — префикс расширения. Платформа связывает метод по префиксу и имени.
- **Строка 5:** &Перед = выполнить ДО типового. Можно подготовить данные или отменить (СтандартнаяОбработка = Ложь).
- **Строка 9:** Элементы.Добавить() — программное создание элемента формы в расширении.
- **Строка 12:** ПутьКДанным связывает элемент с реквизитом (который добавлен через расширение конфигурации).
- **Строка 16:** &Вместо = полная замена типового метода. Но можно вызвать ПродолжитьВызов() для оригинального.
- **Строка 22:** ПродолжитьВызов() — вызывает типовой метод, который мы заменили. Позволяет добавить логику ДО и ПОСЛЕ.

**Квиз:** Что делает ПродолжитьВызов() в расширении?

- ⬜ **A.** Завершает текущий метод
- ✅ **B.** Вызывает типовой метод, который был заменён через &Вместо
- ⬜ **C.** Создаёт новый фоновый процесс
- ⬜ **D.** Перезапускает форму

*Объяснение: ПродолжитьВызов() вызывает оригинальный типовой метод. Используется с &Вместо для добавления логики до/после типового кода.*

---

## ic-27: Программный вывод отчёта СКД на форму

*Категория: СКД*

Как сгенерировать и вывести СКД-отчёт в табличный документ на форме

```bsl
&НаСервере
Процедура СформироватьОтчётНаСервере()
    // 1. Загружаем схему СКД из макета
    СхемаСКД = ПолучитьМакет('ОсновнаяСхемаКомпоновкиДанных');
    Компоновщик = Новый КомпоновщикМакетаКомпоновкиДанных;
    Настройки = Компоновщик.Настройки;

    // 2. Устанавливаем параметры периода
    Настройки.ПараметрыДанных.Элементы.Найти('ПериодНачало').Значение = НачалоМесяца(ТекущаяДата());
    Настройки.ПараметрыДанных.Элементы.Найти('ПериодКонец').Значение = КонецМесяца(ТекущаяДата());
    Настройки.ПараметрыДанных.Элементы.Найти('Организация').Значение = Объект.Организация;

    // 3. Применяем пользовательский отбор
    Если ЗначениеЗаполнено(Объект.Подразделение) Тогда
        ОтборСтатья = Настройки.Отбор.Элементы.Добавить(Тип('ЭлементОтбораКомпоновкиДанных'));
        ОтборСтатья.ВидСравнения = ВидСравнения.Равно;
        ОтборСтатья.ЛевоеЗначение = Новый ПолеКомпоновкиДанных('Подразделение');
        ОтборСтатья.ПравоеЗначение = Объект.Подразделение;
    КонецЕсли;

    // 4. Формируем макет
    МакетКомпоновки = Компоновщик.Выполнить(СхемаСКД, Настройки);
    Процессор = Новый ПроцессорКомпоновкиДанных;
    Процессор.Инициализировать(МакетКомпоновки);

    // 5. Вывод в ТабличныйДокумент
    ДокументРезультат = Новый ТабличныйДокумент;
    ПроцессорВывода = Новый ПроцессорВыводаРезультатаКомпоновкиДанныхВТабличныйДокумент;
    ПроцессорВывода.УстановитьДокумент(ДокументРезультат);
    ПроцессорВывода.Вывести();

    ЗначениеВРеквизитФормы(ДокументРезультат, 'Результат');
КонецПроцедуры
```

**Пояснения:**

- **Строка 1:** &НаСервере — ВСЯ работа с СКД выполняется ТОЛЬКО на сервере. ТабличныйДокумент — серверный объект.
- **Строка 4:** Схема компоновки обычно хранится в макете конфигурации (в обработке или отчёте).
- **Строка 9:** Параметры — критично! Если не заполнить &ПериодНачало/Конец, виртуальные таблицы вернут пустой результат.
- **Строка 15:** Программный отбор = аналог пользовательского отбора. Добавляется в коллекцию Настройки.Отбор.
- **Строка 22:** Компоновщик.Выполнить() — ключевой метод. Генерирует макет, учитывая настройки и параметры.
- **Строка 27:** ТабличныйДокумент = результат. Через ЗначениеВРеквизитФормы передаётся на форму для отображения.
- **Строка 32:** ЗначениеВРеквизитФормы() — передаёт серверный объект в реквизит формы для отображения на клиенте.

**Квиз:** Где формируется макет СКД и где выводится результат?

- ⬜ **A.** Всё на клиенте — СКД работает на клиенте
- ✅ **B.** Формирование на сервере, вывод тоже на сервере, передача на форму через ЗначениеВРеквизитФормы
- ⬜ **C.** Формирование макета на клиенте, вывод на сервере
- ⬜ **D.** Всё в фоновом задании, результат в временном хранилище

*Объяснение: Всё на сервере! СхемаКомпоновкиДанных, Компоновщик, Процессор — серверные объекты. ТабличныйДокумент тоже серверный. На клиент передаётся готовый для отображения объект через ЗначениеВРеквизитФормы().*

---

## ic-28: Многопакетный запрос: План/Факт с ВТ

*Категория: Сложные запросы*

Как написать запрос из 3 пакетов для объединения плана и факта в одном отчёте

```bsl
ВЫБРАТЬ
    БюджетныеЛимитыОстатки.Подразделение КАК Подразделение,
    БюджетныеЛимитыОстатки.СтатьяРасхода КАК СтатьяРасхода,
    БюджетныеЛимитыОстатки.ЛимитОстаток КАК СуммаПлан
ПОМЕСТИТЬ ВТ_План
ИЗ РегистрНакопления.БюджетныеЛимиты.Остатки(
    &КонецПериода, ,
    Подразделение В (&МассивПодразделений),
    Организация = &Организация) КАК БюджетныеЛимитыОстатки;

ВЫБРАТЬ
    ФактОбороты.Подразделение КАК Подразделение,
    ФактОбороты.СтатьяРасхода КАК СтатьяРасхода,
    СУММА(ФактОбороты.СуммаОборот) КАК СуммаФакт
ПОМЕСТИТЬ ВТ_Факт
ИЗ РегистрНакопления.ФактическиеРасходы.Обороты(
    &НачалоПериода, &КонецПериода, 
    Подразделение В (&МассивПодразделений),
    Организация = &Организация) КАК ФактОбороты
СГРУППИРОВАТЬ ПО
    ФактОбороты.Подразделение,
    ФактОбороты.СтатьяРасхода;

ВЫБРАТЬ
    ВТ_План.Подразделение,
    ВТ_План.СтатьяРасхода,
    ВТ_План.СуммаПлан,
    ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) КАК СуммаФакт,
    ВТ_План.СуммаПлан - ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) КАК Отклонение,
    ВЫРАЗИТЬ(ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0)
        / ВТ_План.СуммаПлан * 100 КАК ПроцентВыполнения)
ИЗ ВТ_План
    ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Факт
    ПО ВТ_План.Подразделение = ВТ_Факт.Подразделение
    И ВТ_План.СтатьяРасхода = ВТ_Факт.СтатьяРасхода
УПОРЯДОЧИТЬ ПО Подразделение, СтатьяРасхода;
```

**Пояснения:**

- **Строка 5:** ПОМЕСТИТЬ ВТ_План — создаёт временную таблицу. Материализуется один раз — быстрый доступ для последующих пакетов.
- **Строка 6:** Остатки(&КонецПериода, ) — виртуальная таблица регистра Остатков. Пустой второй параметр = без отбора по измерениям.
- **Строка 14:** СУММА() — агрегатная функция. В регистре Обороты ресурс уже суммирован, но СГРУППИРОВАТЬ ПО требует явного SUM().
- **Строка 28:** ЕСТЬNULL(ВТ_Факт.СуммаФакт, 0) — LEFT JOIN может вернуть NULL для статей без расходов. Заменяем на 0.
- **Строка 29:** Отклонение = План - Факт. Отрицательное = перерасход, положительное = экономия.
- **Строка 33:** ЛЕВОЕ СОЕДИНЕНИЕ — берём ВСЕ статьи из плана, даже если по ним не было факта. Важно для полноты картины.

**Квиз:** Почему пакет 3 использует ЛЕВОЕ СОЕДИНЕНИЕ, а не ВНУТРЕННЕЕ?

- ⬜ **A.** ВНУТРЕННЕЕ быстрее — СУБД оптимизирует лучше
- ✅ **B.** ЛЕВОЕ нужно чтобы показать ВСЕ плановые статьи, даже без факта
- ⬜ **C.** Без разницы, можно использовать любое
- ⬜ **D.** ЛЕВОЕ СОЕДИНЕНИЕ работает только с ВТ

*Объяснение: ЛЕВОЕ СОЕДИНЕНИЕ сохраняет ВСЕ записи из ВТ_План. Если по статье бюджета не было расходов — СуммаФакт = NULL (заменяю на 0 через ЕСТЬNULL). ВНУТРЕННЕЕ потеряло бы статьи без факта — пользователь не увидел бы их в отчёте.*

---

## ic-29: Два указателя: проверка палиндрома на 1С

*Категория: Алгоритмы*

Классический алгоритм собеседования — определение палиндрома без разворота строки

```bsl
Функция ЭтоПалиндром(Стр) Экспорт
    // Паттерн: два указателя
    Левый = 1;
    Правый = СтрДлина(Стр);
    Пока Левый < Правый Цикл
        ЛевойСимвол = Сред(Стр, Левый, 1);
        ПравыйСимвол = Сред(Стр, Правый, 1);
        Если ЛевойСимвол <> ПравыйСимвол Тогда
            Возврат Ложь; // Не палиндром
        КонецЕсли;
        Левый = Левый + 1;
        Правый = Правый - 1;
    КонецЦикла;
    Возврат Истина; // Палиндром
КонецФункции

// Примеры:
// ЭтоПалиндром('abcba')  = Истина
// ЭтоПалиндром('abcde')  = Ложь
// ЭтоПалиндром('123321') = Истина
```

**Пояснения:**

- **Строка 1:** Функция Экспорт = доступна из других модулей. Встроенный язык 1С.
- **Строка 3:** Левый указатель = начало строки (индексация в 1С с 1).
- **Строка 5:** Пока Левый < Правый — не нужно сравнивать середину, она совпадает сама с собой.
- **Строка 8:** Если хоть одна пара символов не совпала — сразу выход. Ранний возврат = оптимизация.
- **Строка 11:** Двигаем оба указателя к центру. Каждый шаг = одно сравнение O(1).
- **Строка 14:** Если цикл завершился без выхода — все пары совпали = палиндром.

**Квиз:** Какова сложность этого алгоритма?

- ⬜ **A.** O(n^2) — вложенный цикл по строке
- ✅ **B.** O(n) по времени, O(1) по дополнительной памяти
- ⬜ **C.** O(log n) — бинарный поиск
- ⬜ **D.** O(n) по времени, O(n) по памяти — нужен массив символов

*Объяснение: O(n) время: каждый символ сравнивается максимум 1 раз (n/2 сравнений). O(1) память: только 2 переменных-указателя. Не создаём дополнительных массивов или строк.*

---

## ic-30: Соответствие как хэш-множество: поиск дубликатов за O(n)

*Категория: Оптимизация*

Как заменить вложенный цикл O(n^2) на линейный проход O(n) через Соответствие

```bsl
Функция НайтиДубликаты(Массив) Экспорт
    // Соответствие = хэш-множество
    Уник = Новый Соответствие;
    Дубли = Новый Массив;

    Для Каждого Элемент Из Массив Цикл
        // Если элемент уже в ключах = дубль
        Если Уник[Элемент] <> Неопределено Тогда
            Дубли.Добавить(Элемент);
        Иначе
            // Первый раз видим — добавляем
            Уник[Элемент] = Истина;
        КонецЕсли;
    КонецЦикла;

    Возврат Дубли;
КонецФункции

// Альтернатива: Использовать .Вставить()
// Уник.Вставить(Ключ, Значение) — НЕ заменяет если ключ есть
// Можно проверить: Уник.Получить(Элемент) <> Неопределено

// Вариант с количеством:
// Если Уник[Элемент] <> Неопределено Тогда
//     Уник[Элемент] = Уник[Элемент] + 1; // Считаем
// Иначе
//     Уник[Элемент] = 1;
// КонецЕсли;
```

**Пояснения:**

- **Строка 3:** Соответствие = хэш-таблица. Доступ к ключу за O(1). Идеально как множество.
- **Строка 6:** Один проход по массиву = O(n) итераций.
- **Строка 8:** Соответствие[Ключ] возвращает Неопределено если ключа нет. Если значение есть — значит элемент уже встречался.
- **Строка 12:** Значение = Истина — любое непустое значение помечает ключ как 'видели'. Можно использовать счётчик.
- **Строка 25:** Расширение: вместо флага Истина можно считать количество. Тогда Соответствие = частотный словарь.

**Квиз:** Почему Соответствие быстрее вложенного цикла для поиска дубликатов?

- ⬜ **A.** Соответствие хранит данные эффективнее
- ✅ **B.** Доступ к ключу в Соответствие = O(1) в среднем (хэш-таблица), а поиск в цикле = O(n)
- ⬜ **C.** Соответствие использует индексы СУБД
- ⬜ **D.** Вложенный цикл не работает в 1С

*Объяснение: Соответствие реализовано как хэш-таблица: вычисляет хэш ключа и напрямую обращается к ячейке памяти = O(1). Вложенный цикл каждый раз сканирует массив заново = O(n) за итерацию = O(n^2) всего.*

---

## ic-31: Расширение: проверка лимита бюджета при проведении

*Категория: Расширения*

Полный пример подписки на событие &Вместо с ПродолжитьВызов() для проверки бюджетных лимитов в Магните

```bsl
// Общий модуль расширения: ПроверкаЛимитов_Расш

&Вместо
Процедура ОбработкаПроведения_Расш(Отказ, РежимПроведения)
  Запрос = Новый Запрос;
  Запрос.Текст =
    "ВЫБРАТЬ
    |   БюджетныеЛимитыОстатки.СтатьяРасхода КАК Статья,
    |   БюджетныеЛимитыОстатки.Подразделение КАК Подразделение,
    |   СУММА(БюджетныеЛимитыОстатки.СуммаЛимита) КАК Лимит
    |ИЗ
    |   РегистрНакопления.БюджетныеЛимиты.Остатки(
    |       &Месяц, ,
    |       Подразделение В (&Подразделения),
    |       СтатьяРасхода В (&Статьи)
    |   ) КАК БюджетныеЛимитыОстатки
    |СГРУППИРОВАТЬ ПО
    |   СтатьяРасхода, Подразделение

  // Собираем параметры из документа
  МассивПодр = Новый Массив;
  МассивСтатей = Новый Массив;
  Для Каждого Стр Из Объект.Товары Цикл
    МассивПодр.Добавить(Стр.Подразделение);
    МассивСтатей.Добавить(Стр.СтатьяРасхода);
  КонецЦикла;

  Запрос.УстановитьПараметр("Месяц", НачалоМесяца(Объект.Дата));
  Запрос.УстановитьПараметр("Подразделения", МассивПодр);
  Запрос.УстановитьПараметр("Статьи", МассивСтатей);

  Результат = Запрос.Выполнить();
  МаппингЛимитов = Новый Соответствие;
  Для Каждого Стр Из Результат Цикл
    Ключ = Стр.Подразделение + Стр.СтатьяРасхода;
    МаппингЛимитов[Ключ] = Стр.Лимит;
  КонецЦикла;

  // Проверяем каждую строку документа
  Для Каждого Стр Из Объект.Товары Цикл
    Ключ = Стр.Подразделение + Стр.СтатьяРасхода;
    Лимит = МаппингЛимитов[Ключ];
    Если Лимит <> Неопределено И Стр.Сумма > Лимит Тогда
      Сообщить("Превышен лимит: " + Стр.СтатьяРасхода);
      Отказ = Истина;
    КонецЕсли;
  КонецЦикла;

  Если НЕ Отказ Тогда
    ПродолжитьВызов(); // Вызываем типовой код!
  КонецЕсли;

&После
Процедура ОбработкаПроведения_После_Расш(Отказ)
  // Логируем результат в РС.ЖурналИнтеграции
  Запись = РегистрыСведений.ЖурналИнтеграции.СоздатьЗапись();
  Запись.Документ = Объект.Ссылка;
  Запись.Статус = ?(Отказ, "Отклонён", "Проведён");
  Запись.Записать();
КонецПроцедуры
```

---

## ic-32: Расширение формы: автоподбор номенклатуры по артикулу

*Категория: Расширения*

Как добавить кнопку и обработчик на форму документа через расширение формы

```bsl
// Модуль формы расширения ЗаявкиНаРасход

&НаКлиенте
Процедура АвтоподборНоменклатуры_Расш(Команда)

  // Собираем артикулы из строк
  Артикулы = Новый Массив;
  Для Каждого Стр Из Объект.Товары Цикл
    Если ЗначениеЗаполнено(Стр.Артикул) И НЕ ЗначениеЗаполнено(Стр.Номенклатура) Тогда
      Артикулы.Добавить(Стр.Артикул);
    КонецЕсли;
  КонецЦикла;

  Если Артикулы.Количество() = 0 Тогда
    ПоказатьПредупреждение(, "Нет строк для подбора");
    Возврат;
  КонецЕсли;

  // Вызываем сервер
  Маппинг = ПодобратьНоменклатуруНаСервере_Расш(Артикулы);

  // Заполняем номенклатуру
  Счётчик = 0;
  Для Каждого Стр Из Объект.Товары Цикл
    Если Маппинг[Стр.Артикул] <> Неопределено Тогда
      Стр.Номенклатура = Маппинг[Стр.Артикул];
      Счётчик = Счётчик + 1;
    КонецЕсли;
  КонецЦикла;

  ПоказатьОповещениеПользователя("ПиктограммаИнформация",,
    "Заполнено: " + Счётчик + " из " + Артикулы.Количество());

КонецПроцедуры

&НаСервере
Функция ПодобратьНоменклатуруНаСервере_Расш(Артикулы)

  Запрос = Новый Запрос;
  Запрос.Текст = "ВЫБРАТЬ Артикул, Ссылка ИЗ Справочник.Номенклатура ГДЕ Артикул В(&Массив)";
  Запрос.УстановитьПараметр("Массив", Артикулы);

  Результат = Запрос.Выполнить();
  Маппинг = Новый Соответствие;
  Для Каждого Стр Из Результат Цикл
    Маппинг[Стр.Артикул] = Стр.Ссылка;
  КонецЦикла;

  Возврат Маппинг;

КонецФункции
```

---

## ic-33: Рефакторинг: от монолита к модулям (Скаенг пример)

*Категория: Рефакторинг*

Как разбить функцию get-default-and-real-prices на чистую архитектуру из 3 модулей

```bsl
// БЫЛО: 200+ строк в одной функции
Функция Обработка_api_internal_v1_price_get...
  // Валидация, запрос, обработка, JSON — всё тут
КонецФункции // 200+ строк!

// ═══ СТАЛО: Модуль 1 — Валидатор ═══
Функция ПроверитьЗапросЦен(ЗапросСтруктуры) Экспорт
  РезультатВалидации = Новый Структура;
  РезультатВалидации.Валидно = Истина;
  РезультатВалидации.Ошибки = Неопределено;
  РезультатВалидации.ИДУченика = Неопределено;
  РезультатВалидации.СекцияЦен = Неопределено;
  РезультатВалидации.Expand = Ложь;

  Если НЕ ЗапросСтруктуры.Свойство(`studentId`)
    ИЛИ ЗначениеЗаполнено(ЗапросСтруктуры.studentId) = Ложь Тогда
    РезультатВалидации.Валидно = Ложь;
    РезультатВалидации.Ошибки = Новый Структура;
    РезультатВалидации.Ошибки.Вставить(`studentId`,
      `ID студента не может быть пустым`);
    Возврат РезультатВалидации;
  КонецЕсли;

  РезультатВалидации.ИДУченика = ЗапросСтруктуры.studentId;
  ЗапросСтруктуры.Свойство(`priceSection`,
    РезультатВалидации.СекцияЦен);
  ЗапросСтруктуры.Свойство(`expand`,
    РезультатВалидации.Expand);

  Возврат РезультатВалидации;
КонецФункции

// ═══ Модуль 2 — Сервис (бизнес-логика) ═══
Функция ПолучитьЦены(ИДУченик, СекцияЦен, Expand)
  Запрос = СоздатьЗапросЦен(СекцияЦен);
  Запрос.УстановитьПараметр(`Ученик`, Ученик);
  Запрос.УстановитьПараметр(`МассивСТК`,
    ПолучитьМассивСТК(СекцияЦен));
  РезЗап = Запрос.Выполнить();
  Возврат РезЗап;
КонецФункции

// ═══ Модуль 3 — Обработчик = 15 строк! ═══
Функция Обработать(Запрос) Экспорт
  ДатаСтарта = ТекущаяУниверсальнаяДатаВМиллисекундах();

  // 1. Валидация
  Валид = ВалидаторЗапросовBilling.Проверить(
    JsonВОбъект(Запрос.ПолучитьТелоКакПоток()));
  Если НЕ Валид.Валидно Тогда
    Возврат Ответ_490(Валид.Ошибки, ДатаСтарта);
  КонецЕсли;

  // 2. Бизнес-логика
  РезЗап = СервисПрайсов.ПолучитьЦены(
    Валид.ИДУченика, Валид.СекцияЦен, Валид.Expand);

  // 3. Маппинг + ответ
  ТелоОтвета = МапперЦен.ВJSON(РезЗап, Валид.Expand);
  Лог.API(`/price/get-default-and-real`, ДатаСтарта, ТелоОтвета);
  Возврат Ответ_200(ТелоОтвета);
КонецФункции
```

---

## ic-34: Автотест HTTP-сервиса: обработка с 3 тестами

*Категория: Тестирование*

Как создать простую обработку для тестирования API без внешних инструментов

```bsl
// Обработка АвтотестAPI

&НаСервере
Процедура ЗапуститьВсеТесты()
  ТЗ = Объект.Тесты; // Таблица значений на форме
  ТЗ.Очистить();
  Результат = ``;

  // Тест 1: валидный запрос math
  ДобавитьТест(ТЗ, `Математика: валидный запрос`,
    "{"studentId": 44006323, "priceSection": "math", "expand": 5}",
    200);

  // Тест 2: без studentId → 490
  ДобавитьТест(ТЗ, `Отсутствует studentId`, "{"priceSection": "math"}", 490);

  // Тест 3: невалидный JSON → 490
  ДобавитьТест(ТЗ, `Невалидный JSON`, "{не_json}", 490);

  // Выполнить все тесты
  Для Каждого СтрТест Из ТЗ Цикл
    Попытка
      ЗапросСервис = Новый HTTPСервисЗапрос;
      ЗапросСервис.УстановитьТелоИзСтроки(СтрТест.ТелоЗапроса);
      Ответ = Обработать(ЗапросСервис);
      ФактическийКод = Ответ.КодСостояния;
    Исключение
      ФактическийКод = 590; // Техническая ошибка
    КонецПопытки;

    // Assert: проверить результат
    СтрТест.Прошёл = (ФактическийКод = СтрТест.ОжидаемыйКод);
    СтрТест.ФактическийКод = ФактическийКод;
    Если НЕ СтрТест.Прошёл Тогда
      Результат = Результат + Символы.ПС
        + `[ПРОВАЛ] ` + СтрТест.Описание;
    КонецЕсли;
  КонецЦикла;

  Если Результат = `` Тогда
    Результат = `Все тесты пройдены!`;
  КонецЕсли;
КонецПроцедуры
```

---

# Все сценарии (отдельный справочник)

## sc-1: Доработка типовой конфигурации

*Категория: Паттерны | Сложность: Средне*

**Теги:** `Расширения`, `БСП`

**Ситуация:** Руководитель просит добавить поле 'Комментарий HR' в форму документа 'ПриказКадровый' типовой БСП. Конфигурация обновляется каждый месяц. Как подойдёшь?

**Варианты:**

- ❌ Открою модуль формы и добавлю реквизит + элемент прямо в конфигураторе
  > При следующем обновлении типовой твои изменения будут затёрты. Нельзя менять код типовой напрямую!
- ✅ Создам расширение, добавлю реквизит и элемент формы через него
  > Расширение — правильный подход. При обновлении типовой расширение не затирается. Можно отключить если что-то пошло не так.
- ❌ Дублирую конфигурацию и буду развивать свою ветку
  > Дублирование = потеря обновлений типовой. Это технический долг, не решение.
- ❌ Создам новый документ 'ПриказКадровыйРасш' с нужным полем
  > Это раздует конфигурацию и нарушит интеграцию с остальными модулями БСП.

---

## sc-2: Логирование всех документов

*Категория: Паттерны | Сложность: Сложно*

**Теги:** `Подписки`, `Регистр сведений`

**Ситуация:** Бухгалтер просит: при записи ЛЮБОГО документа в базе логировать кто, когда и какие поля изменил. Документов 15 типов. Как реализуешь?

**Варианты:**

- ❌ Добавлю код логирования в ПередЗаписью() каждого из 15 документов
  > 15 модулей менять — боль при обновлении и появлении новых документов. Поддержка nightmare.
- ✅ Создам подписку на событие ПередЗаписью для всех документов + регистр сведений для лога
  > Подписка — ОДИН обработчик для ВСЕХ документов. При появлении нового документа — автоматически работает. Регистр сведений = хранилище логов.
- ❌ Сделаю регламентное задание, которое раз в минуту проверяет изменения
  > Регламентное задание не видит СТАРЫЕ значения полей. Нужно сравнивать текущее с предыдущим — сложно и ненадёжно.
- ❌ Использую технологический журнал для логирования
  > ТЖ логирует события платформы, не изменения бизнес-данных. Нет доступа к конкретным полям документов.

---

## sc-3: Периодическая проверка заказов

*Категория: Паттерны | Сложность: Средне*

**Теги:** `Обработчики ожидания`, `Фоновые задания`

**Ситуация:** Нужно каждый час проверять: есть ли новые заказы с доставкой 'Срочная' и отправлять уведомление менеджеру. Как реализуешь?

**Варианты:**

- ✅ Регламентное задание — стандартный механизм для периодических задач
  > Регламентное задание — правильный подход. Периодичность, автозапуск, логирование ошибок. Можно настроить расписание.
- ❌ Обработчик ожидания на сервере
  > Обработчик ожидания привязан к конкретному сеансу. Если сеанс завершится — обработчик остановится. Для постоянных задач — регламентное задание.
- ❌ Бесконечный цикл в фоновом задании
  > Фоновое задание одноразовое. Бесконечный цикл = утеччка памяти и блокировка потока. Платформа его прибьёт по таймауту.
- ❌ Клиентский таймер на форме менеджера
  > Таймер работает только пока форма открыта. Менеджер закроет 1С — проверки не будет.

---

## sc-4: Где разместить функцию?

*Категория: Стандарты кода | Сложность: Средне*

**Теги:** `Общие модули`, `Архитектура`

**Ситуация:** Функция ПолучитьЦену(Номенклатура, Склад, Дата) нужна в 5 разных формах (справочник, 3 документа, обработка). Код использует Запрос и читает из регистра. Где разместить?

**Варианты:**

- ❌ В каждом модуле формы — копипаст функции
  > Дублирование кода = при изменении логики нужно менять в 5 местах. Забудешь одно — баг.
- ✅ В серверном общем модуле (например, ЦенообразованиеСервер)
  > Серверный общий модуль — правильное место. Один раз написал, используется везде. Только серверный контекст — можно обращаться к БД.
- ❌ В модуле менеджера Справочника Номенклатура
  > Модуль менеджера — логика КОНКРЕТНОГО объекта. Цены — сквозная логика, не привязана к номенклатуре.
- ❌ В разделяемом общем модуле
  > Разделяемый = компилируется для клиента и сервера. Запросы и регистры — серверные объекты, клиент не может их использовать.

---

## sc-5: Код коллеги: что не так?

*Категория: Стандарты кода | Сложность: Сложно*

**Теги:** `Попытка/Исключение`, `Антипаттерны`

**Ситуация:** Коллега показал код:

Попытка
  Элемент = Справочники.Контрагенты.НайтиПоИНН(ИНН);
Исключение
  Элемент = Справочники.Контрагенты.СоздатьЭлемент();
КонецПопытки;

Что не так с этим подходом?

**Варианты:**

- ❌ Ничего, код рабочий
  > Код формально работает, но это грубый антипаттерн. НайтиПоИНН не выбрасывает исключение при отсутствии элемента — он возвращает ПустаяСсылка.
- ✅ Попытка/Исключение используется для управления потоком вместо обычной проверки
  > Именно! НайтиПоИНН возвращает ПустуюСсылку, а не выбрасывает исключение. Правильно: НайтиПоИНН + проверка .Пустая(). Попытка/Исключение — ТОЛЬКО для настоящих ошибок.
- ❌ Нельзя создавать СправочникОбъект в Исключении
  > СоздатьЭлемент() можно вызывать в Исключении, проблема не в этом. Проблема в логике — исключение тут никогда не возникнет.
- ❌ Нужно добавить НачатьТранзакцию() перед Попытка
  > Транзакция здесь не нужна — мы только ищем или создаём элемент, ещё не записываем.

---

## sc-6: Форма тормозит при открытии

*Категория: Стандарты кода | Сложность: Сложно*

**Теги:** `Производительность`, `Условное оформление`

**Ситуация:** Форма списка 'РеализацияТоваровУслуг' открывается 8 секунд. При быстром анализе ты видишь: 25 правил условного оформления, 6 динамических списков, 3 картинки на форме. Что правишь ПЕРВЫМ?

**Варианты:**

- ❌ Уберу все картинки с формы — они грузятся медленно
  > Картинки могут влиять, но 3 картинки — не причина 8 секунд. Ищи в другом месте.
- ✅ Уменьшу количество правил условного оформления до 10-15
  > 25 правил УО = платформа формирует ОГРОМНЫЙ запрос для данных оформления. Это частая причина тормозов. Сократи до 10-15, сгруппируй по условиям.
- ❌ Заменю все 6 динамических списков на обычные списки
  > Динамические списки — это нормальный механизм 1С. 6 списков на форме — много, но не главная причина 8 секунд.
- ❌ Добавлю индексы в регистры
  > Индексы помогают при ЗАПРОСАХ к данным, а не при открытии формы. УО тормозит на этапе формирования отображения.

---

## sc-7: Обмен между двумя филиалами

*Категория: Обмен данными | Сложность: Сложно*

**Теги:** `РИБ`, `План обмена`

**Ситуация:** Есть две ИБ 1С: 'Головной офис' (Москва) и 'Филиал' (Питер). Нужно синхронизировать справочники и документы. Изменения идут в обе стороны. Как реализуешь?

**Варианты:**

- ❌ HTTP-сервисы: каждый сервер получает POST от другого при изменениях
  > Ручной обмен = нужно отслеживать изменения самому, обрабатывать сбои, дубли. Всё уже придумано до нас.
- ✅ План обмена + авто-регистрация изменений + узлы в обе стороны
  > РИБ — стандартный механизм 1С для двустороннего обмена. Авто-регистрация изменений, приоритеты узлов, формат XML/JSON. Не изобретай велосипед.
- ❌ Простая выгрузка в Excel каждую ночь + загрузка через обработку
  > Excel = потеря данных, нет отслеживания изменений, конфликтов при двустороннем обмене. Для двух точек — plan обмена.
- ❌ Общая база PostgreSQL с доступом из обеих точек через VPN
  > 1С не поддерживает два кластера на одной БД. Кластер = единственный владелец БД.

---

## sc-8: Массовая загрузка 50 000 номенклатур

*Категория: Типовые задачи | Сложность: Средне*

**Теги:** `Массовая загрузка`, `Производительность`

**Ситуация:** Завезли новый прайс-лист: 50 000 позиций в Excel. Нужно загрузить в справочник за ночь. База PostgreSQL, 8 ГБ RAM. Что НЕЛЬЗЯ делать?

**Варианты:**

- ✅ Читаю Excel построчно в цикле, для каждой строки создаю и записываю элемент
  > 50 000 отдельных Записать() = 50 000 транзакций, 50 000 запросов к СУБД, минимум 2-3 часа. ЧИСТЫЙ АНТИПАТТЕРН.
- ❌ Загружаю весь Excel в ТаблицуЗначений, валидирую, потом загружаю пакетами по 1000
  > Пакетная загрузка — правильный подход. Уменьшает количество транзакций и запросов к СУБД.
- ❌ Отключаю регламентные задания и индексы перед загрузкой
  > Отключение регламентных заданий — хорошая практика при массовой загрузке, чтобы не было конфликтов.
- ❌ Использую ОбменДанными.Загрузить() для быстрой загрузки
  > ОбменДанными.Загрузить() — один из самых быстрых способов загрузки в 1С. Обходит часть проверок.

---

## sc-9: Deadlock в продакшене

*Категория: Типовые задачи | Сложность: Сложно*

**Теги:** `Deadlock`, `Блокировки`, `Продакшен`

**Ситуация:** В ЦУП видишь: 3 раза за последний час взаимная блокировка (deadlock) между сеансами при проведении документов 'Реализация' и 'Оплата'. Пользователи жалуются на зависание. Что делаешь?

**Варианты:**

- ❌ Увеличу таймаут ожидания блокировок до 60 секунд
  > Увеличение таймаута НЕ решает deadlock — только делает ожидание ДОЛЬШЕ. Пользователи будут ждать 60 сек вместо 30.
- ✅ Найду порядок блокировок в обоих документах и привяжу к ЕДИНОМУ порядку
  > Deadlock возникает из-за разного порядка захвата ресурсов. Приведи оба документа к ОДНОМУ порядку блокировки (например: Контрагент, затем Документ).
- ❌ Переключу на автоматические блокировки — СУБД сама разберётся
  > Автоматические блокировки = СУБД ставит блокировки при UPDATE. Это НЕ предотвращает deadlock, а наоборот — может их создать (СУБД сама обнаруживает).
- ❌ Уберу транзакции из обоих документов
  > Без транзакций данные могут быть неконсистентны. Другая проблема вместо deadlock — потерянные обновления и порча данных.

---

## sc-10: API для мобильного приложения

*Категория: Типовые задачи | Сложность: Средне*

**Теги:** `HTTP-сервис`, `REST`, `Интеграция`

**Ситуация:** Директор: 'Хочу приложение для менеджеров — видеть заказы и создавать новые'. 1С должна отдавать данные. Какой подход выберешь?

**Варианты:**

- ❌ Веб-сервис (SOAP) с WSDL описанием
  > SOAP тяжёлый для мобильных приложений. XML = overhead, парсинг сложнее. Устаревший подход для мобильных.
- ✅ HTTP-сервис (REST) с JSON — GET для списка, POST для создания
  > REST + JSON — стандарт для мобильных API. Лёгкий формат, быстрый парсинг, стандартные HTTP-коды. 1С HTTP-сервис идеально подходит.
- ❌ Прямое подключение мобильного приложения к СУБД PostgreSQL
  > НИКОГДА не давай прямого доступа к СУБД. Нет авторизации 1С, нет бизнес-логики, нет контроля прав.
- ❌ Файловый обмен: 1С выгружает XML в папку, приложение читает
  > Файлы = задержка, нет real-time, проблемы с одновременным доступом. Только для bulk-выгрузки раз в сутки.

---

## sc-29: Где исправить ошибку в отчёте СКД

*Категория: СКД | Сложность: Сложно*

**Теги:** `СКД`, `Отладка`, `Виртуальные таблицы`

**Ситуация:** Пользователь жалуется: в отчёте СКД 'Исполнение бюджета за квартал' колонка 'СуммаФакт' показывает нули для всех подразделений. В СКД поле Ресурса 'СуммаФакт' настроено, в запросе оборотов ресурс есть. При этом в консоли запросов запрос возвращает правильные числа. Что проверишь?

**Варианты:**

- ✅ Проверю заполнение параметров СКД — период, организация. Возможно параметр &ПериодНачало не заполняется
  > Параметры СКД — частая причина пустых данных. Если &ПериодНачало/Конец не заполнены, виртуальная таблица Обороты вернёт пустой результат. В консоли запросов ты подставляешь вручную, а СКД берёт из параметров формы.
- ❌ Перепишу запрос, заменив виртуальную таблицу на прямую к таблице регистра
  > Не используй прямые запросы к таблицам БД! Виртуальные таблицы — единственный правильный способ работы с регистрами. Проблема в параметрах, не в запросе.
- ❌ Удалю поле и добавлю заново в настройках СКД
  > Если поле есть в запросе и настроено в СКД — проблема не в настройке поля. Пересоздание поля не поможет.
- ❌ Добавлю условие ГДЕ СуммаФакт > 0
  > Фильтрация в запросе не исправит проблему с параметрами. Если параметры не заполнены — запрос вообще ничего не вернёт.

---

## sc-30: Медленный отчёт с вложенными запросами

*Категория: Сводные таблицы | Сложность: Сложно*

**Теги:** `Оптимизация`, `Временные таблицы`, `Индексы`

**Ситуация:** Отчёт 'Анализ расходов по центрам ответственности' работает 25 секунд. В нём 4 вложенных запроса с ВТ, JOIN к 5 справочникам, группировки по 3 измерениям. База — 200 000 движений регистра. Как ускоришь?

**Варианты:**

- ❌ Замени временные таблицы на вложенные запросы — СУБД сама оптимизирует
  > ВТ быстрее вложенных запросов в 1С! СУБД выполняет подзапросы для КАЖДОЙ строки основного, а ВТ материализуются один раз. Замена наоборот замедлит.
- ✅ Добавлю индексы на поля измерений регистра (СтатьяРасхода, Подразделение, Период)
  > Индексы на измерениях — основной способ ускорения запросов к регистрам. 200 000 записей с группировкой по 3 полям = полным сканированием без индексов. Индексы дадут Index Scan.
- ❌ Перенесу вычисления на клиент — у клиента быстрее процессор
  > На клиенте нельзя работать с запросами и регистрами. Все вычисления с данными — только на сервере.
- ❌ Уменьшу период: за месяц вместо года
  > Это не оптимизация отчёта — это изменение бизнес-требования. Если пользователю нужен год — нужно оптимизировать для года, а не урезать функциональность.

---

## sc-31: Как вывести отклонение от бюджета в СКД

*Категория: СКД | Сложность: Сложно*

**Теги:** `СКД`, `ВычисляемыеПоля`, `Регистры`

**Ситуация:** Нужно вывести в отчёте СКД 4 колонки: 'План', 'Факт', 'Абсолютное отклонение', 'Процент выполнения'. План и Факт — из разных регистров накопления. Как реализуешь процент выполнения?

**Варианты:**

- ✅ Вычисляемое поле СКД с выражением: Вычислить('Факт / План * 100')
  > Вычисляемые поля СКД — правильный механизм. Можно ссылаться на другие поля текущей строки. Формат: Вычислить('СуммаФакт / СуммаПлан * 100'). Дополнительно — условное оформление для значений < 80% (красный).
- ❌ Добавлю ресурс Процент прямо в запросе к регистрам
  > В одном запросе нельзя делить ресурсы из разных источников (РН.БюджетныеЛимиты vs РН.ФактическиеРасходы). Нужны ВТ или отдельные пакеты. СКД сделает это сама через вычисляемые поля.
- ❌ Создам регистр сведений для хранения процента, буду заполнять при проведении
  > Вычисляемый показатель не нужно хранить! Он всегда = Факт/План. Хранение избыточных данных = рассинхрон при изменении.
- ❌ Использую условное оформление для разных цветов колонки
  > Условное оформление — это визуализация, не вычисление. Сначала нужно посчитать процент, потом через УО показать цветом.

---

## sc-32: Срез последних: почему данные не актуальные

*Категория: Сложные запросы | Сложность: Средне*

**Теги:** `СрезПоследних`, `Регистр сведений`, `Отладка`

**Ситуация:** Отчёт 'Текущие должности сотрудников' показывает должность за прошлый месяц, хотя сотрудники уже переведены. Используется запрос к РегистрСведений.СрезПоследних(&ТекущаяДата, ...). Что не так?

**Варианты:**

- ✅ &ТекущаяДата не заполняется — передаётся Неопределено
  > Если параметр не заполнен, СрезПоследних может использовать дату по умолчанию или пустую дату. Всегда проверяй заполнение параметров: &ТекущаяДата = ТекущаяДата() на сервере.
- ❌ Регистр нужно переключить с периода 'День' на 'Год'
  > Периодичность влияет на гранулярность, но СрезПоследних корректно работает с любым периодом. Проблема в другом.
- ❌ Нужно использовать Обороты вместо СрезПоследних
  > Обороты — для регистров накопления. Регистр сведений не имеет Оборотов. СрезПоследних — правильная виртуальная таблица.
- ❌ Добавлю ГДЕ Регистратор ССЫЛКА Документ.Перевод
  > СрезПоследних автоматически находит последнюю запись по дате. Фильтрация по регистратору ограничит выборку, а не исправит дату.

---

## sc-33: Как отразить плановые и фактические данные в одном отчёте

*Категория: Бюджетирование | Сложность: Сложно*

**Теги:** `СКД`, `Бюджетирование`, `Архитектура`

**Ситуация:** Руководитель хочет один отчёт: по каждой статье бюджета — план на год, факт за прошедшие месяцы, прогноз на оставшиеся. Данные: РН.БюджетныеЛимиты (план) и РН.ФактическиеРасходы (факт). Как спроектируешь СКД?

**Варианты:**

- ✅ Два отдельных источника данных в СКД, связанных через общие измерения (Статья, Подразделение)
  > В СКД можно использовать несколько наборов данных. Первый — запрос к РН.БюджетныеЛимиты (Остатки). Второй — запрос к РН.ФактическиеРасходы (Обороты). Связь через общие поля. Вычисляемые поля для отклонений.
- ❌ Один запрос с UNION ALL двух виртуальных таблиц
  > UNION ALL объединяет результаты с одинаковой структурой. План и Факт имеют РАЗНЫЕ поля (Лимит vs Сумма). Нужны отдельные источники СКД или ВТ.
- ❌ Создам один регистр, где будут и план и факт в разных ресурсах
  > План и факт — разные по смыслу величины. План вводится ДО периода (утверждение бюджета), Факт формируется В течение периода (проведение документов). Разные документы = разные регистры.
- ❌ Два отдельных отчёта, пользователь смотрит рядом
  > Раздельные отчёты = неудобно, нет возможности сравнения и расчёта отклонений. СКД создана именно для объединения разных источников.

---

## sc-34: Задача: определение является ли строка палиндромом

*Категория: Алгоритмы | Сложность: Средне*

**Теги:** `Два указателя`, `Строки`, `Алгоритмы`

**Ситуация:** На собеседовании дают задачу: 'Напишите функцию, которая определяет, является ли строка палиндромом. Без использования обратной строки. 1С не имеет встроенной функции разворота строки.' Как решите?

**Варианты:**

- ✅ Цикл по индексам: сравнить первый символ с последним, второй с предпоследним и т.д.
  > Паттерн 'два указателя': начало и конец строки двигаются к центру. O(n) по времени, O(1) по памяти. Стр(Найти(Строка, КодСимвола(Строка, ЛевыйУказатель))) = ЛевыйУказатель + ПравыйУказатель - 1 значит палиндром.
- ❌ Вызываю СтрЗаменить для каждого символа и сравниваю с оригиналом
  > СтрЗаменить не меняет порядок символов. Этот подход не решает задачу палиндрома — нужно сравнивать символы с разных концов строки.
- ❌ Записываю строку в массив, сортирую, сравниваю
  > Сортировка меняет порядок символов — палиндром превратится в другой порядок. 'abcba' после сортировки = 'abbca' — это не палиндром. Подход не работает.
- ❌ Создаю вторую строку в обратном порядке через цикл и сравниваю
  > Формально работает, но задача сказана 'без обратной строки'. Дополнительно: O(n) памяти для второй строки. Паттерн двух указателей — O(1) памяти.

---

## sc-35: Задача: найти дубли в массиве без сортировки

*Категория: Алгоритмы | Сложность: Средне*

**Теги:** `Хэш-таблица`, `Соответствие`, `Алгоритмы`

**Ситуация:** Дан массив из N элементов (числа). Есть ли дубли? Нельзя использовать сортировку (алгоритмическая задача на собеседовании). Доступны: Массив, Соответствие, ТаблицаЗначений. Как решите в 1С?

**Варианты:**

- ❌ Вложенный цикл: для каждого элемента проверить все остальные. O(n^2)
  > Работает, но O(n^2) — медленно для 100 000 элементов = 10 млрд операций. На собеседовании скажут 'можно ли быстрее?'.
- ✅ Соответствие как хэш-множество: обойти массив, если элемента нет в ключах — добавить, если есть — дубль найден. O(n)
  > Соответствие в 1С = хэш-таблица с O(1) поиском. Обходим массив один раз. Если элемента нет в ключах — добавляем. Если есть — нашли дубль. O(n) время, O(n) память.
- ❌ Записать в ТаблицуЗначений и использовать НайтиСтроки
  > ТЗ.НайтиСтроки() — линейный поиск внутри таблицы. В сумме: O(n*m) где m = размер ТЗ на момент поиска. Хуже чем Соответствие.
- ❌ Запрос с ГРУППИРОВАТЬ ПО и ВЫБРАТЬ С Число > 1
  > Для массива в памяти нельзя написать запрос. Запрос работает с БД (справочники, регистры). Массив = объект встроенного языка, не источник данных для запросов.

---

## sc-36: Задача: оптимизировать медленный цикл с поиском

*Категория: Оптимизация | Сложность: Сложно*

**Теги:** `Оптимизация`, `Соответствие`, `Пакетные операции`

**Ситуация:** Код работает медленно:
Для Каждого Стр Из Список Цикл
  Найденная = Справочники.Номенклатура.НайтиПоРеквизиту('Артикул', Стр.Артикул);
  // ...
КонецЦикла
Список = 10 000 элементов. Как оптимизируешь?

**Варианты:**

- ✅ Сделаю Запрос с ВЫБРАТЬ из справочника ГДЕ Артикул В Массиве
  > Один запрос к БД вместо 10 000 отдельных поисков! СУБД выполнит за миллисекунды используя индекс. Параметр: МассивАртикулов. Результат в Соответствие для быстрого доступа.
- ❌ Добавлю индекс на реквизит Артикул
  > Индекс ускорит отдельный НайтиПоРеквизиту, но 10 000 вызовов = 10 000 SQL-запросов к СУБД. Индекс не решит проблему количества вызовов. Нужен ОДИН запрос.
- ❌ Сгруппирую артикулы и вызову НайтиПоРеквизиту один раз для каждой группы
  > Сгруппировка артикулов уменьшит количество вызовов, но всё равно будет много SQL-запросов. Один запрос с В Массиве = оптимально.
- ❌ Использую Справочники.Номенклатура.СоздатьДокументНаСервере()
  > Этот метод не существует. Наверное имелось в виду СоздатьЭлемент(), но это не решает проблему поиска — нужен поиск по реквизиту.

---

## sc-37: Задача: реверс строки на собеседовании

*Категория: Алгоритмы | Сложность: Легко*

**Теги:** `Строки`, `Базовый алгоритм`, `Собеседование`

**Ситуация:** Напишите алгоритм разворота строки без встроенных функций (если бы их не было). 1С: СтрПолучитьСтроку() и СтрДлина() доступны. '12345' → '54321'.

**Варианты:**

- ✅ Цикл от конца строки к началу, собираю символы в новый массив, потом СтрСоединить()
  > Классический алгоритм: для i = СтрДлина(Стр) по 1 шаг -1. Добавляю Сред(Стр, i, 1) в Массив. Потом СтрСоединить(Массив, ''). O(n) время, O(n) память. Паттерн: обратный проход.
- ❌ Рекурсивно: первый символ + реверс(остальная строка)
  > Рекурсия в 1С = переполнение стека при длинных строках. 1С не оптимизирует хвостовую рекурсию. Для 10 000 символов = 10 000 вложенных вызовов = ошибка стека.
- ❌ Использую Сред(Стр, СтрДлина(Стр), 1) для первого символа
  > Сред(Стр, СтрДлина(Стр), 1) = последний символ. Это лишь первый шаг — нужен цикл по всем символам. Неполное решение.
- ❌ СтрЗаменить(Стр, '', ' обратный порядок')
  > СтрЗаменить заменяет подстроку, не разворачивает. Нет встроенной функции для реверса строки в 1С — нужно писать алгоритм.

---

## sc-38: Задача: подсчёт уникальных элементов в массиве за O(n)

*Категория: Разделяй и властвуй | Сложность: Средне*

**Теги:** `Соответствие`, `Хэш-таблица`, `O(n)`

**Ситуация:** Дан массив с дубликатами: [5, 3, 5, 2, 3, 7, 2, 5]. Нужно посчитать сколько уникальных чисел. На собеседовании просят O(n). Как?

**Варианты:**

- ✅ Соответствие: обойти массив, добавлять элементы как ключи. Количество ключей = количество уникальных
  > Соответствие = хэш-множество. Каждая вставка O(1). Дубли не создают новые ключи. Итого: O(n) время, O(k) память где k = кол-во уникальных. Ответ: Соответствие.Количество().
- ❌ Отсортировать массив, потом посчитать уникальные (отличные от предыдущего)
  > Сортировка O(n log n), а нужно O(n). Хотя это правильный подход если нельзя использовать доп. память (in-place), но на собеседовании сказали O(n).
- ❌ Вложенный цикл: для каждого проверить, встречался ли раньше
  > O(n^2). Не удовлетворяет условию O(n). На 100 000 элементов будет слишком медленно.
- ❌ Создать ТаблицуЗначений с колонкой 'Значение' и использовать НоваяСтрока()
  > ТЗ.НайтиСтроку() = линейный поиск. В сумме O(n^2). Соответствие с O(1) доступом по ключу — правильный выбор.

---

## sc-39: Создать расширение для проверки лимитов

*Категория: Расширения | Сложность: Средне*

**Теги:** 

**Контекст:** В Магните на типовой УТ нужно добавить автоматическую проверку бюджетных лимитов при проведении ЗаявкиНаРасход. Требование: не ломать обновления типовой.

**Шаги:**

**Шаг 1:** Создать расширение конфигурации в EDT: Конфигурация → Расширения → Добавить
> 💡 Имя: Магнит_Расш. Платформа 8.3.6+ поддерживает расширения.

**Шаг 2:** Создать РегистрСведений ЖурналПроверокЛимитов для логирования
> 💡 Измерения: Документ, Подразделение, СтатьяРасхода. Ресурс: СтатусПроверки (перечисление). Периодический: Да.

**Шаг 3:** Создать ПодпискаНаСобытие на ОбработкаПроведения ЗаявкиНаРасход
> 💡 В расширении: Подписки → Добавить. Событие: ОбработкаПроведения. Объект: Документ.ЗаявкаНаРасход. Обработчик: процедуру в общем модуле расширения.

**Шаг 4:** Написать обработчик: &Вместо процедура ОбработкаПроведения(Отказ)
> 💡 Проверить бюджет через РН.БюджетныеЛимиты.Остатки. Если превышен → Отказ = Истина, Сообщить пользователю. Если норма → ПродолжитьВызов().

**Шаг 5:** Добавить запись в ЖурналПроверокЛимитов после проверки
> 💡 В &После обработчике — записать результат: пройти/превышен/недостаточно данных. Для аналитики руководителя.

**Шаг 6:** Протестировать: включить/выключить расширение и провести документ
> 💡 С расширением: проверка работает, отклоняет превышение. Без расширения: типовой функционал без изменений.

---

## sc-40: Конфликт двух расширений на одном событии

*Категория: Расширения | Сложность: Средне*

**Теги:** 

**Контекст:** В Магните работают два разработчика: один создал расширение для логирования (Расш1), второй — для проверки лимитов (Расш2). Оба подписались на ОбработкаПроведения ЗаявкиНаРасход. Возник конфликт.

**Шаги:**

**Шаг 1:** Определить текущий порядок расширений в свойствах конфигурации
> 💡 Конфигурация → Расширения → порядок списка = порядок вызовов. Первый в списке = вызывается первым.

**Шаг 2:** Проверить: оба используют &Вместо? Оба вызывают ПродолжитьВызов()?
> 💡 Если оба &Вместо с ПродолжитьВызов() — цепочка сработает корректно. Если кто-то забыл → типовой код потерян.

**Шаг 3:** Установить правильный порядок: логирование(&После) → проверка(&Вместо)
> 💡 Логирование лучше поставить &После — не мешает проверке. Проверка &Вместо может отменить проведение.

**Шаг 4:** Протестировать все сценарии: нормальное проведение, превышение лимита, ошибка данных
> 💡 Проверить: (1) Оба расширения вызываются. (2) Проверка лимита работает. (3) Логирование записывает все попытки. (4) Типовой код выполняется.

**Шаг 5:** Добавить префиксы методов для избежания конфликтов имён
> 💡 Расш1: ЛогированиеЗаявок_Расш1_ОбработкаПроведения. Расш2: ПроверкаЛимитов_Расш2_ОбработкаПроведения.

---

## sc-41: Расширение формы: автоподбор номенклатуры

*Категория: Расширения | Сложность: Средне*

**Теги:** 

**Контекст:** Менеджер Магнита просит: добавить кнопку Автоподбор в форму ЗаявкиНаРасход. По кнопке — заполнить номенклатуру по артикулу из массива строк.

**Шаги:**

**Шаг 1:** Создать расширение формы ЗаявкиНаРасход
> 💡 В расширении: Формы → Добавить → выбрать типовую форму документа. Платформа создаст расширение формы.

**Шаг 2:** Добавить команду формы Автоподбор и кнопку
> 💡 Команды → Добавить. Элементы → Кнопка → связать с командой. Разместить в командной панели.

**Шаг 3:** Написать &НаКлиенте обработчик кнопки Автоподбор
> 💡 Собрать артикулы из строк таблицы формы. Передать на сервер. На сервере: Запрос к Справочник.Номенклатура ГДЕ Артикул В(&Массив). Вернуть Соответствие.

**Шаг 4:** Заполнить Номенклатуру в строках из Соответствия
> 💡 Для Каждого Строка Из Объект.Товары Цикл: Если Маппинг[Строка.Артикул] <> Неопределено Тогда Строка.Номенклатура = Маппинг[Строка.Артикул] КонецЕсли.

**Шаг 5:** Протестировать: ввести артикулы, нажать кнопку, проверить заполнение
> 💡 Проверить: неизвестные артикулы пропускаются без ошибки. Найденные — заполняются. Форма обновляется.

---

## sc-42: Миграция расширения на новую версию типовой

*Категория: Расширения | Сложность: Средне*

**Теги:** 

**Контекст:** Вышло обновление типовой УТ. В расширении Magnit_Расш есть подписка на ОбработкаПроведения и расширение формы. Нужно мигрировать.

**Шаги:**

**Шаг 1:** Обновить типовую конфигурацию в хранилище
> 💡 Выгрузить типовую, сравнить с текущей, принять изменения. Расширение останется подключённым.

**Шаг 2:** Открыть расширение в EDT и проверить ошибки компиляции
> 💡 Платформа покажет: ссылки на несуществующие объекты, переименованые методы, изменённые параметры обработчиков.

**Шаг 3:** Обновить подписки: проверить что события и объекты не переименовали
> 💡 Если типовая переименовала Документ.ЗаявкаНаРасход → ЗаявкаНаРасходНовый — обновить в подписке.

**Шаг 4:** Обновить расширение формы: переустановить расширение формы
> 💡 Удалить старое расширение формы, создать новое на базе обновлённой типовой формы. Перенести доработки.

**Шаг 5:** Запустить все автотесты и ручное тестирование
> 💡 Провести ЗаявкуНаРасход. Проверить: (1) Проверка лимита работает. (2) Логирование записывается. (3) Автоподбор работает.

---

## sc-43: Архитектура: всё расширение для интеграции ЭДО

*Категория: Расширения | Сложность: Средне*

**Теги:** 

**Контекст:** В Магните нужно реализовать интеграцию с ЭДО для ЗаявокНаРасход. Всё решение — в расширении, чтобы при удалении не осталось следов в типовой.

**Шаги:**

**Шаг 1:** Спроектировать структуру расширения
> 💡 Общие модули: ЭДО_Расш (логика), HTTP_Расш (запросы). Объекты: РС.СопоставлениеЭДО, РС.ЖурналИнтеграции. HTTP-сервис. СКД-отчёт.

**Шаг 2:** Создать HTTP-сервис POST /edo/orders в расширении
> 💡 Шаблон URL: /edo/* Обработчик: POST → СоздатьИзJSON. Принимать JSON, создавать ЗаявкуНаРасход, возвращать HTTP 201.

**Шаг 3:** Создать РегистрСведений СопоставлениеЭДО в расширении
> 💡 Ресурсы: IDЭДО, ИДДокумента1С, Статус (строка), ДатаОтправки, ДатаОтвета. Измерение: UUID (уникальный идентификатор).

**Шаг 4:** Подписка на ПередЗаписью ЗаявкиНаРасход: доп. проверка и заполнение
> 💡 &Перед: проверить что сумма > 0, заполнить Статус = Черновик. Записать в СопоставлениеЭДО.

**Шаг 5:** СКД-отчёт ИсполнениеБюджета_Расш в расширении
> 💡 Создать Отчёт в расширении. Макет СКД. 2 набора: план и факт. Диаграмма. Автономно от типовой.

**Шаг 6:** Тест: отключить расширение и проверить что типовая работает без изменений
> 💡 Всё что добавлено — исчезает. HTTP-сервис недоступен. СопоставлениеЭДО не существует. Типовая УТ = чистая.

---

## sc-44: Диагностика медленного API-эндпоинта

*Категория: Оптимизация | Сложность: Средне*

**Теги:** 

**Контекст:** В Скаенг эндпоинт /api/internal/v1/price/get-default-and-real-prices отвечает за 8 секунд вместо ожидаемых 500мс. Нужно найти причину и исправить.

**Шаги:**

**Шаг 1:** Поставить точку останова в начале функции и измерить время каждого этапа
> 💡 ТекущаяУниверсальнаяДатаВМиллисекундах() — до и после каждого блока. Найти самый медленный.

**Шаг 2:** Проверить: Запрос.Выполнить() возвращает быстро или медленно?
> 💡 Если запрос медленный → проблема в SQL. Если быстрый → проблема в обработке результата.

**Шаг 3:** Получить план запроса в консоли
> 💡 Открыть консоль запросов, скопировать текст запроса, заполнить параметры, выполнить. Показать план.

**Шаг 4:** Найти Table Scan в плане запроса
> 💡 Полное сканирование = нет индекса. Добавить индекс по полям группировки: СТК + КодЛокализации.

**Шаг 5:** Проверить: выборка по группировкам занимает сколько времени?
> 💡 ОбходРезультатаЗапроса.ПоГруппировкам + внутренний цикл = N записей. Если N > 1000 → оптимизировать.

**Шаг 6:** Рефакторинг: вынести обработку результата в отдельную функцию
> 💡 Функция СформироватьОтветПоЦенам(Выборка, УказатьСкидки). Убрать дублирование кода.

---

## sc-45: Рефакторинг функции 200+ строк в 3 модуля

*Категория: Рефакторинг | Сложность: Средне*

**Теги:** 

**Контекст:** Функция Обработка_api_internal_v1_price_get_default_and_real_prices содержит 200+ строк с валидацией, бизнес-логикой и маппингом. Нужно разбить.

**Шаги:**

**Шаг 1:** Выделить валидацию в отдельный модуль: ВалидаторЗапросов Billing
> 💡 Функция ПроверитьЗапросЦен(ЗапросСтруктуры). Возвращает Структура с ошибками или Неопределено.

**Шаг 2:** Выделить бизнес-логику в модуль: СервисПрайсов
> 💡 Функция ПолучитьЦеныДляУченика(ИДУченика, СекцияЦен, Expand). Возвращает ТаблицуЗначений с ценами.

**Шаг 3:** Выделить маппинг в модуль: МапперЦен
> 💡 Функция ПреобразоватьВJSON(ТаблицаЦен, Expand). Возвращает структуру для JSON.

**Шаг 4:** Обработчик HTTP-сервиса = только маршрутизация
> 💡 Валидация → Сервис → Маппинг → Логирование → Ответ. 10-15 строк вместо 200.

**Шаг 5:** Протестировать: прогнать 3 эталонных запроса до и после рефакторинга
> 💡 Результаты должны совпадать. Время выполнения должно уменьшиться или остаться тем же.

---

## sc-46: Создание автотеста для HTTP-сервиса

*Категория: Тестирование | Сложность: Средне*

**Теги:** 

**Контекст:** Нужно создать обработку для автотестирования API-эндпоинтов Скаенга без внешних инструментов.

**Шаги:**

**Шаг 1:** Создать обработку АвтотестAPI с реквизитами: ТестЗапрос, ТестОтвет, ФактическийОтвет, Результат
> 💡 Модуль формы: реквизиты + кнопки Запустить, Сравнить, Все тесты.

**Шаг 2:** Создать вкладку с эталонными тестами (3-5 сценариев)
> 💡 Таблица значений: Описание, URL, ТелоЗапроса, ОжидаемыйКод, ОжидаемыйОтвет.

**Шаг 3:** Написать кнопку Запустить: вызвать функцию обработчика напрямую
> 💡 На сервере: сформировать HTTPСервисЗапрос, вызвать функцию, получить HTTPСервисОтвет.

**Шаг 4:** Написать кнопку Сравнить: фактический vs ожидаемый
> 💡 Сравнить код ответа. Сравнить JSON: разобрать обе структуры, сравнить по ключам.

**Шаг 5:** Добавить кнопку Все тесты: прогнать все сценарии и показать результат
> 💡 Цикл по строкам ТЗ, запуск каждого теста, результат = зелёный/красный.

---

## sc-47: Исправление пустого ответа math в Скаенг

*Категория: Скаенг: разбор | Сложность: Средне*

**Теги:** 

**Контекст:** Реальный баг из твоего опыта: get-default-and-real-prices возвращает пустой math массив, хотя в БД данные есть.

**Шаги:**

**Шаг 1:** Вызвать функцию в отладчике с параметрами из баг-репорта
> 💡 studentId: 44006323, priceSection: math, expand: 5. Точка останова в начале функции.

**Шаг 2:** Проверить: МассивСТКНоменклатуры содержит 'mathematics_kids'?
> 💡 Если МассивСТКНоменклатуры пустой или не содержит нужный СТК → запрос вернёт пусто.

**Шаг 3:** Скопировать текст запроса в консоль запросов и выполнить вручную
> 💡 Заполнить параметры: МассивСТКНоменклатуры, Ученик, ТекущаяДата. Проверить результат.

**Шаг 4:** Если запрос в консоли пустой — проверить ВидыНоменклатуры
> 💡 ПолучениеВидовНоменклатурыЕдиничек() возвращает все нужные виды? Математика загружена?

**Шаг 5:** Исправление: перенести виды номенклатуры и перезапустить автозагрузку
> 💡 Внешняя обработка загрузки видов номенклатуры. Или: расширение с подпиской на обновление.

---

## sc-48: Настройка finance_gate.proxy.1c для новой среды

*Категория: Скаенг: разбор | Сложность: Средне*

**Теги:** 

**Контекст:** При переносе 12→13 нужно обновить все exchange names, добавив префикс finance_gate.proxy.1c

**Шаги:**

**Шаг 1:** Найти все исходящие сообщения в конфигурации
> 💡 Поиск по конфигурации: event.billing, event.crm, trigger_gateway. Без префикса = нужно обновить.

**Шаг 2:** Обновить каждое имя: добавить finance_gate.proxy.1c. в начало
> 💡 event.billing_marketing.certificate.expired → finance_gate.proxy.1c.event.billing_marketing.certificate.expired

**Шаг 3:** Обновить очереди обмена и точки обмена для стейджинга
> 💡 Стейджинг = другой адрес RabbitMQ. Настройки = по аналогии из 13 базы, но с новым адресом.

**Шаг 4:** Проверить: если очередь заполнена — не добавлять finance_gate.proxy.1c
> 💡 Исключение: уже настроенные точки обмена. Не трогать — только новые и пустые.

**Шаг 5:** Обновить код: все обращения к прайсетченджд
> 💡 Поиск по коду: все строковые литералы с именами обменов. Заменить на переменные конфигурации.

---

# Все порции теории (отдельный справочник)

## Почему NULL появляется ТОЛЬКО из запросов

*Категория: NULL в запросах*

В 1С есть важное различие: встроенный язык НЕ имеет значения NULL. Все переменные либо заполнены, либо Неопределено. NULL существует ТОЛЬКО в языке запросов. Появляется при LEFT/RIGHT/FULL JOIN без совпадения. Также при выборке пустой ссылки через запрос.

**Ключевые моменты:**

- NULL = концепция ТОЛЬКО языка запросов
- Появляется при LEFT/RIGHT/FULL JOIN
- Проверяется ТОЛЬКО через ЕСТЬ NULL
- ГДЕ Поле = NULL - ВСЕГДА ЛОЖЬ
- Агрегатные функции игнорируют NULL (кроме СЧЁТ(*))
- ЕСТЬNULL(Поле, Значение) - подстановка

**Советы:**

- Никогда не пиши = NULL в запросах!
- При LEFT JOIN проверяй правую таблицу на NULL
- ЕСТЬNULL() для дефолтного значения
- Неопределено (встроенный язык) не равно NULL (запросы)

---

## Транзакция в 1С: определение ИТС

*Категория: Транзакции*

По ИТС: Транзакция = последовательность операций, рассматриваемая как единое целое. ACID: Атомарность, Согласованность, Изоляция (Serializable), Долговечность. НачатьТранзакцию() / Зафиксировать() / Отменить(). Неявная при Записать(). Вложенные НЕ поддерживаются.

**Ключевые моменты:**

- НачатьТранзакцию/Зафиксировать/Отменить
- Вложенные НЕ поддерживаются
- Неявная транзакция при Записать()
- Уровень: Serializable
- Правило 20 секунд
- ОтменитьТранзакцию() НЕ откатывает переменные!

**Советы:**

- Читай данные ДО транзакции, внутри - только пиши
- Оборачивай в Попытка/Исключение
- Не полагайся на переменные после отката

---

## HTTP-сервис vs Web-сервис

*Категория: HTTP/Web сервисы*

HTTP-сервис (REST): JSON, /hs/..., GET/POST/PUT/DELETE. Легковесный, для мобильных. Web-сервис (SOAP): XML, WSDL, /ws/... Тяжёлый, для enterprise. Для новых проектов - HTTP-сервис.

**Ключевые моменты:**

- HTTP: REST/JSON, легче, быстрее
- Web: SOAP/XML, WSDL, enterprise
- HTTP: /hs/ИмяСервиса/Метод
- Web: /ws/ИмяСервис?wsdl
- JSON легче XML в 3-5 раз
- HTTP проще отлаживать

**Советы:**

- Для новых интеграций - HTTP-сервис
- SOAP нужен только если клиент требует WSDL
- JSON + Postman/curl для отладки

---

## WebSocket: двусторонняя связь

*Категория: WebSocket*

Протокол полнодуплексной связи поверх TCP. Начинается как HTTP с Upgrade: websocket. В 1С НЕ встроен. Обход: внешние компоненты, long polling, COM, .NET DLL.

**Ключевые моменты:**

- Полнодуплексный: обе стороны WHEN THEY WANT
- Меньше overhead чем HTTP polling
- В 1С: НЕ встроен
- Long polling = простая альтернатива

**Советы:**

- Для push в 1С - long polling через HTTP-сервис
- Не путай с HTTP keep-alive
- WebSocket + 1С = внешние компоненты

---

## HTTP-методы: руководство

*Категория: HTTP-методы*

GET (безопасный, идемпотентный, кэшируемый), POST (НЕ идемпотентный), PUT (полное обновление), PATCH (частичное), DELETE (идемпотентный), HEAD (заголовки), OPTIONS (CORS). Идемпотентность важна для retry.

**Ключевые моменты:**

- GET: безопасный, идемпотентный
- POST: НЕ идемпотентный
- PUT: полное, PATCH: частичное
- DELETE: идемпотентный
- Статусы: 200, 201, 204, 400, 401, 403, 404, 500
- Content-Type: application/json

**Советы:**

- Идемпотентность важна для retry
- GET + URL параметры, POST + тело
- HTTP-статусы = часть API контракта

---

## Как отвечать и позиционировать себя

*Категория: Собеседование*

Будь честен. Показывай стандарты: БСП, EDT, Git. Говори конкретно: 'сделал HTTP-сервис'. STAR-метод: Situation, Task, Action, Result. Задавай вопросы. Не критикуй бывших.

**Ключевые моменты:**

- Будь честен - опытный интервьюер заметит ложь
- STAR: Situation -> Task -> Action -> Result
- Говори о ПРОЕКТАХ, не о годах
- Не зная -> расскажи что знаешь по смежной теме

**Советы:**

- Топ-5: клиент-сервер, регистры, транзакции, запросы, оптимизация
- Практикуй ответы вслух
- Фокус на задачах которые РЕШАЛ

---

## Архитектура HTTP-интеграции 1С с внешними системами

*Категория: HTTP-интеграция*

Интеграция 1С с внешними системами (ЭДО, CRM, складские системы) через HTTP-сервисы - один из самых частых паттернов в современной разработке 1С. Схема: внешняя система отправляет HTTP-запрос (GET/POST) на HTTP-сервис 1С. HTTP-сервис парсит входящий JSON, выполняет бизнес-логику (создание документов, запись регистров) и возвращает HTTP-ответ с JSON. Ключевые принципы: (1) Разделение ответственности - HTTP-сервис только приёмка, модуль менеджера - бизнес-логика. (2) Идемпотентность - повторный запрос не должен создать дубль. (3) Обработка ошибок - корректные HTTP-статусы (200/201/400/409/500). (4) Логирование - запись входящих запросов для отладки и аудита.

**Ключевые моменты:**

- HTTP-сервис = REST/JSON, URL начинается с /hs/
- Разделение: HTTP-сервис (маршалинг) + модуль менеджера (бизнес-логика)
- Идемпотентность через РС с проверкой внешнего ID
- HTTP-статусы: 201 Created, 400 Bad Request, 409 Conflict, 500 Error
- Неявная транзакция при Документ.Записать() - атомарность
- Json.Прочитать() (8.3.6+) - основной способ парсинга JSON

**Советы:**

- Всегда валидируйте JSON перед обработкой
- Проверяйте обязательные поля и возвращайте 400
- Используйте модуль менеджера для бизнес-логики, не пишите в HTTP-сервисе
- Тестируйте через Postman/cURL без внешней системы
- Логируйте входящие JSON в отдельный РС для аудита

---

## РН.Остатки vs РН.Обороты: когда какой выбрать для задачи

*Категория: Регистры*

Выбор типа РегистрНакопления - фундаментальное решение при проектировании. РН.Остатки: хранит баланс (приход + расход). Виртуальные таблицы: Остатки(&Дата), Обороты(&Начало, &Конец). Подходит когда нужно знать ТЕКУЩЕЕ состояние на момент времени. Пример: бюджетные лимиты, складские остатки, взаиморасчёты. РН.Обороты: хранит только движения (приход). Виртуальная таблица: Обороты(&Начало, &Конец). Подходит когда нужна аналитика движений за период. Пример: фактические расходы, продажи за месяц, обороты по счетам.

**Ключевые моменты:**

- РН.Остатки: баланс = Приход + Расход. Нужен контроль текущего состояния
- РН.Обороты: только движения. Нужна аналитика за период
- Остатки: виртуальная таблица Остатки(&Дата) - мгновенный остаток
- Обороты: виртуальная таблица Обороты(&Начало, &Конец) - движения за период
- БюджетныеЛимиты = Остатки (контроль лимита на момент)
- ФактическиеРасходы = Обороты (сколько потрачено за месяц)

**Советы:**

- Спроси себя: мне нужен баланс или аналитика - это определит тип
- РН.Остатки автоматически хранит сальдо - не нужно считать вручную
- РН.Обороты легче и быстрее для простой аналитики

---

## Где размещать код: HTTP-сервис, менеджер, объект, форма

*Категория: Разделение модулей*

Правильное разделение кода по модулям - критический навык для 1С-разработчика. Модуль HTTP-сервиса: ТОЛЬКО приёмка HTTP-запроса, парсинг JSON, вызов модуля менеджера, формирование HTTP-ответа. НЕ должен содержать бизнес-логику создания документов. Модуль менеджера (МодульМенеджера): общие операции для типа документов. СоздатьИзJSON(), Печать(), СоздатьДокумент(). Модуль объекта (МодульОбъекта): логика конкретного экземпляра. ОбработкаПроведения(), ПередЗаписью(), ПриЗаписи(). Модуль формы: обработчики элементов формы, вызов серверных функций, управление UI.

**Ключевые моменты:**

- HTTP-сервис = приёмка + маршрутизация (НЕ бизнес-логика!)
- Модуль менеджера = общие операции типа (Печать, СоздатьИзJSON)
- Модуль объекта = логика экземпляра (Проведение, ПередЗаписью)
- Модуль формы = UI и вызовы сервера
- Печать = модуль менеджера (общая операция)
- ОбработкаПроведения = модуль объекта (экземпляр)

**Советы:**

- Если сомневаешься - спрашивай: это для конкретного документа или для всех?
- HTTP-сервис не должен вызывать Справочники.Найти() - это бизнес-логика!
- Модуль формы = только точка входа, основная логика на сервере

---

## Схема компоновки данных (СКД): полное руководство

*Категория: СКД*

СКД = язык описания отчётов в 1С. Не пишет SQL напрямую — вместо этого описывает: какие данные брать (наборы данных), как группировать (группировки), какие поля выводить (поля), какие параметры использовать (параметры), как оформлять (условное оформление). СКД компилируется платформой в SQL-запросы к БД. Ключевой цикл: СхемаКомпоновкиДанных → КомпоновщикМакета → ПроцессорКомпоновкиДанных → ПроцессорВыводаРезультатаКомпоновкиДанных. Каждый компонент отвечает за свой этап: схема = описание, компоновщик = применение настроек, процессор = выполнение запроса, вывод = рендеринг.

**Ключевые моменты:**

- Наборы данных = источники (запросы к регистрам, справочникам, ВТ)
- Группировки = иерархия: Подразделение → Статья → Месяц
- Параметры = заполняются программно, НЕЛЬЗЯ оставить пустыми!
- Вычисляемые поля = выражения над другими полями (Факт/План*100)
- Условное оформление = цвет/шрифт по условию (красный если >100%)
- Макет хранится в обработке/отчёте, компоновщик — на сервере

**Советы:**

- Сначала настрой схему в консоли запросов, потом перенеси в СКД
- Параметры — причина №1 пустых отчётов! Всегда проверяй заполнение
- Используй Иерархию группировок для многоуровневых отчётов
- Вычисляемые поля проще чем вычисления в запросе для отклонений
- Условное оформление = визуальный сигнал для руководства

---

## Временные таблицы и многопакетные запросы: когда нужны и как работают

*Категория: Многопакетные запросы*

Многопакетный запрос = несколько SELECT в одном запросе, связанных через ВТ (ПОМЕСТИТЬ). Каждый последующий пакет может обращаться к ВТ из предыдущих. Зачем: (1) Объединение данных из разных регистров (План + Факт). (2) Последовательная обработка: фильтр → обогащение → группировка → финал. (3) Производительность: ВТ материализуются один раз, а вложенные запросы выполняются для каждой строки. СУБД создаёт физическую таблицу в temp-пространстве, заполняет её, потом следующие пакеты читают из неё. Платформа 1С оборачивает всё в одну транзакцию.

**Ключевые моменты:**

- ПОМЕСТИТЬ ВТ_Имя = создаёт временную таблицу на стороне СУБД
- ВТ живёт только внутри одного запроса — не persists
- ЛЕВОЕ СОЕДИНЕНИЕ ВТ_Факт = показывает ВСЕ записи из левой таблицы
- ЕСТЬNULL(Поле, 0) = замена NULL на 0 при LEFT JOIN
- Максимальный пакет запроса = 1 ВТ (ОБЪЕДИНИТЬ ВСЕ не создаёт ВТ)
- Порядок пакетов = порядок выполнения: нельзя ссылаться на ВТ из будущего

**Советы:**

- Начинай с малого: один пакет, проверь результат, потом добавляй ВТ
- В ТЫ ВСЁ в консоли запросов! Отдельные пакеты = отдельные SELECT
- Не создавай больше 5-7 ВТ в одном запросе — читаемость падает
- Для СКД — многопакетные запросы = основной источник данных

---

## Стратегии решения алгоритмических задач на собеседовании по 1С

*Категория: Алгоритмы*

На технических собеседованиях (Магнит, Ozon, BigTech) дают алгоритмические задачи. 1С-разработчики часто не готовы, т.к. думают 'у нас это не спрашивают'. Но спрашивают! Ключевые стратегии: (1) Паттерн 'два указателя' — для строк и массивов (палиндром, сумма двух). (2) Хэш-таблица через Соответствие — замена вложенных циклов O(n^2) на линейный O(n). (3) Разделяй и властвуй — разбить задачу на подзадачи. (4) Скользящее окно — для подмассивов/подстрок. (5) Сортировка + один проход — O(n log n) вместо O(n^2). Главное правило: сначала скажи решение, потом оптимизируй.

**Ключевые моменты:**

- Сначала работающее решение (O(n^2)), потом оптимизация (O(n))
- Паттерн 'два указателя': Левый = начало, Правый = конец, двигаются к центру
- Соответствие = хэш-множество для O(1) поиска по ключу
- Говори сложность вслух: O(n), O(n^2), O(log n)
- 1С-специфика: Массив, Соответствие, ТаблицаЗначений как структуры данных
- Не молчи! Рассуждай вслух — интервьюер оценивает ход мысли

**Советы:**

- Всегда начинай с простого решения, потом предлагай оптимизацию
- Говори сложность: O(что) по времени и памяти
- В 1С нет ArrayList/HashMap — используй Соответствие как хэш-таблицу
- Если застрял — приведи пример на конкретных данных
- Практикуй: решай 1 задачу в день на LeetCode (простые)

---

## Массовые операции: замена циклов с вызовами к БД на пакетные запросы

*Категория: Оптимизация запросов*

Частая задача на собеседовании и в проде: нужно обработать N элементов, обращаясь к БД. Антипаттерн: цикл с НайтиПоРеквизиту/Записать() внутри = N отдельных SQL-запросов. Паттерн: один запрос с В Массиве + Соответствие для маппинга + пакетная запись. Шаблон: (1) Собрать все ключи поиска в Массив. (2) Один Запрос с ГДЕ Артикул В(&МассивАртикулов). (3) Результат в Соответствие: Соответствие[Артикул] = Ссылка. (4) Обойти исходный массив, брать из Соответствия. Замена 10 000 SQL-запросов на 1 запрос + 10 000 обращений к памяти.

**Ключевые моменты:**

- Цикл с НайтиПоРеквизиту внутри = N SQL-запросов = ОЧЕНЬ медленно
- Один запрос с В(&Массив) = 1 SQL-запрос = быстро
- Соответствие для маппинга: Ключ → Значение за O(1)
- Запрос.Выгрузить() в ТаблицуЗначений для удобства
- Для записи: НаборЗаписей + загрузка таблицы = пакетная запись
- Правило: минимальное количество переходов клиент ↔ сервер ↔ СУБД

**Советы:**

- Сначала считай сколько SQL-запросов делает код
- Если цикл внутри цикла — это точно O(n^2), нужно оптимизировать
- Пакетная обработка: по 500-1000 записей за транзакцию
- Соответствие = лучший друг для маппинга данных в 1С

---

## Расширения конфигурации: эволюция доработок 1С

*Категория: Расширения*

Расширения появились в платформе 8.3.6 как ответ на главную боль 1С-разработчиков — конфликты при обновлении типовых конфигураций. До расширений существовали два подхода: прямая доработка (изменение модулей типовой) и внешние обработки (ограниченный функционал). Прямая доработка = потеря обновляемости. Внешние обработки = нельзя доработать логику проведения. Расширения решили обе проблемы: они хранятся отдельно от типовой конфигурации, но имеют полный доступ к объектам через механизм аннотаций (&Вместо, &Перед, &После). Современный стандарт разработки в 1С: МАКСИМАЛЬНО использовать расширения, МИНИМИЗИРУТЬ прямые доработки. Для enterprise-компаний вроде Магнит это критически важно — обновления типовых выходят 2-4 раза в год.

**Ключевые моменты:**

- Расширение = отдельный объект конфигурации с собственным кодом
- Не изменяет типовые модули — обновления проходят без конфликтов
- Аннотации: &Вместо (замена), &Перед (до), &После (после)
- ПродолжитьВызов() = вызов типового обработчика из расширения
- Включение/выключение одним флагом в свойствах конфигурации
- Несколько расширений могут работать параллельно на одной конфигурации
- Нельзя: добавить реквизит объекта, изменить роли, модифицировать структуру БД
- Платформа 8.3.20+: расширения расширений, расширения-common

**Советы:**

- Всегда используй ПродолжитьВызов() в &Вместо — частая ошибка!
- Префиксируй методы расширения: *_Расш1_ для избежания конфликтов
- Тестируй с включённым и выключенным расширением
- Для хранения данных: РС, ДопРеквизиты, СохраняемыеЗначения

---

## Расширение как контур интеграции: эволюция подхода Магнит

*Категория: Интеграции*

Современный подход к интеграциям в 1С: ВСЯ логика интеграции живёт в расширении. Это значит: HTTP-сервисы, обработчики JSON, создание документов, проведение, регистры для логирования — всё в одном расширении. При удалении расширения от конфигурации не остаётся НИКАКОГО следа интеграции. Это идеальный паттерн для компаний с несколькими интеграциями (ЭДО, складская система, банк, маркетплейс). Каждая интеграция = отдельное расширение. Архитектура: расширение конфигурации содержит общие модули (логика), регистры (сопоставление, журнал), HTTP-сервисы (эндпоинты), подписки на события (доработка проведения), отчёты (аналитика). Формы документа дорабатываются через расширения форм. Связь между расширением и типовой = через события (&Вместо + ПродолжитьВызов) и через запросы к типовым регистрам.

**Ключевые моменты:**

- Всё расширение = атомарная единица интеграции (включил/выключил)
- HTTP-сервисы, регистры, подписки, отчёты — внутри расширения
- Нет следов при удалении = идеально для тестирования и демо
- Несколько расширений-интеграций на одной конфигурации
- Магнит-паттерн: ЭДО, склад, банк = 3 отдельных расширения
- Каждое расширение имеет свой общий модуль с логикой

**Советы:**

- Делай каждую интеграцию отдельным расширением
- Храни настройки в СохраняемыеЗначения или РС внутри расширения
- Логируй все запросы и ответы в РС.ЖурналИнтеграции
- Используй &Вместо + ПродолжитьВызов для доработки проведения

---

## Оптимизация запросов 1С: от Middle к Senior подходу

*Категория: Оптимизация*

Middle-разработчик решает задачу: написать запрос, получить данные. Senior-разработчик решает задачу: написать ЗАПРОС КОТОРЫЙ БЫСТРО работает на продакшене с 1 миллионом записей. Разница не в синтаксисе — в понимании как СУБД выполняет запрос. PostgreSQL и SQL Server работают по-разному с одними и теми же данными. Индексы — это не магия: каждый индекс замедляет запись (INSERT/UPDATE) и ускоряет чтение (SELECT). Добавлять индекс только когда план запроса показывает сканирование таблицы. Виртуальные таблицы регистров (Остатки, Обороты) — это заранее оптимизированные SQL-запросы, которые платформа генерирует. Если не заполнить параметр периода — СУБД сканирует всю историю регистра. Правило: ВСЕГДА передавай период в виртуальные таблицы. Временные таблицы — это не про красоту кода, а про количество roundtrip к СУБД. Каждый вложенный запрос = отдельный вызов. 5 вложенных = 5 вызовов. ВТ = 1 вызов с 5 пакетами внутри.

**Ключевые моменты:**

- План запроса — первый инструмент диагностики, не интуиция
- Table Scan в плане = нужен индекс
- Виртуальные таблицы БЕЗ параметров = сканирование всей истории
- ВТ vs вложенные: 1 вызов СУБД вместо N
- СхемаЗапроса = программное управление запросом
- Индексы ускоряют чтение но замедляют запись
- PostgreSQL vs SQL Server: разные планы для одного запроса
- Удаление ненужных итогов в СхемеЗапроса — реальная оптимизация

**Советы:**

- Никогда не добавляй индекс без плана запроса
- Тестируй на реальном объёме данных, не на 100 записях
- Для СКД: параметры периода = обязательны
- Используй Консоль запросов = твоя лаборатория

---

## Чистая архитектура HTTP-сервисов в 1С: принципы для Senior

*Категория: Рефакторинг*

Чистая архитектура = разделение ответственности на слои. В контексте 1С HTTP-сервисов это значит: (1) Слой валидации — проверка входящих данных без бизнес-логики. (2) Слой бизнес-логики — работа с БД, вычисления, без знания о HTTP. (3) Слой маппинга — преобразование данных в формат ответа без бизнес-логики. (4) Слой логирования — фиксация вызовов без изменения данных. Каждый слой = отдельный общий модуль. Каждый модуль содержит функции < 50 строк с одной ответственностью. Преимущества: тестируемость (можно протестировать бизнес-логику без HTTP), переиспользование (валидация общая для всех эндпоинтов), читаемость (новый разработчик понимает за 10 минут), изменение формата JSON не затрагивает бизнес-логику. В реальном проекте Скаенг: функция 200 строк содержит все 4 слоя. После рефакторинга: 4 модуля по 30-50 строк каждый.

**Ключевые моменты:**

- 1 функция = 1 ответственность
- Валидация, логика, маппинг, логирование — отдельные модули
- Обработчик HTTP = 10-15 строк, только маршрутизация
- Общие модули = переиспользование между эндпоинтами
- Тестируемость = можно вызвать бизнес-логику без HTTP
- Магические числа → Константы или Перечисления
- Дублирование кода → общая функция с параметрами

**Советы:**

- Начни рефакторинг с извлечения валидации — самый простой слой
- Создай мок-тесты ДО рефакторинга — зафиксируй текущее поведение
- Используй СхемаЗапроса вместо конкатенации строк запроса
- Логируй время каждого этапа для будущей оптимизации

---

# Статистика Study Lab

| Метрика | Количество |
|---------|------------|
| Всего дней | 15 |
| Всего карточек | 415 |
| Всего диаграмм | 31 |
| Всего кодов с квизом | 23 |
| Всего сценариев | 30 |
| Всего порций теории | 17 |

### Карточек по дням

| День | Название | Карточек |
|------|----------|----------|
| 1 | День 1: Фундамент | 12 |
| 2 | День 2: Запросы и индексы | 20 |
| 3 | День 3: Транзакции и СУБД | 15 |
| 4 | День 4: Кластер и безопасность | 15 |
| 5 | День 5: ACID и инфраструктура | 15 |
| 6 | День 6: Git, Web, хранилище | 15 |
| 7 | День 7: Паттерны и практика | 15 |
| 8 | День 8: Структура и регистры | 30 |
| 9 | День 9: NULL, HTTP, WebSocket, собеседование | 40 |
| 10 | День 10: Мок-собеседование Магнит | 43 |
| 11 | День 11: HTTP-интеграция Магнит | 40 |
| 12 | День 12: СКД и сложные запросы | 40 |
| 13 | День 13: Алгоритмы и задачи на собеседовании | 40 |
| 14 | День 14: Расширения конфигурации | 40 |
| 15 | День 15: Оптимизация и рефакторинг (Middle → Senior) | 35 |

### Сценарии по типу

| Тип | Количество |
|-----|------------|
| Квиз (выбор ответа) | 20 |
| Пошаговые | 10 |
